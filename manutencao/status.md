# Status atual — Acervo Poético

> Checklist curto. Schema atual: `schema.md`. Porquês: `decisoes.md`. Riscos de retomada: `licoes-de-sessao.md`.

## Fechado

- **Exportação .csv (formato largo) da seleção de Poemas, Prosas e Autores.** Botão "⬇ CSV" (Poemas/Prosas) e "Baixar .csv" (Autores) nas barras de seleção, depois do JSON. `js/exportar-csv.js` (infra `celulaCsv`/`gerarCsv`/`baixarCsv` + `colunasTexto` + `analisarTexto`), `COLUNAS_AUTOR`/`exportarAutoresCsv` em `exportar-autores.js`, `exportarSelecaoCsv` em `exportar.js`, `exportarSelecaoAtualCsv` em `selecao-massa.js`, bindings em `main.js`. `tokenizar`/`limparTexto` movidos de `estatisticas.js` pra `utils.js`. Decisões e porquês em `decisoes.md`; mapa de colunas em `schema.md`. Testes: `tests/exportar-csv.test.js` (37, inclui teste de consistência com `forms.js`). Suíte: **1425/1425**, `eslint` sem novidade (só o erro antigo em `tests/importar-aditivo.test.js:45`), `prettier --check` limpo nos arquivos novos/alterados de JS. Rodado também sobre `data/arquivo-poetico-backup.json` (284 poemas, 21 prosas: mesmo nº de colunas em toda linha). **Sem conferência no navegador**: falta selecionar textos, baixar o .csv e abrir no pandas e no Excel (assistente de importação).

- **`fonteTexto.grafia` — enum atual/etimológica/quinhentista, consumido pela Sonoridade.** Grupo Fonte (Poema/Prosa) ganhou um quarto subcampo, `grafia` (`''` = atual/padrão, `GRAFIA_ETIMOLOGICA` = `'etimológica'` ou `GRAFIA_QUINHENTISTA` = `'quinhentista'`, `utils.js`): select nos dois modais, `lerFonteTexto`/`preencherFonteTexto` (`forms.js`), linha "Grafia" na Visualização/`.md` (via `linhasFonteTexto`, só aparece quando marcada), entrada `fonteGrafia` em `CAMPOS_PREENCHIVEIS` (tipo novo `'opcoes'`, que `selecao-massa.js` passou a saber renderizar como `<select>` no painel "Preencher campo ▾"). Não conta em "Campos Preenchidos" e não tem coluna nem Estatísticas, como o resto do grupo Fonte — decisão de escopo, não faltou implementar. Consumidor: o modal de Sonoridade mostra um aviso não-bloqueante (`son-grafia-aviso`, `atualizarAvisoGrafiaSonoridade` em `forms.js`) quando o poema selecionado está marcado com grafia antiga, já que a grade conta sílaba por regras atuais e erraria em silêncio. Pacotes da Biblioteca propagam `pacote.ortografia` (texto livre, continua existindo pra leitura humana na aba Biblioteca) pro novo enum via `normalizarGrafia` (`utils.js`, heurística sobre palavras como "original"/"anterior"/"antiga"/"pré-reforma"/"etimológica" e "quinhentista"/"século XVI"/"camoniana"); `meta.textos[título].grafia` sobrescreve por texto, pro caso de pacote com grafias mistas (motivador direto: o pacote Cruz e Sousa em andamento, com sonetos em grafias diferentes — ver `biblioteca/fontes/cruz-e-sousa.meta.json`, ainda sem `.md` companheiro nem link por soneto, portanto ainda não construído). Renomeado de `GRAFIA_ANTERIOR_REFORMA`/`'anterior à reforma'` pra `GRAFIA_ETIMOLOGICA`/`'etimológica'` (nome antigo presumia uma única reforma ortográfica; houve pelo menos 4 — 1911 PT, 1943 BR, 1971 BR, 1990 Acordo Ortográfico — e só a fronteira de 1911/1943 muda o que conta como vogal/dígrafo, que é o que motiva o campo), e estendido com `GRAFIA_QUINHENTISTA` pro português de Camões. De organização: `biblioteca/cruz-e-sousa.meta.json` estava solto em `biblioteca/` (fora de `biblioteca/fontes/`) e quebrava `montarIndice` (`scripts/montar-pacote.js` esperava `autores`, achou `autor`) — só movido de volta pro lugar certo, nenhum conteúdo alterado. Suíte: **1291/1291**, `eslint` sem novidade (só o erro antigo em `tests/importar-aditivo.test.js:45`), `prettier --check` limpo nos arquivos tocados. **Sem conferência no navegador**: falta abrir o select de Grafia nos dois modais, rodar o preenchimento em massa de verdade na tela, e ver o aviso da Sonoridade aparecer/sumir trocando de poema. **Cruz e Sousa ainda não construído**: falta o `.md` companheiro do `.meta.json` e a decisão de `grafia` por soneto (ver `manutencao/schema.md`). **Correção nesta sessão (bug pré-existente sinalizado na sessão da Assinatura)**: a descrição acima ("`''` = atual/padrão") ficou desatualizada — `OPCOES_GRAFIA` já tinha 4 estados (`''` = Não definida, `GRAFIA_ATUAL`/`'atual'` = Atual (padrão) escolhido de propósito, mais as 2 grafias antigas), mas os selects de `modal-poema.html`/`modal-prosa.html` ainda tinham só 3 `<option>`s com `value=""` na opção "Atual (padrão)" — escolher isso de propósito no modal gravava o mesmo `''` de "nunca mexi", então a distinção "—"/"Atual" da coluna Grafia (`celulaGrafia`) nunca disparava de verdade editando um Poema/Prosa normalmente, só via o painel "Preencher campo ▾". Corrigido: os dois selects ganharam a 4ª opção (`<option value="">Não definida</option>` antes de `<option value="atual">Atual (padrão)</option>`). `tests/selecao-massa-preencher.test.js:93` (o teste que falhava) esperava só 3 valores no select do painel — esse painel já lia `OPCOES_GRAFIA` direto (4 valores) desde sempre; era o teste que estava atrasado, corrigido pra `['', 'atual', 'etimológica', 'quinhentista']`. Ver `decisoes.md`. **`npm install`/`npm test` ainda não rodados com essa correção (sem rede neste ambiente)** — falta confirmar que a suíte fecha 100% e fazer a conferência manual: abrir o select de Grafia nos dois modais, escolher "Atual (padrão)" de propósito e ver a coluna da tabela mostrar "Atual" (não "—").

- Os 10 itens do plano original de reestruturação de schema.
- Cadastro central de Pessoas/Grupos/Autores.
- Épocas como cadastro próprio.
- Mesclar Pessoas / Mesclar Épocas (com checagem de nome duplicado ao salvar).
- Grupos — referência direta (`gruposDiretos`).
- Status "Privado" (🔒).
- Reorganização "Elos, Referências e Intertextualidade" → "Intratextualidade" (Elos + Ecos) e "Intertextualidade e Referências" (Intertextualidade + Referências novo, campo `referenciasExternas`). Ver `decisoes.md` e `schema.md`.
- Aba Sonoridade (Análise > Sonoridade), a partir de `Aba_Sonoridade.md`: Bloco 1 (dados + tabela), Bloco 2 (grade de sílabas + Modo Sílaba Tônica + Mapeamento de Rimas completo, incluindo classificação de par e painel "Pares de Rima" com remoção/correção) e **Bloco 3 (Matriz de Validação em Cascata)**. Bloco 3: `calcularOpcoesCascataSonoridade` (`utils.js`) filtra (nunca autopreenche/desabilita) os selects de `regularidadeMetrica`/`tamanhoVerso`/`esquemaRimasPresenca`/`esquemaRimasPadrao`/`origemTradicao` conforme `formaPoema` (e o caso composto Forma Livre + Versos Livres); cobre `Soneto (Genérico)`/`Soneto Petrarquiano / Camoniano`/`Soneto Shakespeariano`/`Haikai`/`Tanka`/`Lira Brasileira`. As 3 regras adicionais da spec (correção Presença→Versos Brancos, sugestão Medida Velha/Nova, avisos não-bloqueantes de monorrima atípica e divergência de sílabas — este último só em poema Isométrico) também implementadas. `registro`/`tom` ficam fora da cascata. Ver `schema.md` pro detalhamento completo. Ajustes finos já aplicados: hífen ortográfico sem peso métrico na grade (igual espaço), e Tom/Registro separados em dois campos independentes (`registro`, `tom`).
- **Tonalidade/Riqueza da classificação de rima foram expandidas** pra bater com a spec original (`Aba_Sonoridade.md`, que não estava disponível quando a classificação de par foi implementada): Tonalidade ganhou `Imperfeita` (3 opções) e Riqueza ganhou `Rara`/`Preciosa` (4 opções) — nenhuma escansão real usa esses campos ainda, então não há dado existente pra migrar.
- **Nova opção `Poesia Narrativa / Cordel` em `FORMAS_POEMA`** (`utils.js`): a spec só citava "narrativa/cordel" como exceção do aviso de Monorrima Absoluta, sem o valor existir de fato como forma selecionável — agora existe, entre `Poema em Redondilhas` e `Forma Livre / Indefinida`. Ganhou linha PARCIAL na Matriz de Validação (Bloco 3): trava `regularidadeMetrica`/`tamanhoVerso`/`esquemaRimasPresenca`/`origemTradicao` (os eixos universais no gênero), mas deixa `esquemaRimasPadrao` livre de propósito — decisão definitiva, não "em estudo" como Limerick/Quadra/Redondilhas, porque o gênero abriga várias estrofes com padrões de rima diferentes entre si.
- **Nova opção `Sextilha Aberta (ABCBDB)` em `ESQUEMA_RIMAS_PADRAO`** (`utils.js`): padrão de rima mais comum da sextilha de cordel (só os versos pares rimam entre si, ímpares soltos). Ver `decisoes.md` pro porquê de não virar trava.
- **Nova opção `Décima Espinela (ABBAACCDDC)` em `ESQUEMA_RIMAS_PADRAO`** (`utils.js`): estrofe de 10 versos consagrada na tradição de décima/repente/trova/cordel. Mesmo tratamento da Sextilha Aberta — não vira trava de nenhuma forma.
- **`Lira` renomeada pra `Lira Brasileira`** em `FORMAS_POEMA` e na chave de `MATRIZ_VALIDACAO_SONORIDADE` (`utils.js`): existem duas tradições distintas chamadas "lira" (hispano-italiana 7+11, brasileira 6+10 — a mapeada aqui); decisão do Victor foi manter uma forma só, mas deixar explícito no nome que é a variante local. Ver `decisoes.md` pro raciocínio completo.
- **`Poema em Redondilhas` removida de `FORMAS_POEMA`** (`utils.js`): não é forma fixa, é só o nome do verso de 5/7 sílabas, já coberto por `tamanhoVerso` (campo independente de `formaPoema`). Sem efeito colateral — confirmado sem lógica presa a esse valor e sem uso no acervo. Ver `decisoes.md`.
- **`Quadra / Trova` separada em `Trova` e `Quadra Popular`** em `FORMAS_POEMA` (`utils.js`): mesmo problema da Lira — um nome cobrindo duas tradições com regras diferentes (Trova rígida: sempre 7 sílabas/1 estrofe/rimada; Quadra solta: qualquer nº de sílabas, não precisa ser monostrófica). `Trova` ganhou linha completa na Matriz de Validação (Bloco 3); `Quadra Popular` ficou de fora de propósito (nada universal o bastante pra travar) e entrou em `FORMAS_SEM_AVISO_MONORRIMA`. Ver `decisoes.md` pro raciocínio completo, incluindo a decisão sobre o esquema `ABCB` (conta como Quadra, não Trova).
- **`Limerick` ganhou linha completa na Matriz de Validação** (`utils.js`): forma fixa (5 versos, AABBA), pesquisada e travada nos 5 eixos. Ver `decisoes.md`.
- **Novas opções `Limerick (AABBA)` e `Quadra / Rima Simples (ABCB)` em `ESQUEMA_RIMAS_PADRAO`** (`utils.js`): padrões nomeados sem equivalente genérico já existente no catálogo, mesmo espírito de Sextilha Aberta/Décima Espinela.

- **Simplificação do Bloco 1 da Sonoridade** (toggle de botões, exportadores md/pdf/docx e visualização somente-leitura): a coluna Ações da tabela de Sonoridade passou a usar o mesmo painel "⚙️ Ações ▾"/`acoes-coluna.js` de Poemas/Prosas (botões Ver/Baixar/Editar/Excluir configuráveis + formato do Baixar), estendido nesta sessão (`celulaAcoesSonoridade` em `render-listas.js`). "Ver" ganhou modal de visualização somente-leitura próprio (`js/visualizar-sonoridade.js` + `modais/modal-visualizar-sonoridade.html`), reaproveitando uma nova `renderGradeLeituraHtml()` em `editor-sonoridade.js` — mesma grade/cores da edição, sem os Modos/controles de edição. Baixar ganhou `.md`/`.pdf`/`.docx` (antes só `.json`), em `js/exportar-sonoridade.js`. Formato padrão do Baixar passou a ser `.md` (herdado de `acoes-coluna.js`), não mais sempre `.json` — `.json` continua disponível pelo seletor de formato.
- **Ajuste na Grade Silábica dos exportadores de Sonoridade** (sessão seguinte à acima, mesmo `js/exportar-sonoridade.js`): a decisão original de "negrito com asterisco simples" no .pdf (citada acima como perda aceitável) foi revertida a pedido do Victor. Rótulos dos campos de classificação passaram a sair em negrito de verdade no .pdf (antes saíam sem nenhum destaque — bug, não decisão); `.docx` ganhou `spacing` de verdade (título/campos/versos/pares não ficam mais colados) via o mesmo padrão de `ESTILOS_PADRAO_DOCUMENTO` de `exportar-docx.js`. Superado pelo item abaixo (a Grade Silábica virou tabela nos dois formatos, não é mais texto linear com separador).
- **Grade Silábica em TABELA real no `.pdf`/`.docx`** (mesma sessão): a pedido do Victor, a Grade Silábica nesses dois formatos deixou de ser texto linear ("1. sí / la (A)") e virou uma tabela alinhada por coluna de sílaba — mesma estrutura visual de `renderGradeLeituraHtml` (editor-sonoridade.js): régua de posições no cabeçalho, sílaba tônica com fundo âmbar + negrito, contorno colorido por SÍLABA rimada (não só por verso, igual a tela), coluna final com a letra do esquema. Nova função `gradeParaTabela` (`exportar-sonoridade.js`) monta esse modelo uma vez, reaproveitado pelos dois; `celulasRimadas` (antes privada em `editor-sonoridade.js`) foi exportada pra isso, mesmo motivo de `dividirSilabas`/`corDaLetra`/`trechoLado` antes dela. `.md` não mudou — continua em texto linear com `**negrito**`/`" / "` (`versosParaSilabas`), já que markdown puro não tem tabela alinhada por coluna que sobreviva a qualquer visualizador.
    - **`.docx`**: `Table`/`TableRow`/`TableCell` nativos da lib — sem limite de largura de página (Word só encolhe a coluna), então a tabela sempre cabe.
    - **`.pdf`**: jsPDF não tem tabela nem plugin de tabela carregado (`jspdf-autotable` não está no `index.html`) — a tabela é desenhada célula a célula (retângulo + texto centralizado) via nova `desenharCelula`/`desenharTabelaGrade`, dentro de `gerarPdfEscansao`. Como colunas de sílaba têm largura mínima legível (18pt), poemas com muitas sílabas por verso (alexandrino, verso livre longo) podem não caber numa página A4 retrato — **decisão do Victor: virar a página inteira pra paisagem só nesse trecho** (`decidirOrientacaoGrade` decide comparando a largura de coluna que sobraria em retrato vs. paisagem; `novaPagina(orientacao)` chama `doc.addPage('a4', orientacao)` e recalcula dimensões). Se "Pares de Rima" vier depois, o documento volta pro retrato antes de desenhá-la — a paisagem é só da grade, não do documento inteiro.
    - **Assimetria deliberada entre os dois formatos**: no `.docx`, quando a grade pede paisagem, o **documento inteiro** nasce em paisagem (`decidirOrientacaoGradeDocx`, uma seção só) — diferente do `.pdf`, que vira só a(s) página(s) da grade e volta. Fazer o `.docx` girar só um trecho exigiria duas seções com orientações diferentes (a lib docx suporta, mas pra um documento curto de uma escansão só, o ganho não pareceu valer a complexidade extra) — decisão de escopo, não limitação técnica; revisitar se algum dia esse `.docx` ficar mais longo (com mais conteúdo além da grade).
    - Cores de rima fixadas em hex (`CORES_RIMA_HEX`, um "-500" por letra da paleta) pra funcionar em fundo de papel branco — a tela usa `-400`/`-600` conforme o tema claro/escuro, que não existe em documento exportado.
    - Suíte rodada: `npm install` + `npm test` passaram, **723/723**. `eslint` limpo nos dois arquivos tocados; `prettier --check` só acusou linhas longas em `exportar-sonoridade.js` (estilo, sem efeito funcional — `editor-sonoridade.js` já está formatado). Falta ainda ABRIR os dois arquivos de verdade antes de considerar isto fechado: conferir a tabela com uma escansão real (Modo Tônico + Modo Rima já usados) e, se possível, um poema com verso bem longo (pra ver a paisagem entrar em ação no `.pdf`) — isso é teste manual, sem cobertura automatizada.

- **Aba Criação (Moldes)** — Bloco 1 (dados + tabela + Meta Estrutural em cascata), **Bloco 2 inteiro** (grade de escrita editável com contagem vs. alvo, Modo Tônica + divergência de Pé Métrico, coluna "Rima" com letra esperada) e **Bloco 3 inteiro**, implementado nesta sessão: sub-passo 1 (Pareamento de Rima — `paresRima` em `db.moldes`, Modo Pareamento de Rima em `editor-molde.js`, réplica do mecanismo de clique/shift-clique de `editor-sonoridade.js`, mas sem classificação de tipo de rima) e "Promover a Poema" (`db.moldes` ganhou `poemaId`/`status`, migrados por `migrarCamposBloco3Molde` em `db.js`; orquestração inteira em `js/forms.js` — `iniciarPromocaoMolde`/`promoverMoldeAoVivo`/`promoverMolde`/`aplicarPromocaoMoldeSePendente`/`limparPromocaoMoldeEmCurso` — abre `modal-poema` pré-preenchido com Título/Texto do Molde via `prepararNovo('poema')`, e só na hora de SALVAR o Poema de verdade cria a Escansão correspondente, reaproveitando `moldeLinhas`/`paresRima` sem re-escandir; tabela de Moldes ganhou botão "Promover a Poema"/atalho pro Poema já promovido em `render-listas.js`+`main.js`). Ver `manutencao/criacao-molde.md` (seção "Bloco 3") pro detalhamento completo. **Sem `npm install`/`npm test` rodados nesta sessão nem na anterior (sem acesso à rede) — `tests/editor-molde.test.js` continua nos 25 testes de antes do Bloco 3, sem cobertura nova pra pareamento de rima nem pra promoção. Rodar a suíte completa e escrever os testes que faltam antes de considerar isto fechado de verdade.**

- **Cascata de exclusão Poema → Molde promovido, e controle de Colunas/Ações na aba Criação (Moldes).** Dois pedidos do Victor na mesma sessão. (1) Excluir um Poema que era o destino de uma promoção (Bloco 3, ver `criacao-molde.md`) deixava o Molde de origem com `status: 'promovido'`/`poemaId` órfãos — o botão da coluna e o selo do modal continuavam apontando pra um Poema inexistente. `_removerParaExclusao`/`_restaurar` (`db.js`) ganharam o mesmo tratamento de vínculo que Época/Autor/Pessoa/Grupo já tinham: excluir o Poema devolve o Molde a `'em andamento'` (status/poemaId limpos), com "Desfazer" restaurando os dois; toast de confirmação avisa antes (`calcularImpactoExclusaoPoema`, nova, mesmo padrão de `calcularImpactoExclusaoEpoca`). O Molde em si nunca é apagado — decisão confirmada com o Victor, mesmo espírito de "o Molde original nunca é apagado na promoção" que já valia pro caminho inverso. (2) Moldes ganhou os mesmos painéis "🧱 Colunas ▾"/"⚙️ Ações ▾" que Poemas/Prosas/Sonoridade/Morfofuncionalidade já tinham: as 7 colunas de classificação do Bloco 1 entraram em `DEFINICAO_COLUNAS.moldes` (`colunas.js`, todas ligadas por padrão — já existiam fixas antes do seletor); a coluna Ações ganhou toggle de Editar/Promover(-ou-Ver-Poema)/Excluir — sem Ver nem Baixar, que Moldes não tem. Isso exigiu estender `acoes-coluna.js`: `DEFINICAO_ACOES` ganhou a chave `promover` (só existe aqui) e um novo `ACOES_APLICAVEIS` por tabela restringe quais botões cada aba realmente oferece no painel (Moldes: só os 3; todo o resto continua nos 4 clássicos por retrocompatibilidade, via `ACOES_APLICAVEIS_PADRAO`) — sem isso, "Promover a Poema" apareceria como checkbox nas outras abas, onde não significa nada. `renderSeletorAcoes` também passou a esconder a seção "Formato do Baixar" quando `baixar` não é uma ação aplicável. Suíte rodou de verdade (`npm install`/`npm test`, ambiente com rede): **1074/1074**, `eslint` limpo, `prettier --check` limpo em todos os arquivos tocados (`db.js`, `colunas.js`, `acoes-coluna.js`, `render-listas.js`, `index.html`, `tests/db.test.js`, `tests/render-dom.test.js`). Testes novos: unitários pra `calcularImpactoExclusaoPoema` e um bloco de integração DOM (aviso/cascata/desfazer/caso-sem-vínculo) em `render-dom.test.js`, mais um bloco cobrindo o toggle de colunas e de ações em Moldes especificamente.

- **Contagem MÉTRICA de sílabas (corte na última sílaba tônica) + aviso de dígrafo dividido (-rr-/-ss-), aba Sonoridade.** Dois pedidos do Victor na mesma sessão, sem mudar a grade de edição (a pessoa continua marcando TODAS as sílabas gramaticais). (1) `classificarTonicidade` (`utils.js`, regras de acentuação do português pesquisadas e documentadas no código) + `calcularContagemMetrica` (`editor-sonoridade.js`) descartam as sílabas átonas finais (0/1/2 conforme oxítona/paroxítona/proparoxítona) da contagem bruta de `dividirSilabas` antes de comparar contra `tamanhoVerso` — `calcularDivergenciaSilabas` e a célula "Cont." do Molde (`editor-molde.js`) foram os dois únicos lugares que realmente precisavam dessa troca (demais usos de `dividirSilabas` cuidam de posição/bound, não de contagem). Limitações documentadas: monossílabo átono/clítico tratado como tônico; hiato/ditongo tratados igual na contagem de núcleos pós-acento; sinalefa fica de fora por completo (pedido explícito de documentar, não só omitir). (2) `digrafoDivididoNoTexto`/`calcularVersosComDigrafoDividido` (`editor-sonoridade.js`) avisam (não-bloqueante, mesmo padrão de `avisoMonorrimaAtipica`) quando uma barra real cai entre as duas letras de um "rr"/"ss" — erro de escansão, vale pra qualquer poema, não só Isométrico. Ver `decisoes.md` pro detalhamento completo das regras. Suíte rodou de verdade: **1097/1097** (1074 + 23 testes novos em `editor-sonoridade.test.js`), `eslint` limpo, `prettier --check` limpo (após `--write` no arquivo de teste).

- **Correção assistida de dígrafo dividido na Grade Silábica — reversão da decisão original de "nunca autocorrige".** Pedido do Victor depois de testar a v1 (só aviso ao salvar): contraintuitivo ter que decorar/anotar versos pra corrigir depois. Agora a célula errada é destacada AO VIVO (borda tracejada vermelha, recalculada a cada tecla, prioridade visual sobre o tracejado âmbar de Pé Métrico) e um clique nela mescla só aquela barra na hora (`mesclarDigrafoDividido`) — com prioridade sobre Modo Sílaba Tônica/Rima/Eco, então funciona mesmo com um desses modos ligado. Aviso em lote acima da grade ("N dígrafos divididos... Corrigir todos") mescla o poema inteiro de uma vez (`mesclarTodosDigrafosDivididos`). Reindexação de `tonicas` reaproveita o mesmo tratamento simplificado (truncar, não remapear) que `onInputTexto` já usava pra qualquer edição manual que encolhe o verso — não é limitação nova. O aviso ao salvar continua existindo do jeito que já estava. Ver `decisoes.md`. Suíte: **1114/1114** (1097 + 17 testes novos), eslint/prettier limpos.

- **Biblioteca de domínio público (Dados → Biblioteca), passo 2 do plano — pacote piloto.** Pacotes são JSONs estáticos em `biblioteca/` (formato da Exportação Geral + `pacote` com fonte/edição/link/`conferido` + `autores` com nacionalidade/nascimento/óbito), listados em `biblioteca/indice.json` e carregados por `fetch` (`js/ui-biblioteca.js`). A aba é só adaptador: "Usar este pacote" entrega o payload à Importação Aditiva (`carregarPayloadImportacaoAditiva`, `ui-importar-aditivo.js`), que segue decidindo Autor/duplicata e tirando o snapshot. `criarEntidade` (`importar-aditivo.js`) passou a copiar nacionalidade/nascimento/óbito do `payload.autores` ao criar Autor (Exportação comum, sem `autores`, segue criando só com nome). Proveniência vai no campo novo `fonteTexto` de cada texto (`{ origem, edicao, link, conferido }`; grupo "Fonte" nos modais de Poema/Prosa, linhas na Visualização e no `.md`; ver `schema.md`). A situação de domínio público NÃO é gravada: `linhasFonteTexto` (`utils.js`) a deriva do óbito do Autor vinculado, então corrigir o óbito no cadastro corrige tudo. Fonte conta como um campo em "Campos Preenchidos" (texto próprio: preencher `Obra própria`, decisão do Victor) e tem autocompletar. Sem coluna própria na tabela nem em Estatísticas. `.pdf`/`.docx` reaproveitam `itemParaMarkdownPartes`, então em tese herdam as linhas novas — conferir abrindo um arquivo de verdade. Pacotes nascem de `scripts/montar-pacote.js` a partir de `biblioteca/fontes/<id>.md` + `.meta.json` (rodar de novo regenera `indice.json`). Piloto: `augusto-dos-anjos` (2 sonetos, `conferido: true`; texto conferido pelo Victor em Escritas.org, edição "Eu (1912)", link por texto via `meta.textos[título]` em `montar-pacote.js` — o link do pacote fica vazio porque cada poema tem o seu). Testes: `tests/biblioteca.test.js`, `tests/ui-biblioteca.test.js`. **Sem conferência no navegador nem suíte completa rodada** (sessão sem rede: faltavam `happy-dom`/`docx`/`jszip`, 18 arquivos de teste não carregaram; o resto passou).

- **Autor "Este autor sou eu" + painel "Preencher campo" em massa.** Duas etapas na mesma sessão. (1) `souEu` (boolean, opcional) em `db.autores`: checkbox no `modal-autor.html`, gravado/relido em `forms.js`, selo "(você)" no card de `renderAutores`. Ao ADICIONAR um chip de Autor marcado assim num texto com Fonte vazia, `criarGrupoDeAutoria` (`editor.js`, parâmetro novo `fonteOrigemId`) preenche a Fonte com `FONTE_OBRA_PROPRIA`; nunca sobrescreve Fonte já digitada e nunca roda em `carregar()` (reabrir texto não mexe na Fonte). Não é importado pela Importação Aditiva nem pelos pacotes da Biblioteca (ver `schema.md`). Vários autores podem ter `souEu` (sem unicidade forçada). (2) `js/campos-preenchiveis.js` (lógica pura: lista curada de campos de valor único, `planejarPreenchimento`/`planejarLimpeza`/`aplicar*`) + painel "✏️ Preencher campo ▾" nas barras de ação de Poemas e Prosas, montado por JS em `selecao-massa.js` (`prepararPainelPreencherMassa`, `aplicarPreenchimentoEmMassa`, `limparCampoEmMassa`); o `index.html` só tem o botão e um `<div>` vazio por tabela. Campo novo = uma entrada em `CAMPOS_PREENCHIVEIS`. Só campos de valor único (Fonte, Edição da fonte, Link da fonte, Texto conferido, Pendência); Idioma fica de fora de propósito (grava `pt-BR` quando vazio). Texto: só onde vazio, com opção "sobrescrever"; booleano define em todos. Confirmação mostra quantos mudam e quantos são pulados. Suíte: **1237/1237** (1201 + 36 novos: `autoria-sou-eu`, `form-autor-sou-eu`, `campos-preenchiveis`, `selecao-massa-preencher`); `prettier` limpo nos arquivos tocados. Falta conferência manual no navegador (abrir o painel em Poemas e Prosas, autocompletar da Fonte, sobrescrever, marcar um Autor "sou eu" e adicioná-lo a um poema novo).

- **Autor: Religião, Classe social, Neurodivergência, Condições clínicas e Escolaridade.** Cinco campos novos no `modal-autor.html`, todos opcionais e gravados/relidos por `initFormAutor`/`editarAutor` (`forms.js`): `religiao` (texto + sugestão), `classeSocial` (select fechado, `CLASSES_SOCIAIS_AUTOR`), `neurodivergencias` (tags), `condicoesClinicas` (tags tipificadas Física/Mental, `criarGrupoDeTagsComTipo` — generalização da fábrica de Nomes Literários), `grauAcademico` (select fechado, `GRAUS_ACADEMICOS_AUTOR`) e `instituicoes` (tags, autocompletar vindo do acervo). Datalists populados em `renderDropdowns` (`ui.js`), chips zerados ao abrir Autor novo. Testes: `tests/form-autor-campos.test.js` (novo, usa o HTML real do modal) + blocos em `utils.test.js`; `form-autor-sou-eu.test.js` passou a usar o HTML real do modal também (o fixture manual já estava desatualizado e falhava). Corrigido de passagem: Enter no input de Ocupações do Autor lançava `ReferenceError` (`adicionarOcupacaoAutor` não estava importada em `forms.js`). Modal de Autor alargado (`max-w-md` → `max-w-3xl`, grid de 2 colunas em telas médias+) e campos agrupados em 3 `<details class="campo-grupo" data-grupo-autor>`: Identidade, Saúde e neurodivergência, Formação e trabalho; Nome/ISNI/Nomes literários/Nacionalidade/Nascimento-Óbito/Sobre ficam sempre visíveis. Grupos começam fechados; `editarAutor` abre só os que têm dados (`autorGruposComDados`, `utils.js`) e `prepararNovo('autor')` fecha todos. Schema/decisões em `schema.md`/`decisoes.md`. Ainda não aparecem nas Estatísticas (mesmo escopo dos demais campos demográficos).

- **Autor: Deficiências + modal de visualização.** Novo campo `deficiencias` (tags tipificadas, `TIPOS_DEFICIENCIA`) no grupo Saúde do modal de edição, em `autorGruposComDados` e nos testes. Novo `modal-visualizar-autor.html`/`visualizar-autor.js` (`renderVisualizacaoAutorHtml` pura + `abrirVisualizacaoAutor` + `editarAutorDoVisualizador`), registrado em `main.js` (`ver-autor`); card de Autor em `renderAutores` (`render-listas.js`) enxugado e com botão "Ver". Sem lista de textos do Autor no visualizador, por decisão do Victor. Testes: `tests/visualizar-autor.test.js` (novo) + blocos em `form-autor-campos.test.js`/`utils.test.js`. Suíte 1368/1368.

- **Estatísticas: controle "Incluir textos de terceiros".** Um texto é "de terceiros" quando tem ao menos um Autor vinculado e nenhum está marcado `souEu` (coautoria/tradução sua contam como suas; sem autoria, ou só com autor excluído do cadastro, conta como seu). A separação só existe se algum Autor do acervo está marcado `souEu` — senão a aba mostra tudo, sem zeros. Por padrão terceiros ficam FORA das contagens (`todosOsTextos`, totais de Poemas/Prosas de `resumoGeral`, Por Livro/Coletânea, Palavras, Etiquetas, Pessoas); o checkbox no cabeçalho da aba liga/desliga (preferência `arquivoPoetico_est_incluirTerceiros`) e só aparece se há ao menos um texto de terceiros. Itens de coletânea que apontam pra texto de terceiros seguem a mesma regra; `textoOverride` conta sempre. **Backups, autobackup e todas as exportações continuam completos, sem mudança** — filtro de origem nas exportações ficou de fora de propósito (risco de a numeração do `.md` deixar de bater com a tabela). Código: `js/estatisticas.js` (`ehTextoDeTerceiros`, `separacaoTerceirosAtiva`, `contarTerceiros`, `definirIncluirTerceiros`), `js/main.js` (`window.definirIncluirTerceiros`), `index.html` (`#est-terceiros`). Testes: 7 novos em `tests/estatisticas.test.js`. Suíte completa NÃO rodada nesta sessão (sem rede: faltavam `happy-dom`/`docx`/`jszip`; `tests/estatisticas.test.js` passou 68/68 e os 21 arquivos que não dependem desses pacotes também). Falta conferência no navegador (marcar `souEu`, carregar o pacote piloto, abrir Estatísticas, ligar/desligar o checkbox, conferir que os cards e gráficos mudam e que a preferência persiste ao reabrir).

- **Biblioteca: "Importar de texto" (Fase 2, nível 1).** Na aba Dados → Biblioteca, um bloco recolhido "Importar de texto": a pessoa cola ou sobe um `.txt`/`.md` no formato `# Título` por texto, informa Autor (nome com sugestão dos Autores do acervo; nacionalidade, ano de nascimento e de óbito para Autor novo) e proveniência (Fonte, Edição, Link, Ortografia, "conferi este texto"), e "Enviar para a Importação" abre a Importação Aditiva com o payload carregado — mesma porta dos pacotes curados. Nada é gravado daqui; Autor, duplicata de título e snapshot seguem sendo decididos na Importação. O parser saiu de `scripts/montar-pacote.js` para `js/pacote-texto.js` (puro, sem `fs`/DOM): `separarTextos`, `montarItem`, `montarPacote` (o script reexporta), mais `temTextoAntesDoPrimeiroTitulo` e `montarPayloadDeTexto`. `separarTextos` ganhou `{ tituloPadrao }` (campo "Título": vale só quando o texto colado não tem nenhum `# Título`); sem essa opção o comportamento é o de antes. Prévia viva: textos reconhecidos, aviso de trecho antes do primeiro título (é descartado), aviso se o Autor já existe (o cadastro dele não muda) e a situação de domínio público (do óbito digitado ou, se o Autor já existe, do cadastro dele). Ano de óbito anterior ao de nascimento, ano que não é número e Autor vazio são recusados ao enviar. `conferido` nasce `false` e só vira `true` por declaração da pessoa. Só ano (sem dia/mês) na tela, porque só o ano entra no cálculo. Código: `js/ui-biblioteca-texto.js`, `js/ui-biblioteca.js` (prepara o formulário ao abrir a aba), `js/main.js` (4 `window.*`), `index.html` (`#biblioteca-importar-texto`). Testes: `tests/pacote-texto.test.js` (16) e `tests/ui-biblioteca-texto.test.js` (11, lendo o formulário do `index.html` real). Suíte: **1272/1272**, `eslint` sem novidade (só o erro antigo em `tests/importar-aditivo.test.js:45`), `prettier` limpo nos arquivos tocados. **Sem conferência no navegador**: falta abrir a aba, subir um `.txt` de verdade (acentos/UTF-8), enviar, ver a Importação e aplicar.
- **Biblioteca: "Importar de texto" — campo Tipo (Poema/Prosa).** Select `#bt-tipo` (padrão Poema) ao lado de "Título" no formulário; vale para todos os textos do envio. `js/pacote-texto.js`: `TIPOS_TEXTO`, `normalizarTipoTexto` (vazio = `poema`; valor desconhecido lança em vez de virar Poema em silêncio), `montarItem(id, texto, meta, tipo)` (Prosa sai sem `descricaoVisual`, único campo exclusivo de Poema), `montarPacote(md, meta, { tipo })` e `montarPayloadDeTexto({ tipo })`. `montarItemDeSelecao` passou a usar o mesmo parâmetro (antes uma Prosa vinda de seleção carregava `descricaoVisual: ''`). `js/ui-biblioteca-texto.js`: lê `bt-tipo`, a prévia diz "reconhecido(s) como Poema/Prosa", "Limpar" devolve o select a Poema. A Importação Aditiva já roteava por `item.tipo` (Prosa → `db.prosas`, duplicata de título checada na coleção do tipo), então não mudou. Só o "Importar de texto" ganhou o campo: os pacotes curados de `biblioteca/` seguem definindo `tipo` por item. Testes: +5 em `tests/pacote-texto.test.js`, +3 em `tests/ui-biblioteca-texto.test.js`; suíte **1433/1433**. **Sem conferência no navegador.**
- **Biblioteca: seis pacotes novos + pipeline de heterônimo, tipo por texto e demografia.** Pacotes (fonte: Wikisource, colados pelo Victor em `textos.txt`; `conferido: false` em todos, sem `link` por texto): `camoes` (5 sonetos + "Verdes são os campos", edição Baudry 1843), `fernando-pessoa` (Autopsicografia, Mar Português, Caeiro, Campos ×2, Reis), `machado-de-assis` (3 poemas + 3 contos, misto), `florbela-espanca` (5 sonetos), `maria-firmina-dos-reis` (capítulo "A Preta Susana" de Úrsula + "O Meu Desejo") e `lima-barreto` (3 contos). Código: `pacote-texto.js` aceita `meta.tipo` e `meta.textos[título].{tipo, assinatura, nomeLiterario, dataEscrita, dataPublicacao}` e valida `autor.{sexo, corRaca, nomesLiterarios}`; `importar-aditivo.js` copia sexo/gênero/corRaca/nomesLiterarios pro Autor novo, preserva `assinatura`/`nomeLiterario` no item (antes a Importação os descartava para QUALQUER exportação) e acrescenta ao Autor existente o nome literário que faltar; `ui-biblioteca.js` rotula poema/prosa no detalhe de pacote misto. Limpeza do bruto: cabeçalho/rodapé da Wikisource, 103 hífens invisíveis (U+00AD) e 12 marcas `[errata N]` removidos; rótulos "Mote:"/"Voltas:" tirados de "Verdes são os campos" (contariam como verso na Sonoridade); "linhaordinária" → "linha ordinária" em "Um Apólogo". Títulos: incipit moderno nos sonetos de Camões; demais em caixa de título com grafia atual. `corRaca` FICA VAZIO em todos até o Victor decidir por autor. `sexo` preenchido nos seis. `augusto-dos-anjos.json` regenerado (só ganhou `grafia: ""`, drift antigo). Testes: `tests/pacote-tipo-assinatura.test.js` (12) e `tests/biblioteca-pacotes-curados.test.js` (guarda: JSON = `.md`+meta, sem resto de página, importável); suíte **1479/1479**. **Sem conferência no navegador.**

- **Pacotes da Biblioteca a partir da seleção exportada do app.** `scripts/montar-pacote.js` passou a aceitar `biblioteca/fontes/<id>.json` (o JSON de "Exportar seleção") além do `<id>.md`, com o mesmo `<id>.meta.json`. Serve pro caso em que os textos já estão no acervo revisados, com Fonte/Edição/Link/conferido preenchidos por texto: monta o pacote sem recopiar pro `.md` nem redigitar a proveniência. Nova `montarPacoteDeSelecao` (+ `itensDeSelecao`, `montarItemDeSelecao`) em `js/pacote-texto.js`, e `cabecalhoPacote` extraída pra os dois caminhos compartilharem a montagem e a validação do `meta`. Só título, texto, `tipo`, `idioma` e `fonteTexto` atravessam — o resto do item vem do molde neutro de `montarItem`, pra a seleção não levar notas/pessoas/livros/envios/pendências pro pacote. Recusa item sem título ou texto, títulos repetidos e item cuja autoria não é a do `meta.autor.nome`. `escolherFonte` (no script) decide `.md` vs `.json` pela extensão informada ou pelo que existe no disco, e recusa se existirem os dois sem extensão informada. Regressão conferida: o piloto `augusto-dos-anjos` regenerado pelo script novo sai **idêntico byte a byte**. Testes: 9 novos em `tests/biblioteca.test.js` (21 no arquivo). **Suíte completa NÃO rodada (sessão sem rede: `npm install` falhou em 403; 22 arquivos que dependem de `happy-dom`/`docx`/`jszip` não carregaram). O que carregou passou: 781/781.** `eslint`/`prettier` não rodaram pelo mesmo motivo — linhas mantidas abaixo de 100 colunas à mão.

- **Assinatura de Autoria (Ortônimo/Heterônimo/Pseudônimo), substituindo o campo único `pseudonimos`.** Distinção de conceitos pedida pelo Victor: Heterônimo (identidade literária com voz/biografia próprias, ex. Fernando Pessoa/Álvaro de Campos) vs. Pseudônimo (só nome diferente de assinatura). `js/utils.js`: `ASSINATURAS_AUTORIA = ['Ortônimo', 'Heterônimo', 'Pseudônimo']` e `TIPOS_NOME_LITERARIO = ['Heterônimo', 'Pseudônimo']`; `paresAutoria`/`nomeAutoriaExibido` atualizadas, com normalização de dado sem os campos novos pra `'Ortônimo'`/`''`. `js/editor.js`: chip de Autoria ganhou o select "Assinatura" (padrão Ortônimo) + select condicional de nome literário filtrado pelo tipo; cadastro central do Autor (`db.autores[].pseudonimos` → `nomesLiterarios: [{nome, tipo}]`) ganhou fábrica própria (`criarGrupoDeNomesLiterariosAutor`), tipo por chip. `js/forms.js`, `js/main.js`, `js/ui.js`, `js/celulas-tabela.js`, `js/exportar-md.js`, `js/render-listas.js`, `modais/modal-autor.html`: todas as referências a "pseudonimo" renomeadas/adaptadas — conferido sem nenhum arquivo com o nome antigo restando. `tests/utils.test.js`: testes de `paresAutoria`/`nomeAutoriaExibido` reescritos pro novo formato. Por enquanto só classificação (tag no cadastro/exibição/exportação), sem efeito funcional a mais; sem migração de dado real, já que nenhum pseudônimo estava cadastrado no acervo antes da troca. Ver `schema.md`/`decisoes.md` pro detalhamento completo. **`tests/form-autor-sou-eu.test.js` corrigido nesta sessão**: o fixture de DOM do teste não tinha o `<input type="hidden" id="au-nomes-literarios">` que `forms.js` já lia incondicionalmente ao salvar o Autor (`salvarAutor`) — dado enviado pelo Victor já com a correção, aplicado ao arquivo. **`npm install`/`npm test` NÃO rodados nesta sessão (sem acesso à rede)** — falta confirmar a suíte completa (a sessão anterior tinha fechado em 1304/1305, com a mesma falha pré-existente de Grafia) e fazer a conferência manual: abrir o modal de Autor, cadastrar um Heterônimo, escolher a Assinatura na Autoria de um Poema/Prosa e conferir o nome exibido na tabela/Visualização/exportação.
- **Autores: busca por campo e exportação da seleção.** Aba Autores continua em cards (decisão: tabela com painel de Colunas seria muito maior e expõe dado sensível na tela; ver `visualizar-autor.js`). `js/busca-autores.js` (puro): `CAMPOS_ATRIBUTO_AUTOR` (mapa de prefixos próprio: nome, literario, heteronimo, pseudonimo, isni, nacionalidade, nascimento, obito, sexo, raca, genero, orientacao, identidade, religiao, classe, neurodivergencia, condicao, deficiencia, grau, instituicao, ocupacao, sobre, dominio, voce, id) + `filtrarAutores`; reaproveita o interpretador de `utils.js` (`parseConsultaBusca` passou a receber o mapa por parâmetro, padrão `CAMPOS_ATRIBUTO`, e foi exportada junto de `valorBateTermo`) — mesma sintaxe de Poemas/Prosas (E, `ou`, aspas, `-termo`, `campo:*`, os 3 interruptores). A busca SEM prefixo só olha campos públicos (nome, nomes literários, nacionalidade, religião, ocupações, instituições, sobre); orientação, condições clínicas, deficiências e neurodivergências só se acham por prefixo. `js/autores-acoes.js`: estado (filtro, opções, `selecaoAutores`) e ações; `js/exportar-autores.js` (puro + download): `.json` (`export_format: 'selecao_autores'`, com bloco `derivados`: idade, signo, domínio público, contagem de textos) e `.md`; exporta TUDO o que está cadastrado, inclusive sensível. Sem `.pdf`/`.docx` (os exportadores atuais montam a partir de texto literário). `renderAutores` (`render-listas.js`) passou a listar `getListaVisivelAutores()`, com checkbox por card; `index.html` ganhou caixa de busca, ajuda, interruptores, "selecionar todos" e barra de ações; bindings em `main.js`. Sem shift-clique nem paginação (não fazem falta com dezenas de cards). Testes: `tests/busca-autores.test.js` e `tests/exportar-autores.test.js` (20, passam). **Suíte completa NÃO rodada (sem rede: sem `happy-dom`); as 24 falhas de carregamento são as mesmas de antes da mudança.** Render/ações conferidos só com DOM simulado, fora da suíte.

## Em aberto

- **Selects de chip (Papel/Assinatura/nome literário em Autoria, tipo em Nomes Literários do Autor) ilegíveis no modo escuro — corrigido nesta sessão.** `js/editor.js`, 4 ocorrências: `[&>option]:text-black` sem variante pro escuro, enquanto `color-scheme: dark` (`style.css`) pinta o popup nativo do `<select>` escuro — texto preto sobre fundo escuro. Adicionado `dark:[&>option]:text-white`. Ver `decisoes.md`. **Sem conferência visual** (precisa abrir o app de verdade em modo escuro e clicar no select — não dá pra confirmar contraste só lendo código).

- **`Quadra Popular` sem linha na Matriz de Validação (Bloco 3)** — decisão definitiva (não pendência): "quadra" aceita qualquer nº de sílabas e não precisa ser monostrófica, então nada nela é universal o bastante pra travar. As outras 2 formas que também ficavam nessa situação (`Limerick`, `Quadra / Trova`) já foram resolvidas — `Limerick` ganhou linha completa, `Quadra / Trova` virou `Trova` (linha completa) + `Quadra Popular`, e `Poema em Redondilhas` foi removida de `FORMAS_POEMA`. Ver `decisoes.md`.
- **Progressão Morfofuncional — quarta leva sem `npm test` rodado** (edição pós-criação de Unidades/Eventos, correção do bug de reconciliação de "Linhas a ignorar", tabela enumerada priorizando `nome`, colunas Unidades/Eventos toggleáveis, paginação em Sonoridade + Morfofuncionalidade): implementação e testes novos escritos, mas a sessão não teve acesso à rede pra `npm install`/`npm test` — só a parte de `estrutura-textual.test.js` sem dependência externa foi executada de verdade (41/41). Rodar a suíte completa antes de dar por fechada. Ver `progressao-morfofuncional.md` (Quarta leva) pro detalhamento.

## Pendências de teste manual (sem cobertura automatizada — padrão do projeto pra motores de DOM/formulário)

- Fluxo dos dois botões de direção de Elo.
- Autocomplete de Época, select de recorte, aba de gestão de Épocas.
- Os grupos novos do modal de Prosa (Época, Livros, Elo, Eco, Referência, Anexos, Status/Pendências).
- Hover/clique dos dropdowns do menu de navegação, dropdown mobile, botão de baixar PNG (Conexões) nos dois temas.
- Criar Grupo novo inline no campo de referência direta; excluir Grupo referenciado diretamente e conferir o "Desfazer"; selecionar "🔒 Privado" e conferir badge/coluna/exportação.
- Bloco novo de Referências (Poema e Prosa): adicionar/editar/cancelar/remover entrada, filtro de sugestão de texto por tipo, exportação/visualização agrupada por tipo.
- Aba Sonoridade: abrir "Adicionar Escansão"/editar, seletor de colunas (persistência no localStorage), aviso não-bloqueante de poema com escansão já cadastrada, exclusão com "Desfazer", download do JSON de uma escansão isolada, digitar `/` e `-` na grade em poemas reais (acentos/ligaduras/caracteres especiais do acervo), redimensionar a janela com a grade aberta (scroll horizontal), Modo Sílaba Tônica e Modo Rima em poema real (gesto de clique/shift-clique/confirmar par, mutuamente exclusivo com o modo tônico), cores do contorno de rima num poema com muitos pares/grupos (paleta de 8 cores, repete no 9º grupo); lista "Pares de Rima": os 3 selects de classificação salvando corretamente por par, Remover com "Desfazer" restaurando o par no lugar certo (inclusive letra/cor recalculada), Corrigir preservando a classificação já feita ao reatribuir um lado, e Cancelar no meio de uma correção devolvendo o par original intacto.
- Aba Sonoridade — Simplificação do Bloco 1 (novo nesta sessão, sem cobertura automatizada ainda): painel "⚙️ Ações ▾" ligando/desligando cada botão e trocando o formato do Baixar (persistência por tabela, `resetarAcoesColuna`); botão "Ver" abrindo `modal-visualizar-sonoridade` com a grade somente-leitura correta (tônica em âmbar, contorno de rima colorido igual ao editor, pares de rima listados) pra uma escansão com Modo Tônico e Modo Rima já usados; download em `.md`/`.pdf`/`.docx`/`.json` tanto pelo botão "Baixar" da linha quanto pelos 4 botões dentro do modal de visualização, conferindo se o `.pdf`/`.docx` abrem sem erro, se a sílaba tônica de fato sai em negrito nos dois (não asterisco), se o separador " / " entre sílabas sai colorido em laranja nos dois, se os rótulos dos campos (Forma, Registro etc.) saem em negrito no `.pdf`, e se o `.docx` está com respiro visual entre título/campos/versos/pares (não colado); escansão sem nenhuma tônica/rima marcada ainda (grade "vazia") não deve quebrar nenhum dos dois modais nem os exportadores; verso muito longo (mais sílabas que cabem numa linha do `.pdf`) deve quebrar de forma legível entre sílabas, não cortar no meio de uma.

- Aba Criação: digitar em versos reais (acentos/hífen), trocar a Meta Estrutural (ex. Forma → Soneto) com texto já escrito no Molde e conferir que o texto sobrevive e só o alvo muda, remover um verso no meio da grade e conferir a renumeração visual, "Editar Molde" reabrindo a grade com o texto salvo.
- Aba Criação — Bloco 3 (novo nesta sessão, sem cobertura automatizada ainda): Modo Pareamento de Rima (clique/shift-clique, confirmar/cancelar/corrigir/remover um par, aviso ⚠ de par fora do esquema atual depois de trocar o Esquema de Rimas); "Promover a Poema" pelos dois caminhos (botão de dentro do modal com edições ainda não salvas, e botão da tabela com um Molde já salvo) — conferir que o `modal-poema` abre pré-preenchido (Título/Texto sem as barras `/` de divisão silábica), que salvar esse Poema cria a Escansão com a grade/pares já pré-preenchidos corretamente, que o Molde original vira "✓ Promovido a Poema #N" (dentro do modal) e ganha o atalho pro Poema na tabela, e que cancelar o Poema pré-preenchido (fechar sem salvar) NÃO promove nada. Conferir também que editar um Poema qualquer (sem relação com Molde nenhum) nunca aciona a promoção por engano.

- Aba Sonoridade — contagem métrica (novo nesta sessão, sem cobertura automatizada de DOM ainda): abrir um poema real terminado em palavras paroxítonas/proparoxítonas e conferir que a divergência de sílabas (Bloco 3) e a célula "Cont." do Molde não disparam mais falso positivo.
- Aba Sonoridade — correção assistida de dígrafo dividido (novo nesta sessão, testado em DOM sintético via happy-dom, mas não visualmente): digitar `/` de propósito no meio de um "rr"/"ss" num poema real e conferir a borda vermelha nas duas células vizinhas (sem colidir visualmente com o tracejado âmbar de Pé Métrico quando os dois calham na mesma célula), clicar pra mesclar uma ocorrência isolada, usar o botão "Corrigir todos" com múltiplas ocorrências espalhadas em versos diferentes, e conferir que o aviso ao salvar (`calcularVersosComDigrafoDividido`) ainda dispara pra uma ocorrência deixada de propósito sem corrigir.
- Aba Autores — busca e exportação (novo, sem teste de DOM na suíte): digitar `nacionalidade:...`, `heteronimo:*`, `-obito:*`, abrir o "?" de ajuda, marcar os 3 interruptores; marcar cards, "Selecionar todos os que a busca mostra", filtrar com seleção ativa, excluir um autor marcado e ver o contador; baixar `.json` e `.md` e abrir os arquivos; modo escuro na barra de ações.

## Suíte

`npm test`: **1290/1290 passando** (última rodada completa, sessão do campo Grafia). `eslint` limpo à parte do erro pré-existente em `tests/importar-aditivo.test.js:45` (`no-unreachable`, não relacionado); `prettier --check` limpo nos arquivos tocados nesta sessão — ainda há arquivos mais antigos fora do padrão do Prettier, não tocados aqui. Falta só a rodada de testes manuais listada acima (Grade Silábica em tabela) pra dar a Simplificação do Bloco 1 por encerrada; contagem métrica e correção assistida de dígrafo de sessões anteriores ainda não tiveram conferência visual num poema real (ver notas acima).

- **Exportar/importar Molde em JSON (aba Criação → Moldes).** `js/molde-json.js` (lógica pura) + `js/ui-molde-json.js` (DOM): botão Baixar na coluna Ações e no modal (estado ao vivo), botão "Importar JSON" no cabeçalho da aba; id novo sempre, snapshot forçado, `poemaId`/`status` de origem descartados, checagem de duplicata de título. Suíte: **1181/1181**; testes novos em `tests/molde-json.test.js` e `tests/ui-molde-json.test.js`. Falta só conferência manual no navegador (baixar → importar no mesmo acervo → duplicata → "Importar mesmo assim"). Erro de ESLint pré-existente em `tests/importar-aditivo.test.js:45` (`no-unreachable`), não relacionado.
