diff --git a/README.md b/README.md
index c55b8ca..e98ef45 100644
--- a/README.md
+++ b/README.md
@@ -214,9 +214,10 @@ No dependencies need to be installed. Tailwind CSS is loaded via CDN; Chart.js a
   (which filters by attribute) and the Structure tab's point export
   ("Export selected", which filters via the tree but only outputs
   structural JSON, with no resolved context and no Markdown option).
-- **Alternative Versions (`filtrar.html`)**: separate tool (linked in the
-  app header) for reviewing poems/prose flagged with sensitive tags and
-  registering alternative versions of the text before exporting to an AI.
+- **Alternative Versions (`filtrar.html`)**: separate tool (reachable from
+  the "Tools" group in the app nav) for reviewing poems/prose flagged with
+  sensitive tags and registering alternative versions of the text before
+  exporting to an AI.
   Accepts either the full backup or the JSON generated by Selective Export.
   Registered versions are saved by title in the browser (its own store,
   separate from the main app's `localStorage`) and are reapplied
@@ -273,8 +274,8 @@ automatically removes the corresponding cover.
 
 ## Alternative Versions (`filtrar.html`)
 
-A separate page (outside the `index.html` SPA, reached via the "Alternative
-Versions" button in the header) for reviewing texts flagged with sensitive
+A separate page (outside the `index.html` SPA, reached via "Alternative
+Versions" in the "Tools" nav group) for reviewing texts flagged with sensitive
 tags and registering an alternative version of each one before exporting the
 collection to an AI. Registered versions (`tituloFiltrado`, `textoFiltrado`,
 `nota`) are saved by title in their own `localStorage` store, separate from
diff --git a/README.pt-br.md b/README.pt-br.md
index cea71d5..93bfc07 100644
--- a/README.pt-br.md
+++ b/README.pt-br.md
@@ -215,9 +215,10 @@ Nenhuma depend├¬ncia precisa ser instalada. O Tailwind CSS ├® carregado via CDN;
   a Exporta├º├úo seletiva (que filtra por atributo) e a exporta├º├úo pontual da
   aba Estrutura ("Exportar selecionados", que filtra pela ├írvore mas s├│ sai
   em JSON estrutural, sem contexto resolvido nem op├º├úo de Markdown).
-- **Vers├Áes Alternativas (`filtrar.html`)**: ferramenta separada (link no
-  header do app) para revisar poemas/prosas marcados com tags sens├¡veis e
-  cadastrar vers├Áes alternativas do texto antes de exportar para uma IA.
+- **Vers├Áes Alternativas (`filtrar.html`)**: ferramenta separada (acess├¡vel
+  pelo grupo "Ferramentas" na nav do app) para revisar poemas/prosas
+  marcados com tags sens├¡veis e cadastrar vers├Áes alternativas do texto
+  antes de exportar para uma IA.
   Aceita tanto o backup completo quanto o JSON gerado pela Exporta├º├úo seletiva.
   As vers├Áes cadastradas ficam salvas por t├¡tulo no navegador (banco pr├│prio,
   separado do `localStorage` do app principal) e s├úo reaplicadas
diff --git a/assets/css/style.css b/assets/css/style.css
index 037637c..c1ff779 100644
--- a/assets/css/style.css
+++ b/assets/css/style.css
@@ -14,6 +14,97 @@
     font-weight: 700;
 }
 
+/* Mesmo refor├ºo de z-index do .nav-dropdown acima, pros outros dois
+   popups da p├ígina com o mesmo risco de ficar atr├ís da tabela com
+   scroll (#painel-tema no header, #painel-menu-mobile no menu mobile). */
+#painel-tema,
+#painel-menu-mobile {
+    z-index: 9999 !important;
+}
+
+/* --- Dropdowns de grupo na nav desktop (abrem no hover) ---
+   Mesma armadilha do .hidden (ver bloco "Hidden" mais abaixo): n├úo d├í
+   pra usar a classe hidden do Tailwind + :hover pra isso, porque o
+   !important do workaround do Five Server venceria sempre ÔÇö por isso
+   uma classe pr├│pria, sem depender de .hidden. :focus-within cobre
+   navega├º├úo por teclado (tab at├® o bot├úo do grupo, ou direto num item
+   de dentro). */
+.nav-dropdown {
+    display: none;
+    /* z-index bem acima de qualquer outra coisa na p├ígina (as tabelas
+       com scroll usam no m├íximo 0, ver .overflow-auto abaixo) ÔÇö sem
+       isso, o menu abre por tr├ís da tabela em vez de por cima; ver
+       relato do usu├írio com print em 2026-09-02. !important porque o
+       Tailwind CDN injeta o .z-20 dele em runtime, ent├úo a ordem das
+       tags no <head> n├úo garante quem vence. */
+    z-index: 9999 !important;
+}
+.nav-dropdown-wrap:hover .nav-dropdown,
+.nav-dropdown-wrap:focus-within .nav-dropdown {
+    display: block;
+}
+
+/* Fechamento for├ºado logo depois de clicar num item: sem isso, o mouse
+   continua fisicamente em cima da ├írea ao navegar, e o :hover acima
+   mant├®m o dropdown aberto por cima da aba nova. abrirAba() (ui.js)
+   adiciona essa classe no wrapper no clique e s├│ a remove quando o
+   mouse de fato sai da ├írea (mouseleave), voltando ao comportamento
+   normal de hover pra pr├│xima vez. Mesma especificidade da regra de
+   hover acima (3 seletores de classe cada) ÔÇö como esta vem depois no
+   arquivo, ela vence sem precisar de !important. */
+.nav-dropdown-wrap.dropdown-fechado-manual .nav-dropdown {
+    display: none;
+}
+
+/* Blindagem contra bug de empilhamento entre os dropdowns da nav e os
+   cont├¬ineres de tabela com scroll (.overflow-auto, usado em Livros,
+   Colet├óneas, Partes etc.) ÔÇö sem z-index/position expl├¡citos, esses
+   cont├¬ineres (que tamb├®m t├¬m `rounded` + `shadow`) ficam com camada
+   de composi├º├úo impl├¡cita, e alguns navegadores erram a ordem de
+   pintura e desenham a tabela por cima do dropdown mesmo ele tendo
+   z-index maior. Fixando z-index:0 expl├¡cito aqui tira essa
+   ambiguidade ÔÇö visualmente n├úo muda nada (n├úo move nada na tela),
+   s├│ garante que o dropdown (9999 acima) sempre ven├ºa. */
+.overflow-auto {
+    position: relative;
+    z-index: 0;
+}
+
+/* Indicador do grupo ativo (quando a aba aberta mora dentro dele) ÔÇö
+   mesmo visual do .active-tab, s├│ que no bot├úo do grupo em vez de
+   cada item. */
+.nav-grupo-ativo {
+    border-bottom: 3px solid #1d4ed8;
+    color: #1d4ed8;
+    font-weight: 700;
+}
+
+/* Item dentro do dropdown: ├® uma lista vertical, ent├úo a borda embaixo
+   do .active-tab (pensada pra nav horizontal) fica estranha ÔÇö troca
+   por fundo destacado, mesmo padr├úo do painel de tema (theme.js). */
+.nav-dropdown .active-tab {
+    border-bottom: none;
+    background-color: #eff6ff;
+}
+html.dark .nav-dropdown .active-tab {
+    background-color: rgba(29, 78, 216, 0.25);
+}
+
+/* Hover nos itens do dropdown: mesmo par cinza (hover) / azul (ativo)
+   j├í usado no menu mobile (ver renderMenuMobile em ui.js), pra manter
+   os dois hovers consistentes e n├úo deixar o item sob o mouse parecer
+   a aba j├í aberta. :not(.active-tab) evita que o cinza sobreponha o
+   fundo azul do item ativo ao passar o mouse por cima dele. */
+.nav-dropdown .tab-btn {
+    transition: background-color 150ms ease;
+}
+.nav-dropdown .tab-btn:hover:not(.active-tab) {
+    background-color: #f9fafb;
+}
+html.dark .nav-dropdown .tab-btn:hover:not(.active-tab) {
+    background-color: #334155;
+}
+
 /* --- Formul├írios --- */
 .form-label {
     font-size: 0.75rem;
@@ -147,6 +238,20 @@ textarea:focus {
     display: none !important;
 }
 
+/* --- Nav de abas (desktop) ---
+   Abaixo do breakpoint md (768px) ela cede lugar ao menu hamb├║rguer
+   (index.html/js/ui.js). N├úo pode reaproveitar a classe .hidden do
+   Tailwind (+ "md:flex") pra isso: o !important do .hidden acima
+   (necess├írio pro workaround do Five Server) vence o md:flex em
+   qualquer largura, deixando a nav escondida pra sempre em vez de s├│
+   abaixo do breakpoint. Por isso um seletor pr├│prio, sem !important,
+   escondido s├│ dentro do media query. */
+@media (max-width: 767px) {
+    .nav-abas-desktop {
+        display: none;
+    }
+}
+
 /* --- Grupos de campos colaps├íveis (ex.: modal de Poema/Prosa) ---
    <details class="campo-grupo"><summary>R├│tulo</summary><div class="campo-grupo-corpo">...</div></details>
    Reaproveita o padr├úo nativo <details>/<summary> j├í usado em coletaneas.js
@@ -256,3 +361,68 @@ html.dark ::-webkit-scrollbar-thumb {
 html.dark ::-webkit-scrollbar-thumb:hover {
     background: #64748b;
 }
+
+/* --- Diagrama de Refer├¬ncias (aba Conex├Áes) ---
+   Grafo desenhado em SVG puro, embutido via innerHTML (sem lib de
+   layout). Cores via classe CSS, n├úo via JS/tema ÔÇö mesmo padr├úo do
+   .nav-dropdown/.campo-grupo acima: o SVG j├í embutido reage sozinho
+   ├á troca de tema (theme.js s├│ alterna a classe `dark` na <html>),
+   sem precisar recalcular nada em JS a cada troca (diferente do
+   gr├ífico de Estat├¡sticas, que usa Chart.js/canvas e por isso *precisa*
+   ler o tema em JS ÔÇö ver coment├írio em estatisticas.js). */
+.grafo-no {
+    cursor: pointer;
+}
+
+.grafo-no-caixa {
+    fill: #ffffff;
+    stroke: #e5e7eb;
+    stroke-width: 1;
+}
+html.dark .grafo-no-caixa {
+    fill: #0f172a; /* slate-900, mesmo tom dos cards da aba */
+    stroke: #334155; /* slate-700 */
+}
+
+.grafo-no-titulo {
+    fill: #374151; /* gray-700 */
+}
+html.dark .grafo-no-titulo {
+    fill: #cbd5e1; /* slate-300 */
+}
+.grafo-no:hover .grafo-no-titulo {
+    fill: #1d4ed8; /* mesmo azul do .active-tab */
+    text-decoration: underline;
+}
+html.dark .grafo-no:hover .grafo-no-titulo {
+    fill: #60a5fa;
+}
+
+.grafo-aresta-linha {
+    stroke: #d1d5db; /* gray-300 */
+    stroke-width: 1.5;
+}
+html.dark .grafo-aresta-linha {
+    stroke: #475569; /* slate-600 */
+}
+
+.grafo-aresta-marcador {
+    fill: #d1d5db;
+}
+html.dark .grafo-aresta-marcador {
+    fill: #475569;
+}
+
+.grafo-aresta-rotulo-fundo {
+    fill: #fae8ff; /* fuchsia-100 ÔÇö mesmo badge usado no resto da aba Conex├Áes */
+}
+html.dark .grafo-aresta-rotulo-fundo {
+    fill: #701a75; /* fuchsia-900 */
+}
+
+.grafo-aresta-rotulo-texto {
+    fill: #a21caf; /* fuchsia-700 */
+}
+html.dark .grafo-aresta-rotulo-texto {
+    fill: #f0abfc; /* fuchsia-300 */
+}
diff --git a/index.html b/index.html
index 0dc8bab..4ce3b7c 100644
--- a/index.html
+++ b/index.html
@@ -16,6 +16,12 @@
             })();
         </script>
         <script src="https://cdn.tailwindcss.com"></script>
+        <!-- jsPDF: s├│ usada pelo bot├úo "Baixar em .pdf" da coluna A├º├Áes de
+             Poemas/Prosas e do modal de Visualiza├º├úo (ver js/exportar-pdf.js).
+             Carregada via CDN como o Tailwind acima ÔÇö precisa de internet na
+             hora de gerar o PDF, mas em compensa├º├úo ├® um clique s├│ (sem a
+             etapa extra de "Salvar como PDF" da impress├úo do navegador). -->
+        <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
         <script>
             // Sem isso o Tailwind usa a variante "media" (segue s├│ o SO) e ignora
             // a classe .dark que a gente aplica manualmente no <html> ÔÇö o toggle
@@ -54,11 +60,6 @@
                                 class="hidden absolute right-0 mt-1 w-36 bg-white dark:bg-slate-900 border border-gray-200 dark:border-slate-700 rounded-lg shadow-lg p-1 z-20"
                             ></div>
                         </div>
-                        <a
-                            href="filtrar.html"
-                            class="border border-gray-300 dark:border-slate-600 text-gray-600 dark:text-slate-300 px-4 py-2 rounded-md hover:bg-gray-50 dark:hover:bg-slate-700 transition text-sm font-medium"
-                            >Vers├Áes Alternativas</a
-                        >
                         <input
                             type="file"
                             id="import-json"
@@ -72,32 +73,38 @@
                         >
                             Importar JSON
                         </button>
-                        <label
-                            class="flex items-center gap-1.5 text-xs text-gray-500 dark:text-slate-400 border border-gray-300 dark:border-slate-600 rounded-md px-2.5 py-2 cursor-pointer hover:bg-gray-50 dark:hover:bg-slate-700 transition"
-                            title="Embute as imagens de capa no pr├│prio arquivo. Sem isso, um backup restaurado num navegador zerado perde as capas (s├│ o texto volta) ÔÇö mas o arquivo fica bem mais leve."
+                        <div
+                            class="flex items-stretch rounded-md overflow-hidden border border-green-600 dark:border-green-500"
                         >
-                            <input
-                                type="checkbox"
-                                id="chk-incluir-capas"
-                                checked
-                                onchange="
-                                    localStorage.setItem(
-                                        'arquivoPoetico_incluirCapasBackup',
-                                        this.checked,
+                            <button
+                                onclick="
+                                    exportarJSON(
+                                        document.getElementById('chk-incluir-capas').checked,
                                     )
                                 "
-                                style="width: 13px; height: 13px; margin: 0"
-                            />
-                            capas
-                        </label>
-                        <button
-                            onclick="
-                                exportarJSON(document.getElementById('chk-incluir-capas').checked)
-                            "
-                            class="bg-green-600 text-white px-4 py-2 rounded-md hover:bg-green-700 dark:hover:bg-green-500 transition text-sm font-medium"
-                        >
-                            Baixar JSON
-                        </button>
+                                class="bg-green-600 text-white px-4 py-2 hover:bg-green-700 dark:hover:bg-green-500 transition text-sm font-medium"
+                            >
+                                Baixar JSON
+                            </button>
+                            <label
+                                class="flex items-center gap-1.5 text-xs text-green-700 dark:text-green-200 bg-green-50 dark:bg-green-950 px-2.5 cursor-pointer hover:bg-green-100 dark:hover:bg-green-900 transition border-l border-green-600 dark:border-green-500"
+                                title="Embute as imagens de capa no pr├│prio arquivo. Sem isso, um backup restaurado num navegador zerado perde as capas (s├│ o texto volta) ÔÇö mas o arquivo fica bem mais leve."
+                            >
+                                <input
+                                    type="checkbox"
+                                    id="chk-incluir-capas"
+                                    checked
+                                    onchange="
+                                        localStorage.setItem(
+                                            'arquivoPoetico_incluirCapasBackup',
+                                            this.checked,
+                                        )
+                                    "
+                                    style="width: 13px; height: 13px; margin: 0"
+                                />
+                                capas
+                            </label>
+                        </div>
                         <button
                             onclick="exportarTudoAninhado()"
                             title="Acervo inteiro em ├írvore (Livro ÔåÆ Parte ÔåÆ Se├º├úo ÔåÆ Poema/Prosa), sem filtro ÔÇö ideal pra mandar pra uma IA processar de uma vez"
@@ -131,92 +138,250 @@
             </div>
         </header>
 
-        <!-- Datalists compartilhadas entre a p├ígina principal e o modal de
-         Poema (carregado sob demanda). Ficam aqui pra funcionar mesmo
-         antes do modal de Poema ser aberto pela primeira vez. -->
+        <!-- Datalists compartilhadas entre a p├ígina principal e os modais
+         de Poema/Prosa (carregados sob demanda). Ficam aqui pra funcionar
+         mesmo antes do modal ser aberto pela primeira vez. Uma por
+         categoria de Sinaliza├º├Áes (ver SINAL_CATEGORIAS em editor.js),
+         em 4 variantes (poema/prosa ├ù normal/bulk) ÔÇö "sugestoes-sinais"
+         sem sufixo ├® a combinada das 6 categorias, usada pelo painel de
+         exporta├º├úo seletiva. -->
         <datalist id="sugestoes-sinais"></datalist>
         <datalist id="sugestoes-pessoas"></datalist>
-        <datalist id="sugestoes-sinais-bulk-prosa"></datalist>
+        <datalist id="sugestoes-autores"></datalist>
+        <datalist id="sugestoes-sinais-estilo"></datalist>
+        <datalist id="sugestoes-sinais-estilo-bulk"></datalist>
+        <datalist id="sugestoes-sinais-estilo-bulk-prosa"></datalist>
+        <datalist id="sugestoes-sinais-tema"></datalist>
+        <datalist id="sugestoes-sinais-tema-bulk"></datalist>
+        <datalist id="sugestoes-sinais-tema-bulk-prosa"></datalist>
+        <datalist id="sugestoes-sinais-relacao"></datalist>
+        <datalist id="sugestoes-sinais-relacao-bulk"></datalist>
+        <datalist id="sugestoes-sinais-relacao-bulk-prosa"></datalist>
+        <datalist id="sugestoes-sinais-sensibilidade"></datalist>
+        <datalist id="sugestoes-sinais-sensibilidade-bulk"></datalist>
+        <datalist id="sugestoes-sinais-sensibilidade-bulk-prosa"></datalist>
+        <datalist id="sugestoes-sinais-tom"></datalist>
+        <datalist id="sugestoes-sinais-tom-bulk"></datalist>
+        <datalist id="sugestoes-sinais-tom-bulk-prosa"></datalist>
+        <datalist id="sugestoes-sinais-outros"></datalist>
+        <datalist id="sugestoes-sinais-outros-bulk"></datalist>
+        <datalist id="sugestoes-sinais-outros-bulk-prosa"></datalist>
         <datalist id="sugestoes-pessoas-bulk-prosa"></datalist>
         <datalist id="sugestoes-genero-bulk-prosa"></datalist>
 
         <main class="max-w-6xl mx-auto px-4">
-            <nav
-                class="flex border-b mb-8 space-x-8 overflow-x-auto border-gray-200 dark:border-slate-700"
-            >
-                <button
-                    id="btn-livros"
-                    onclick="openTab('livros')"
-                    class="tab-btn pb-2 font-medium active-tab whitespace-nowrap"
-                >
-                    Livros
-                </button>
-                <button
-                    id="btn-coletaneas"
-                    onclick="openTab('coletaneas')"
-                    class="tab-btn pb-2 font-medium whitespace-nowrap"
-                >
-                    Colet├óneas
-                </button>
-                <button
-                    id="btn-partes"
-                    onclick="openTab('partes')"
-                    class="tab-btn pb-2 font-medium whitespace-nowrap"
-                >
-                    Partes
-                </button>
-                <button
-                    id="btn-secoes"
-                    onclick="openTab('secoes')"
-                    class="tab-btn pb-2 font-medium whitespace-nowrap"
-                >
-                    Se├º├Áes
-                </button>
-                <button
-                    id="btn-poemas"
-                    onclick="openTab('poemas')"
-                    class="tab-btn pb-2 font-medium whitespace-nowrap"
-                >
-                    Poemas
-                </button>
-                <button
-                    id="btn-prosas"
-                    onclick="openTab('prosas')"
-                    class="tab-btn pb-2 font-medium whitespace-nowrap"
-                >
-                    Prosas
-                </button>
+            <!-- Mobile (abaixo de md): bot├úo hamb├║rguer + painel dropdown
+             agrupado, montado em JS a partir de GRUPOS_NAV (ui.js) ÔÇö mesma
+             lista de abas da nav desktop abaixo, s├│ numa lente diferente.
+             Desktop (md+): nav horizontal de sempre, agora com divisores
+             visuais entre os 4 grupos. -->
+            <div class="relative md:hidden mb-8">
                 <button
-                    id="btn-elementos"
-                    onclick="openTab('elementos')"
-                    class="tab-btn pb-2 font-medium whitespace-nowrap"
-                >
-                    Elementos Textuais
-                </button>
-                <button
-                    id="btn-estrutura"
-                    onclick="openTab('estrutura')"
-                    class="tab-btn pb-2 font-medium whitespace-nowrap"
+                    id="btn-menu-mobile"
+                    type="button"
+                    onclick="toggleMenuMobile()"
+                    class="w-full flex items-center gap-2 border border-gray-300 dark:border-slate-600 text-gray-700 dark:text-slate-200 px-3 py-2 rounded-md hover:bg-gray-50 dark:hover:bg-slate-700 transition text-sm font-medium"
                 >
-                    Estrutura
+                    <span id="icone-menu-mobile" class="text-base leading-none">Ôÿ░</span>
+                    <span id="rotulo-aba-atual">Livros</span>
                 </button>
+                <div
+                    id="painel-menu-mobile"
+                    class="hidden md:hidden absolute left-0 right-0 mt-1 bg-white dark:bg-slate-900 border border-gray-200 dark:border-slate-700 rounded-lg shadow-lg p-2 z-20 max-h-[70vh] overflow-y-auto"
+                ></div>
+            </div>
+
+            <!-- Nav desktop: 3 grupos com dropdown (abrem no hover ÔÇö ver
+             .nav-dropdown em style.css) + Exporta├º├úo solta. Cada dropdown
+             marca o bot├úo do grupo como ativo (.nav-grupo-ativo) quando a
+             aba aberta mora nele ÔÇö ver atualizarGrupoAtivoNavDesktop em
+             ui.js. -->
+            <nav
+                class="nav-abas-desktop md:flex flex-wrap items-start gap-x-6 gap-y-2 mb-8 pb-2 border-b border-gray-200 dark:border-slate-700"
+            >
+                <div class="relative nav-dropdown-wrap">
+                    <button
+                        type="button"
+                        id="grupo-estrutura-acervo-btn"
+                        class="grupo-nav-btn nav-grupo-ativo pb-2 font-medium whitespace-nowrap inline-flex items-center gap-1 hover:text-blue-700 dark:hover:text-blue-300"
+                    >
+                        Estrutura do acervo
+                        <span class="text-[9px] opacity-60">Ôû¥</span>
+                    </button>
+                    <div class="nav-dropdown absolute left-0 top-full pt-1 z-20 min-w-[190px]">
+                        <div
+                            class="flex flex-col gap-0.5 bg-white dark:bg-slate-900 border border-gray-200 dark:border-slate-700 rounded-lg shadow-lg p-1"
+                        >
+                            <button
+                                id="btn-livros"
+                                onclick="abrirAba('livros')"
+                                class="tab-btn active-tab text-left text-sm py-1.5 px-2 rounded-md whitespace-nowrap"
+                            >
+                                Livros
+                            </button>
+                            <button
+                                id="btn-coletaneas"
+                                onclick="abrirAba('coletaneas')"
+                                class="tab-btn text-left text-sm py-1.5 px-2 rounded-md whitespace-nowrap"
+                            >
+                                Colet├óneas
+                            </button>
+                            <button
+                                id="btn-partes"
+                                onclick="abrirAba('partes')"
+                                class="tab-btn text-left text-sm py-1.5 px-2 rounded-md whitespace-nowrap"
+                            >
+                                Partes
+                            </button>
+                            <button
+                                id="btn-secoes"
+                                onclick="abrirAba('secoes')"
+                                class="tab-btn text-left text-sm py-1.5 px-2 rounded-md whitespace-nowrap"
+                            >
+                                Se├º├Áes
+                            </button>
+                            <button
+                                id="btn-pessoas"
+                                onclick="abrirAba('pessoas')"
+                                class="tab-btn text-left text-sm py-1.5 px-2 rounded-md whitespace-nowrap"
+                            >
+                                Pessoas
+                            </button>
+                            <button
+                                id="btn-grupos"
+                                onclick="abrirAba('grupos')"
+                                class="tab-btn text-left text-sm py-1.5 px-2 rounded-md whitespace-nowrap"
+                            >
+                                Grupos
+                            </button>
+                            <button
+                                id="btn-autores"
+                                onclick="abrirAba('autores')"
+                                class="tab-btn text-left text-sm py-1.5 px-2 rounded-md whitespace-nowrap"
+                            >
+                                Autores
+                            </button>
+                            <button
+                                id="btn-epocas"
+                                onclick="abrirAba('epocas')"
+                                class="tab-btn text-left text-sm py-1.5 px-2 rounded-md whitespace-nowrap"
+                            >
+                                ├ëpocas
+                            </button>
+                        </div>
+                    </div>
+                </div>
+
+                <div class="relative nav-dropdown-wrap">
+                    <button
+                        type="button"
+                        id="grupo-conteudo-btn"
+                        class="grupo-nav-btn pb-2 font-medium whitespace-nowrap inline-flex items-center gap-1 hover:text-blue-700 dark:hover:text-blue-300"
+                    >
+                        Conte├║do
+                        <span class="text-[9px] opacity-60">Ôû¥</span>
+                    </button>
+                    <div class="nav-dropdown absolute left-0 top-full pt-1 z-20 min-w-[190px]">
+                        <div
+                            class="flex flex-col gap-0.5 bg-white dark:bg-slate-900 border border-gray-200 dark:border-slate-700 rounded-lg shadow-lg p-1"
+                        >
+                            <button
+                                id="btn-poemas"
+                                onclick="abrirAba('poemas')"
+                                class="tab-btn text-left text-sm py-1.5 px-2 rounded-md whitespace-nowrap"
+                            >
+                                Poemas
+                            </button>
+                            <button
+                                id="btn-prosas"
+                                onclick="abrirAba('prosas')"
+                                class="tab-btn text-left text-sm py-1.5 px-2 rounded-md whitespace-nowrap"
+                            >
+                                Prosas
+                            </button>
+                            <button
+                                id="btn-elementos"
+                                onclick="abrirAba('elementos')"
+                                class="tab-btn text-left text-sm py-1.5 px-2 rounded-md whitespace-nowrap"
+                            >
+                                Elementos Textuais
+                            </button>
+                        </div>
+                    </div>
+                </div>
+
+                <div class="relative nav-dropdown-wrap">
+                    <button
+                        type="button"
+                        id="grupo-analise-btn"
+                        class="grupo-nav-btn pb-2 font-medium whitespace-nowrap inline-flex items-center gap-1 hover:text-blue-700 dark:hover:text-blue-300"
+                    >
+                        An├ílise
+                        <span class="text-[9px] opacity-60">Ôû¥</span>
+                    </button>
+                    <div class="nav-dropdown absolute left-0 top-full pt-1 z-20 min-w-[190px]">
+                        <div
+                            class="flex flex-col gap-0.5 bg-white dark:bg-slate-900 border border-gray-200 dark:border-slate-700 rounded-lg shadow-lg p-1"
+                        >
+                            <button
+                                id="btn-estrutura"
+                                onclick="abrirAba('estrutura')"
+                                class="tab-btn text-left text-sm py-1.5 px-2 rounded-md whitespace-nowrap"
+                            >
+                                Estrutura
+                            </button>
+                            <button
+                                id="btn-conexoes"
+                                onclick="abrirAba('conexoes')"
+                                class="tab-btn text-left text-sm py-1.5 px-2 rounded-md whitespace-nowrap"
+                            >
+                                Conex├Áes
+                            </button>
+                            <button
+                                id="btn-estatisticas"
+                                onclick="abrirAba('estatisticas')"
+                                class="tab-btn text-left text-sm py-1.5 px-2 rounded-md whitespace-nowrap"
+                            >
+                                Estat├¡sticas
+                            </button>
+                        </div>
+                    </div>
+                </div>
+
                 <button
                     id="btn-exportar-filtrado"
-                    onclick="openTab('exportar-filtrado')"
+                    onclick="abrirAba('exportar-filtrado')"
                     class="tab-btn pb-2 font-medium whitespace-nowrap"
                 >
                     Exporta├º├úo
                 </button>
-                <button
-                    id="btn-estatisticas"
-                    onclick="
-                        openTab('estatisticas');
-                        renderEstatisticas();
-                    "
-                    class="tab-btn pb-2 font-medium whitespace-nowrap"
-                >
-                    Estat├¡sticas
-                </button>
+
+                <div class="relative nav-dropdown-wrap">
+                    <button
+                        type="button"
+                        id="grupo-ferramentas-btn"
+                        class="grupo-nav-btn pb-2 font-medium whitespace-nowrap inline-flex items-center gap-1 hover:text-blue-700 dark:hover:text-blue-300"
+                    >
+                        Ferramentas
+                        <span class="text-[9px] opacity-60">Ôû¥</span>
+                    </button>
+                    <div class="nav-dropdown absolute left-0 top-full pt-1 z-20 min-w-[190px]">
+                        <div
+                            class="flex flex-col gap-0.5 bg-white dark:bg-slate-900 border border-gray-200 dark:border-slate-700 rounded-lg shadow-lg p-1"
+                        >
+                            <a
+                                href="localizar-substituir.html"
+                                class="tab-btn text-left text-sm py-1.5 px-2 rounded-md whitespace-nowrap block"
+                                >Localizar e Substituir</a
+                            >
+                            <a
+                                href="filtrar.html"
+                                class="tab-btn text-left text-sm py-1.5 px-2 rounded-md whitespace-nowrap block"
+                                >Vers├Áes Alternativas</a
+                            >
+                        </div>
+                    </div>
+                </div>
             </nav>
 
             <section id="livros" class="tab-content active">
@@ -326,6 +491,58 @@
                 <div id="lista-secoes" class="grid grid-cols-1 md:grid-cols-2 gap-4"></div>
             </section>
 
+            <section id="pessoas" class="tab-content">
+                <div class="flex justify-between items-center mb-6">
+                    <h2 class="text-xl font-semibold">Pessoas</h2>
+                    <button
+                        onclick="prepararNovo('pessoa')"
+                        class="bg-blue-600 text-white px-4 py-2 rounded shadow hover:bg-blue-700 dark:hover:bg-blue-500"
+                    >
+                        Nova Pessoa
+                    </button>
+                </div>
+                <div id="lista-pessoas" class="grid grid-cols-1 md:grid-cols-2 gap-4"></div>
+            </section>
+
+            <section id="grupos" class="tab-content">
+                <div class="flex justify-between items-center mb-6">
+                    <h2 class="text-xl font-semibold">Grupos</h2>
+                    <button
+                        onclick="prepararNovo('grupo')"
+                        class="bg-blue-600 text-white px-4 py-2 rounded shadow hover:bg-blue-700 dark:hover:bg-blue-500"
+                    >
+                        Novo Grupo
+                    </button>
+                </div>
+                <div id="lista-grupos" class="grid grid-cols-1 md:grid-cols-2 gap-4"></div>
+            </section>
+
+            <section id="autores" class="tab-content">
+                <div class="flex justify-between items-center mb-6">
+                    <h2 class="text-xl font-semibold">Autores</h2>
+                    <button
+                        onclick="prepararNovo('autor')"
+                        class="bg-blue-600 text-white px-4 py-2 rounded shadow hover:bg-blue-700 dark:hover:bg-blue-500"
+                    >
+                        Novo Autor
+                    </button>
+                </div>
+                <div id="lista-autores" class="grid grid-cols-1 md:grid-cols-2 gap-4"></div>
+            </section>
+
+            <section id="epocas" class="tab-content">
+                <div class="flex justify-between items-center mb-6">
+                    <h2 class="text-xl font-semibold">├ëpocas</h2>
+                    <button
+                        onclick="prepararNovo('epoca')"
+                        class="bg-blue-600 text-white px-4 py-2 rounded shadow hover:bg-blue-700 dark:hover:bg-blue-500"
+                    >
+                        Nova ├ëpoca
+                    </button>
+                </div>
+                <div id="lista-epocas" class="grid grid-cols-1 md:grid-cols-2 gap-4"></div>
+            </section>
+
             <section id="poemas" class="tab-content">
                 <div class="flex justify-between items-center mb-6">
                     <h2 class="text-xl font-semibold">Conte├║do Liter├írio</h2>
@@ -383,6 +600,7 @@
                         <option value="completos">S├│ completos ÔÜ¬</option>
                         <option value="incompletos">S├│ incompletos ­ƒƒí</option>
                         <option value="migrados">S├│ migrados ­ƒöÁ</option>
+                        <option value="pendentes">S├│ pendentes ­ƒƒá</option>
                         <option value="descartados">S├│ descartados ­ƒö┤</option>
                     </select>
                     <select
@@ -396,45 +614,76 @@
 
                 <div
                     id="ajuda-busca-poemas"
-                    class="hidden mb-4 -mt-2 p-3 bg-blue-50 dark:bg-blue-950 border border-blue-100 dark:border-blue-900 text-blue-800 dark:text-blue-200 text-xs rounded-lg"
+                    class="hidden mb-4 -mt-2 p-3 bg-blue-50 dark:bg-blue-950 border border-blue-100 dark:border-blue-900 text-blue-800 dark:text-blue-200 text-xs rounded-lg space-y-3"
                 >
-                    <ul class="list-disc pl-4 space-y-1">
-                        <li>
-                            O primeiro campo busca <strong>metadados</strong> (t├¡tulo, ano, etiqueta,
-                            pessoa, livro); o segundo busca s├│ o <strong>conte├║do</strong> do texto
-                            (versos/corpo do poema).
-                        </li>
-                        <li>
-                            O bot├úo <strong>E / OU</strong> entre os dois campos decide se um poema
-                            precisa bater nos dois pra aparecer (E) ou se basta bater em um deles
-                            (OU) ÔÇö s├│ faz diferen├ºa quando os dois campos t├¬m algo digitado.
-                        </li>
-                        <li>Separe os termos por espa├ºo pra combinar ÔÇö todos precisam aparecer.</li>
-                        <li>
-                            Use a palavra <code>ou</code> entre termos pra pedir alternativas dentro
-                            do mesmo campo: <code>Dalton ou Gabriela</code>.
-                        </li>
-                        <li>Use aspas pra buscar a frase exata: <code>"beira do mar"</code>.</li>
-                        <li>
-                            Prefixe com <code>-</code> pra excluir: <code>-rascunho</code> ou
-                            <code>-2023</code> ÔÇö a exclus├úo vale sempre, mesmo dos dois lados de um
-                            <code>ou</code>.
-                        </li>
-                        <li>
-                            No campo de metadados, n├úo usa data de escrita/publica├º├úo (pra isso, veja
-                            o filtro de data logo abaixo).
-                        </li>
-                        <li>
-                            No campo de metadados, pra restringir a um atributo espec├¡fico, prefixe o
-                            termo com o nome dele e dois-pontos: <code>titulo:</code>,
-                            <code>etiqueta:</code>, <code>pessoa:</code>, <code>nota:</code>,
-                            <code>livro:</code>, <code>parte:</code>, <code>secao:</code>.
-                        </li>
-                        <li>
-                            Exemplos: <code>pessoa:Dalton</code>,
-                            <code>secao:"Fragmentos do Fim"</code>, <code>-etiqueta:rascunho</code>.
-                        </li>
-                    </ul>
+                    <div>
+                        <p class="font-bold mb-1">Sintaxe</p>
+                        <ul class="list-disc pl-4 space-y-1">
+                            <li>
+                                Primeiro campo busca <strong>metadados</strong>; segundo busca s├│ o
+                                <strong>conte├║do</strong> do texto (versos/corpo do poema). O bot├úo
+                                <strong>E / OU</strong> entre os dois decide se precisa bater nos
+                                dois (E) ou s├│ num deles (OU).
+                            </li>
+                            <li>
+                                Espa├ºo = E l├│gico; <code>ou</code> = alternativa (<code
+                                    >Dalton ou Gabriela</code
+                                >); aspas = frase exata (<code>"beira do mar"</code>);
+                                <code>-termo</code> exclui.
+                            </li>
+                            <li>
+                                Acento n├úo importa, nem no termo nem no prefixo ÔÇö
+                                <code>arvore</code> acha "├írvore", <code>sensivel:</code> e
+                                <code>sens├¡vel:</code> d├úo no mesmo.
+                            </li>
+                            <li>
+                                N├úo inclui data de escrita/publica├º├úo ÔÇö use o filtro de data logo
+                                abaixo.
+                            </li>
+                        </ul>
+                    </div>
+                    <div>
+                        <p class="font-bold mb-1">Restringir a um campo</p>
+                        <p class="mb-1">
+                            Prefixe o termo com <code>campo:</code>. A maioria dos campos j├í tem
+                            lupa ­ƒöì no cabe├ºalho da coluna ÔÇö clique nela em vez de digitar. S├│ estes
+                            aqui n├úo t├¬m coluna pr├│pria:
+                        </p>
+                        <ul class="list-disc pl-4 space-y-1">
+                            <li>
+                                <code>estilo:</code> <code>tema:</code> <code>relacao:</code>
+                                <code>tom:</code> <code>sensibilidade:</code> <code>outros:</code> ÔÇö
+                                categorias de Etiqueta (a coluna Etiquetas mostra todas juntas; use
+                                um destes pra restringir a s├│ uma categoria).
+                            </li>
+                            <li>
+                                <code>livro:</code> <code>parte:</code> <code>secao:</code> ÔÇö
+                                posi├º├úo na estrutura.
+                            </li>
+                            <li>
+                                <code>papel:</code> ÔÇö restringe aos pap├®is marcados nas pessoas do
+                                texto (ex.: <code>papel:dedicat├íria</code>), sem olhar nome ÔÇö a
+                                coluna Pessoas (e o prefixo <code>pessoa:</code>) j├í busca nome e
+                                papel juntos; use este quando quiser s├│ o papel.
+                            </li>
+                        </ul>
+                    </div>
+                    <div>
+                        <p class="font-bold mb-1">Presen├ºa de campo</p>
+                        <p>
+                            <code>campo:*</code> (asterisco sozinho) acha quem tem aquele campo
+                            preenchido, n├úo importa com o qu├¬ ÔÇö ex.: <code>sensivel:*</code>.
+                            <code>-campo:*</code> inverte: s├│ quem est├í com o campo vazio.
+                        </p>
+                    </div>
+                    <div>
+                        <p class="font-bold mb-1">Exemplos</p>
+                        <p>
+                            <code>pessoa:Dalton</code>, <code>papel:"melhor amiga"</code>,
+                            <code>grupo:Fam├¡lia</code>, <code>secao:"Fragmentos do Fim"</code>,
+                            <code>-etiqueta:rascunho</code>, <code>sensivel:*</code>
+                        </p>
+                    </div>
                 </div>
 
                 <div class="mb-4">
@@ -710,18 +959,33 @@
                     </div>
                 </div>
 
-                <div class="mb-4">
-                    <button
-                        type="button"
-                        onclick="togglePainel('painel-colunas-poemas', this)"
-                        class="text-xs font-bold text-blue-600 dark:text-blue-400 hover:underline"
-                    >
-                        ­ƒº▒ Colunas Ôû¥
-                    </button>
-                    <div
-                        id="painel-colunas-poemas"
-                        class="hidden mt-2 p-3 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-lg flex flex-wrap gap-x-4"
-                    ></div>
+                <div class="mb-4 flex flex-wrap gap-x-6 gap-y-2">
+                    <div>
+                        <button
+                            type="button"
+                            onclick="togglePainel('painel-colunas-poemas', this)"
+                            class="text-xs font-bold text-blue-600 dark:text-blue-400 hover:underline"
+                        >
+                            ­ƒº▒ Colunas Ôû¥
+                        </button>
+                        <div
+                            id="painel-colunas-poemas"
+                            class="hidden mt-2 p-3 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-lg flex flex-wrap gap-x-4"
+                        ></div>
+                    </div>
+                    <div>
+                        <button
+                            type="button"
+                            onclick="togglePainel('painel-acoes-poemas', this)"
+                            class="text-xs font-bold text-blue-600 dark:text-blue-400 hover:underline"
+                        >
+                            ÔÜÖ´©Å A├º├Áes Ôû¥
+                        </button>
+                        <div
+                            id="painel-acoes-poemas"
+                            class="hidden mt-2 p-3 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-lg"
+                        ></div>
+                    </div>
                 </div>
 
                 <div
@@ -751,11 +1015,24 @@
                     >
                         ÔêÆ Remover
                     </button>
+                    <select
+                        id="bulk-sinal-categoria"
+                        onchange="atualizarListaSinalBulk(this, 'bulk-sinal-input')"
+                        class="text-xs mb-0"
+                        style="max-width: 140px"
+                    >
+                        <option value="sinalizacoesEstilo">Estilo</option>
+                        <option value="sinalizacoesTema">Tema</option>
+                        <option value="sinalizacoesRelacao">Rela├º├úo</option>
+                        <option value="sinalizacoesSensibilidade">Sensibilidade</option>
+                        <option value="sinalizacoesTom">Tom</option>
+                        <option value="sinalizacoesOutros">Outros</option>
+                    </select>
                     <input
                         type="text"
                         id="bulk-sinal-input"
-                        list="sugestoes-sinais"
-                        placeholder="Sinaliza├º├úo/tema..."
+                        list="sugestoes-sinais-estilo-bulk"
+                        placeholder="Sinaliza├º├úo..."
                         class="flex-1 min-w-[160px] mb-0"
                     />
                     <button
@@ -789,6 +1066,12 @@
                     >
                         Ô¼ç MD
                     </button>
+                    <button
+                        onclick="exportarSelecaoPoemasPdf()"
+                        class="bg-white dark:bg-slate-900 border border-teal-300 dark:border-teal-700 text-teal-700 dark:text-teal-300 px-3 py-2 rounded text-xs font-bold whitespace-nowrap"
+                    >
+                        Ô¼ç PDF
+                    </button>
                     <button
                         onclick="excluirSelecaoPoemas()"
                         class="bg-red-600 text-white px-3 py-2 rounded text-xs font-bold whitespace-nowrap ml-auto"
@@ -807,7 +1090,8 @@
                         class="hidden w-full mt-2 p-3 bg-white dark:bg-slate-900 border border-rose-200 dark:border-rose-800 rounded-lg flex flex-wrap items-end gap-2"
                     >
                         <div>
-                            <label class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
+                            <label
+                                class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
                                 >Aplicar em</label
                             >
                             <select id="bulk-data-tipo" class="text-xs p-2">
@@ -816,7 +1100,8 @@
                             </select>
                         </div>
                         <div>
-                            <label class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
+                            <label
+                                class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
                                 >Dia</label
                             >
                             <input
@@ -828,7 +1113,8 @@
                             />
                         </div>
                         <div>
-                            <label class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
+                            <label
+                                class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
                                 >M├¬s</label
                             >
                             <input
@@ -840,7 +1126,8 @@
                             />
                         </div>
                         <div>
-                            <label class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
+                            <label
+                                class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
                                 >Ano</label
                             >
                             <input
@@ -963,45 +1250,82 @@
 
                 <div
                     id="ajuda-busca-prosas"
-                    class="hidden mb-4 -mt-2 p-3 bg-blue-50 dark:bg-blue-950 border border-blue-100 dark:border-blue-900 text-blue-800 dark:text-blue-200 text-xs rounded-lg"
+                    class="hidden mb-4 -mt-2 p-3 bg-blue-50 dark:bg-blue-950 border border-blue-100 dark:border-blue-900 text-blue-800 dark:text-blue-200 text-xs rounded-lg space-y-3"
                 >
-                    <ul class="list-disc pl-4 space-y-1">
-                        <li>
-                            O primeiro campo busca <strong>metadados</strong> (t├¡tulo, ano, etiqueta,
-                            pessoa); o segundo busca s├│ o <strong>conte├║do</strong> do texto (corpo da
-                            prosa).
-                        </li>
-                        <li>
-                            O bot├úo <strong>E / OU</strong> entre os dois campos decide se uma prosa
-                            precisa bater nos dois pra aparecer (E) ou se basta bater em um deles
-                            (OU) ÔÇö s├│ faz diferen├ºa quando os dois campos t├¬m algo digitado.
-                        </li>
-                        <li>Separe os termos por espa├ºo pra combinar ÔÇö todos precisam aparecer.</li>
-                        <li>
-                            Use a palavra <code>ou</code> entre termos pra pedir alternativas dentro
-                            do mesmo campo: <code>Dalton ou Gabriela</code>.
-                        </li>
-                        <li>Use aspas pra buscar a frase exata: <code>"beira do mar"</code>.</li>
-                        <li>
-                            Prefixe com <code>-</code> pra excluir: <code>-rascunho</code> ou
-                            <code>-2023</code> ÔÇö a exclus├úo vale sempre, mesmo dos dois lados de um
-                            <code>ou</code>.
-                        </li>
-                        <li>
-                            No campo de metadados, n├úo usa data de escrita/publica├º├úo (pra isso, veja
-                            o filtro de data logo abaixo).
-                        </li>
-                        <li>
-                            No campo de metadados, pra restringir a um atributo espec├¡fico, prefixe o
-                            termo com o nome dele e dois-pontos: <code>titulo:</code>,
-                            <code>etiqueta:</code>, <code>pessoa:</code>, <code>nota:</code>,
-                            <code>livro:</code>, <code>parte:</code>, <code>secao:</code>.
-                        </li>
-                        <li>
-                            Exemplos: <code>pessoa:Dalton</code>,
-                            <code>secao:"Fragmentos do Fim"</code>, <code>-etiqueta:rascunho</code>.
-                        </li>
-                    </ul>
+                    <div>
+                        <p class="font-bold mb-1">Sintaxe</p>
+                        <ul class="list-disc pl-4 space-y-1">
+                            <li>
+                                Primeiro campo busca <strong>metadados</strong>; segundo busca s├│ o
+                                <strong>conte├║do</strong> do texto (corpo da prosa). O bot├úo
+                                <strong>E / OU</strong> entre os dois decide se precisa bater nos
+                                dois (E) ou s├│ num deles (OU).
+                            </li>
+                            <li>
+                                Espa├ºo = E l├│gico; <code>ou</code> = alternativa (<code
+                                    >Dalton ou Gabriela</code
+                                >); aspas = frase exata (<code>"beira do mar"</code>);
+                                <code>-termo</code> exclui.
+                            </li>
+                            <li>
+                                Acento n├úo importa, nem no termo nem no prefixo ÔÇö
+                                <code>arvore</code> acha "├írvore", <code>sensivel:</code> e
+                                <code>sens├¡vel:</code> d├úo no mesmo.
+                            </li>
+                            <li>
+                                N├úo inclui data de escrita/publica├º├úo ÔÇö use o filtro de data logo
+                                abaixo.
+                            </li>
+                        </ul>
+                    </div>
+                    <div>
+                        <p class="font-bold mb-1">Restringir a um campo</p>
+                        <p class="mb-1">
+                            Prefixe o termo com <code>campo:</code>. A maioria dos campos j├í tem
+                            lupa ­ƒöì no cabe├ºalho da coluna ÔÇö clique nela em vez de digitar. S├│ estes
+                            aqui n├úo t├¬m coluna pr├│pria:
+                        </p>
+                        <ul class="list-disc pl-4 space-y-1">
+                            <li>
+                                <code>estilo:</code> <code>tema:</code> <code>relacao:</code>
+                                <code>tom:</code> <code>sensibilidade:</code> <code>outros:</code> ÔÇö
+                                categorias de Etiqueta (a coluna Etiquetas mostra todas juntas; use
+                                um destes pra restringir a s├│ uma categoria).
+                            </li>
+                            <li>
+                                <code>livro:</code> <code>parte:</code> <code>secao:</code> ÔÇö
+                                posi├º├úo na estrutura.
+                            </li>
+                            <li>
+                                <code>papel:</code> ÔÇö restringe aos pap├®is marcados nas pessoas do
+                                texto (ex.: <code>papel:dedicat├íria</code>), sem olhar nome ÔÇö a
+                                coluna Pessoas (e o prefixo <code>pessoa:</code>) j├í busca nome e
+                                papel juntos; use este quando quiser s├│ o papel.
+                            </li>
+                        </ul>
+                        <p class="mt-1">
+                            Os demais campos de Poema (Oculta├º├úo, Conte├║do Sens├¡vel, Vocabul├írio
+                            Hiperacionante, Descarte, Contexto Hist├│rico etc.) ainda n├úo existem em
+                            Prosa ÔÇö ela ganha esses campos quando o item 4 do schema (paridade com
+                            Poema) for implementado.
+                        </p>
+                    </div>
+                    <div>
+                        <p class="font-bold mb-1">Presen├ºa de campo</p>
+                        <p>
+                            <code>campo:*</code> (asterisco sozinho) acha quem tem aquele campo
+                            preenchido, n├úo importa com o qu├¬ ÔÇö ex.: <code>etiqueta:*</code>.
+                            <code>-campo:*</code> inverte: s├│ quem est├í com o campo vazio.
+                        </p>
+                    </div>
+                    <div>
+                        <p class="font-bold mb-1">Exemplos</p>
+                        <p>
+                            <code>pessoa:Dalton</code>, <code>papel:"melhor amiga"</code>,
+                            <code>grupo:Fam├¡lia</code>, <code>secao:"Fragmentos do Fim"</code>,
+                            <code>-etiqueta:rascunho</code>, <code>etiqueta:*</code>
+                        </p>
+                    </div>
                 </div>
 
                 <div class="mb-4">
@@ -1190,18 +1514,33 @@
                     </div>
                 </div>
 
-                <div class="mb-4">
-                    <button
-                        type="button"
-                        onclick="togglePainel('painel-colunas-prosas', this)"
-                        class="text-xs font-bold text-blue-600 dark:text-blue-400 hover:underline"
-                    >
-                        ­ƒº▒ Colunas Ôû¥
-                    </button>
-                    <div
-                        id="painel-colunas-prosas"
-                        class="hidden mt-2 p-3 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-lg flex flex-wrap gap-x-4"
-                    ></div>
+                <div class="mb-4 flex flex-wrap gap-x-6 gap-y-2">
+                    <div>
+                        <button
+                            type="button"
+                            onclick="togglePainel('painel-colunas-prosas', this)"
+                            class="text-xs font-bold text-blue-600 dark:text-blue-400 hover:underline"
+                        >
+                            ­ƒº▒ Colunas Ôû¥
+                        </button>
+                        <div
+                            id="painel-colunas-prosas"
+                            class="hidden mt-2 p-3 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-lg flex flex-wrap gap-x-4"
+                        ></div>
+                    </div>
+                    <div>
+                        <button
+                            type="button"
+                            onclick="togglePainel('painel-acoes-prosas', this)"
+                            class="text-xs font-bold text-blue-600 dark:text-blue-400 hover:underline"
+                        >
+                            ÔÜÖ´©Å A├º├Áes Ôû¥
+                        </button>
+                        <div
+                            id="painel-acoes-prosas"
+                            class="hidden mt-2 p-3 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-lg"
+                        ></div>
+                    </div>
                 </div>
 
                 <div
@@ -1231,11 +1570,24 @@
                     >
                         ÔêÆ Remover
                     </button>
+                    <select
+                        id="bulk-sinal-categoria-prosa"
+                        onchange="atualizarListaSinalBulk(this, 'bulk-sinal-input-prosa', '-prosa')"
+                        class="text-xs mb-0"
+                        style="max-width: 140px"
+                    >
+                        <option value="sinalizacoesEstilo">Estilo</option>
+                        <option value="sinalizacoesTema">Tema</option>
+                        <option value="sinalizacoesRelacao">Rela├º├úo</option>
+                        <option value="sinalizacoesSensibilidade">Sensibilidade</option>
+                        <option value="sinalizacoesTom">Tom</option>
+                        <option value="sinalizacoesOutros">Outros</option>
+                    </select>
                     <input
                         type="text"
                         id="bulk-sinal-input-prosa"
-                        list="sugestoes-sinais-bulk-prosa"
-                        placeholder="Sinaliza├º├úo/tema..."
+                        list="sugestoes-sinais-estilo-bulk-prosa"
+                        placeholder="Sinaliza├º├úo..."
                         class="flex-1 min-w-[160px] mb-0"
                     />
                     <button
@@ -1288,6 +1640,12 @@
                     >
                         Ô¼ç MD
                     </button>
+                    <button
+                        onclick="exportarSelecaoProsasPdf()"
+                        class="bg-white dark:bg-slate-900 border border-teal-300 dark:border-teal-700 text-teal-700 dark:text-teal-300 px-3 py-2 rounded text-xs font-bold whitespace-nowrap"
+                    >
+                        Ô¼ç PDF
+                    </button>
                     <button
                         onclick="excluirSelecaoProsas()"
                         class="bg-red-600 text-white px-3 py-2 rounded text-xs font-bold whitespace-nowrap ml-auto"
@@ -1306,7 +1664,8 @@
                         class="hidden w-full mt-2 p-3 bg-white dark:bg-slate-900 border border-rose-200 dark:border-rose-800 rounded-lg flex flex-wrap items-end gap-2"
                     >
                         <div>
-                            <label class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
+                            <label
+                                class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
                                 >Aplicar em</label
                             >
                             <select id="bulk-data-tipo-prosa" class="text-xs p-2">
@@ -1315,7 +1674,8 @@
                             </select>
                         </div>
                         <div>
-                            <label class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
+                            <label
+                                class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
                                 >Dia</label
                             >
                             <input
@@ -1327,7 +1687,8 @@
                             />
                         </div>
                         <div>
-                            <label class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
+                            <label
+                                class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
                                 >M├¬s</label
                             >
                             <input
@@ -1339,7 +1700,8 @@
                             />
                         </div>
                         <div>
-                            <label class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
+                            <label
+                                class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
                                 >Ano</label
                             >
                             <input
@@ -1493,6 +1855,46 @@
                 </div>
             </section>
 
+            <section id="conexoes" class="tab-content">
+                <div class="flex justify-between items-center mb-6">
+                    <h2 class="text-xl font-semibold">Conex├Áes</h2>
+                </div>
+                <p class="text-xs text-gray-400 dark:text-slate-500 mb-6">
+                    Vis├úo computada sobre Elos e Refer├¬ncias (item 10 do plano de schema) ÔÇö n├úo ├®
+                    dona de nenhum dado, s├│ uma lente sobre o que j├í est├í cadastrado no campo
+                    "Elos"/"Refer├¬ncias" de cada poema. Pra editar um v├¡nculo, clique no t├¡tulo do
+                    poema pra abrir o modal normalmente.
+                </p>
+
+                <div class="mb-8">
+                    <h3 class="text-sm font-bold text-gray-600 dark:text-slate-300 mb-1">
+                        ÔÜá´©Å Buracos (Elos com s├│ um lado cadastrado)
+                    </h3>
+                    <p class="text-[11px] text-gray-400 dark:text-slate-500 mb-3">
+                        Mesmo c├ílculo do painel derivado do modal de Poema, s├│ que pra todos os
+                        poemas de uma vez.
+                    </p>
+                    <div
+                        id="conexoes-buracos"
+                        class="bg-white dark:bg-slate-900 rounded-xl border border-gray-200 dark:border-slate-700 p-4"
+                    ></div>
+                </div>
+
+                <div class="mb-8">
+                    <h3 class="text-sm font-bold text-gray-600 dark:text-slate-300 mb-3">
+                        Elos (pares e clusters)
+                    </h3>
+                    <div id="conexoes-elos" class="grid grid-cols-1 md:grid-cols-2 gap-4"></div>
+                </div>
+
+                <div>
+                    <h3 class="text-sm font-bold text-gray-600 dark:text-slate-300 mb-3">
+                        Refer├¬ncias (cadeias)
+                    </h3>
+                    <div id="conexoes-referencias" class="grid grid-cols-1 gap-4"></div>
+                </div>
+            </section>
+
             <section id="exportar-filtrado" class="tab-content">
                 <div class="flex justify-between items-center mb-6">
                     <h2 class="text-xl font-semibold">Exporta├º├úo</h2>
@@ -1630,11 +2032,13 @@
                             <option value="completos">S├│ completos (Poemas)</option>
                             <option value="incompletos">S├│ incompletos (Poemas)</option>
                             <option value="migrados">S├│ migrados (Poemas)</option>
+                            <option value="pendentes">S├│ pendentes (Poemas)</option>
                             <option value="descartados">S├│ descartados (Poemas)</option>
                         </select>
                         <p class="text-[10px] text-gray-400 dark:text-slate-500 mt-1">
-                            "Completos", "Incompletos", "Migrados" e "Descartados" s├│ existem em
-                            Poemas ÔÇö Prosas n├úo t├¬m essa distin├º├úo e ficam de fora dessas op├º├Áes.
+                            "Completos", "Incompletos", "Migrados", "Pendentes" e "Descartados" s├│
+                            existem em Poemas ÔÇö Prosas n├úo t├¬m essa distin├º├úo e ficam de fora dessas
+                            op├º├Áes.
                         </p>
                     </div>
 
@@ -1700,6 +2104,13 @@
                         >
                             Baixar .md seletivo
                         </button>
+                        <button
+                            onclick="executarExportacaoSeletivaPdf()"
+                            class="bg-slate-600 text-white px-4 py-2 rounded shadow font-bold text-sm"
+                            title="Mesma sele├º├úo acima, num PDF pronto pra ler ou imprimir"
+                        >
+                            Baixar .pdf seletivo
+                        </button>
                         <span
                             id="exp-resultado"
                             class="text-xs text-gray-500 dark:text-slate-400"
@@ -1734,13 +2145,19 @@
                                 onclick="exportarTudoFlatJson()"
                                 class="bg-blue-700 dark:bg-blue-600 text-white px-4 py-2 rounded shadow font-bold text-sm"
                             >
-                                Baixar tudo ÔÇö JSON flat
+                                Baixar tudo - JSON flat
                             </button>
                             <button
                                 onclick="exportarTudoFlatMarkdown()"
                                 class="bg-slate-700 dark:bg-slate-600 text-white px-4 py-2 rounded shadow font-bold text-sm"
                             >
-                                Baixar tudo ÔÇö .md
+                                Baixar tudo - .md
+                            </button>
+                            <button
+                                onclick="exportarTudoFlatPdf()"
+                                class="bg-slate-700 dark:bg-slate-600 text-white px-4 py-2 rounded shadow font-bold text-sm"
+                            >
+                                Baixar tudo - .pdf
                             </button>
                         </div>
                     </div>
diff --git a/js/busca-campo.js b/js/busca-campo.js
index 782152a..9d11b75 100644
--- a/js/busca-campo.js
+++ b/js/busca-campo.js
@@ -218,12 +218,13 @@ function substituirTudo() {
 
 // Fecha e limpa a barra ÔÇö remover `el` do DOM j├í derruba junto o
 // listener de keydown do substituirInput (n├úo ├® preciso remover ├á
-// m├úo); s├│ o listener no textarea, que fica fora de `el`, precisa de
-// removeEventListener expl├¡cito.
+// m├úo); s├│ os listeners no textarea, que ficam fora de `el`, precisam
+// de removeEventListener expl├¡cito.
 export function fecharBuscaEmCampo() {
     if (!barraAtual) return;
-    const { textarea, el, onKeydown } = barraAtual;
+    const { textarea, el, onKeydown, onPaste } = barraAtual;
     textarea.removeEventListener('keydown', onKeydown, true);
+    textarea.removeEventListener('paste', onPaste, true);
     el.remove();
     barraAtual = null;
     if (document.body.contains(textarea)) textarea.focus();
@@ -345,6 +346,25 @@ function criarBarra(textarea) {
     // ao textarea (ex.: atalhos de formata├º├úo em editor.js).
     textarea.addEventListener('keydown', onKeydown, true);
 
+    // Ctrl+V n├úo passa pelo onKeydown acima: colar ├® um evento de
+    // 'paste' ├á parte, n├úo uma tecla normal, e o bloco "outros atalhos
+    // (Ctrl+S etc.) passam direto" do onKeydown deixa Ctrl+V cair no
+    // comportamento nativo do textarea ÔÇö ou seja, o texto colado ia
+    // direto pro POEMA de verdade (inclusive substituindo a ocorr├¬ncia
+    // destacada, se havia uma selecionada), nunca pro termo de busca.
+    // Aqui a gente intercepta o 'paste' e redireciona o conte├║do
+    // colado pro termo de busca, do mesmo jeito que uma tecla normal
+    // faria (ver "e.key.length === 1" no onKeydown): substitui o termo
+    // se ele estava "selecionado" (Ctrl+A), sen├úo concatena no final.
+    const onPaste = (e) => {
+        e.preventDefault();
+        e.stopPropagation();
+        const texto = (e.clipboardData || window.clipboardData)?.getData('text') || '';
+        if (!texto) return;
+        buscar((barraAtual.termoSelecionado ? '' : barraAtual.termo) + texto);
+    };
+    textarea.addEventListener('paste', onPaste, true);
+
     barraAtual = {
         textarea,
         el,
@@ -357,6 +377,7 @@ function criarBarra(textarea) {
         termoSelecionado: false,
         expandido: false,
         onKeydown,
+        onPaste,
     };
     atualizarVisual();
 
diff --git a/js/colunas.js b/js/colunas.js
index bd821c5..46fa1e8 100644
--- a/js/colunas.js
+++ b/js/colunas.js
@@ -11,9 +11,14 @@ const LS_PREFIX = 'arquivoPoetico_colunas_';
 
 // Ordem de defini├º├úo = ordem padr├úo de exibi├º├úo (usada s├│ at├® o
 // usu├írio reordenar manualmente pelo seletor ÔÇö a partir da├¡ quem
-// manda ├® a ordem salva no localStorage, ver lerEstado()).
-// `default: true` s├úo as colunas que j├í existiam antes desse recurso
-// (mantidas ativas de cara); as demais come├ºam desligadas.
+// manda ├® a ordem salva no localStorage, ver lerEstado()). A ordem
+// abaixo espelha a ordem dos campos no pr├│prio modal de edi├º├úo (ver
+// modal-poema.html/modal-prosa.html ÔÇö mesma sequ├¬ncia de <details>
+// de cima pra baixo), pra quem j├í conhece o formul├írio reconhecer o
+// mesmo fluxo no seletor de colunas, em vez de uma lista que s├│ foi
+// crescendo por ordem de chegada da feature. `default: true` s├úo as
+// colunas que j├í existiam antes desse recurso (mantidas ativas de
+// cara); as demais come├ºam desligadas.
 // `sortType` s├│ existe nas colunas de Poemas (├║nica tabela com cabe├ºalho
 // clic├ível por enquanto ÔÇö ver thOrdenavel() em render-listas.js):
 //   'estrutura'  ÔÇö ordem padr├úo (array j├í vem nessa ordem; desc = invertida)
@@ -22,12 +27,30 @@ const LS_PREFIX = 'arquivoPoetico_colunas_';
 //   'status'     ÔÇö pelos tr├¬s estados poss├¡veis, ver ORDEM_STATUS
 export const DEFINICAO_COLUNAS = {
     poemas: [
+        // Campo simples logo abaixo de T├¡tulo/Sequ├¬ncia no modal.
+        { key: 'idioma', label: 'Idioma', default: false, sortType: 'alfabetico' },
+        // Data de Escrita / Publica├º├úo / ├ëpoca Retratada / Contexto ÔÇö
+        // primeiro bloco do modal, j├í aberto por padr├úo.
         { key: 'dataEscrita', label: 'Escrito em', default: true, sortType: 'data' },
         { key: 'dataPublicacao', label: 'Publica├º├úo', default: true, sortType: 'data' },
-        { key: 'estrutura', label: 'Estrutura', default: true, sortType: 'estrutura' },
-        { key: 'status', label: 'Status', default: true, sortType: 'status' },
-        { key: 'pessoas', label: 'Dedicado a', default: true, sortType: 'alfabetico' },
         { key: 'epocaRetratada', label: '├ëpoca Retratada', default: false, sortType: 'data' },
+        {
+            key: 'contextoHistorico',
+            label: 'Contexto Hist├│rico/Pessoal',
+            default: false,
+            sortType: 'alfabetico',
+        },
+        // Notas ÔÇö logo depois do campo Texto no modal.
+        { key: 'notas', label: 'Notas', default: false, sortType: 'alfabetico' },
+        // Autoria ÔÇö chip logo abaixo de Notas no modal (Autor/Coautor).
+        { key: 'autoria', label: 'Autoria', default: false },
+        // Envios e Rea├º├Áes ÔÇö item 7, quando/pra quem o texto foi enviado.
+        { key: 'envios', label: 'Envios', default: false },
+        // Reconhecimentos ÔÇö item 8, pr├¬mios/men├º├Áes recebidos pelo texto.
+        { key: 'reconhecimentos', label: 'Reconhecimentos', default: false },
+        // "Livros e destino" ÔÇö v├¡nculo estrutural.
+        { key: 'estrutura', label: 'Estrutura', default: true, sortType: 'estrutura' },
+        // "Elos, refer├¬ncias e intertextualidade".
         { key: 'elos', label: 'Elos', default: false, sortType: 'alfabetico' },
         { key: 'referencias', label: 'Refer├¬ncias', default: false, sortType: 'alfabetico' },
         {
@@ -36,6 +59,7 @@ export const DEFINICAO_COLUNAS = {
             default: false,
             sortType: 'alfabetico',
         },
+        // "Anexos".
         { key: 'anexos', label: 'Anexos', default: false, sortType: 'alfabetico' },
         {
             key: 'anexosNotaGeral',
@@ -43,6 +67,7 @@ export const DEFINICAO_COLUNAS = {
             default: false,
             sortType: 'alfabetico',
         },
+        // "Anota├º├Áes marginais e descri├º├úo visual".
         {
             key: 'anotacoesMarginais',
             label: 'Anota├º├Áes Marginais',
@@ -55,14 +80,7 @@ export const DEFINICAO_COLUNAS = {
             default: false,
             sortType: 'alfabetico',
         },
-        {
-            key: 'contextoHistorico',
-            label: 'Contexto Hist├│rico/Pessoal',
-            default: false,
-            sortType: 'alfabetico',
-        },
-        { key: 'etiquetas', label: 'Etiquetas', default: false, sortType: 'alfabetico' },
-        { key: 'notas', label: 'Notas', default: false, sortType: 'alfabetico' },
+        // "Oculta├º├úo e conte├║do sens├¡vel".
         { key: 'ocultacao', label: 'Oculta├º├úo', default: false, sortType: 'alfabetico' },
         {
             key: 'conteudoSensivel',
@@ -76,18 +94,75 @@ export const DEFINICAO_COLUNAS = {
             default: false,
             sortType: 'alfabetico',
         },
-        { key: 'descarte', label: 'Descarte', default: false, sortType: 'alfabetico' },
+        // "Sinaliza├º├Áes e dedicat├│ria" ÔÇö sinaliza├º├Áes + pessoas (+ Grupos,
+        // que ├® caracter├¡stica da pessoa, ver criarGrupoDePessoas em
+        // editor.js ÔÇö n├úo tem se├º├úo pr├│pria no modal, mas mora aqui por
+        // andar sempre junto de Pessoas).
+        { key: 'etiquetas', label: 'Etiquetas', default: false, sortType: 'alfabetico' },
+        { key: 'pessoas', label: 'Pessoas', default: true, sortType: 'alfabetico' },
+        { key: 'grupos', label: 'Grupos', default: false, sortType: 'alfabetico' },
+        // "Status e Pend├¬ncias".
+        { key: 'status', label: 'Status', default: true, sortType: 'status' },
         { key: 'cortadoDe', label: 'Cortado de', default: false, sortType: 'alfabetico' },
         { key: 'lancadoEm', label: 'Lan├ºado em', default: false, sortType: 'alfabetico' },
+        {
+            key: 'justificativaMigracao',
+            label: 'Justificativa da Migra├º├úo',
+            default: false,
+            sortType: 'alfabetico',
+        },
+        { key: 'pendencia', label: 'Pend├¬ncia', default: false, sortType: 'alfabetico' },
+        { key: 'descarte', label: 'Descarte', default: false, sortType: 'alfabetico' },
+        // N├úo corresponde a nenhum campo do modal ÔÇö ├® uma m├®trica derivada
+        // (ver contarCamposPreenchidos em exportar-md.js), por isso mora no
+        // fim da lista em vez de emparelhada com alguma se├º├úo do formul├írio.
+        // ├Ütil pra identificar rapidamente os textos com estrutura mais
+        // rica/complexa (mais campos preenchidos) sem abrir cada um.
+        {
+            key: 'camposPreenchidos',
+            label: 'Campos Preenchidos',
+            default: false,
+            sortType: 'numero',
+        },
     ],
     prosas: [
+        { key: 'idioma', label: 'Idioma', default: false },
         { key: 'dataEscrita', label: 'Data', default: true },
         { key: 'dataPublicacao', label: 'Publica├º├úo', default: true },
+        // Item 4: mesmas colunas novas de Poemas, na ordem em que os
+        // grupos aparecem no modal de Prosa (ver coment├írio no topo
+        // do arquivo ÔÇö ordem espelha o modal).
+        { key: 'epocaRetratada', label: '├ëpoca Retratada', default: false },
+        { key: 'contextoHistorico', label: 'Contexto Hist├│rico/Pessoal', default: false },
         { key: 'vinculo', label: 'V├¡nculo', default: true },
-        { key: 'pessoas', label: 'Dedicado a', default: true },
         { key: 'genero', label: 'G├¬nero', default: true },
         { key: 'etiquetas', label: 'Etiquetas', default: false },
+        { key: 'pessoas', label: 'Pessoas', default: true },
+        { key: 'grupos', label: 'Grupos', default: false },
         { key: 'notas', label: 'Notas', default: false },
+        { key: 'autoria', label: 'Autoria', default: false },
+        { key: 'envios', label: 'Envios', default: false },
+        { key: 'reconhecimentos', label: 'Reconhecimentos', default: false },
+        // "Elos, refer├¬ncias e intertextualidade".
+        { key: 'elos', label: 'Elos', default: false },
+        { key: 'referencias', label: 'Refer├¬ncias', default: false },
+        { key: 'intertextualidade', label: 'Intertextualidade', default: false },
+        // "Anexos".
+        { key: 'anexos', label: 'Anexos', default: false },
+        { key: 'anexosNotaGeral', label: 'Nota Anexos', default: false },
+        // "Oculta├º├úo e conte├║do sens├¡vel".
+        { key: 'ocultacao', label: 'Oculta├º├úo', default: false },
+        { key: 'conteudoSensivel', label: 'Conte├║do Sens├¡vel', default: false },
+        { key: 'vocabularioHiperacionante', label: 'Vocabul├írio Hiperacionante', default: false },
+        // "Status e Pend├¬ncias".
+        { key: 'status', label: 'Status', default: true },
+        { key: 'cortadoDe', label: 'Cortado de', default: false },
+        { key: 'lancadoEm', label: 'Lan├ºado em', default: false },
+        { key: 'justificativaMigracao', label: 'Justificativa da Migra├º├úo', default: false },
+        { key: 'pendencia', label: 'Pend├¬ncia', default: false },
+        { key: 'descarte', label: 'Descarte', default: false },
+        // Ver coment├írio equivalente em poemas[] acima.
+        { key: 'camposPreenchidos', label: 'Campos Preenchidos', default: false },
     ],
 };
 
@@ -172,6 +247,18 @@ export function desmarcarTodasColunas(tabela) {
     disparaAlteracao(tabela);
 }
 
+// Descarta a personaliza├º├úo salva (ordem reordenada + colunas ligadas/desligadas
+// manualmente) e volta pro padr├úo de f├íbrica ÔÇö mesmas colunas com default: true,
+// na ordem de defini├º├úo. Basta apagar a chave do localStorage: lerEstado() j├í
+// recalcula ordem/ativas do zero a partir de DEFINICAO_COLUNAS sempre que n├úo
+// encontra nada salvo (mesmo caminho usado pra JSON ausente/corrompido).
+export function resetarColunas(tabela) {
+    if (!DEFINICAO_COLUNAS[tabela]) return;
+
+    localStorage.removeItem(LS_PREFIX + tabela);
+    disparaAlteracao(tabela);
+}
+
 // Alterna uma coluna e dispara 'colunas:alteradas' pra quem estiver
 // escutando (render-listas.js) re-renderizar a tabela em quest├úo.
 // N├úo mexe na ordem ÔÇö s├│ liga/desliga dentro dela.
@@ -227,6 +314,11 @@ export function renderSeletorColunas(tabela) {
                 class="text-[10px] font-semibold text-blue-600 dark:text-blue-400 hover:underline">
                 Desmarcar todas
             </button>
+            <button type="button" onclick="resetarColunas('${tabela}')"
+                title="Volta pras colunas e pra ordem padr├úo, descartando a personaliza├º├úo"
+                class="text-[10px] font-semibold text-gray-500 dark:text-slate-400 hover:underline ml-auto">
+                Restaurar padr├úo
+            </button>
         </div>`;
 
     return (
diff --git a/js/db.js b/js/db.js
index d26c47b..200c134 100644
--- a/js/db.js
+++ b/js/db.js
@@ -13,6 +13,7 @@ import {
     abrirModalExclusao,
     mostrarAvisoComAcao,
     fecharAviso,
+    gerarId,
 } from './utils.js';
 import {
     salvarCapa,
@@ -45,10 +46,18 @@ export let db = JSON.parse(localStorage.getItem(DB_KEY)) || {
     elementos: [],
     coletaneas: [], // legado, n├úo usado pela aba Colet├óneas atual ÔÇö mantido s├│ por compatibilidade na importa├º├úo de backups antigos
     itensColetanea: [], // itens de Colet├ónea de fato (ver coletaneas.js); cada item referencia uma Parte via parteId
+    pessoas: [], // cadastro central de Pessoas ({ id, nome, grupoIds }) ÔÇö ver migrarPessoasParaCadastro
+    grupos: [], // cadastro central de Grupos ({ id, nome }) ÔÇö quem uma Pessoa ├® na vida de quem escreve, constante entre poemas
+    autores: [], // cadastro central de Autores ({ id, nome, sobre }) ÔÇö quem escreveu o item, ver migrarAutoria
+    epocas: [], // cadastro central de ├ëpocas ({ id, nome, contextoRelacao, notas }) ÔÇö a que per├¡odo um poema se refere, ver migrarEpocas
 };
 
-// Garante que dados importados de vers├Áes antigas tenham o campo coletaneas
+// Garante que dados importados de vers├Áes antigas tenham os campos novos
 if (!db.coletaneas) db.coletaneas = [];
+if (!db.pessoas) db.pessoas = [];
+if (!db.grupos) db.grupos = [];
+if (!db.autores) db.autores = [];
+if (!db.epocas) db.epocas = [];
 
 // Migra├º├úo: em Poemas, o campo `publicado` (boolean) virou `status`, com
 // 3 valores ÔÇö 'publicado' | 'completo' | 'incompleto' ÔÇö pra diferenciar
@@ -83,6 +92,462 @@ function migrarIntertextualidadePoemas(poemas) {
 }
 migrarIntertextualidadePoemas(db.poemas);
 
+// Migra├º├úo: Sinaliza├º├Áes era um ├║nico campo `sinalizacoes` (string,
+// separada por v├¡rgula) misturando 4 categorias semanticamente
+// diferentes ÔÇö estilo, tema, rela├º├úo e sensibilidade ÔÇö mais um uso que
+// na pr├ítica era "tom/registro" (ex.: "Muito meloso"). Isso impedia
+// filtrar s├│ por categoria (ver CAMPOS_ATRIBUTO em utils.js). Vira 5
+// campos: sinalizacoesEstilo / sinalizacoesTema / sinalizacoesRelacao /
+// sinalizacoesSensibilidade / sinalizacoesTom.
+//
+// "Conte├║do sens├¡vel" n├úo migra pra nenhum campo novo ÔÇö deixou de ser
+// tag solta porque j├í existe o campo `conteudoSensivel` (o par├ígrafo
+// descritivo), que ├® a fonte de verdade; exibir/filtrar por "tem
+// conte├║do sens├¡vel" passa a checar esse campo diretamente, n├úo uma
+// tag redundante que podia dessincronizar dele.
+// "Rascunho" tamb├®m n├úo migra ÔÇö o que ela tentava dizer j├í ├® coberto
+// pelo status 'incompleto', que j├í existe; manter os dois seria a mesma
+// informa├º├úo disputando dois lugares.
+// Tags n├úo reconhecidas (inclui, por ora, "Premiados", "Tradu├º├úo" e
+// "Varia├º├Áes" ÔÇö que v├úo virar Reconhecimentos e Elos tipados de
+// Deriva├º├úo numa etapa seguinte) v├úo pra sinalizacoesOutros, pra n├úo
+// perder o dado enquanto esses campos n├úo existem ainda.
+const MAPA_MIGRACAO_SINALIZACOES = {
+    Concretista: 'sinalizacoesEstilo',
+    Brasil: 'sinalizacoesTema',
+    'Ôê× Pedrictor': 'sinalizacoesRelacao',
+    'Linguagem obscena': 'sinalizacoesSensibilidade',
+    'Muito meloso ( ­ƒì»)': 'sinalizacoesTom',
+};
+const TAGS_DESCARTADAS_NA_MIGRACAO = new Set(['Conte├║do sens├¡vel', 'Rascunho']);
+
+export function migrarSinalizacoes(itens) {
+    itens.forEach((item) => {
+        if (item.sinalizacoes === undefined) return; // j├í migrado (ou nunca teve o campo)
+
+        const tags = (item.sinalizacoes || '')
+            .split(',')
+            .map((s) => s.trim())
+            .filter(Boolean);
+
+        const porCategoria = {
+            sinalizacoesEstilo: [],
+            sinalizacoesTema: [],
+            sinalizacoesRelacao: [],
+            sinalizacoesSensibilidade: [],
+            sinalizacoesTom: [],
+            sinalizacoesOutros: [],
+        };
+
+        tags.forEach((tag) => {
+            if (TAGS_DESCARTADAS_NA_MIGRACAO.has(tag)) return;
+            const campo = MAPA_MIGRACAO_SINALIZACOES[tag] || 'sinalizacoesOutros';
+            porCategoria[campo].push(tag);
+        });
+
+        Object.entries(porCategoria).forEach(([campo, lista]) => {
+            // S├│ cria o campo se j├í n├úo existir (n├úo sobrescreve nada
+            // preenchido manualmente numa rodada anterior de migra├º├úo).
+            if (item[campo] === undefined) item[campo] = lista.join(', ');
+        });
+
+        delete item.sinalizacoes;
+    });
+}
+migrarSinalizacoes(db.poemas);
+migrarSinalizacoes(db.prosas);
+
+// Migra├º├úo: Elos e Refer├¬ncias eram arrays de ID cru apontando pra outro
+// poema (ex.: [1776745104803]). Passam a ser arrays de objeto
+// { id, tipo, texto } ÔÇö um elo/refer├¬ncia pode ter um tipo espec├¡fico
+// (Reescrita de / Tradu├º├úo de / Personagem em comum / etc., ver
+// TIPOS_ELO/TIPOS_REFERENCIA em utils.js) e uma nota livre opcional. S├│ se
+// aplica a Poema ÔÇö Prosa ainda n├úo tem `conceitos` (item 4 do schema,
+// paridade com Poema, ainda pendente).
+// Idempotente: entradas que j├í s├úo objeto (rodada anterior de migra├º├úo,
+// ou item criado depois que o item 1 j├í existia) passam intactas.
+function migrarElosReferencias(poemas) {
+    const migrarLista = (lista) => {
+        if (!Array.isArray(lista)) return lista;
+        return lista.map((entrada) => {
+            if (entrada && typeof entrada === 'object') return entrada; // j├í migrado
+            const id = typeof entrada === 'number' ? entrada : parseInt(entrada, 10);
+            return { id, tipo: '', texto: '' };
+        });
+    };
+    poemas.forEach((p) => {
+        if (!p.conceitos) return;
+        p.conceitos.elos = migrarLista(p.conceitos.elos);
+        p.conceitos.referencias = migrarLista(p.conceitos.referencias);
+    });
+}
+migrarElosReferencias(db.poemas);
+
+// Migra├º├úo: Elos tinham um `tipo` de uma lista fechada de 11 valores
+// (Reescrita de, Continua├º├úo de, Tradu├º├úo de, Traduzido para...) ÔÇö um
+// valor por r├│tulo poss├¡vel, com s├│ 3 pares tendo o "outro lado"
+// nomeado. Passam a ter `{ relacao, direcao }`: Rela├º├úo ├® uma das 8
+// rela├º├Áes (ver RELACOES_ELO em utils.js), Dire├º├úo ├® 'origem' (texto
+// mais antigo/base) ou 'destino' (texto derivado/mais novo) ÔÇö o r├│tulo
+// mostrado (ver rotuloElo em utils.js) j├í ├® derivado dos dois, sem
+// precisar de mapa de inverso. Refer├¬ncias N├âO entram nessa migra├º├úo ÔÇö
+// continuam s├│ com `tipo` (schema unidirecional, sem Dire├º├úo).
+// Idempotente: elo que j├í tem `relacao` (rodada anterior de migra├º├úo,
+// ou elo criado depois que essa migra├º├úo j├í existia) passa intacto.
+const MAPA_MIGRACAO_TIPO_ELO = {
+    'Reescrita de': { relacao: 'Reescrita', direcao: 'destino' },
+    'Continua├º├úo de': { relacao: 'Continuidade', direcao: 'destino' },
+    'Tradu├º├úo de': { relacao: 'Tradu├º├úo', direcao: 'destino' },
+    'Traduzido para': { relacao: 'Tradu├º├úo', direcao: 'origem' },
+    'Varia├º├úo de': { relacao: 'Varia├º├úo', direcao: 'destino' },
+    'Vers├úo anterior (descartada) de': { relacao: 'Vers├úo', direcao: 'origem' },
+    'Vers├úo oficial de': { relacao: 'Vers├úo', direcao: 'destino' },
+    'D├¡ptico com': { relacao: 'D├¡ptico', direcao: '' },
+    'Resposta a': { relacao: 'Resposta', direcao: 'destino' },
+    'Respondido em': { relacao: 'Resposta', direcao: 'origem' },
+    Outro: { relacao: 'Outro', direcao: '' },
+};
+export function migrarElosParaRelacaoDirecao(poemas) {
+    poemas.forEach((p) => {
+        const elos = p.conceitos?.elos;
+        if (!Array.isArray(elos)) return;
+        p.conceitos.elos = elos.map((elo) => {
+            if (elo.relacao !== undefined) return elo; // j├í migrado
+            const mapeado = MAPA_MIGRACAO_TIPO_ELO[elo.tipo];
+            if (mapeado)
+                return {
+                    id: elo.id,
+                    relacao: mapeado.relacao,
+                    direcao: mapeado.direcao,
+                    texto: elo.texto || '',
+                };
+            // tipo vazio (nunca preenchido) ou tipo desconhecido/legado:
+            // sem tipo vira elo sem rela├º├úo definida (mesmo estado de
+            // "n├úo preenchido" de antes); tipo desconhecido cai em Outro,
+            // sem dire├º├úo pra n├úo inventar um lado que n├úo d├í pra inferir.
+            return {
+                id: elo.id,
+                relacao: elo.tipo ? 'Outro' : '',
+                direcao: '',
+                texto: elo.texto || '',
+            };
+        });
+    });
+}
+migrarElosParaRelacaoDirecao(db.poemas);
+
+// Migra├º├úo: pessoas era string "Pedro, Dani" (por v├¡rgula, misturando
+// nomes sem distinguir o tipo de v├¡nculo com o texto). Passa a ser array
+// de objeto { nome, papel }: `papel` ├® um dos 4 valores fechados de
+// PAPEIS_PESSOA (Retratado(a)/Inspirado(a) por/Dedicat├írio(a)/Mencionado(a)/Aludido(a)) ÔÇö
+// ou "" (n├úo especificado). Todo nome migrado da string antiga vira
+// papel: "" (n├úo d├í pra inferir o papel a partir s├│ do nome). Aplica a
+// Poema e Prosa (mesmo campo/schema nos dois).
+// Idempotente: item cujo `pessoas` j├í ├® array passa intacto.
+export function migrarPessoas(itens) {
+    itens.forEach((item) => {
+        if (Array.isArray(item.pessoas)) return; // j├í migrado
+        const nomes = (item.pessoas || '')
+            .split(',')
+            .map((s) => s.trim())
+            .filter(Boolean);
+        item.pessoas = nomes.map((nome) => ({ nome, papel: '' }));
+    });
+}
+migrarPessoas(db.poemas);
+migrarPessoas(db.prosas);
+
+// Migra├º├úo: papel era string ├║nica fechada em PAPEIS_PESSOA ÔÇö uma pessoa
+// s├│ podia ocupar um papel por texto. Na pr├ítica, boa parte do acervo
+// (poemas de endere├ºamento direto) tem a mesma pessoa sendo Retratado(a),
+// Inspirado(a) por e Dedicat├írio(a) ao mesmo tempo ÔÇö for├ºar escolha ├║nica
+// jogava fora essa distin├º├úo pro maior bloco de dados do acervo. Passa a
+// ser `papeis`: array (0+ valores de PAPEIS_PESSOA), na ordem em que
+// foram marcados no editor ÔÇö n├úo ├® uma hierarquia fixa por categoria,
+// ├® o que a pessoa achou mais forte, poema a poema (ver conversa que
+// definiu isso: a intensidade de cada papel varia por texto, n├úo ├®
+// propriedade fixa da categoria).
+// Idempotente: item cuja pessoa j├í tem `papeis` (array) passa intacta.
+export function migrarPapeisPessoa(itens) {
+    itens.forEach((item) => {
+        if (!Array.isArray(item.pessoas)) return; // precisa de migrarPessoas rodar antes
+        item.pessoas = item.pessoas.map((p) => {
+            if (Array.isArray(p.papeis)) return p; // j├í migrado
+            const { papel, ...resto } = p;
+            return { ...resto, papeis: papel ? [papel] : [] };
+        });
+    });
+}
+migrarPapeisPessoa(db.poemas);
+migrarPapeisPessoa(db.prosas);
+
+// Migra├º├úo: os nomes de 3 dos 5 valores de PAPEIS_PESSOA mudaram numa
+// sess├úo (padroniza├º├úo de g├¬nero ÔÇö ver status-acervo-poetico.md):
+// "Alus├úo" ÔåÆ "Aludido(a)", "Dedicat├íria" ÔåÆ "Dedicat├írio(a)", "Inspirado
+// por" ÔåÆ "Inspirado(a) por". A troca s├│ mudou a constante e o c├│digo ÔÇö
+// dado j├í salvo com o nome antigo (dentro de `papeis`, array de string)
+// n├úo foi tocado, ent├úo ficava com uma string que n├úo bate mais com
+// nenhuma op├º├úo do <select> (aparecia como papel "invis├¡vel": sem
+// marca├º├úo em nenhum item do dropdown, mas contando pra inicial exibida
+// e pra iniciaisPapeisPessoa) e, se a pessoa marcasse o papel novo
+// correspondente (ex. Aludido(a)) no mesmo item, os dois conviviam no
+// array (["Alus├úo", "Aludido(a)"]), gerando inicial duplicada ("A┬ÀA") na
+// coluna. Renomeia in-place os 3 valores antigos pros novos; roda depois
+// de migrarPapeisPessoa (precisa de `papeis` j├í ser array) e ├® seguro
+// rodar de novo (s├│ troca o que ainda est├í no nome antigo).
+const RENOMEACOES_PAPEL = {
+    Alus├úo: 'Aludido(a)',
+    Dedicat├íria: 'Dedicat├írio(a)',
+    'Inspirado por': 'Inspirado(a) por',
+};
+export function migrarNomesDePapel(itens) {
+    itens.forEach((item) => {
+        if (!Array.isArray(item.pessoas)) return;
+        item.pessoas.forEach((p) => {
+            if (!Array.isArray(p.papeis)) return;
+            p.papeis = [...new Set(p.papeis.map((papel) => RENOMEACOES_PAPEL[papel] || papel))];
+        });
+    });
+}
+migrarNomesDePapel(db.poemas);
+migrarNomesDePapel(db.prosas);
+
+// Migra├º├úo: Pessoa passa a ser entidade pr├│pria em `db.pessoas`
+// ({ id, nome, grupoIds }), em vez de nome solto repetido em cada
+// poema/prosa. `papel`/`papeis` ├® v├¡nculo do texto com a pessoa (varia
+// por poema) e continua em `item.pessoas`; o que muda ├® a chave: de
+// `{ nome, papeis }` pra `{ pessoaId, papeis }` ÔÇö quem a pessoa ├ë
+// (nome, grupos) passa a morar s├│ no cadastro central, uma vez.
+//
+// Dedup por nome exato (mesmo crit├®rio que extrairPessoasUnicas j├í
+// usava) ÔÇö nomes que s├│ diferem por acento/mai├║scula/espa├ºo extra
+// viram pessoas diferentes aqui. Isso ├® uma limita├º├úo conhecida, n├úo
+// um bug: decidir se "Dani" e "dani " s├úo a mesma pessoa ├® uma escolha
+// de quem usa o sistema, n├úo algo pra migra├º├úo adivinhar sozinha. Uma
+// fun├º├úo de mesclar pessoas (mover todas as refer├¬ncias de um id pra
+// outro e apagar o duplicado) fica pra quando o cadastro existir de
+// fato ÔÇö nesse momento fica f├ícil ver os quase-duplicados e juntar.
+//
+// Idempotente: item cuja entrada de pessoa j├í tem `pessoaId` passa
+// intacta; nome que j├í tem pessoa cadastrada com esse nome reaproveita
+// o id em vez de duplicar.
+export function migrarPessoasParaCadastro(dbRef) {
+    const porNome = new Map(dbRef.pessoas.map((p) => [p.nome, p]));
+
+    function idParaNome(nome) {
+        let pessoa = porNome.get(nome);
+        if (!pessoa) {
+            pessoa = { id: gerarId(), nome, grupoIds: [] };
+            dbRef.pessoas.push(pessoa);
+            porNome.set(nome, pessoa);
+        }
+        return pessoa.id;
+    }
+
+    [dbRef.poemas, dbRef.prosas].forEach((itens) => {
+        itens.forEach((item) => {
+            if (!Array.isArray(item.pessoas)) return;
+            item.pessoas = item.pessoas.map((p) => {
+                if (p.pessoaId !== undefined) return p; // j├í migrado
+                return { pessoaId: idParaNome(p.nome), papeis: p.papeis || [] };
+            });
+        });
+    });
+}
+migrarPessoasParaCadastro(db);
+
+// Migra├º├úo: campo `idioma` (item 9 do plano de schema) n├úo existia antes
+// ÔÇö complementa a Rela├º├úo "Tradu├º├úo" do item 1 (sem ele n├úo dava pra
+// saber em que l├¡ngua um texto traduzido est├í). Todo item sem o campo
+// recebe "pt-BR" (o idioma majorit├írio do acervo at├® aqui), n├úo texto
+// vazio ÔÇö evita ficar "sem valor" em toda a base retroativamente s├│
+// porque o campo ├® novo. Aplica a Poema e Prosa desde j├í (mesma
+// antecipa├º├úo j├í feita em Sinaliza├º├Áes ÔÇö Prosa ainda n├úo tem Elos/
+// Tradu├º├úo do item 4, mas ganha idioma junto).
+// Idempotente: s├│ toca item sem `idioma` (n├úo sobrescreve valor j├í
+// preenchido, seja o padr├úo ou uma escolha manual).
+export function migrarIdioma(itens) {
+    itens.forEach((item) => {
+        if (item.idioma === undefined) item.idioma = 'pt-BR';
+    });
+}
+migrarIdioma(db.poemas);
+migrarIdioma(db.prosas);
+
+// Migra├º├úo: campo `reconhecimentos` ÔÇö lista de pr├¬mios/men├º├Áes que um
+// texto recebeu (item 8 do plano de schema), separada de tag solta. At├®
+// aqui, "Premiados" era uma tag gen├®rica dentro do balde tempor├írio
+// sinalizacoesOutros (ver MAPA_MIGRACAO_SINALIZACOES acima) ÔÇö todo item
+// ganha `reconhecimentos: []`, e quem j├í tinha a tag "Premiados" ganha
+// uma entrada em branco (`{ premio: '', posicao: '', ano: null, texto: ''
+// }`) pra completar manualmente depois ÔÇö o texto livre da tag e de Notas
+// n├úo d├í pra parsear com confian├ºa em pr├¬mio/posi├º├úo/ano estruturados ÔÇö,
+// com a tag removida de sinalizacoesOutros. Aplica a Poema e Prosa desde
+// j├í (mesma antecipa├º├úo de Idioma/Autoria/Envios).
+// Idempotente: s├│ toca item sem `reconhecimentos` (undefined, n├úo
+// sobrescreve preenchimento manual de uma rodada anterior); a remo├º├úo da
+// tag "Premiados" tamb├®m ├® idempotente por natureza ÔÇö rodar de novo n├úo
+// encontra mais a tag pra remover nem cria uma segunda entrada em branco.
+export function migrarReconhecimentos(itens) {
+    itens.forEach((item) => {
+        if (item.reconhecimentos !== undefined) return; // j├í migrado
+
+        const tags = (item.sinalizacoesOutros || '')
+            .split(',')
+            .map((s) => s.trim())
+            .filter(Boolean);
+        const tinhaTagPremiados = tags.includes('Premiados');
+
+        item.reconhecimentos = tinhaTagPremiados
+            ? [{ premio: '', posicao: '', ano: null, texto: '' }]
+            : [];
+
+        if (tinhaTagPremiados) {
+            item.sinalizacoesOutros = tags.filter((t) => t !== 'Premiados').join(', ');
+        }
+    });
+}
+migrarReconhecimentos(db.poemas);
+migrarReconhecimentos(db.prosas);
+
+// Migra├º├úo: campo `autoria` ÔÇö de quem ├® a responsabilidade autoral por
+// cada item (nem tudo no acervo ├® necessariamente solo ÔÇö coautoria
+// acontece). Cadastro central de Autores (db.autores: { id, nome,
+// sobre }), ├á parte do de Pessoas ÔÇö autoria ├® v├¡nculo de autoria, bem
+// diferente do papel que uma Pessoa ocupa NO texto (Retratado(a)/
+// Dedicat├írio(a)/etc.). Todo item do acervo at├® aqui foi escrito pelo
+// Victor Leme, ent├úo a migra├º├úo garante esse autor no cadastro (criando
+// s├│ se ainda n├úo existir, e s├│ quando de fato h├í item pra migrar ÔÇö n├úo
+// resgata um "Victor Leme" apagado manualmente se n├úo sobrar nenhum
+// item sem `autoria`) e vincula como Autor todo item que ainda n├úo tem
+// o campo ÔÇö evita 305 itens ficarem "sem autoria" retroativamente s├│
+// porque o campo ├® novo (mesmo racioc├¡nio de migrarIdioma). Aplica a
+// Poema e Prosa desde j├í.
+// Idempotente: s├│ toca item sem `autoria` (undefined); reaproveita o
+// Victor Leme j├í cadastrado em vez de duplicar se a fun├º├úo rodar de novo.
+export function migrarAutoria(dbRef) {
+    let victorId = null;
+    function obterVictorId() {
+        if (victorId !== null) return victorId;
+        let victor = dbRef.autores.find((a) => a.nome === 'Victor Leme');
+        if (!victor) {
+            victor = { id: gerarId(), nome: 'Victor Leme', sobre: '' };
+            dbRef.autores.push(victor);
+        }
+        victorId = victor.id;
+        return victorId;
+    }
+
+    [dbRef.poemas, dbRef.prosas].forEach((itens) => {
+        itens.forEach((item) => {
+            if (item.autoria === undefined) {
+                item.autoria = [{ autorId: obterVictorId(), papel: 'Autor' }];
+            }
+        });
+    });
+}
+migrarAutoria(db);
+
+// Migra├º├úo: item 3 do plano de schema. `epocaRetratada.nome` era texto
+// livre repetido em cada poema ÔÇö vira refer├¬ncia (`epocaId`) a um
+// cadastro central pr├│prio, `db.epocas` ({ id, nome, contextoRelacao,
+// notas }), mesmo padr├úo de Pessoas/Autores acima. O que muda ├® s├│ a
+// chave: de `{ nome, inicio, fim, na }` pra `{ epocaId, inicio, fim,
+// recorte, na }` ÔÇö as datas/N-A continuam por item (o mesmo per├¡odo
+// pode valer datas diferentes num poema e noutro, ver
+// obterSugestaoEpocaPorId em utils.js), s├│ o nome passa a morar uma vez
+// s├│ no cadastro; `recorte` (RECORTES_EPOCA em utils.js) ├® campo novo,
+// sem tentativa de adivinhar a partir do nome antigo ÔÇö fica null pra
+// preencher manualmente (mesmo racioc├¡nio de migrarReconhecimentos:
+// texto livre n├úo d├í pra parsear com confian├ºa).
+//
+// Dedup por nome exato (mesmo crit├®rio/mesma limita├º├úo conhecida de
+// migrarPessoasParaCadastro) ÔÇö inclusive entre nomes que hoje
+// distinguem "X" de "X e P├│s" como strings diferentes: cada string
+// ├║nica vira uma ├ëpoca pr├│pria aqui; juntar as duas manualmente (se for
+// o caso) fica pra uma fun├º├úo de mesclar ├ëpocas, mesma pend├¬ncia j├í
+// registrada pra Pessoas.
+//
+// S├│ se aplica a Poema ÔÇö Prosa ainda n├úo tem epocaRetratada (item 4).
+// Idempotente: item cuja epocaRetratada j├í tem `epocaId` passa intacta;
+// nome que j├í tem ├ëpoca cadastrada com esse nome reaproveita o id.
+export function migrarEpocas(dbRef) {
+    const porNome = new Map(dbRef.epocas.map((e) => [e.nome, e]));
+
+    function idParaNome(nome) {
+        let epoca = porNome.get(nome);
+        if (!epoca) {
+            epoca = { id: gerarId(), nome, contextoRelacao: '', notas: '' };
+            dbRef.epocas.push(epoca);
+            porNome.set(nome, epoca);
+        }
+        return epoca.id;
+    }
+
+    (dbRef.poemas || []).forEach((p) => {
+        const epoca = p.epocaRetratada;
+        if (!epoca || epoca.epocaId !== undefined) return; // vazio ou j├í migrado
+        const nome = (epoca.nome || '').trim();
+        p.epocaRetratada = {
+            epocaId: nome ? idParaNome(nome) : null,
+            inicio: epoca.inicio || null,
+            fim: epoca.fim || null,
+            recorte: epoca.recorte ?? null,
+            na: !!epoca.na,
+        };
+    });
+}
+migrarEpocas(db);
+
+// Resolve nome ÔåÆ Autor no cadastro central, criando um novo se ainda
+// n├úo existir (dedup por nome exato, mesmo crit├®rio de
+// obterOuCriarPessoaPorNome logo abaixo). Usado por editor.js: caminho
+// de confirma├º├úo expl├¡cita do usu├írio ao digitar um nome novo no chip
+// de Autoria (ver criarGrupoDeAutoria).
+export function obterOuCriarAutorPorNome(nome) {
+    const nomeLimpo = String(nome ?? '').trim();
+    let autor = db.autores.find((a) => a.nome === nomeLimpo);
+    if (!autor) {
+        autor = { id: gerarId(), nome: nomeLimpo, sobre: '' };
+        db.autores.push(autor);
+    }
+    return autor;
+}
+
+// Resolve nome ÔåÆ Pessoa no cadastro central, criando uma nova se ainda
+// n├úo existir (dedup por nome exato, mesmo crit├®rio de
+// migrarPessoasParaCadastro acima). Usada por editor.js: tanto no
+// caminho de confirma├º├úo expl├¡cita do usu├írio (criarGrupoDePessoas ÔåÆ
+// adicionar) quanto no caminho defensivo de carregar dado ainda n├úo
+// migrado ({nome, papeis} ou string solta) pro formato {pessoaId,
+// papeis} atual.
+export function obterOuCriarPessoaPorNome(nome) {
+    const nomeLimpo = String(nome ?? '').trim();
+    let pessoa = db.pessoas.find((p) => p.nome === nomeLimpo);
+    if (!pessoa) {
+        pessoa = { id: gerarId(), nome: nomeLimpo, grupoIds: [] };
+        db.pessoas.push(pessoa);
+    }
+    return pessoa;
+}
+
+// Resolve nome ÔåÆ ├ëpoca no cadastro central, criando uma nova se ainda
+// n├úo existir (dedup por nome exato, mesmo crit├®rio de
+// obterOuCriarPessoaPorNome acima). Usada por forms.js ao gravar o
+// campo "├ëpoca" do modal de Poema ÔÇö texto digitado, resolvido/criado no
+// submit, mesmo caminho de obterOuCriarAutorPorNome (n├úo chip de lista,
+// j├í que s├│ existe uma ├ëpoca por poema).
+export function obterOuCriarEpocaPorNome(nome) {
+    const nomeLimpo = String(nome ?? '').trim();
+    if (!nomeLimpo) return null;
+    let epoca = db.epocas.find((e) => e.nome === nomeLimpo);
+    if (!epoca) {
+        epoca = { id: gerarId(), nome: nomeLimpo, contextoRelacao: '', notas: '' };
+        db.epocas.push(epoca);
+    }
+    return epoca;
+}
+
 // ÔöÇÔöÇÔöÇ Ordena├º├Áes ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
 // Recebem os arrays como par├ómetro (em vez de fechar sobre o `db` do
 // m├│dulo) pra poderem ser testadas isoladamente, com dados de mentira,
@@ -237,8 +702,31 @@ export async function importarDB(novoDb) {
     db.elementos = novoDb.elementos || [];
     db.coletaneas = novoDb.coletaneas || [];
     db.itensColetanea = novoDb.itensColetanea || [];
+    db.pessoas = novoDb.pessoas || [];
+    db.grupos = novoDb.grupos || [];
+    db.autores = novoDb.autores || [];
+    db.epocas = novoDb.epocas || [];
     migrarStatusPoemas(db.poemas);
     migrarIntertextualidadePoemas(db.poemas);
+    migrarSinalizacoes(db.poemas);
+    migrarSinalizacoes(db.prosas);
+    migrarPessoas(db.poemas);
+    migrarPessoas(db.prosas);
+    migrarPapeisPessoa(db.poemas);
+    migrarPapeisPessoa(db.prosas);
+    migrarNomesDePapel(db.poemas);
+    migrarNomesDePapel(db.prosas);
+    migrarPessoasParaCadastro(db);
+    migrarIdioma(db.poemas);
+    migrarIdioma(db.prosas);
+    migrarAutoria(db);
+    // migrarReconhecimentos tinha ficado de fora daqui (s├│ rodava no
+    // load do m├│dulo) ÔÇö mesmo padr├úo de risco de "arquivo/chamada que
+    // fica pra tr├ís" j├í documentado no status-acervo-poetico.md; pego
+    // ao mexer neste bloco pra ├ëpocas, sem rela├º├úo direta com o item 3.
+    migrarReconhecimentos(db.poemas);
+    migrarReconhecimentos(db.prosas);
+    migrarEpocas(db);
 
     // Se o backup foi gerado com "incluir capas" marcado, ele traz um
     // _capasBase64 com as imagens embutidas ÔÇö restaura pro IndexedDB
@@ -359,6 +847,10 @@ const ROTULOS_COL = {
     elementos: 'Elemento',
     itensColetanea: 'Item de Colet├ónea',
     coletaneas: 'Colet├ónea',
+    pessoas: 'Pessoa',
+    grupos: 'Grupo',
+    autores: 'Autor',
+    epocas: '├ëpoca',
 };
 
 // Plural + partic├¡pio com concord├óncia de g├¬nero certa pro toast de
@@ -390,6 +882,153 @@ export function calcularCascataColetanea(dbRef, livroId) {
     return { partesIds, itensIds };
 }
 
+/**
+ * Quem referencia a Pessoa `pessoaId` ÔÇö poemas e prosas onde ela est├í
+ * vinculada (independente do papel). Usado tanto pra avisar quantos
+ * textos ser├úo afetados (deleteItem) quanto pra de fato desvincular na
+ * exclus├úo (_removerParaExclusao), garantindo que os dois nunca divirjam
+ * (mesmo esp├¡rito de calcularCascataColetanea acima).
+ */
+export function calcularImpactoExclusaoPessoa(dbRef, pessoaId) {
+    const acha = (item) => (item.pessoas || []).some((p) => p.pessoaId == pessoaId);
+    return {
+        poemasIds: (dbRef.poemas || []).filter(acha).map((p) => p.id),
+        prosasIds: (dbRef.prosas || []).filter(acha).map((p) => p.id),
+    };
+}
+
+/**
+ * Quem referencia o Grupo `grupoId` ÔÇö pessoas do cadastro que pertencem
+ * a ele. Excluir o Grupo n├úo exclui a Pessoa, s├│ tira ela desse grupo
+ * (ela pode pertencer a v├írios ÔÇö sobreposi├º├úo ├® o ponto do campo).
+ */
+export function calcularImpactoExclusaoGrupo(dbRef, grupoId) {
+    const pessoasIds = (dbRef.pessoas || [])
+        .filter((p) => (p.grupoIds || []).includes(grupoId))
+        .map((p) => p.id);
+    return { pessoasIds };
+}
+
+/**
+ * Quem referencia o Autor `autorId` ÔÇö poemas e prosas onde ele est├í
+ * vinculado (independente do papel). Mesmo esp├¡rito de
+ * calcularImpactoExclusaoPessoa acima.
+ */
+export function calcularImpactoExclusaoAutor(dbRef, autorId) {
+    const acha = (item) => (item.autoria || []).some((a) => a.autorId == autorId);
+    return {
+        poemasIds: (dbRef.poemas || []).filter(acha).map((p) => p.id),
+        prosasIds: (dbRef.prosas || []).filter(acha).map((p) => p.id),
+    };
+}
+
+/**
+ * Quem referencia a ├ëpoca `epocaId` ÔÇö poemas e prosas cuja
+ * epocaRetratada aponta pra ela. Mesmo esp├¡rito de
+ * calcularImpactoExclusaoPessoa/Autor acima: excluir a ├ëpoca n├úo exclui
+ * o texto, s├│ desvincula.
+ *
+ * Corrigido numa sess├úo posterior: s├│ checava `dbRef.poemas` ÔÇö resqu├¡cio
+ * de coment├írio desatualizado (dizia "Prosa ainda n├úo tem epocaRetratada,
+ * ver item 4", mas o item 4 j├í deu Prosa o campo h├í sess├Áes). Prosa com
+ * ├ëpoca vinculada n├úo entrava na contagem de impacto nem era desvinculada
+ * ao excluir/mesclar a ├ëpoca ÔÇö ficava com um `epocaId` ├│rf├úo apontando
+ * pra uma ├ëpoca j├í removida do cadastro. Ver hist├│rico no .md de status.
+ */
+export function calcularImpactoExclusaoEpoca(dbRef, epocaId) {
+    const acha = (item) => item.epocaRetratada?.epocaId == epocaId;
+    return {
+        poemasIds: (dbRef.poemas || []).filter(acha).map((p) => p.id),
+        prosasIds: (dbRef.prosas || []).filter(acha).map((p) => p.id),
+    };
+}
+
+// ÔöÇÔöÇÔöÇ Mesclar (Pessoa/├ëpoca) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Pedido do Victor: renomear uma Pessoa/├ëpoca pra igual a outra n├úo
+// mescla nada sozinho ÔÇö s├│ deixa duas entradas com nome (e, no caso de
+// ├ëpoca, Contexto do relacionamento) id├¬nticos, cada uma com seu
+// cadastro e v├¡nculos pr├│prios. A dedup autom├ítica (migrarPessoas/
+// migrarEpocas, obterOuCriarPessoaPorNome/obterOuCriarEpocaPorNome) s├│
+// cobre o momento em que o nome ├® criado (migra├º├úo ou digita├º├úo no
+// modal de Poema/Prosa) ÔÇö nunca o caso de duas entradas que j├í existiam
+// separadas e s├│ ficaram "iguais" depois, por uma delas ser renomeada
+// na aba de gest├úo. Mesma lacuna j├í registrada no .md de status como
+// "mesclar Pessoas" pendente ÔÇö implementada agora pra Pessoa e ├ëpoca
+// junto (mesma mec├ónica pros dois).
+//
+// `origemId` "morre" (some do cadastro); `destinoId` "sobrevive" e
+// herda todos os v├¡nculos da origem. Fun├º├úo pura (s├│ muda `dbRef`,
+// n├úo chama save() nem toca DOM) ÔÇö mesmo crit├®rio de migrarPessoas/
+// migrarEpocas, quem chama decide quando persistir (ver abrirModalMesclar
+// em forms.js).
+
+/**
+ * Reatribui pra `destinoId` todo v├¡nculo de Pessoa que apontava pra
+ * `origemId` (em `item.pessoas`, Poema e Prosa), une `grupoIds` das
+ * duas (sem duplicar) e remove a origem do cadastro. Se um mesmo item
+ * j├í vinculava as duas pessoas (raro, mas poss├¡vel ÔÇö ex. cadastradas
+ * separadamente antes de algu├®m perceber que eram a mesma), os `papeis`
+ * das duas entradas s├úo unidos numa s├│, em vez de deixar duas entradas
+ * pra mesma pessoa (agora id├¬ntica) no mesmo item.
+ */
+export function mesclarPessoas(dbRef, origemId, destinoId) {
+    if (origemId == destinoId) return;
+    const origem = dbRef.pessoas?.find((p) => p.id == origemId);
+    const destino = dbRef.pessoas?.find((p) => p.id == destinoId);
+    if (!origem || !destino) return;
+
+    ['poemas', 'prosas'].forEach((col) => {
+        (dbRef[col] || []).forEach((it) => {
+            if (!Array.isArray(it.pessoas)) return;
+            const entradaOrigem = it.pessoas.find((p) => p.pessoaId == origemId);
+            if (!entradaOrigem) return;
+            const entradaDestino = it.pessoas.find((p) => p.pessoaId == destinoId);
+            if (entradaDestino) {
+                entradaDestino.papeis = [
+                    ...new Set([...(entradaDestino.papeis || []), ...(entradaOrigem.papeis || [])]),
+                ];
+                it.pessoas = it.pessoas.filter((p) => p.pessoaId != origemId);
+            } else {
+                entradaOrigem.pessoaId = destinoId;
+            }
+        });
+    });
+
+    destino.grupoIds = [...new Set([...(destino.grupoIds || []), ...(origem.grupoIds || [])])];
+    dbRef.pessoas = dbRef.pessoas.filter((p) => p.id != origemId);
+}
+
+/**
+ * Reatribui pra `destinoId` todo `epocaRetratada.epocaId` que apontava
+ * pra `origemId` (Poema e Prosa) e remove a origem do cadastro.
+ * Contexto do relacionamento/Notas da origem preenchem o destino s├│ se
+ * o destino estiver com o campo vazio ÔÇö nunca sobrescreve o que a
+ * sobrevivente j├í tinha preenchido (mesmo crit├®rio de "s├│ entra se
+ * estiver vazio" j├í usado em aplicarSugestaoEpoca, forms.js).
+ */
+export function mesclarEpocas(dbRef, origemId, destinoId) {
+    if (origemId == destinoId) return;
+    const origem = dbRef.epocas?.find((e) => e.id == origemId);
+    const destino = dbRef.epocas?.find((e) => e.id == destinoId);
+    if (!origem || !destino) return;
+
+    ['poemas', 'prosas'].forEach((col) => {
+        (dbRef[col] || []).forEach((it) => {
+            if (it.epocaRetratada?.epocaId == origemId) {
+                it.epocaRetratada.epocaId = destinoId;
+            }
+        });
+    });
+
+    if (!destino.contextoRelacao && origem.contextoRelacao) {
+        destino.contextoRelacao = origem.contextoRelacao;
+    }
+    if (!destino.notas && origem.notas) {
+        destino.notas = origem.notas;
+    }
+    dbRef.epocas = dbRef.epocas.filter((e) => e.id != origemId);
+}
+
 // ÔöÇÔöÇÔöÇ Exclus├úo com "desfazer" ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
 // Excluir um item some da lista na hora (e salva), mas a capa associada
 // (se houver) s├│ ├® apagada de verdade do IndexedDB depois de alguns
@@ -439,6 +1078,88 @@ function _removerParaExclusao(col, id) {
         db.itensColetanea = (db.itensColetanea || []).filter((i) => !itensIds.includes(i.id));
     }
 
+    // Excluir Pessoa: some do cadastro (j├í feito pelo splice acima) e
+    // precisa deixar de aparecer em todo poema/prosa que a referenciava
+    // ÔÇö sen├úo sobra um pessoaId ├│rf├úo apontando pra ningu├®m. Guarda
+    // cada v├¡nculo removido (com o papel que tinha) pra restaurar no
+    // "Desfazer"; a ordem dentro de item.pessoas n├úo ├® significativa
+    // (diferente de `papeis`, que preserva ordem de marca├º├úo), ent├úo
+    // restaurar no fim do array ├® suficiente.
+    let vinculosPessoaRemovidos = [];
+    if (col === 'pessoas') {
+        ['poemas', 'prosas'].forEach((itemCol) => {
+            (db[itemCol] || []).forEach((it) => {
+                if (!Array.isArray(it.pessoas)) return;
+                for (let i = it.pessoas.length - 1; i >= 0; i--) {
+                    if (it.pessoas[i].pessoaId == id) {
+                        vinculosPessoaRemovidos.push({
+                            itemCol,
+                            itemId: it.id,
+                            entrada: it.pessoas[i],
+                        });
+                        it.pessoas.splice(i, 1);
+                    }
+                }
+            });
+        });
+    }
+
+    // Excluir Autor: some do cadastro e precisa deixar de aparecer em
+    // todo poema/prosa que o referenciava ÔÇö sen├úo sobra um autorId
+    // ├│rf├úo apontando pra ningu├®m. Mesmo padr├úo de vinculosPessoaRemovidos
+    // acima, mas em `item.autoria` (v├¡nculo { autorId, papel }).
+    let vinculosAutoriaRemovidos = [];
+    if (col === 'autores') {
+        ['poemas', 'prosas'].forEach((itemCol) => {
+            (db[itemCol] || []).forEach((it) => {
+                if (!Array.isArray(it.autoria)) return;
+                for (let i = it.autoria.length - 1; i >= 0; i--) {
+                    if (it.autoria[i].autorId == id) {
+                        vinculosAutoriaRemovidos.push({
+                            itemCol,
+                            itemId: it.id,
+                            entrada: it.autoria[i],
+                        });
+                        it.autoria.splice(i, 1);
+                    }
+                }
+            });
+        });
+    }
+
+    // Excluir ├ëpoca: some do cadastro e o `epocaId` de todo poema/prosa
+    // que a referenciava volta pra null ÔÇö o texto continua existindo, s├│
+    // "perde" a refer├¬ncia ao per├¡odo (mesmo esp├¡rito de v├¡nculos de
+    // Autor/Pessoa acima). Guarda `itemCol` (poemas ou prosas) igual aos
+    // dois padr├Áes acima ÔÇö corrigido junto com calcularImpactoExclusaoEpoca
+    // (ver coment├írio l├í): antes s├│ varria `db.poemas`.
+    let vinculosEpocaRemovidos = [];
+    if (col === 'epocas') {
+        ['poemas', 'prosas'].forEach((itemCol) => {
+            (db[itemCol] || []).forEach((it) => {
+                if (it.epocaRetratada?.epocaId == id) {
+                    vinculosEpocaRemovidos.push({ itemCol, itemId: it.id, epocaId: id });
+                    it.epocaRetratada.epocaId = null;
+                }
+            });
+        });
+    }
+
+    // Excluir Grupo: some do cadastro e some de `grupoIds` de toda
+    // Pessoa que pertencia a ele ÔÇö a Pessoa continua existindo, s├│ deixa
+    // de fazer parte desse grupo espec├¡fico (ela pode estar em outros).
+    let vinculosGrupoRemovidos = [];
+    if (col === 'grupos') {
+        (db.pessoas || []).forEach((p) => {
+            if (!Array.isArray(p.grupoIds)) return;
+            const idx = p.grupoIds.indexOf(id);
+            if (idx !== -1) {
+                vinculosGrupoRemovidos.push({ pessoaId: p.id });
+                p.grupoIds.splice(idx, 1);
+            }
+        });
+    }
+
     // Fecha o buraco deixado na numera├º├úo do grupo de onde o item saiu
     // (mesma l├│gica de sempre ÔÇö s├│ guardamos os "irm├úos" pra poder
     // reverter com abrirEspaco() se a exclus├úo for desfeita).
@@ -457,14 +1178,37 @@ function _removerParaExclusao(col, id) {
     }
     if (irmaos) fecharEspaco(irmaos, posicaoRemovida);
 
-    return { col, item, partesRemovidas, itensRemovidos, capasParaDescartar, posicaoRemovida, irmaos };
+    return {
+        col,
+        item,
+        partesRemovidas,
+        itensRemovidos,
+        vinculosPessoaRemovidos,
+        vinculosAutoriaRemovidos,
+        vinculosGrupoRemovidos,
+        vinculosEpocaRemovidos,
+        capasParaDescartar,
+        posicaoRemovida,
+        irmaos,
+    };
 }
 
 // Devolve o item (e cascata) pros arrays do db, reabrindo o espa├ºo na
 // numera├º├úo que fecharEspaco tinha fechado. N├úo salva sozinho ÔÇö ver
 // coment├írio em _removerParaExclusao.
 function _restaurar(removido) {
-    const { col, item, partesRemovidas, itensRemovidos, posicaoRemovida, irmaos } = removido;
+    const {
+        col,
+        item,
+        partesRemovidas,
+        itensRemovidos,
+        vinculosPessoaRemovidos,
+        vinculosAutoriaRemovidos,
+        vinculosGrupoRemovidos,
+        vinculosEpocaRemovidos,
+        posicaoRemovida,
+        irmaos,
+    } = removido;
 
     if (irmaos) abrirEspaco(irmaos, posicaoRemovida);
 
@@ -473,6 +1217,22 @@ function _restaurar(removido) {
     if (itensRemovidos.length) {
         db.itensColetanea = [...(db.itensColetanea || []), ...itensRemovidos];
     }
+    (vinculosPessoaRemovidos || []).forEach(({ itemCol, itemId, entrada }) => {
+        const it = db[itemCol]?.find((i) => i.id == itemId);
+        if (it) (it.pessoas ||= []).push(entrada);
+    });
+    (vinculosAutoriaRemovidos || []).forEach(({ itemCol, itemId, entrada }) => {
+        const it = db[itemCol]?.find((i) => i.id == itemId);
+        if (it) (it.autoria ||= []).push(entrada);
+    });
+    (vinculosGrupoRemovidos || []).forEach(({ pessoaId }) => {
+        const p = db.pessoas?.find((i) => i.id == pessoaId);
+        if (p) (p.grupoIds ||= []).push(item.id);
+    });
+    (vinculosEpocaRemovidos || []).forEach(({ itemCol, itemId, epocaId }) => {
+        const it = db[itemCol]?.find((i) => i.id == itemId);
+        if (it && it.epocaRetratada) it.epocaRetratada.epocaId = epocaId;
+    });
 }
 
 // Confirma a exclus├úo pendente de vez: apaga a(s) capa(s) do IndexedDB.
@@ -506,7 +1266,7 @@ function _desfazerExclusaoPendente() {
 
 export function deleteItem(col, id) {
     const item = db[col]?.find((i) => i.id == id);
-    const titulo = item?.titulo || item?.tipo || `#${id}`;
+    const titulo = item?.titulo || item?.tipo || item?.nome || `#${id}`;
     let rotulo = ROTULOS_COL[col] || col;
 
     // Para colet├óneas, informa quantas partes e itens ser├úo removidos em cascata
@@ -521,6 +1281,50 @@ export function deleteItem(col, id) {
         }
     }
 
+    // Para Pessoa, avisa em quantos poemas/prosas ela vai deixar de
+    // aparecer (o v├¡nculo some, o texto em si n├úo ├® afetado).
+    if (col === 'pessoas') {
+        const { poemasIds, prosasIds } = calcularImpactoExclusaoPessoa(db, id);
+        const total = poemasIds.length + prosasIds.length;
+        rotulo =
+            total > 0
+                ? `Pessoa ┬À deixar├í de aparecer em ${total} texto${total !== 1 ? 's' : ''}`
+                : 'Pessoa';
+    }
+
+    // Para Autor, avisa em quantos poemas/prosas ele vai deixar de
+    // aparecer (o v├¡nculo some, o texto em si n├úo ├® afetado).
+    if (col === 'autores') {
+        const { poemasIds, prosasIds } = calcularImpactoExclusaoAutor(db, id);
+        const total = poemasIds.length + prosasIds.length;
+        rotulo =
+            total > 0
+                ? `Autor ┬À deixar├í de aparecer em ${total} texto${total !== 1 ? 's' : ''}`
+                : 'Autor';
+    }
+
+    // Para ├ëpoca, avisa em quantos poemas/prosas ela vai deixar de
+    // aparecer (o v├¡nculo some ÔÇö epocaId volta pra null ÔÇö, o texto em
+    // si n├úo ├® afetado). Mesmo padr├úo de Pessoa/Autor acima.
+    if (col === 'epocas') {
+        const { poemasIds, prosasIds } = calcularImpactoExclusaoEpoca(db, id);
+        const total = poemasIds.length + prosasIds.length;
+        rotulo =
+            total > 0
+                ? `├ëpoca ┬À deixar├í de aparecer em ${total} texto${total !== 1 ? 's' : ''}`
+                : '├ëpoca';
+    }
+
+    // Para Grupo, avisa quantas pessoas deixar├úo de pertencer a ele.
+    if (col === 'grupos') {
+        const { pessoasIds } = calcularImpactoExclusaoGrupo(db, id);
+        const total = pessoasIds.length;
+        rotulo =
+            total > 0
+                ? `Grupo ┬À ${total} pessoa${total !== 1 ? 's' : ''} deixar├í de pertencer a ele`
+                : 'Grupo';
+    }
+
     abrirModalExclusao(titulo, rotulo, () => {
         // S├│ um "desfazer" pendente por vez ÔÇö uma nova exclus├úo confirma
         // a anterior de vez (apaga a capa dela) antes de continuar.
@@ -560,10 +1364,8 @@ export function deleteItemsEmMassa(col, ids) {
         plural: `${(ROTULOS_COL[col] || col).toLowerCase()}s`,
         participio: 'exclu├¡dos',
     };
-    const toast = mostrarAvisoComAcao(
-        `${n} ${info.plural} ${info.participio}`,
-        'Desfazer',
-        () => _desfazerExclusaoPendente(),
+    const toast = mostrarAvisoComAcao(`${n} ${info.plural} ${info.participio}`, 'Desfazer', () =>
+        _desfazerExclusaoPendente(),
     );
     const timeoutId = setTimeout(_finalizarExclusaoPendente, 6000);
     _pendingExclusao = { removidos, timeoutId, toast };
diff --git a/js/editor.js b/js/editor.js
index dc2ec67..4ce9353 100644
--- a/js/editor.js
+++ b/js/editor.js
@@ -3,16 +3,28 @@
 // Importado por: main.js (inicializa├º├úo)
 // ============================================================
 
-import { db } from './db.js';
+import { db, obterOuCriarPessoaPorNome, obterOuCriarAutorPorNome } from './db.js';
 import {
     extrairSinalizacoesUnicas,
-    extrairPessoasUnicas,
     extrairGenerosUnicos,
     extrairValoresUnicosDeAnotacoes,
     extrairValoresUnicosDeIntertextualidade,
-    extrairNomesEpocaUnicos,
+    extrairIdiomasUnicos,
+    extrairMeiosEnviosUnicos,
+    extrairPremiosUnicos,
     escapeHtml,
     mostrarAviso,
+    abrirModalConfirmacao,
+    rotuloElo,
+    direcaoInversa,
+    PAPEIS_PESSOA,
+    iniciaisPapeisPessoa,
+    paresGrupoPessoa,
+    classesCorGrupo,
+    AUTORIA_PAPEIS,
+    lerDataParcial,
+    preencherDataParcial,
+    formatarDataParcial,
 } from './utils.js';
 
 // ÔöÇÔöÇÔöÇ Estado local ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
@@ -134,40 +146,460 @@ function criarGrupoDeTags({ inputId, containerId, hiddenInputId, corClasse, nome
     return { adicionar, remover, renderizar, reset, carregar };
 }
 
-const grupoTagsPoema = criarGrupoDeTags({
-    inputId: 'p-sinal-input',
-    containerId: 'p-tags-container',
-    hiddenInputId: 'p-sinal',
-    corClasse: 'bg-blue-600',
-    nomeFuncaoRemover: 'removerTag',
+// Sinaliza├º├Áes viraram 5 grupos (Estilo/Tema/Rela├º├úo/Sensibilidade/Tom)
+// em vez de 1 ÔÇö mesma engine de sempre (criarGrupoDeTags), s├│ que
+// instanciada 5x por Poema e 5x por Prosa. SINAL_CATEGORIAS ├® a lista
+// ├║nica de configura├º├úo; os m├│dulos e os exports nomeados abaixo (que
+// window.* em main.js precisa, um por fun├º├úo, por causa do onclick="..."
+// embutido no HTML renderizado) s├úo gerados a partir dela pra n├úo
+// repetir os IDs de DOM em dois lugares.
+const SINAL_CATEGORIAS = [
+    { chave: 'Estilo', cor: 'bg-blue-600' },
+    { chave: 'Tema', cor: 'bg-emerald-600' },
+    { chave: 'Relacao', cor: 'bg-purple-600' },
+    { chave: 'Sensibilidade', cor: 'bg-amber-600' },
+    { chave: 'Tom', cor: 'bg-pink-600' },
+    // Balde tempor├írio pra tags migradas que ainda n├úo t├¬m categoria de
+    // verdade (hoje: "Premiados", "Tradu├º├úo", "Varia├º├Áes" ÔÇö que devem
+    // virar Reconhecimentos e Elos tipados de Deriva├º├úo numa etapa
+    // futura, ver An├ílise de estrutura e metadados po├®ticos). Fica
+    // vis├¡vel no modal em vez de escondido no JSON pra n├úo se perder de
+    // vista at├® esses campos existirem.
+    { chave: 'Outros', cor: 'bg-gray-500' },
+];
+
+function criarModuloDeTags(config) {
+    const grupo = criarGrupoDeTags(config);
+    return {
+        adicionar: (valor = null) => grupo.adicionar(valor),
+        remover: (v) => grupo.remover(v),
+        renderizar: () => grupo.renderizar(),
+        reset: () => grupo.reset(),
+        carregar: (str) => grupo.carregar(str),
+    };
+}
+
+// slugDom: "Estilo" -> "estilo" (pro id de DOM, ex.: p-sinal-estilo-input)
+const slugDom = (chave) => chave.charAt(0).toLowerCase() + chave.slice(1);
+
+const modulosSinalPoema = {};
+const modulosSinalProsa = {};
+SINAL_CATEGORIAS.forEach(({ chave, cor }) => {
+    const slug = slugDom(chave);
+    modulosSinalPoema[chave] = criarModuloDeTags({
+        inputId: `p-sinal-${slug}-input`,
+        containerId: `p-sinal-${slug}-container`,
+        hiddenInputId: `p-sinal-${slug}`,
+        corClasse: cor,
+        nomeFuncaoRemover: `removerSinal${chave}`,
+    });
+    modulosSinalProsa[chave] = criarModuloDeTags({
+        inputId: `pr-sinal-${slug}-input`,
+        containerId: `pr-sinal-${slug}-container`,
+        hiddenInputId: `pr-sinal-${slug}`,
+        corClasse: cor,
+        nomeFuncaoRemover: `removerSinal${chave}Prosa`,
+    });
 });
-const grupoPessoasPoema = criarGrupoDeTags({
+
+const grupoGeneroProsa = criarGrupoDeTags({
+    inputId: 'pr-genero-input',
+    containerId: 'pr-genero-container',
+    hiddenInputId: 'pr-genero',
+    corClasse: 'bg-amber-600',
+    nomeFuncaoRemover: 'removerGeneroProsa',
+});
+
+// ÔöÇÔöÇÔöÇ F├íbrica de grupo de Pessoas (chip + papel) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Variante de criarGrupoDeTags: guarda um array de objeto
+// { pessoaId, papeis } em vez de string simples ÔÇö pessoaId referencia
+// o cadastro central db.pessoas (ver migrarPessoasParaCadastro em
+// db.js), papeis ├® o v├¡nculo espec├¡fico daquele texto com a pessoa
+// (Retratado(a)/Inspirado(a) por/Dedicat├írio(a)/Mencionado(a)/Aludido(a) ÔÇö ver
+// PAPEIS_PESSOA em utils.js). Sem hiddenInputId: diferente dos grupos
+// de tags, que gravam a string combinada num input escondido (lido por
+// `.value` em forms.js), este exp├Áe `obterItens()` ÔÇö forms.js l├¬ o
+// array direto na hora do submit, mesmo padr├úo de Intertextualidade/
+// Anexos (`obterIntertextualidade`/`obterAnexos`, ver criarListaDeEntradas
+// acima), que tamb├®m guardam objeto em vez de string simples.
+function criarGrupoDePessoas({
+    inputId,
+    containerId,
+    corClasse,
+    nomeFuncaoRemover,
+    nomeFuncaoAlternarPapel,
+    nomeFuncaoAlternarDropdown,
+    infoGruposId = null,
+}) {
+    let itens = [];
+    // dropdown de pap├®is aberto no momento (pessoaId), pra fechar ao
+    // abrir outro ou ao clicar fora ÔÇö s├│ um aberto por vez.
+    let dropdownAberto = null;
+
+    function nomeDe(pessoaId) {
+        return db.pessoas.find((p) => p.id == pessoaId)?.nome || '(pessoa removida)';
+    }
+
+    function adicionarPorId(pessoaId) {
+        if (!itens.some((i) => i.pessoaId == pessoaId)) {
+            itens.push({ pessoaId, papeis: [] });
+            renderizar();
+        }
+    }
+
+    // Resolve nome digitado ÔåÆ pessoaId. Nome que bate exatamente com
+    // algu├®m j├í cadastrado reaproveita o id direto; nome sem
+    // correspond├¬ncia pede confirma├º├úo expl├¡cita antes de criar pessoa
+    // nova (ver conversa que definiu isso) ÔÇö evita que um typo vire uma
+    // pessoa nova por acidente no cadastro central.
+    function adicionar(valor = null) {
+        const input = document.getElementById(inputId);
+        const nome = (valor ?? input?.value ?? '').trim();
+        if (input) input.value = '';
+        if (!nome) return;
+
+        const existente = db.pessoas.find((p) => p.nome === nome);
+        if (existente) {
+            adicionarPorId(existente.id);
+            return;
+        }
+
+        abrirModalConfirmacao({
+            titulo: `Criar pessoa "${nome}"?`,
+            rotulo: 'Pessoa nova',
+            mensagem: `"${nome}" ainda n├úo est├í no cadastro de Pessoas. Criar agora (sem grupo ÔÇö d├í pra atribuir depois na aba Pessoas)?`,
+            textoConfirmar: 'Criar',
+            corConfirmar: '#e11d48',
+            onConfirmar: () => {
+                const pessoa = obterOuCriarPessoaPorNome(nome);
+                adicionarPorId(pessoa.id);
+            },
+        });
+    }
+
+    function remover(pessoaId) {
+        itens = itens.filter((i) => i.pessoaId != pessoaId);
+        if (dropdownAberto == pessoaId) dropdownAberto = null;
+        renderizar();
+    }
+
+    // Marcar acrescenta ao fim de `papeis` (= ordem de marca├º├úo vira
+    // ordem de exibi├º├úo ÔÇö n├úo ├® uma hierarquia fixa por categoria, ver
+    // migrarPapeisPessoa em db.js); desmarcar remove do array, mantendo
+    // a ordem relativa dos que sobraram.
+    function alternarPapel(pessoaId, papel, marcado) {
+        const item = itens.find((i) => i.pessoaId == pessoaId);
+        if (!item) return;
+        if (marcado) {
+            if (!item.papeis.includes(papel)) item.papeis.push(papel);
+        } else {
+            item.papeis = item.papeis.filter((p) => p !== papel);
+        }
+        renderizar(); // precisa redesenhar: a ordem dos pap├®is j├í marcados pode mudar
+    }
+
+    function alternarDropdown(pessoaId) {
+        dropdownAberto = dropdownAberto == pessoaId ? null : pessoaId;
+        renderizar();
+    }
+
+    // Fecha o dropdown ao clicar fora dele ÔÇö sem isso ficaria aberto at├®
+    // a pessoa clicar em alguma outra coisa dentro do pr├│prio grupo.
+    // Usa composedPath() (caminho do clique fixado no momento do
+    // dispatch) em vez de container.contains(ev.target): o pr├│prio bot├úo
+    // que abre o dropdown chama renderizar() (troca o innerHTML) antes
+    // do clique terminar de borbulhar at├® aqui, o que desconectaria
+    // ev.target do container e fecharia o dropdown no mesmo clique que
+    // acabou de abrir ele.
+    document.addEventListener('click', (ev) => {
+        if (!dropdownAberto) return;
+        const container = document.getElementById(containerId);
+        if (container && !ev.composedPath().includes(container)) {
+            dropdownAberto = null;
+            renderizar();
+        }
+    });
+
+    // Painel somente-leitura, embaixo dos chips: mostra os Grupos que as
+    // pessoas selecionadas trazem consigo (n├úo ├® edit├ível por aqui ÔÇö Grupo
+    // ├® caracter├¡stica da Pessoa, atribu├¡da na aba Pessoas; ver
+    // paresGrupoPessoa em utils.js, mesma resolu├º├úo usada na coluna
+    // "Grupos" das tabelas ÔÇö ver badgesGrupos em render-listas.js ÔÇö e na
+    // exporta├º├úo em Markdown ÔÇö ver exportar-md.js). Some quando ningu├®m
+    // selecionado est├í em grupo nenhum, pra n├úo sobrar um r├│tulo vazio.
+    function renderPainelGrupos() {
+        if (!infoGruposId) return;
+        const painel = document.getElementById(infoGruposId);
+        if (!painel) return;
+        const pares = paresGrupoPessoa({ pessoas: itens }, db.pessoas, db.grupos);
+        if (!pares.length) {
+            painel.innerHTML = '';
+            return;
+        }
+        const badges = pares
+            .map(
+                ({ grupo, pessoa }) =>
+                    `<span class="text-[9px] ${classesCorGrupo(grupo.cor)} px-1.5 py-0.5 rounded mr-1 mb-1 inline-block">${escapeHtml(grupo.nome)} <span class="opacity-70">(${escapeHtml(pessoa.nome)})</span></span>`,
+            )
+            .join('');
+        painel.innerHTML = `<span class="mr-1">Grupos:</span>${badges}`;
+    }
+
+    function renderizar() {
+        const container = document.getElementById(containerId);
+        if (!container) return;
+
+        const checkboxesPapel = (i) =>
+            PAPEIS_PESSOA.map((p) => {
+                const marcado = i.papeis.includes(p);
+                return `
+                <label class="flex items-center gap-1 px-2 py-1 text-[10px] text-gray-700 dark:text-slate-200 hover:bg-gray-100 dark:hover:bg-slate-700 cursor-pointer whitespace-nowrap">
+                    <input type="checkbox" ${marcado ? 'checked' : ''}
+                        onchange="${nomeFuncaoAlternarPapel}(${JSON.stringify(i.pessoaId)}, '${escapeHtml(p).replace(/'/g, "\\'")}', this.checked)"
+                        class="rounded border-gray-300 dark:border-slate-600 text-rose-500 focus:ring-rose-400" />
+                    ${escapeHtml(p)}
+                </label>`;
+            }).join('');
+
+        container.innerHTML = itens
+            .map((i) => {
+                const nome = nomeDe(i.pessoaId);
+                const rotuloPapeis = i.papeis.length
+                    ? `<span class="opacity-80">${escapeHtml(iniciaisPapeisPessoa(i.papeis))}</span>`
+                    : `<span class="opacity-50 italic">sem papel</span>`;
+                const aberto = dropdownAberto == i.pessoaId;
+                return `
+            <span class="relative ${corClasse} text-white text-[10px] pl-2 pr-1 py-1 rounded-full inline-flex items-center gap-1">
+                ${escapeHtml(nome)}
+                <button type="button" data-id="${escapeHtml(String(i.pessoaId))}" onclick="${nomeFuncaoAlternarDropdown}(this.dataset.id)"
+                    class="text-[9px] bg-white/20 rounded px-1 py-0 hover:bg-white/30">
+                    ${rotuloPapeis}
+                </button>
+                <button type="button" data-id="${escapeHtml(String(i.pessoaId))}" onclick="${nomeFuncaoRemover}(this.dataset.id)" class="hover:text-red-200 font-bold ml-1">├ù</button>
+                ${
+                    aberto
+                        ? `<div class="absolute z-10 top-full left-0 mt-1 bg-white dark:bg-slate-800 border border-gray-200 dark:border-slate-600 rounded shadow-lg py-1 min-w-max">${checkboxesPapel(i)}</div>`
+                        : ''
+                }
+            </span>`;
+            })
+            .join('');
+        renderPainelGrupos();
+    }
+
+    function reset() {
+        itens = [];
+        dropdownAberto = null;
+        renderizar();
+    }
+
+    // Aceita o array j├í migrado ({pessoaId, papeis}) e ÔÇö s├│ por defesa ÔÇö
+    // formatos antigos: {nome, papeis}/{nome, papel} (schema anterior ├á
+    // virada pro cadastro central) e string "a, b, c" (n├úo deveria mais
+    // acontecer, j├í que db.js normaliza tudo no load, mas evita quebrar
+    // se esta fun├º├úo for chamada antes disso por algum motivo). Nome
+    // legado sem pessoa cadastrada correspondente cria uma (sem
+    // confirma├º├úo aqui ÔÇö carregar() ├® preenchimento autom├ítico do
+    // formul├írio, n├úo digita├º├úo da pessoa, ent├úo n├úo faz sentido pedir
+    // confirma├º├úo de cria├º├úo nesse caminho).
+    function carregar(pessoas) {
+        if (Array.isArray(pessoas)) {
+            itens = pessoas.map((p) => {
+                if (typeof p === 'string')
+                    return { pessoaId: obterOuCriarPessoaPorNome(p).id, papeis: [] };
+                if (p.pessoaId !== undefined)
+                    return { pessoaId: p.pessoaId, papeis: [...(p.papeis || [])] };
+                if (Array.isArray(p.papeis))
+                    return {
+                        pessoaId: obterOuCriarPessoaPorNome(p.nome).id,
+                        papeis: [...p.papeis],
+                    };
+                return {
+                    pessoaId: obterOuCriarPessoaPorNome(p.nome).id,
+                    papeis: p.papel ? [p.papel] : [],
+                };
+            });
+        } else {
+            itens = (pessoas || '')
+                .split(',')
+                .map((s) => s.trim())
+                .filter(Boolean)
+                .map((nome) => ({ pessoaId: obterOuCriarPessoaPorNome(nome).id, papeis: [] }));
+        }
+        dropdownAberto = null;
+        renderizar();
+    }
+
+    function obterItens() {
+        return itens;
+    }
+
+    return {
+        adicionar,
+        remover,
+        alternarPapel,
+        alternarDropdown,
+        renderizar,
+        reset,
+        carregar,
+        obterItens,
+    };
+}
+
+// ÔöÇÔöÇÔöÇ F├íbrica de grupo de Autoria (chip + papel ├║nico) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Variante mais simples de criarGrupoDePessoas: o v├¡nculo itemÔåöAutor
+// ├® single-role (Autor OU Coautor, nunca os dois pro mesmo texto ÔÇö ver
+// AUTORIA_PAPEIS em utils.js), ent├úo cada chip mostra um <select>
+// inline em vez do dropdown de checkboxes de Pessoas ÔÇö n├úo tem "sem
+// papel" poss├¡vel aqui, todo autor adicionado j├í entra com um papel
+// (padr├úo 'Autor'). Resolve nome digitado ÔåÆ autorId no cadastro
+// central db.autores (ver migrarAutoria/obterOuCriarAutorPorNome em
+// db.js), mesmo padr├úo de confirma├º├úo de "criar autor novo" que
+// criarGrupoDePessoas usa pra Pessoa.
+function criarGrupoDeAutoria({
+    inputId,
+    containerId,
+    corClasse,
+    nomeFuncaoRemover,
+    nomeFuncaoAlterarPapel,
+}) {
+    let itens = [];
+
+    function nomeDe(autorId) {
+        return db.autores.find((a) => a.id == autorId)?.nome || '(autor removido)';
+    }
+
+    function adicionarPorId(autorId) {
+        if (!itens.some((i) => i.autorId == autorId)) {
+            itens.push({ autorId, papel: AUTORIA_PAPEIS[0] });
+            renderizar();
+        }
+    }
+
+    function adicionar(valor = null) {
+        const input = document.getElementById(inputId);
+        const nome = (valor ?? input?.value ?? '').trim();
+        if (input) input.value = '';
+        if (!nome) return;
+
+        const existente = db.autores.find((a) => a.nome === nome);
+        if (existente) {
+            adicionarPorId(existente.id);
+            return;
+        }
+
+        abrirModalConfirmacao({
+            titulo: `Criar autor "${nome}"?`,
+            rotulo: 'Autor novo',
+            mensagem: `"${nome}" ainda n├úo est├í no cadastro de Autores. Criar agora?`,
+            textoConfirmar: 'Criar',
+            corConfirmar: '#e11d48',
+            onConfirmar: () => {
+                const autor = obterOuCriarAutorPorNome(nome);
+                adicionarPorId(autor.id);
+            },
+        });
+    }
+
+    function remover(autorId) {
+        itens = itens.filter((i) => i.autorId != autorId);
+        renderizar();
+    }
+
+    function alterarPapel(autorId, papel) {
+        const item = itens.find((i) => i.autorId == autorId);
+        if (!item || !AUTORIA_PAPEIS.includes(papel)) return;
+        item.papel = papel;
+        renderizar();
+    }
+
+    function renderizar() {
+        const container = document.getElementById(containerId);
+        if (!container) return;
+
+        const opcoesPapel = (i) =>
+            AUTORIA_PAPEIS.map(
+                (p) =>
+                    `<option value="${escapeHtml(p)}" ${i.papel === p ? 'selected' : ''}>${escapeHtml(p)}</option>`,
+            ).join('');
+
+        container.innerHTML = itens
+            .map((i) => {
+                const nome = nomeDe(i.autorId);
+                return `
+            <span class="relative ${corClasse} text-white text-[10px] pl-2 pr-1 py-1 rounded-full inline-flex items-center gap-1">
+                ${escapeHtml(nome)}
+                <select data-id="${escapeHtml(String(i.autorId))}"
+                    onchange="${nomeFuncaoAlterarPapel}(this.dataset.id, this.value)"
+                    class="text-[9px] bg-white/20 rounded px-1 py-0 border-0 text-white [&>option]:text-black">
+                    ${opcoesPapel(i)}
+                </select>
+                <button type="button" data-id="${escapeHtml(String(i.autorId))}" onclick="${nomeFuncaoRemover}(this.dataset.id)" class="hover:text-red-200 font-bold ml-1">├ù</button>
+            </span>`;
+            })
+            .join('');
+    }
+
+    function reset() {
+        itens = [];
+        renderizar();
+    }
+
+    // Aceita o array j├í migrado ({autorId, papel}) e ÔÇö s├│ por defesa ÔÇö
+    // nome legado sem correspond├¬ncia (cria autor sem confirma├º├úo, mesmo
+    // racioc├¡nio de carregar() em criarGrupoDePessoas: preenchimento
+    // autom├ítico do formul├írio, n├úo digita├º├úo).
+    function carregar(autoria) {
+        itens = (Array.isArray(autoria) ? autoria : []).map((a) => {
+            if (a.autorId !== undefined)
+                return { autorId: a.autorId, papel: a.papel || AUTORIA_PAPEIS[0] };
+            return {
+                autorId: obterOuCriarAutorPorNome(a.nome).id,
+                papel: a.papel || AUTORIA_PAPEIS[0],
+            };
+        });
+        renderizar();
+    }
+
+    function obterItens() {
+        return itens;
+    }
+
+    return { adicionar, remover, alterarPapel, renderizar, reset, carregar, obterItens };
+}
+
+const grupoPessoasPoema = criarGrupoDePessoas({
     inputId: 'p-pessoa-input',
     containerId: 'p-pessoas-container',
-    hiddenInputId: 'p-pessoas',
     corClasse: 'bg-rose-500',
     nomeFuncaoRemover: 'removerPessoa',
+    nomeFuncaoAlternarPapel: 'alternarPapelPessoa',
+    nomeFuncaoAlternarDropdown: 'alternarDropdownPapelPessoa',
+    infoGruposId: 'p-pessoas-grupos-info',
 });
-const grupoTagsProsa = criarGrupoDeTags({
-    inputId: 'pr-sinal-input',
-    containerId: 'pr-tags-container',
-    hiddenInputId: 'pr-sinal',
-    corClasse: 'bg-blue-600',
-    nomeFuncaoRemover: 'removerTagProsa',
-});
-const grupoPessoasProsa = criarGrupoDeTags({
+const grupoPessoasProsa = criarGrupoDePessoas({
     inputId: 'pr-pessoa-input',
     containerId: 'pr-pessoas-container',
-    hiddenInputId: 'pr-pessoas',
     corClasse: 'bg-rose-500',
     nomeFuncaoRemover: 'removerPessoaProsa',
+    nomeFuncaoAlternarPapel: 'alternarPapelPessoaProsa',
+    nomeFuncaoAlternarDropdown: 'alternarDropdownPapelPessoaProsa',
+    infoGruposId: 'pr-pessoas-grupos-info',
 });
-const grupoGeneroProsa = criarGrupoDeTags({
-    inputId: 'pr-genero-input',
-    containerId: 'pr-genero-container',
-    hiddenInputId: 'pr-genero',
-    corClasse: 'bg-amber-600',
-    nomeFuncaoRemover: 'removerGeneroProsa',
+
+const grupoAutoriaPoema = criarGrupoDeAutoria({
+    inputId: 'p-autor-input',
+    containerId: 'p-autoria-container',
+    corClasse: 'bg-indigo-600',
+    nomeFuncaoRemover: 'removerAutoria',
+    nomeFuncaoAlterarPapel: 'alterarPapelAutoria',
+});
+const grupoAutoriaProsa = criarGrupoDeAutoria({
+    inputId: 'pr-autor-input',
+    containerId: 'pr-autoria-container',
+    corClasse: 'bg-indigo-600',
+    nomeFuncaoRemover: 'removerAutoriaProsa',
+    nomeFuncaoAlterarPapel: 'alterarPapelAutoriaProsa',
 });
 
 // ÔöÇÔöÇÔöÇ Listas gen├®ricas de entradas (objetos ou texto livre) ÔöÇÔöÇÔöÇÔöÇ
@@ -443,6 +875,696 @@ export function resetAnexos() {
     atualizarBotaoAnexo();
 }
 
+// ÔöÇÔöÇÔöÇ Elos / Refer├¬ncias (lista de poema-alvo+tipo+texto) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Item 1 do plano de schema: cada elo/refer├¬ncia aponta pra outro poema
+// (`id`) mais uma nota livre opcional. Elos = liga├º├úo estrutural/de
+// deriva├º├úo, sempre BILATERAL (Reescrita, Tradu├º├úo, Resposta...);
+// Refer├¬ncias = liga├º├úo mais solta, sempre UNIDIRECIONAL (Personagem em
+// comum, Imagem central compartilhada, Aceno a...), ainda com um `tipo`
+// de lista fechada simples (TIPOS_REFERENCIA em utils.js).
+//
+// Elos usa um schema diferente de Refer├¬ncias desde o redesenho
+// Rela├º├úo+Dire├º├úo: em vez de `tipo` (lista fechada de r├│tulos, um valor
+// por r├│tulo poss├¡vel ÔÇö "Reescrita de" e "Reescrito em" eram dois
+// valores em vez de dois lados da mesma rela├º├úo), guarda `relacao` (uma
+// das 8 rela├º├Áes, ver RELACOES_ELO em utils.js) + `direcao` ('origem' =
+// texto mais antigo/base, 'destino' = texto derivado/mais novo). O
+// r├│tulo mostrado (ver rotuloElo em utils.js) ├® sempre derivado dos
+// dois ÔÇö os dois bot├Áes de dire├º├úo no modal j├í mostram o r├│tulo de
+// verdade pra Rela├º├úo escolhida (ver atualizarRotulosDirecaoElo
+// abaixo), n├úo "Origem"/"Destino" cru.
+//
+// Dois motores separados (n├úo um s├│ reaproveitado) porque cada lista
+// tem seu pr├│prio container e campos no modal.
+
+function renderItemEloBilateral(it) {
+    const poema = db.poemas.find((p) => p.id == it.id);
+    const rotulo = it.relacao ? rotuloElo(it.relacao, it.direcao) : '';
+    const badge = rotulo
+        ? `<span class="inline-block px-1.5 py-0.5 mr-1 rounded bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 text-[10px] font-bold uppercase align-middle">${escapeHtml(rotulo)}</span>`
+        : '';
+    const titulo = poema
+        ? escapeHtml(poema.titulo)
+        : `<span class="italic text-gray-400 dark:text-slate-500">(poema removido)</span>`;
+    const nota = it.texto ? ` ÔÇö ${escapeHtml(it.texto)}` : '';
+    return `${badge}${titulo}${nota}`;
+}
+
+function renderItemReferencia(it) {
+    const poema = db.poemas.find((p) => p.id == it.id);
+    const badge = it.tipo
+        ? `<span class="inline-block px-1.5 py-0.5 mr-1 rounded bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 text-[10px] font-bold uppercase align-middle">${escapeHtml(it.tipo)}</span>`
+        : '';
+    const titulo = poema
+        ? escapeHtml(poema.titulo)
+        : `<span class="italic text-gray-400 dark:text-slate-500">(poema removido)</span>`;
+    const nota = it.texto ? ` ÔÇö ${escapeHtml(it.texto)}` : '';
+    return `${badge}${titulo}${nota}`;
+}
+
+const listaElosPoema = criarListaDeEntradas({
+    containerId: 'p-elos-lista',
+    renderItem: renderItemEloBilateral,
+    nomeFuncaoRemover: 'removerElo',
+    nomeFuncaoEditar: 'editarElo',
+});
+
+const listaReferenciasPoema = criarListaDeEntradas({
+    containerId: 'p-refs-lista',
+    renderItem: renderItemReferencia,
+    nomeFuncaoRemover: 'removerReferencia',
+    nomeFuncaoEditar: 'editarReferencia',
+});
+
+function atualizarBotaoElo() {
+    const btnAdd = document.getElementById('p-elo-btn-add');
+    const btnCancelar = document.getElementById('p-elo-btn-cancelar');
+    const emEdicao = listaElosPoema.estaEditando();
+    if (btnAdd) btnAdd.textContent = emEdicao ? 'Ô£ô' : '+';
+    if (btnCancelar) btnCancelar.classList.toggle('hidden', !emEdicao);
+}
+
+function atualizarBotaoReferencia() {
+    const btnAdd = document.getElementById('p-ref-btn-add');
+    const btnCancelar = document.getElementById('p-ref-btn-cancelar');
+    const emEdicao = listaReferenciasPoema.estaEditando();
+    if (btnAdd) btnAdd.textContent = emEdicao ? 'Ô£ô' : '+';
+    if (btnCancelar) btnCancelar.classList.toggle('hidden', !emEdicao);
+}
+
+// Atualiza o texto dos dois bot├Áes de dire├º├úo pra o r├│tulo de verdade
+// da Rela├º├úo escolhida (ex.: Rela├º├úo "Reescrita" ÔåÆ bot├Áes viram
+// "Reescrito em" / "Reescrita de") ÔÇö sem rela├º├úo escolhida ainda,
+// caem pro r├│tulo gen├®rico Origem/Destino. N├úo mexe em qual bot├úo est├í
+// marcado como ativo; ver marcarDirecaoElo pra isso.
+export function atualizarRotulosDirecaoElo() {
+    const relacao = document.getElementById('p-elo-relacao')?.value || '';
+    const btnOrigem = document.getElementById('p-elo-direcao-origem');
+    const btnDestino = document.getElementById('p-elo-direcao-destino');
+    if (btnOrigem) btnOrigem.textContent = relacao ? rotuloElo(relacao, 'origem') : 'Origem';
+    if (btnDestino) btnDestino.textContent = relacao ? rotuloElo(relacao, 'destino') : 'Destino';
+}
+
+// Chamado pelo onchange do select de Rela├º├úo: atualiza os r├│tulos dos
+// bot├Áes E limpa a dire├º├úo j├í marcada (uma dire├º├úo escolhida pra
+// Rela├º├úo anterior n├úo necessariamente faz sentido pra nova).
+export function onRelacaoEloAlterada() {
+    atualizarRotulosDirecaoElo();
+    marcarDirecaoElo('');
+}
+
+// Marca visualmente qual bot├úo de dire├º├úo est├í ativo e grava o valor
+// no input escondido `p-elo-direcao`, que ├® o que adicionarElo/editarElo
+// de fato leem/gravam.
+function marcarDirecaoElo(direcao) {
+    const hidden = document.getElementById('p-elo-direcao');
+    if (hidden) hidden.value = direcao;
+    const ativa =
+        'bg-blue-100 dark:bg-blue-900 border-blue-400 dark:border-blue-600 text-blue-700 dark:text-blue-300';
+    const inativa =
+        'bg-transparent border-slate-200 dark:border-slate-700 text-gray-500 dark:text-slate-400';
+    const btnOrigem = document.getElementById('p-elo-direcao-origem');
+    const btnDestino = document.getElementById('p-elo-direcao-destino');
+    if (btnOrigem)
+        btnOrigem.className = `elo-direcao-btn text-xs flex-1 px-2 py-1 rounded border ${direcao === 'origem' ? ativa : inativa}`;
+    if (btnDestino)
+        btnDestino.className = `elo-direcao-btn text-xs flex-1 px-2 py-1 rounded border ${direcao === 'destino' ? ativa : inativa}`;
+}
+
+export function selecionarDirecaoElo(direcao) {
+    marcarDirecaoElo(direcao);
+}
+
+export function adicionarElo() {
+    const poemaEl = document.getElementById('p-elo-poema');
+    const relacaoEl = document.getElementById('p-elo-relacao');
+    const direcaoEl = document.getElementById('p-elo-direcao');
+    const textoEl = document.getElementById('p-elo-texto');
+    const id = poemaEl?.value ? parseInt(poemaEl.value, 10) : null;
+    if (!id) return;
+    listaElosPoema.salvar({
+        id,
+        relacao: relacaoEl?.value || '',
+        direcao: direcaoEl?.value || '',
+        texto: (textoEl?.value || '').trim(),
+    });
+    if (poemaEl) poemaEl.value = '';
+    if (relacaoEl) relacaoEl.value = '';
+    if (textoEl) textoEl.value = '';
+    atualizarRotulosDirecaoElo();
+    marcarDirecaoElo('');
+    atualizarBotaoElo();
+}
+export function editarElo(indice) {
+    const item = listaElosPoema.iniciarEdicao(indice);
+    const poemaEl = document.getElementById('p-elo-poema');
+    const relacaoEl = document.getElementById('p-elo-relacao');
+    const textoEl = document.getElementById('p-elo-texto');
+    if (poemaEl) poemaEl.value = item.id ?? '';
+    if (relacaoEl) relacaoEl.value = item.relacao || '';
+    if (textoEl) textoEl.value = item.texto || '';
+    atualizarRotulosDirecaoElo();
+    marcarDirecaoElo(item.direcao || '');
+    atualizarBotaoElo();
+}
+export function cancelarEdicaoElo() {
+    listaElosPoema.cancelarEdicao();
+    const poemaEl = document.getElementById('p-elo-poema');
+    const relacaoEl = document.getElementById('p-elo-relacao');
+    const textoEl = document.getElementById('p-elo-texto');
+    if (poemaEl) poemaEl.value = '';
+    if (relacaoEl) relacaoEl.value = '';
+    if (textoEl) textoEl.value = '';
+    atualizarRotulosDirecaoElo();
+    marcarDirecaoElo('');
+    atualizarBotaoElo();
+}
+export function removerElo(indice) {
+    listaElosPoema.remover(indice);
+    atualizarBotaoElo();
+}
+export function obterElos() {
+    return listaElosPoema.obterItens();
+}
+export function carregarElos(lista) {
+    listaElosPoema.carregar(lista);
+    atualizarBotaoElo();
+}
+export function resetElos() {
+    listaElosPoema.reset();
+    const relacaoEl = document.getElementById('p-elo-relacao');
+    if (relacaoEl) relacaoEl.value = '';
+    atualizarRotulosDirecaoElo();
+    marcarDirecaoElo('');
+    atualizarBotaoElo();
+}
+
+export function adicionarReferencia() {
+    const poemaEl = document.getElementById('p-ref-poema');
+    const tipoEl = document.getElementById('p-ref-tipo');
+    const textoEl = document.getElementById('p-ref-texto');
+    const id = poemaEl?.value ? parseInt(poemaEl.value, 10) : null;
+    if (!id) return;
+    listaReferenciasPoema.salvar({
+        id,
+        tipo: tipoEl?.value || '',
+        texto: (textoEl?.value || '').trim(),
+    });
+    if (poemaEl) poemaEl.value = '';
+    if (tipoEl) tipoEl.value = '';
+    if (textoEl) textoEl.value = '';
+    atualizarBotaoReferencia();
+}
+export function editarReferencia(indice) {
+    const item = listaReferenciasPoema.iniciarEdicao(indice);
+    const poemaEl = document.getElementById('p-ref-poema');
+    const tipoEl = document.getElementById('p-ref-tipo');
+    const textoEl = document.getElementById('p-ref-texto');
+    if (poemaEl) poemaEl.value = item.id ?? '';
+    if (tipoEl) tipoEl.value = item.tipo || '';
+    if (textoEl) textoEl.value = item.texto || '';
+    atualizarBotaoReferencia();
+}
+export function cancelarEdicaoReferencia() {
+    listaReferenciasPoema.cancelarEdicao();
+    const poemaEl = document.getElementById('p-ref-poema');
+    const tipoEl = document.getElementById('p-ref-tipo');
+    const textoEl = document.getElementById('p-ref-texto');
+    if (poemaEl) poemaEl.value = '';
+    if (tipoEl) tipoEl.value = '';
+    if (textoEl) textoEl.value = '';
+    atualizarBotaoReferencia();
+}
+export function removerReferencia(indice) {
+    listaReferenciasPoema.remover(indice);
+    atualizarBotaoReferencia();
+}
+export function obterReferencias() {
+    return listaReferenciasPoema.obterItens();
+}
+export function carregarReferencias(lista) {
+    listaReferenciasPoema.carregar(lista);
+    atualizarBotaoReferencia();
+}
+export function resetReferencias() {
+    listaReferenciasPoema.reset();
+    atualizarBotaoReferencia();
+}
+
+// ÔöÇÔöÇÔöÇ Painel de Elos derivados (refinamento do item 1) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// S├│ pra Elos (bilaterais) ÔÇö Refer├¬ncias s├úo unidirecionais por
+// natureza, sem "outro lado" a inferir. Quando o poema B ├® alvo de um
+// elo cadastrado no poema A, mas o poema B n├úo tem um elo manual de
+// volta pra A, mostra aqui um aviso calculado ("Referenciado por..."),
+// sem duplicar o cadastro. Se o v├¡nculo manual do lado de B for criado,
+// o painel derivado some ÔÇö pra n├úo mostrar a mesma rela├º├úo duas vezes.
+// Desde o redesenho Rela├º├úo+Dire├º├úo, o r├│tulo do lado derivado ├® sempre
+// calculado (mesma rela├º├úo, dire├º├úo invertida via direcaoInversa em
+// utils.js) ÔÇö n├úo depende mais de um mapa de tipos com par nomeado.
+// Item 4: Elos agora podem ligar PoemaÔåöProsa (n├úo s├│ PoemaÔåöPoema), ent├úo
+// o c├ílculo de derivados precisa varrer os dois arrays dos dois lados ÔÇö
+// tanto pra achar o item atual (que pode ser um poema OU uma prosa)
+// quanto pros "outros" que podem ter um elo apontando pra ele. ids nunca
+// colidem entre os dois arrays (gerarId() ├® um contador global ├║nico,
+// ver resolverTituloPoemaOuProsa em render-listas.js pro mesmo padr├úo).
+function todosItensComElos() {
+    return [...db.poemas, ...(db.prosas || [])];
+}
+
+function elosDerivados(itemId) {
+    if (!itemId) return [];
+    const idAtual = typeof itemId === 'string' ? parseInt(itemId, 10) : itemId;
+    if (!idAtual) return [];
+    const todos = todosItensComElos();
+    const itemAtual = todos.find((it) => it.id == idAtual);
+    const idsJaLigados = new Set((itemAtual?.conceitos?.elos || []).map((e) => e.id));
+    const derivados = [];
+    for (const outro of todos) {
+        if (outro.id == idAtual) continue;
+        for (const elo of outro.conceitos?.elos || []) {
+            if (elo.id == idAtual && !idsJaLigados.has(outro.id)) {
+                derivados.push({
+                    id: outro.id,
+                    titulo: outro.titulo,
+                    rotulo: elo.relacao ? rotuloElo(elo.relacao, direcaoInversa(elo.direcao)) : '',
+                });
+            }
+        }
+    }
+    return derivados;
+}
+
+function renderizarPainelElosDerivadosEm(containerId, itemId) {
+    const container = document.getElementById(containerId);
+    if (!container) return;
+    const derivados = elosDerivados(itemId);
+    if (!derivados.length) {
+        container.innerHTML = '';
+        container.classList.add('hidden');
+        return;
+    }
+    container.classList.remove('hidden');
+    const linhas = derivados
+        .map((d) => {
+            const badge = d.rotulo
+                ? `<span class="inline-block px-1.5 py-0.5 mr-1 rounded bg-gray-100 dark:bg-slate-800 text-gray-500 dark:text-slate-400 text-[10px] font-bold uppercase align-middle">${escapeHtml(d.rotulo)}</span>`
+                : '';
+            return `<div class="text-xs text-gray-500 dark:text-slate-400 italic">${badge}${escapeHtml(d.titulo)}</div>`;
+        })
+        .join('');
+    container.innerHTML =
+        `<div class="text-[11px] text-gray-400 dark:text-slate-500 mb-1">Referenciado por (calculado ÔÇö sem elo cadastrado de volta):</div>` +
+        linhas;
+}
+
+export function renderPainelElosDerivados(poemaId) {
+    renderizarPainelElosDerivadosEm('p-elos-derivados', poemaId);
+}
+
+export function renderPainelElosDerivadosProsa(prosaId) {
+    renderizarPainelElosDerivadosEm('pr-elos-derivados', prosaId);
+}
+
+// ÔöÇÔöÇÔöÇ Elos / Refer├¬ncias / Intertextualidade / Anexos (Prosa) ÔöÇÔöÇÔöÇ
+// Item 4 do plano de schema: mesmo motor de cada campo do Poema acima,
+// espelhado pra Prosa (ids `p-`ÔåÆ`pr-`, fun├º├Áes com sufixo `Prosa`).
+// Diferen├ºa deliberada: o alvo de Elos/Refer├¬ncias de Prosa pode ser um
+// Poema OU outra Prosa (ver renderDropdowns em ui.js ÔÇö optgroup
+// Poemas+Prosas no `<select>` pr-elo-poema/pr-ref-poema), enquanto o
+// Modal de Poema por ora s├│ oferece outros Poemas como alvo ÔÇö por isso
+// resolverItemVinculado abaixo busca nos dois arrays, diferente de
+// renderItemEloBilateral/renderItemReferencia (Poema) que buscam s├│ em
+// db.poemas.
+function resolverItemVinculado(id) {
+    return db.poemas.find((p) => p.id == id) || (db.prosas || []).find((pr) => pr.id == id);
+}
+
+function renderItemEloBilateralProsa(it) {
+    const item = resolverItemVinculado(it.id);
+    const rotulo = it.relacao ? rotuloElo(it.relacao, it.direcao) : '';
+    const badge = rotulo
+        ? `<span class="inline-block px-1.5 py-0.5 mr-1 rounded bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 text-[10px] font-bold uppercase align-middle">${escapeHtml(rotulo)}</span>`
+        : '';
+    const titulo = item
+        ? escapeHtml(item.titulo)
+        : `<span class="italic text-gray-400 dark:text-slate-500">(texto removido)</span>`;
+    const nota = it.texto ? ` ÔÇö ${escapeHtml(it.texto)}` : '';
+    return `${badge}${titulo}${nota}`;
+}
+
+function renderItemReferenciaProsa(it) {
+    const item = resolverItemVinculado(it.id);
+    const badge = it.tipo
+        ? `<span class="inline-block px-1.5 py-0.5 mr-1 rounded bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 text-[10px] font-bold uppercase align-middle">${escapeHtml(it.tipo)}</span>`
+        : '';
+    const titulo = item
+        ? escapeHtml(item.titulo)
+        : `<span class="italic text-gray-400 dark:text-slate-500">(texto removido)</span>`;
+    const nota = it.texto ? ` ÔÇö ${escapeHtml(it.texto)}` : '';
+    return `${badge}${titulo}${nota}`;
+}
+
+const listaElosProsa = criarListaDeEntradas({
+    containerId: 'pr-elos-lista',
+    renderItem: renderItemEloBilateralProsa,
+    nomeFuncaoRemover: 'removerEloProsa',
+    nomeFuncaoEditar: 'editarEloProsa',
+});
+
+const listaReferenciasProsa = criarListaDeEntradas({
+    containerId: 'pr-refs-lista',
+    renderItem: renderItemReferenciaProsa,
+    nomeFuncaoRemover: 'removerReferenciaProsa',
+    nomeFuncaoEditar: 'editarReferenciaProsa',
+});
+
+function atualizarBotaoEloProsa() {
+    const btnAdd = document.getElementById('pr-elo-btn-add');
+    const btnCancelar = document.getElementById('pr-elo-btn-cancelar');
+    const emEdicao = listaElosProsa.estaEditando();
+    if (btnAdd) btnAdd.textContent = emEdicao ? 'Ô£ô' : '+';
+    if (btnCancelar) btnCancelar.classList.toggle('hidden', !emEdicao);
+}
+
+function atualizarBotaoReferenciaProsa() {
+    const btnAdd = document.getElementById('pr-ref-btn-add');
+    const btnCancelar = document.getElementById('pr-ref-btn-cancelar');
+    const emEdicao = listaReferenciasProsa.estaEditando();
+    if (btnAdd) btnAdd.textContent = emEdicao ? 'Ô£ô' : '+';
+    if (btnCancelar) btnCancelar.classList.toggle('hidden', !emEdicao);
+}
+
+export function atualizarRotulosDirecaoEloProsa() {
+    const relacao = document.getElementById('pr-elo-relacao')?.value || '';
+    const btnOrigem = document.getElementById('pr-elo-direcao-origem');
+    const btnDestino = document.getElementById('pr-elo-direcao-destino');
+    if (btnOrigem) btnOrigem.textContent = relacao ? rotuloElo(relacao, 'origem') : 'Origem';
+    if (btnDestino) btnDestino.textContent = relacao ? rotuloElo(relacao, 'destino') : 'Destino';
+}
+
+export function onRelacaoEloAlteradaProsa() {
+    atualizarRotulosDirecaoEloProsa();
+    marcarDirecaoEloProsa('');
+}
+
+function marcarDirecaoEloProsa(direcao) {
+    const hidden = document.getElementById('pr-elo-direcao');
+    if (hidden) hidden.value = direcao;
+    const ativa =
+        'bg-blue-100 dark:bg-blue-900 border-blue-400 dark:border-blue-600 text-blue-700 dark:text-blue-300';
+    const inativa =
+        'bg-transparent border-slate-200 dark:border-slate-700 text-gray-500 dark:text-slate-400';
+    const btnOrigem = document.getElementById('pr-elo-direcao-origem');
+    const btnDestino = document.getElementById('pr-elo-direcao-destino');
+    if (btnOrigem)
+        btnOrigem.className = `elo-direcao-btn text-xs flex-1 px-2 py-1 rounded border ${direcao === 'origem' ? ativa : inativa}`;
+    if (btnDestino)
+        btnDestino.className = `elo-direcao-btn text-xs flex-1 px-2 py-1 rounded border ${direcao === 'destino' ? ativa : inativa}`;
+}
+
+export function selecionarDirecaoEloProsa(direcao) {
+    marcarDirecaoEloProsa(direcao);
+}
+
+export function adicionarEloProsa() {
+    const alvoEl = document.getElementById('pr-elo-poema');
+    const relacaoEl = document.getElementById('pr-elo-relacao');
+    const direcaoEl = document.getElementById('pr-elo-direcao');
+    const textoEl = document.getElementById('pr-elo-texto');
+    const id = alvoEl?.value ? parseInt(alvoEl.value, 10) : null;
+    if (!id) return;
+    listaElosProsa.salvar({
+        id,
+        relacao: relacaoEl?.value || '',
+        direcao: direcaoEl?.value || '',
+        texto: (textoEl?.value || '').trim(),
+    });
+    if (alvoEl) alvoEl.value = '';
+    if (relacaoEl) relacaoEl.value = '';
+    if (textoEl) textoEl.value = '';
+    atualizarRotulosDirecaoEloProsa();
+    marcarDirecaoEloProsa('');
+    atualizarBotaoEloProsa();
+}
+export function editarEloProsa(indice) {
+    const item = listaElosProsa.iniciarEdicao(indice);
+    const alvoEl = document.getElementById('pr-elo-poema');
+    const relacaoEl = document.getElementById('pr-elo-relacao');
+    const textoEl = document.getElementById('pr-elo-texto');
+    if (alvoEl) alvoEl.value = item.id ?? '';
+    if (relacaoEl) relacaoEl.value = item.relacao || '';
+    if (textoEl) textoEl.value = item.texto || '';
+    atualizarRotulosDirecaoEloProsa();
+    marcarDirecaoEloProsa(item.direcao || '');
+    atualizarBotaoEloProsa();
+}
+export function cancelarEdicaoEloProsa() {
+    listaElosProsa.cancelarEdicao();
+    const alvoEl = document.getElementById('pr-elo-poema');
+    const relacaoEl = document.getElementById('pr-elo-relacao');
+    const textoEl = document.getElementById('pr-elo-texto');
+    if (alvoEl) alvoEl.value = '';
+    if (relacaoEl) relacaoEl.value = '';
+    if (textoEl) textoEl.value = '';
+    atualizarRotulosDirecaoEloProsa();
+    marcarDirecaoEloProsa('');
+    atualizarBotaoEloProsa();
+}
+export function removerEloProsa(indice) {
+    listaElosProsa.remover(indice);
+    atualizarBotaoEloProsa();
+}
+export function obterElosProsa() {
+    return listaElosProsa.obterItens();
+}
+export function carregarElosProsa(lista) {
+    listaElosProsa.carregar(lista);
+    atualizarBotaoEloProsa();
+}
+export function resetElosProsa() {
+    listaElosProsa.reset();
+    const relacaoEl = document.getElementById('pr-elo-relacao');
+    if (relacaoEl) relacaoEl.value = '';
+    atualizarRotulosDirecaoEloProsa();
+    marcarDirecaoEloProsa('');
+    atualizarBotaoEloProsa();
+}
+
+export function adicionarReferenciaProsa() {
+    const alvoEl = document.getElementById('pr-ref-poema');
+    const tipoEl = document.getElementById('pr-ref-tipo');
+    const textoEl = document.getElementById('pr-ref-texto');
+    const id = alvoEl?.value ? parseInt(alvoEl.value, 10) : null;
+    if (!id) return;
+    listaReferenciasProsa.salvar({
+        id,
+        tipo: tipoEl?.value || '',
+        texto: (textoEl?.value || '').trim(),
+    });
+    if (alvoEl) alvoEl.value = '';
+    if (tipoEl) tipoEl.value = '';
+    if (textoEl) textoEl.value = '';
+    atualizarBotaoReferenciaProsa();
+}
+export function editarReferenciaProsa(indice) {
+    const item = listaReferenciasProsa.iniciarEdicao(indice);
+    const alvoEl = document.getElementById('pr-ref-poema');
+    const tipoEl = document.getElementById('pr-ref-tipo');
+    const textoEl = document.getElementById('pr-ref-texto');
+    if (alvoEl) alvoEl.value = item.id ?? '';
+    if (tipoEl) tipoEl.value = item.tipo || '';
+    if (textoEl) textoEl.value = item.texto || '';
+    atualizarBotaoReferenciaProsa();
+}
+export function cancelarEdicaoReferenciaProsa() {
+    listaReferenciasProsa.cancelarEdicao();
+    const alvoEl = document.getElementById('pr-ref-poema');
+    const tipoEl = document.getElementById('pr-ref-tipo');
+    const textoEl = document.getElementById('pr-ref-texto');
+    if (alvoEl) alvoEl.value = '';
+    if (tipoEl) tipoEl.value = '';
+    if (textoEl) textoEl.value = '';
+    atualizarBotaoReferenciaProsa();
+}
+export function removerReferenciaProsa(indice) {
+    listaReferenciasProsa.remover(indice);
+    atualizarBotaoReferenciaProsa();
+}
+export function obterReferenciasProsa() {
+    return listaReferenciasProsa.obterItens();
+}
+export function carregarReferenciasProsa(lista) {
+    listaReferenciasProsa.carregar(lista);
+    atualizarBotaoReferenciaProsa();
+}
+export function resetReferenciasProsa() {
+    listaReferenciasProsa.reset();
+    atualizarBotaoReferenciaProsa();
+}
+
+// ÔöÇÔöÇÔöÇ Intertextualidade (Prosa) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+const listaIntertextoProsa = criarListaDeEntradas({
+    containerId: 'pr-intertexto-lista',
+    renderItem: (it) =>
+        `${
+            it.tipo
+                ? `<span class="inline-block px-1.5 py-0.5 mr-1 rounded bg-purple-100 dark:bg-purple-900 text-purple-700 dark:text-purple-300 text-[10px] font-bold uppercase align-middle">${escapeHtml(it.tipo)}</span>`
+                : ''
+        }${escapeHtml(it.texto || '')}`,
+    nomeFuncaoRemover: 'removerIntertextoProsa',
+    nomeFuncaoEditar: 'editarIntertextoProsa',
+});
+
+function atualizarBotaoIntertextoProsa() {
+    const btnAdd = document.getElementById('pr-intertexto-btn-add');
+    const btnCancelar = document.getElementById('pr-intertexto-btn-cancelar');
+    const emEdicao = listaIntertextoProsa.estaEditando();
+    if (btnAdd) btnAdd.textContent = emEdicao ? 'Ô£ô' : '+';
+    if (btnCancelar) btnCancelar.classList.toggle('hidden', !emEdicao);
+}
+
+export function atualizarDatalistIntertextoProsa() {
+    const datalist = document.getElementById('sugestoes-intertexto-prosa');
+    if (datalist) {
+        datalist.innerHTML = extrairValoresUnicosDeIntertextualidade(db.prosas || [])
+            .map((v) => `<option value="${escapeHtml(v)}">`)
+            .join('');
+    }
+}
+
+export function adicionarIntertextoProsa() {
+    const tipoEl = document.getElementById('pr-intertexto-tipo');
+    const textoEl = document.getElementById('pr-intertexto-texto');
+    const tipo = tipoEl?.value || '';
+    const texto = (textoEl?.value || '').trim();
+    if (!tipo && !texto) return;
+    listaIntertextoProsa.salvar({ tipo, texto });
+    if (tipoEl) tipoEl.value = '';
+    if (textoEl) textoEl.value = '';
+    atualizarBotaoIntertextoProsa();
+    atualizarDatalistIntertextoProsa();
+}
+export function editarIntertextoProsa(indice) {
+    const item = listaIntertextoProsa.iniciarEdicao(indice);
+    const tipoEl = document.getElementById('pr-intertexto-tipo');
+    const textoEl = document.getElementById('pr-intertexto-texto');
+    if (tipoEl) tipoEl.value = item.tipo || '';
+    if (textoEl) textoEl.value = item.texto || '';
+    textoEl?.focus();
+    atualizarBotaoIntertextoProsa();
+}
+export function cancelarEdicaoIntertextoProsa() {
+    listaIntertextoProsa.cancelarEdicao();
+    const tipoEl = document.getElementById('pr-intertexto-tipo');
+    const textoEl = document.getElementById('pr-intertexto-texto');
+    if (tipoEl) tipoEl.value = '';
+    if (textoEl) textoEl.value = '';
+    atualizarBotaoIntertextoProsa();
+}
+export function removerIntertextoProsa(indice) {
+    listaIntertextoProsa.remover(indice);
+    atualizarBotaoIntertextoProsa();
+}
+export function obterIntertextualidadeProsa() {
+    return listaIntertextoProsa.obterItens();
+}
+export function carregarIntertextualidadeProsa(lista) {
+    listaIntertextoProsa.carregar(lista);
+    atualizarBotaoIntertextoProsa();
+}
+export function resetIntertextualidadeProsa() {
+    listaIntertextoProsa.reset();
+    atualizarBotaoIntertextoProsa();
+}
+
+// ÔöÇÔöÇÔöÇ Anexos (Prosa) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+const listaAnexosProsa = criarListaDeEntradas({
+    containerId: 'pr-anexos-lista',
+    renderItem: (it) => {
+        const badge = it.tipo
+            ? `<span class="inline-block px-1.5 py-0.5 mr-1 rounded bg-indigo-100 dark:bg-indigo-900 text-indigo-700 dark:text-indigo-300 text-[10px] font-bold uppercase align-middle">${escapeHtml(it.tipo)}</span>`
+            : '';
+        const link = it.link
+            ? ` <a href="${escapeHtml(it.link)}" target="_blank" rel="noopener" class="text-blue-600 dark:text-blue-400 underline text-[11px]">${escapeHtml(it.link)}</a>`
+            : '';
+        return `${badge}${escapeHtml(it.texto || '')}${link}`;
+    },
+    nomeFuncaoRemover: 'removerAnexoProsa',
+    nomeFuncaoEditar: 'editarAnexoProsa',
+});
+
+function atualizarBotaoAnexoProsa() {
+    const btnAdd = document.getElementById('pr-anexo-btn-add');
+    const btnCancelar = document.getElementById('pr-anexo-btn-cancelar');
+    const emEdicao = listaAnexosProsa.estaEditando();
+    if (btnAdd) btnAdd.textContent = emEdicao ? 'Ô£ô Salvar edi├º├úo' : '+ Adicionar anexo';
+    if (btnCancelar) btnCancelar.classList.toggle('hidden', !emEdicao);
+}
+
+export function adicionarAnexoProsa(valor = null) {
+    const tipoEl = document.getElementById('pr-anexo-tipo');
+    const linkEl = document.getElementById('pr-anexo-link');
+    const textoEl = document.getElementById('pr-anexo-input');
+
+    const tipo = tipoEl?.value || '';
+    const link = (linkEl?.value || '').trim();
+    const texto = (valor ?? textoEl?.value ?? '').trim();
+
+    if (!tipo && !texto && !link) return;
+
+    if (TIPOS_ANEXO_COM_LINK_OBRIGATORIO.includes(tipo) && !link) {
+        mostrarAviso(`Anexos do tipo "${tipo}" precisam de um link.`);
+        return;
+    }
+
+    listaAnexosProsa.salvar({ tipo, texto, link });
+    if (tipoEl) tipoEl.value = '';
+    if (linkEl) linkEl.value = '';
+    if (textoEl) textoEl.value = '';
+    atualizarBotaoAnexoProsa();
+}
+export function editarAnexoProsa(indice) {
+    const item = listaAnexosProsa.iniciarEdicao(indice);
+    const tipoEl = document.getElementById('pr-anexo-tipo');
+    const linkEl = document.getElementById('pr-anexo-link');
+    const textoEl = document.getElementById('pr-anexo-input');
+    if (tipoEl) tipoEl.value = item.tipo || '';
+    if (linkEl) linkEl.value = item.link || '';
+    if (textoEl) textoEl.value = item.texto || '';
+    textoEl?.focus();
+    atualizarBotaoAnexoProsa();
+}
+export function cancelarEdicaoAnexoProsa() {
+    listaAnexosProsa.cancelarEdicao();
+    const tipoEl = document.getElementById('pr-anexo-tipo');
+    const linkEl = document.getElementById('pr-anexo-link');
+    const textoEl = document.getElementById('pr-anexo-input');
+    if (tipoEl) tipoEl.value = '';
+    if (linkEl) linkEl.value = '';
+    if (textoEl) textoEl.value = '';
+    atualizarBotaoAnexoProsa();
+}
+export function removerAnexoProsa(indice) {
+    listaAnexosProsa.remover(indice);
+    atualizarBotaoAnexoProsa();
+}
+export function obterAnexosProsa() {
+    return listaAnexosProsa.obterItens();
+}
+export function carregarAnexosProsa(lista) {
+    const normalizada = Array.isArray(lista)
+        ? lista.map((it) => (typeof it === 'string' ? { tipo: '', texto: it, link: '' } : it))
+        : [];
+    listaAnexosProsa.carregar(normalizada);
+    atualizarBotaoAnexoProsa();
+}
+export function resetAnexosProsa() {
+    listaAnexosProsa.reset();
+    atualizarBotaoAnexoProsa();
+}
+
 // ÔöÇÔöÇÔöÇ Anota├º├Áes Marginais (lista de trecho+posi├º├úo+fonte+texto) ÔöÇ
 // Coment├írios de outra "voz" escritos por cima do texto ÔÇö em geral
 // numa fonte cursiva diferente da do poema ÔÇö associados a um verso ou
@@ -562,29 +1684,439 @@ export function resetAnotacoes() {
     atualizarBotaoAnotacao();
 }
 
+// ÔöÇÔöÇÔöÇ Envios e Rea├º├Áes (item 7 ÔÇö lista de pessoa+data+meio+rea├º├úo+notas) ÔöÇ
+// Registro de quando um texto foi enviado/mostrado pra algu├®m e como
+// essa pessoa reagiu. Poema e Prosa desde j├í (ver AUTORIA_PAPEIS/
+// extrairMeiosEnviosUnicos em utils.js pro racioc├¡nio da antecipa├º├úo).
+// Mesmo motor gen├®rico de Anota├º├Áes (criarListaDeEntradas), mas com um
+// campo a mais fora do texto livre: `data`, parcial (dia/m├¬s/ano ÔÇö
+// ver lerDataParcial/preencherDataParcial em utils.js), lida/escrita
+// direto nos tr├¬s `<input type="number">` do formul├írio de adicionar,
+// n├úo guardada em estado local separado. `pessoa` ├® texto livre (s├│
+// reaproveita o datalist de nomes j├í cadastrados como sugest├úo ÔÇö ver
+// coment├írio em extrairMeiosEnviosUnicos), n├úo um v├¡nculo por id como
+// em Autoria/Pessoas.
+
+const listaEnviosPoema = criarListaDeEntradas({
+    containerId: 'p-envios-lista',
+    renderItem: (it) => {
+        const meta = [it.pessoa, it.meio, formatarDataParcial(it.data)]
+            .filter((v) => v && v !== 'ÔÇö')
+            .map(escapeHtml)
+            .join(' ┬À ');
+        const metaHtml = meta
+            ? `<span class="inline-block px-1.5 py-0.5 mr-1 rounded bg-teal-100 dark:bg-teal-900 text-teal-700 dark:text-teal-300 text-[10px] font-bold align-middle">${meta}</span>`
+            : '';
+        const notas = it.notas
+            ? `<span class="block text-[11px] text-gray-400 dark:text-slate-500 italic mt-0.5">${escapeHtml(it.notas)}</span>`
+            : '';
+        return `${metaHtml}${escapeHtml(it.reacao || '')}${notas}`;
+    },
+    nomeFuncaoRemover: 'removerEnvio',
+    nomeFuncaoEditar: 'editarEnvio',
+});
+
+const listaEnviosProsa = criarListaDeEntradas({
+    containerId: 'pr-envios-lista',
+    renderItem: (it) => {
+        const meta = [it.pessoa, it.meio, formatarDataParcial(it.data)]
+            .filter((v) => v && v !== 'ÔÇö')
+            .map(escapeHtml)
+            .join(' ┬À ');
+        const metaHtml = meta
+            ? `<span class="inline-block px-1.5 py-0.5 mr-1 rounded bg-teal-100 dark:bg-teal-900 text-teal-700 dark:text-teal-300 text-[10px] font-bold align-middle">${meta}</span>`
+            : '';
+        const notas = it.notas
+            ? `<span class="block text-[11px] text-gray-400 dark:text-slate-500 italic mt-0.5">${escapeHtml(it.notas)}</span>`
+            : '';
+        return `${metaHtml}${escapeHtml(it.reacao || '')}${notas}`;
+    },
+    nomeFuncaoRemover: 'removerEnvioProsa',
+    nomeFuncaoEditar: 'editarEnvioProsa',
+});
+
+function atualizarBotaoEnvio(prefixo, lista) {
+    const btnAdd = document.getElementById(`${prefixo}-envio-btn-add`);
+    const btnCancelar = document.getElementById(`${prefixo}-envio-btn-cancelar`);
+    const emEdicao = lista.estaEditando();
+    if (btnAdd) btnAdd.textContent = emEdicao ? 'Ô£ô Salvar edi├º├úo' : '+ Adicionar envio';
+    if (btnCancelar) btnCancelar.classList.toggle('hidden', !emEdicao);
+}
+
+// Um datalist por modal (mesmo motivo de sugestoes-idioma/
+// sugestoes-idioma-prosa ÔÇö cada modal carrega o pr├│prio HTML sob
+// demanda via garantirModal), preenchidos com a mesma lista combinada
+// Poemas+Prosas.
+export function atualizarDatalistEnvios() {
+    const todos = [...db.poemas, ...(db.prosas || [])];
+    const valores = extrairMeiosEnviosUnicos(todos);
+    ['sugestoes-meios-envio', 'sugestoes-meios-envio-prosa'].forEach((id) => {
+        const datalist = document.getElementById(id);
+        if (datalist) {
+            datalist.innerHTML = valores.map((v) => `<option value="${escapeHtml(v)}">`).join('');
+        }
+    });
+}
+
+function limparCamposEnvio(prefixo) {
+    ['pessoa', 'meio', 'reacao', 'notas'].forEach((campo) => {
+        const el = document.getElementById(`${prefixo}-envio-${campo}`);
+        if (el) el.value = '';
+    });
+    preencherDataParcial(`${prefixo}-envio`, null);
+}
+
+export function adicionarEnvio() {
+    const pessoa = (document.getElementById('p-envio-pessoa')?.value || '').trim();
+    const meio = (document.getElementById('p-envio-meio')?.value || '').trim();
+    const reacao = (document.getElementById('p-envio-reacao')?.value || '').trim();
+    const notas = (document.getElementById('p-envio-notas')?.value || '').trim();
+    const data = lerDataParcial('p-envio');
+    if (!pessoa && !meio && !reacao && !notas && !data) return;
+
+    listaEnviosPoema.salvar({ pessoa, data, meio, reacao, notas });
+    limparCamposEnvio('p');
+    atualizarBotaoEnvio('p', listaEnviosPoema);
+    atualizarDatalistEnvios();
+}
+export function editarEnvio(indice) {
+    const item = listaEnviosPoema.iniciarEdicao(indice);
+    const pessoaEl = document.getElementById('p-envio-pessoa');
+    const meioEl = document.getElementById('p-envio-meio');
+    const reacaoEl = document.getElementById('p-envio-reacao');
+    const notasEl = document.getElementById('p-envio-notas');
+    if (pessoaEl) pessoaEl.value = item.pessoa || '';
+    if (meioEl) meioEl.value = item.meio || '';
+    if (reacaoEl) reacaoEl.value = item.reacao || '';
+    if (notasEl) notasEl.value = item.notas || '';
+    preencherDataParcial('p-envio', item.data);
+    pessoaEl?.focus();
+    atualizarBotaoEnvio('p', listaEnviosPoema);
+}
+export function cancelarEdicaoEnvio() {
+    listaEnviosPoema.cancelarEdicao();
+    limparCamposEnvio('p');
+    atualizarBotaoEnvio('p', listaEnviosPoema);
+}
+export function removerEnvio(indice) {
+    listaEnviosPoema.remover(indice);
+    atualizarBotaoEnvio('p', listaEnviosPoema);
+}
+export function obterEnvios() {
+    return listaEnviosPoema.obterItens();
+}
+export function carregarEnvios(lista) {
+    listaEnviosPoema.carregar(lista);
+    atualizarBotaoEnvio('p', listaEnviosPoema);
+}
+export function resetEnvios() {
+    listaEnviosPoema.reset();
+    atualizarBotaoEnvio('p', listaEnviosPoema);
+}
+
+export function adicionarEnvioProsa() {
+    const pessoa = (document.getElementById('pr-envio-pessoa')?.value || '').trim();
+    const meio = (document.getElementById('pr-envio-meio')?.value || '').trim();
+    const reacao = (document.getElementById('pr-envio-reacao')?.value || '').trim();
+    const notas = (document.getElementById('pr-envio-notas')?.value || '').trim();
+    const data = lerDataParcial('pr-envio');
+    if (!pessoa && !meio && !reacao && !notas && !data) return;
+
+    listaEnviosProsa.salvar({ pessoa, data, meio, reacao, notas });
+    limparCamposEnvio('pr');
+    atualizarBotaoEnvio('pr', listaEnviosProsa);
+    atualizarDatalistEnvios();
+}
+export function editarEnvioProsa(indice) {
+    const item = listaEnviosProsa.iniciarEdicao(indice);
+    const pessoaEl = document.getElementById('pr-envio-pessoa');
+    const meioEl = document.getElementById('pr-envio-meio');
+    const reacaoEl = document.getElementById('pr-envio-reacao');
+    const notasEl = document.getElementById('pr-envio-notas');
+    if (pessoaEl) pessoaEl.value = item.pessoa || '';
+    if (meioEl) meioEl.value = item.meio || '';
+    if (reacaoEl) reacaoEl.value = item.reacao || '';
+    if (notasEl) notasEl.value = item.notas || '';
+    preencherDataParcial('pr-envio', item.data);
+    pessoaEl?.focus();
+    atualizarBotaoEnvio('pr', listaEnviosProsa);
+}
+export function cancelarEdicaoEnvioProsa() {
+    listaEnviosProsa.cancelarEdicao();
+    limparCamposEnvio('pr');
+    atualizarBotaoEnvio('pr', listaEnviosProsa);
+}
+export function removerEnvioProsa(indice) {
+    listaEnviosProsa.remover(indice);
+    atualizarBotaoEnvio('pr', listaEnviosProsa);
+}
+export function obterEnviosProsa() {
+    return listaEnviosProsa.obterItens();
+}
+export function carregarEnviosProsa(lista) {
+    listaEnviosProsa.carregar(lista);
+    atualizarBotaoEnvio('pr', listaEnviosProsa);
+}
+export function resetEnviosProsa() {
+    listaEnviosProsa.reset();
+    atualizarBotaoEnvio('pr', listaEnviosProsa);
+}
+
+// ÔöÇÔöÇÔöÇ Reconhecimentos (item 8 ÔÇö lista de pr├¬mio+posi├º├úo+ano+texto) ÔöÇÔöÇÔöÇ
+// Pr├¬mios/men├º├Áes que um texto recebeu. Poema e Prosa desde j├í (mesmo
+// padr├úo de antecipa├º├úo de Envios/Autoria/Idioma). Migra a tag solta
+// "Premiados" que hoje mora no balde tempor├írio sinalizacoesOutros ÔÇö
+// ver migrarReconhecimentos em db.js. Mesmo motor gen├®rico
+// (criarListaDeEntradas) de Envios, com `ano` lido/escrito direto num
+// ├║nico <input type="number"> (n├úo parcial como a data de Envios ÔÇö
+// aqui ├® s├│ o ano da premia├º├úo, sem dia/m├¬s). `premio` ├® texto livre
+// (s├│ reaproveita o datalist dos nomes j├í cadastrados como sugest├úo ÔÇö
+// ver extrairPremiosUnicos em utils.js), n├úo um v├¡nculo por id.
+// `posicao` tamb├®m ├® texto livre ÔÇö pr├¬mios diferentes nomeiam
+// coloca├º├úo de formas diferentes ("1┬║ lugar", "Men├º├úo honrosa" etc.).
+
+const listaReconhecimentosPoema = criarListaDeEntradas({
+    containerId: 'p-reconhecimentos-lista',
+    renderItem: (it) => {
+        const meta = [it.premio, it.posicao, it.ano]
+            .filter((v) => v || v === 0)
+            .map((v) => escapeHtml(String(v)))
+            .join(' ┬À ');
+        const metaHtml = meta
+            ? `<span class="inline-block px-1.5 py-0.5 mr-1 rounded bg-amber-100 dark:bg-amber-900 text-amber-700 dark:text-amber-300 text-[10px] font-bold align-middle">${meta}</span>`
+            : '';
+        const texto = it.texto
+            ? `<span class="block text-[11px] text-gray-400 dark:text-slate-500 italic mt-0.5">${escapeHtml(it.texto)}</span>`
+            : '';
+        return `${metaHtml}${texto}`;
+    },
+    nomeFuncaoRemover: 'removerReconhecimento',
+    nomeFuncaoEditar: 'editarReconhecimento',
+});
+
+const listaReconhecimentosProsa = criarListaDeEntradas({
+    containerId: 'pr-reconhecimentos-lista',
+    renderItem: (it) => {
+        const meta = [it.premio, it.posicao, it.ano]
+            .filter((v) => v || v === 0)
+            .map((v) => escapeHtml(String(v)))
+            .join(' ┬À ');
+        const metaHtml = meta
+            ? `<span class="inline-block px-1.5 py-0.5 mr-1 rounded bg-amber-100 dark:bg-amber-900 text-amber-700 dark:text-amber-300 text-[10px] font-bold align-middle">${meta}</span>`
+            : '';
+        const texto = it.texto
+            ? `<span class="block text-[11px] text-gray-400 dark:text-slate-500 italic mt-0.5">${escapeHtml(it.texto)}</span>`
+            : '';
+        return `${metaHtml}${texto}`;
+    },
+    nomeFuncaoRemover: 'removerReconhecimentoProsa',
+    nomeFuncaoEditar: 'editarReconhecimentoProsa',
+});
+
+function atualizarBotaoReconhecimento(prefixo, lista) {
+    const btnAdd = document.getElementById(`${prefixo}-reconhecimento-btn-add`);
+    const btnCancelar = document.getElementById(`${prefixo}-reconhecimento-btn-cancelar`);
+    const emEdicao = lista.estaEditando();
+    if (btnAdd) btnAdd.textContent = emEdicao ? 'Ô£ô Salvar edi├º├úo' : '+ Adicionar reconhecimento';
+    if (btnCancelar) btnCancelar.classList.toggle('hidden', !emEdicao);
+}
+
+// Um datalist por modal (mesmo motivo de sugestoes-meios-envio/
+// sugestoes-meios-envio-prosa), preenchido com a mesma lista
+// combinada Poemas+Prosas.
+export function atualizarDatalistReconhecimentos() {
+    const todos = [...db.poemas, ...(db.prosas || [])];
+    const valores = extrairPremiosUnicos(todos);
+    ['sugestoes-premios', 'sugestoes-premios-prosa'].forEach((id) => {
+        const datalist = document.getElementById(id);
+        if (datalist) {
+            datalist.innerHTML = valores.map((v) => `<option value="${escapeHtml(v)}">`).join('');
+        }
+    });
+}
+
+function limparCamposReconhecimento(prefixo) {
+    ['premio', 'posicao', 'ano', 'texto'].forEach((campo) => {
+        const el = document.getElementById(`${prefixo}-reconhecimento-${campo}`);
+        if (el) el.value = '';
+    });
+}
+
+export function adicionarReconhecimento() {
+    const premio = (document.getElementById('p-reconhecimento-premio')?.value || '').trim();
+    const posicao = (document.getElementById('p-reconhecimento-posicao')?.value || '').trim();
+    const anoStr = (document.getElementById('p-reconhecimento-ano')?.value || '').trim();
+    const ano = anoStr ? parseInt(anoStr, 10) : null;
+    const texto = (document.getElementById('p-reconhecimento-texto')?.value || '').trim();
+    if (!premio && !posicao && !ano && !texto) return;
+
+    listaReconhecimentosPoema.salvar({ premio, posicao, ano, texto });
+    limparCamposReconhecimento('p');
+    atualizarBotaoReconhecimento('p', listaReconhecimentosPoema);
+    atualizarDatalistReconhecimentos();
+}
+export function editarReconhecimento(indice) {
+    const item = listaReconhecimentosPoema.iniciarEdicao(indice);
+    const premioEl = document.getElementById('p-reconhecimento-premio');
+    const posicaoEl = document.getElementById('p-reconhecimento-posicao');
+    const anoEl = document.getElementById('p-reconhecimento-ano');
+    const textoEl = document.getElementById('p-reconhecimento-texto');
+    if (premioEl) premioEl.value = item.premio || '';
+    if (posicaoEl) posicaoEl.value = item.posicao || '';
+    if (anoEl) anoEl.value = item.ano ?? '';
+    if (textoEl) textoEl.value = item.texto || '';
+    premioEl?.focus();
+    atualizarBotaoReconhecimento('p', listaReconhecimentosPoema);
+}
+export function cancelarEdicaoReconhecimento() {
+    listaReconhecimentosPoema.cancelarEdicao();
+    limparCamposReconhecimento('p');
+    atualizarBotaoReconhecimento('p', listaReconhecimentosPoema);
+}
+export function removerReconhecimento(indice) {
+    listaReconhecimentosPoema.remover(indice);
+    atualizarBotaoReconhecimento('p', listaReconhecimentosPoema);
+}
+export function obterReconhecimentos() {
+    return listaReconhecimentosPoema.obterItens();
+}
+export function carregarReconhecimentos(lista) {
+    listaReconhecimentosPoema.carregar(lista);
+    atualizarBotaoReconhecimento('p', listaReconhecimentosPoema);
+}
+export function resetReconhecimentos() {
+    listaReconhecimentosPoema.reset();
+    atualizarBotaoReconhecimento('p', listaReconhecimentosPoema);
+}
+
+export function adicionarReconhecimentoProsa() {
+    const premio = (document.getElementById('pr-reconhecimento-premio')?.value || '').trim();
+    const posicao = (document.getElementById('pr-reconhecimento-posicao')?.value || '').trim();
+    const anoStr = (document.getElementById('pr-reconhecimento-ano')?.value || '').trim();
+    const ano = anoStr ? parseInt(anoStr, 10) : null;
+    const texto = (document.getElementById('pr-reconhecimento-texto')?.value || '').trim();
+    if (!premio && !posicao && !ano && !texto) return;
+
+    listaReconhecimentosProsa.salvar({ premio, posicao, ano, texto });
+    limparCamposReconhecimento('pr');
+    atualizarBotaoReconhecimento('pr', listaReconhecimentosProsa);
+    atualizarDatalistReconhecimentos();
+}
+export function editarReconhecimentoProsa(indice) {
+    const item = listaReconhecimentosProsa.iniciarEdicao(indice);
+    const premioEl = document.getElementById('pr-reconhecimento-premio');
+    const posicaoEl = document.getElementById('pr-reconhecimento-posicao');
+    const anoEl = document.getElementById('pr-reconhecimento-ano');
+    const textoEl = document.getElementById('pr-reconhecimento-texto');
+    if (premioEl) premioEl.value = item.premio || '';
+    if (posicaoEl) posicaoEl.value = item.posicao || '';
+    if (anoEl) anoEl.value = item.ano ?? '';
+    if (textoEl) textoEl.value = item.texto || '';
+    premioEl?.focus();
+    atualizarBotaoReconhecimento('pr', listaReconhecimentosProsa);
+}
+export function cancelarEdicaoReconhecimentoProsa() {
+    listaReconhecimentosProsa.cancelarEdicao();
+    limparCamposReconhecimento('pr');
+    atualizarBotaoReconhecimento('pr', listaReconhecimentosProsa);
+}
+export function removerReconhecimentoProsa(indice) {
+    listaReconhecimentosProsa.remover(indice);
+    atualizarBotaoReconhecimento('pr', listaReconhecimentosProsa);
+}
+export function obterReconhecimentosProsa() {
+    return listaReconhecimentosProsa.obterItens();
+}
+export function carregarReconhecimentosProsa(lista) {
+    listaReconhecimentosProsa.carregar(lista);
+    atualizarBotaoReconhecimento('pr', listaReconhecimentosProsa);
+}
+export function resetReconhecimentosProsa() {
+    listaReconhecimentosProsa.reset();
+    atualizarBotaoReconhecimento('pr', listaReconhecimentosProsa);
+}
+
 // ÔöÇÔöÇÔöÇ Tags (Sinaliza├º├Áes) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
 
 export function atualizarDatalist() {
-    const datalist = document.getElementById('sugestoes-sinais');
-    if (datalist) {
-        datalist.innerHTML = extrairSinalizacoesUnicas(db.poemas)
-            .map((tag) => `<option value="${escapeHtml(tag)}">`)
+    // Uma fonte por categoria: sugest├Áes v├¬m de Poemas + Prosas juntos
+    // (mesmo padr├úo que Pessoas j├í usava), pra "Concretista" digitado
+    // numa prosa aparecer como sugest├úo num poema e vice-versa.
+    const todos = [...db.poemas, ...(db.prosas || [])];
+    const combinadas = new Set();
+    SINAL_CATEGORIAS.forEach(({ chave }) => {
+        const slug = slugDom(chave);
+        const campo = `sinalizacoes${chave}`;
+        const valores = extrairSinalizacoesUnicas(todos, campo);
+        valores.forEach((v) => combinadas.add(v));
+        [
+            `sugestoes-sinais-${slug}`,
+            `sugestoes-sinais-${slug}-prosa`,
+            `sugestoes-sinais-${slug}-bulk`,
+            `sugestoes-sinais-${slug}-bulk-prosa`,
+        ].forEach((id) => {
+            const datalist = document.getElementById(id);
+            if (datalist) {
+                datalist.innerHTML = valores
+                    .map((v) => `<option value="${escapeHtml(v)}">`)
+                    .join('');
+            }
+        });
+    });
+    // Datalist "sugestoes-sinais" (sem categoria): usada pelo painel de
+    // exporta├º├úo seletiva (exp-temas-incluir/excluir), que filtra pelas
+    // 6 categorias combinadas (ver correspondeFiltro em exportar.js) ÔÇö
+    // por isso a sugest├úo tamb├®m precisa vir combinada, n├úo s├│ de Estilo.
+    const datalistCombinada = document.getElementById('sugestoes-sinais');
+    if (datalistCombinada) {
+        datalistCombinada.innerHTML = [...combinadas]
+            .sort((a, b) => a.localeCompare(b, 'pt-BR'))
+            .map((v) => `<option value="${escapeHtml(v)}">`)
             .join('');
     }
     atualizarDatalistPessoas();
+    atualizarDatalistAutores();
     atualizarDatalistMigracao();
     atualizarDatalistAnotacoes();
     atualizarDatalistIntertexto();
     atualizarDatalistEpoca();
+    atualizarDatalistIdioma();
+    atualizarDatalistEnvios();
+    atualizarDatalistReconhecimentos();
 }
 
+// Item 3 do plano de schema: ├ëpoca deixou de ser texto livre varrido de
+// db.poemas e passou a ser cadastro central (db.epocas) ÔÇö a sugest├úo do
+// datalist agora vem de l├í (nomes j├í cadastrados), n├úo mais dos nomes
+// j├í digitados em poemas. Item 4: mesmo cadastro central vale pra
+// Prosa ÔÇö um datalist por modal (mesmo padr├úo de sugestoes-pessoas/
+// sugestoes-pessoas-prosa), preenchidos com a mesma lista.
 export function atualizarDatalistEpoca() {
-    const datalist = document.getElementById('sugestoes-epoca-nome');
-    if (datalist) {
-        datalist.innerHTML = extrairNomesEpocaUnicos(db.poemas)
-            .map((v) => `<option value="${escapeHtml(v)}">`)
-            .join('');
-    }
+    const nomes = [...db.epocas].map((e) => e.nome).sort((a, b) => a.localeCompare(b, 'pt-BR'));
+    ['sugestoes-epoca-nome', 'sugestoes-epoca-nome-prosa'].forEach((id) => {
+        const datalist = document.getElementById(id);
+        if (datalist) {
+            datalist.innerHTML = nomes.map((v) => `<option value="${escapeHtml(v)}">`).join('');
+        }
+    });
+}
+
+// Idioma (item 9) ÔÇö Poemas + Prosas juntos, mesmo motivo de somar as
+// duas fontes em atualizarDatalist (Sinaliza├º├Áes) ÔÇö um idioma digitado
+// numa prosa deve sugerir numa pr├│xima edi├º├úo de poema e vice-versa.
+// Dois `<datalist>` (um por modal, mesmo padr├úo de
+// sugestoes-pessoas/sugestoes-pessoas-prosa ÔÇö cada modal carrega o
+// pr├│prio HTML sob demanda via garantirModal), preenchidos com a mesma
+// lista combinada.
+export function atualizarDatalistIdioma() {
+    const todos = [...db.poemas, ...(db.prosas || [])];
+    const valores = extrairIdiomasUnicos(todos);
+    ['sugestoes-idioma', 'sugestoes-idioma-prosa'].forEach((id) => {
+        const datalist = document.getElementById(id);
+        if (datalist) {
+            datalist.innerHTML = valores.map((v) => `<option value="${escapeHtml(v)}">`).join('');
+        }
+    });
 }
 
 // Sugest├Áes pros campos "Cortado de"/"Lan├ºado em" (Livro e Parte/Se├º├úo)
@@ -600,15 +2132,49 @@ export function atualizarDatalistEpoca() {
 // origem antiga, nunca cadastrada aqui), a sugest├úo de Se├º├úo volta a
 // mostrar a lista inteira, sem filtro.
 
-const PARES_MIGRACAO = [
-    { livro: 'p-cortado-livro', secao: 'p-cortado-secao', datalist: 'sugestoes-secoes-migracao-cortado' },
-    { livro: 'p-lancado-livro', secao: 'p-lancado-secao', datalist: 'sugestoes-secoes-migracao-lancado' },
+const PARES_MIGRACAO_POEMA = [
+    {
+        livro: 'p-cortado-livro',
+        secao: 'p-cortado-secao',
+        datalist: 'sugestoes-secoes-migracao-cortado',
+    },
+    {
+        livro: 'p-lancado-livro',
+        secao: 'p-lancado-secao',
+        datalist: 'sugestoes-secoes-migracao-lancado',
+    },
+];
+
+// Item 4: mesmos pares, lado Prosa (ids `p-`ÔåÆ`pr-`) ÔÇö reaproveita toda a
+// l├│gica abaixo (mapaSecoesMigracao, autopreenchimento, filtro) sem
+// duplicar nada. Lista separada de PARES_MIGRACAO_POEMA (em vez de uma
+// ├║nica lista combinada) porque initListenersMigracao/
+// initListenersMigracaoProsa precisam ligar os listeners em momentos
+// diferentes ÔÇö modal-poema e modal-prosa carregam sob demanda, cada um
+// s├│ tem seus pr├│prios campos no DOM quando abre pela primeira vez (ver
+// os dois initListenersMigracao* abaixo); atualizarFiltroSecoesMigracao,
+// que s├│ l├¬/escreve (sem risco de listener duplicado), continua
+// varrendo as duas listas juntas.
+const PARES_MIGRACAO_PROSA = [
+    {
+        livro: 'pr-cortado-livro',
+        secao: 'pr-cortado-secao',
+        datalist: 'sugestoes-secoes-migracao-cortado-prosa',
+    },
+    {
+        livro: 'pr-lancado-livro',
+        secao: 'pr-lancado-secao',
+        datalist: 'sugestoes-secoes-migracao-lancado-prosa',
+    },
 ];
 
+const PARES_MIGRACAO = [...PARES_MIGRACAO_POEMA, ...PARES_MIGRACAO_PROSA];
+
 // Se├º├úo pode estar presa direto no Livro ou dentro de uma Parte.
 function livroIdDaSecao(secao) {
     if (secao.paiTipo === 'livro') return secao.paiId;
-    if (secao.paiTipo === 'parte') return db.partes.find((p) => p.id == secao.paiId)?.livroId ?? null;
+    if (secao.paiTipo === 'parte')
+        return db.partes.find((p) => p.id == secao.paiId)?.livroId ?? null;
     return null;
 }
 
@@ -619,7 +2185,10 @@ function mapaSecoesMigracao() {
     const secoes = db.secoes.map((s) => ({ titulo: s.titulo, livroId: livroIdDaSecao(s) }));
     return [...partes, ...secoes]
         .filter((x) => x.titulo && x.livroId != null)
-        .map((x) => ({ ...x, livroTitulo: db.livros.find((l) => l.id == x.livroId)?.titulo || null }))
+        .map((x) => ({
+            ...x,
+            livroTitulo: db.livros.find((l) => l.id == x.livroId)?.titulo || null,
+        }))
         .filter((x) => x.livroTitulo);
 }
 
@@ -633,10 +2202,14 @@ function preencherDatalistSecoesMigracao(datalistId, livroInputId) {
     const mapa = mapaSecoesMigracao();
     const livroDigitado = (document.getElementById(livroInputId)?.value || '').trim();
     const livro = livroDigitado
-        ? db.livros.find((l) => (l.titulo || '').trim().toLowerCase() === livroDigitado.toLowerCase())
+        ? db.livros.find(
+              (l) => (l.titulo || '').trim().toLowerCase() === livroDigitado.toLowerCase(),
+          )
         : null;
 
-    const titulos = new Set((livro ? mapa.filter((x) => x.livroId == livro.id) : mapa).map((x) => x.titulo));
+    const titulos = new Set(
+        (livro ? mapa.filter((x) => x.livroId == livro.id) : mapa).map((x) => x.titulo),
+    );
     datalist.innerHTML = Array.from(titulos)
         .sort((a, b) => a.localeCompare(b, 'pt-BR'))
         .map((titulo) => `<option value="${escapeHtml(titulo)}">`)
@@ -662,15 +2235,17 @@ function autopreencherLivroDaSecao(secaoInputId, livroInputId) {
 }
 
 export function atualizarDatalistMigracao() {
-    const datalistLivros = document.getElementById('sugestoes-livros-migracao');
-    if (datalistLivros) {
-        datalistLivros.innerHTML = db.livros
-            .map((l) => l.titulo)
-            .filter(Boolean)
-            .sort((a, b) => a.localeCompare(b, 'pt-BR'))
-            .map((titulo) => `<option value="${escapeHtml(titulo)}">`)
-            .join('');
-    }
+    const titulos = db.livros
+        .map((l) => l.titulo)
+        .filter(Boolean)
+        .sort((a, b) => a.localeCompare(b, 'pt-BR'));
+    // Item 4: mesmo datalist de nomes de livro, lado Prosa.
+    ['sugestoes-livros-migracao', 'sugestoes-livros-migracao-prosa'].forEach((id) => {
+        const datalist = document.getElementById(id);
+        if (datalist) {
+            datalist.innerHTML = titulos.map((titulo) => `<option value="${escapeHtml(titulo)}">`).join('');
+        }
+    });
 
     atualizarFiltroSecoesMigracao();
 }
@@ -679,13 +2254,21 @@ export function atualizarDatalistMigracao() {
 // ÔÇö chamado sozinho depois que editarPoema() seta os 4 campos de migra├º├úo
 // de uma vez (setar .value direto no JS n├úo dispara 'input').
 export function atualizarFiltroSecoesMigracao() {
-    PARES_MIGRACAO.forEach(({ livro, datalist }) => preencherDatalistSecoesMigracao(datalist, livro));
+    PARES_MIGRACAO.forEach(({ livro, datalist }) =>
+        preencherDatalistSecoesMigracao(datalist, livro),
+    );
 }
 
-// Liga os listeners dos 4 campos de migra├º├úo (livro Ôçä se├º├úo) ÔÇö chamado
-// uma vez por initEditor(), quando modal-poema ├® carregado pela primeira vez.
-function initListenersMigracao() {
-    PARES_MIGRACAO.forEach(({ livro, secao, datalist }) => {
+// Liga os listeners dos campos de migra├º├úo (livro Ôçä se├º├úo) de um grupo
+// de pares ÔÇö f├íbrica compartilhada pelas duas fun├º├Áes abaixo, cada uma
+// chamada uma vez s├│, quando o respectivo modal ├® carregado pela
+// primeira vez (garantirModal/registrarModal, ver main.js) ÔÇö n├úo d├í pra
+// ligar os dois grupos juntos numa fun├º├úo s├│ porque os campos de Prosa
+// ainda n├úo existem no DOM na primeira vez que modal-poema abre (e
+// vice-versa), ent├úo rodar duas vezes sobre a lista combinada
+// duplicaria os listeners do grupo que j├í estava presente.
+function ligarListenersMigracao(pares) {
+    pares.forEach(({ livro, secao, datalist }) => {
         document
             .getElementById(livro)
             ?.addEventListener('input', () => preencherDatalistSecoesMigracao(datalist, livro));
@@ -696,48 +2279,189 @@ function initListenersMigracao() {
     });
 }
 
-export function adicionarTag(valor = null) {
-    grupoTagsPoema.adicionar(valor);
+// Chamado uma vez por initEditor(), quando modal-poema ├® carregado pela
+// primeira vez.
+function initListenersMigracao() {
+    ligarListenersMigracao(PARES_MIGRACAO_POEMA);
+}
+
+// Chamado uma vez quando modal-prosa ├® carregado pela primeira vez (ver
+// registrarModal('modal-prosa', ...) em main.js).
+export function initListenersMigracaoProsa() {
+    ligarListenersMigracao(PARES_MIGRACAO_PROSA);
+}
+
+// ÔöÇÔöÇÔöÇ Sinaliza├º├Áes por categoria (Poema) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Um conjunto de fun├º├Áes por categoria, no mesmo padr├úo pra todas
+// (Estilo inclu├¡do ÔÇö antes tinha nomes especiais adicionarTag/
+// removerTag/renderizarTags/resetTags/carregarTags de quando s├│ existia
+// essa categoria; renomeado aqui pra consist├¬ncia com as outras 5, j├í
+// que nada mais depende do nome antigo). resetSinalizacoes/
+// carregarSinalizacoes abaixo cobrem as 6 de uma vez, pra quem abre/
+// fecha o modal n├úo precisar chamar 6 fun├º├Áes.
+
+export function adicionarSinalEstilo(valor = null) {
+    modulosSinalPoema.Estilo.adicionar(valor);
+}
+export function removerSinalEstilo(tag) {
+    modulosSinalPoema.Estilo.remover(tag);
+}
+export function renderizarSinalEstilo() {
+    modulosSinalPoema.Estilo.renderizar();
+}
+
+export function adicionarSinalTema(valor = null) {
+    modulosSinalPoema.Tema.adicionar(valor);
+}
+export function removerSinalTema(tag) {
+    modulosSinalPoema.Tema.remover(tag);
+}
+export function renderizarSinalTema() {
+    modulosSinalPoema.Tema.renderizar();
+}
+
+export function adicionarSinalRelacao(valor = null) {
+    modulosSinalPoema.Relacao.adicionar(valor);
 }
-export function removerTag(tag) {
-    grupoTagsPoema.remover(tag);
+export function removerSinalRelacao(tag) {
+    modulosSinalPoema.Relacao.remover(tag);
 }
-export function renderizarTags() {
-    grupoTagsPoema.renderizar();
+export function renderizarSinalRelacao() {
+    modulosSinalPoema.Relacao.renderizar();
 }
-export function resetTags() {
-    grupoTagsPoema.reset();
+
+export function adicionarSinalSensibilidade(valor = null) {
+    modulosSinalPoema.Sensibilidade.adicionar(valor);
+}
+export function removerSinalSensibilidade(tag) {
+    modulosSinalPoema.Sensibilidade.remover(tag);
+}
+export function renderizarSinalSensibilidade() {
+    modulosSinalPoema.Sensibilidade.renderizar();
+}
+
+export function adicionarSinalTom(valor = null) {
+    modulosSinalPoema.Tom.adicionar(valor);
+}
+export function removerSinalTom(tag) {
+    modulosSinalPoema.Tom.remover(tag);
+}
+export function renderizarSinalTom() {
+    modulosSinalPoema.Tom.renderizar();
+}
+
+export function adicionarSinalOutros(valor = null) {
+    modulosSinalPoema.Outros.adicionar(valor);
+}
+export function removerSinalOutros(tag) {
+    modulosSinalPoema.Outros.remover(tag);
+}
+export function renderizarSinalOutros() {
+    modulosSinalPoema.Outros.renderizar();
+}
+
+// Ao contr├írio de Intertextualidade/Anexos/Anota├º├Áes (arrays de objetos,
+// que precisam de um getter porque n├úo h├í onde o navegador guardaria o
+// valor sozinho), cada categoria de Sinaliza├º├Áes j├í escreve sua string
+// atual no pr├│prio hidden input (p-sinal-{categoria}) a cada
+// adicionar/remover ÔÇö igual Pessoas. Por isso forms.js l├¬
+// document.getElementById('p-sinal-estilo').value etc. direto no
+// submit, sem precisar de um obterSinalizacoes() aqui.
+export function resetSinalizacoes() {
+    SINAL_CATEGORIAS.forEach(({ chave }) => modulosSinalPoema[chave].reset());
 }
-export function carregarTags(sinalizacoesStr) {
-    grupoTagsPoema.carregar(sinalizacoesStr);
+export function carregarSinalizacoes(item) {
+    modulosSinalPoema.Estilo.carregar(item.sinalizacoesEstilo || '');
+    modulosSinalPoema.Tema.carregar(item.sinalizacoesTema || '');
+    modulosSinalPoema.Relacao.carregar(item.sinalizacoesRelacao || '');
+    modulosSinalPoema.Sensibilidade.carregar(item.sinalizacoesSensibilidade || '');
+    modulosSinalPoema.Outros.carregar(item.sinalizacoesOutros || '');
+    modulosSinalPoema.Tom.carregar(item.sinalizacoesTom || '');
 }
 
-// ÔöÇÔöÇÔöÇ Pessoas (Dedicado a) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// ÔöÇÔöÇÔöÇ Pessoas ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
 // Mesmo padr├úo das Sinaliza├º├Áes, mas em grupo separado: pessoas
 // n├úo s├úo tema, s├úo "a quem o texto se refere/├® dedicado".
 
 export function atualizarDatalistPessoas() {
     const datalist = document.getElementById('sugestoes-pessoas');
     if (!datalist) return;
-    datalist.innerHTML = extrairPessoasUnicas(db.poemas)
+    datalist.innerHTML = nomesPessoasCadastro()
         .map((nome) => `<option value="${escapeHtml(nome)}">`)
         .join('');
 }
 
+// Nomes do cadastro central de Pessoas (db.pessoas), ordenados ÔÇö
+// alimenta os datalists de autocomplete de nome nos chips de Pessoas
+// (poema e prosa, modal e bulk-edit). Pessoa ├® entidade pr├│pria desde
+// migrarPessoasParaCadastro (db.js): a lista de sugest├úo passou a vir
+// direto do cadastro, n├úo mais varrendo item.pessoas de cada poema
+// (que hoje s├│ guarda pessoaId, n├úo nome).
+function nomesPessoasCadastro() {
+    return db.pessoas.map((p) => p.nome).sort((a, b) => a.localeCompare(b, 'pt-BR'));
+}
+
 export function adicionarPessoa(valor = null) {
     grupoPessoasPoema.adicionar(valor);
 }
 export function removerPessoa(nome) {
     grupoPessoasPoema.remover(nome);
 }
+export function alternarPapelPessoa(nome, papel, marcado) {
+    grupoPessoasPoema.alternarPapel(nome, papel, marcado);
+}
+export function alternarDropdownPapelPessoa(nome) {
+    grupoPessoasPoema.alternarDropdown(nome);
+}
 export function renderizarPessoas() {
     grupoPessoasPoema.renderizar();
 }
 export function resetPessoas() {
     grupoPessoasPoema.reset();
 }
-export function carregarPessoas(pessoasStr) {
-    grupoPessoasPoema.carregar(pessoasStr);
+export function carregarPessoas(pessoas) {
+    grupoPessoasPoema.carregar(pessoas);
+}
+export function obterPessoas() {
+    return grupoPessoasPoema.obterItens();
+}
+
+// Nomes do cadastro central de Autores (db.autores), ordenados ÔÇö
+// alimenta o datalist de autocomplete do chip de Autoria (poema e
+// prosa). Mesmo padr├úo de nomesPessoasCadastro acima.
+function nomesAutoresCadastro() {
+    return db.autores.map((a) => a.nome).sort((a, b) => a.localeCompare(b, 'pt-BR'));
+}
+
+export function atualizarDatalistAutores() {
+    const nomes = nomesAutoresCadastro();
+    ['sugestoes-autores', 'sugestoes-autores-prosa'].forEach((id) => {
+        const datalist = document.getElementById(id);
+        if (!datalist) return;
+        datalist.innerHTML = nomes.map((nome) => `<option value="${escapeHtml(nome)}">`).join('');
+    });
+}
+
+export function adicionarAutoria(valor = null) {
+    grupoAutoriaPoema.adicionar(valor);
+}
+export function removerAutoria(autorId) {
+    grupoAutoriaPoema.remover(autorId);
+}
+export function alterarPapelAutoria(autorId, papel) {
+    grupoAutoriaPoema.alterarPapel(autorId, papel);
+}
+export function renderizarAutoria() {
+    grupoAutoriaPoema.renderizar();
+}
+export function resetAutoria() {
+    grupoAutoriaPoema.reset();
+}
+export function carregarAutoria(autoria) {
+    grupoAutoriaPoema.carregar(autoria);
+}
+export function obterAutoria() {
+    return grupoAutoriaPoema.obterItens();
 }
 
 // ÔöÇÔöÇÔöÇ Inicializa├º├úo dos listeners ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
@@ -745,18 +2469,12 @@ export function carregarPessoas(pessoasStr) {
 // ÔöÇÔöÇÔöÇ Tags/Pessoas: Prosa (espelha o padr├úo do Poema) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
 
 export function atualizarDatalistProsa() {
-    const sinaisUnicos = extrairSinalizacoesUnicas([...db.poemas, ...(db.prosas || [])]);
-    const pessoasUnicas = extrairPessoasUnicas([...db.poemas, ...(db.prosas || [])]);
+    // As 5 categorias de Sinaliza├º├Áes (poema+prosa+bulk, todas as
+    // variantes de ID) j├í s├úo preenchidas centralmente por
+    // atualizarDatalist() ÔÇö chamado logo depois desta, ver main.js.
+    const pessoasUnicas = nomesPessoasCadastro();
     const generosUnicos = extrairGenerosUnicos(db.prosas || []);
 
-    // Datalists dentro do modal de Prosa (s├│ existem depois que o modal
-    // ├® carregado ao menos uma vez ÔÇö ver modal-prosa.html / modais.js)
-    const datalistSinais = document.getElementById('sugestoes-sinais-prosa');
-    if (datalistSinais) {
-        datalistSinais.innerHTML = sinaisUnicos
-            .map((tag) => `<option value="${escapeHtml(tag)}">`)
-            .join('');
-    }
     const datalistPessoas = document.getElementById('sugestoes-pessoas-prosa');
     if (datalistPessoas) {
         datalistPessoas.innerHTML = pessoasUnicas
@@ -766,12 +2484,6 @@ export function atualizarDatalistProsa() {
 
     // Datalists sempre presentes no index.html, usados pela barra de
     // edi├º├úo em massa da aba Prosas (independem do modal ter sido aberto)
-    const datalistSinaisBulk = document.getElementById('sugestoes-sinais-bulk-prosa');
-    if (datalistSinaisBulk) {
-        datalistSinaisBulk.innerHTML = sinaisUnicos
-            .map((tag) => `<option value="${escapeHtml(tag)}">`)
-            .join('');
-    }
     const datalistPessoasBulk = document.getElementById('sugestoes-pessoas-bulk-prosa');
     if (datalistPessoasBulk) {
         datalistPessoasBulk.innerHTML = pessoasUnicas
@@ -791,22 +2503,87 @@ export function atualizarDatalistProsa() {
             .map((g) => `<option value="${escapeHtml(g)}">`)
             .join('');
     }
+
+    // Item 4: Intertextualidade sugere s├│ a partir de db.prosas (cada
+    // tipo de texto sugere pelo pr├│prio hist├│rico, n├úo combinado ÔÇö ver
+    // atualizarDatalistIntertexto acima, que s├│ l├¬ db.poemas). ├ëpoca e
+    // Migra├º├úo (ver atualizarDatalistEpoca/atualizarDatalistMigracao)
+    // j├í foram generalizadas pra preencher os dois datalists (Poema e
+    // Prosa) de uma vez s├│, ent├úo n├úo precisam ser chamadas de novo
+    // aqui ÔÇö atualizarDatalist() (Poema) j├í cobre as duas pontas.
+    atualizarDatalistIntertextoProsa();
+}
+
+export function adicionarSinalEstiloProsa(valor = null) {
+    modulosSinalProsa.Estilo.adicionar(valor);
+}
+export function removerSinalEstiloProsa(tag) {
+    modulosSinalProsa.Estilo.remover(tag);
+}
+export function renderizarSinalEstiloProsa() {
+    modulosSinalProsa.Estilo.renderizar();
+}
+
+export function adicionarSinalTemaProsa(valor = null) {
+    modulosSinalProsa.Tema.adicionar(valor);
+}
+export function removerSinalTemaProsa(tag) {
+    modulosSinalProsa.Tema.remover(tag);
+}
+export function renderizarSinalTemaProsa() {
+    modulosSinalProsa.Tema.renderizar();
 }
 
-export function adicionarTagProsa(valor = null) {
-    grupoTagsProsa.adicionar(valor);
+export function adicionarSinalRelacaoProsa(valor = null) {
+    modulosSinalProsa.Relacao.adicionar(valor);
+}
+export function removerSinalRelacaoProsa(tag) {
+    modulosSinalProsa.Relacao.remover(tag);
 }
-export function removerTagProsa(tag) {
-    grupoTagsProsa.remover(tag);
+export function renderizarSinalRelacaoProsa() {
+    modulosSinalProsa.Relacao.renderizar();
 }
-export function renderizarTagsProsa() {
-    grupoTagsProsa.renderizar();
+
+export function adicionarSinalSensibilidadeProsa(valor = null) {
+    modulosSinalProsa.Sensibilidade.adicionar(valor);
 }
-export function resetTagsProsa() {
-    grupoTagsProsa.reset();
+export function removerSinalSensibilidadeProsa(tag) {
+    modulosSinalProsa.Sensibilidade.remover(tag);
 }
-export function carregarTagsProsa(sinalizacoesStr) {
-    grupoTagsProsa.carregar(sinalizacoesStr);
+export function renderizarSinalSensibilidadeProsa() {
+    modulosSinalProsa.Sensibilidade.renderizar();
+}
+
+export function adicionarSinalTomProsa(valor = null) {
+    modulosSinalProsa.Tom.adicionar(valor);
+}
+export function removerSinalTomProsa(tag) {
+    modulosSinalProsa.Tom.remover(tag);
+}
+export function renderizarSinalTomProsa() {
+    modulosSinalProsa.Tom.renderizar();
+}
+
+export function adicionarSinalOutrosProsa(valor = null) {
+    modulosSinalProsa.Outros.adicionar(valor);
+}
+export function removerSinalOutrosProsa(tag) {
+    modulosSinalProsa.Outros.remover(tag);
+}
+export function renderizarSinalOutrosProsa() {
+    modulosSinalProsa.Outros.renderizar();
+}
+
+export function resetSinalizacoesProsa() {
+    SINAL_CATEGORIAS.forEach(({ chave }) => modulosSinalProsa[chave].reset());
+}
+export function carregarSinalizacoesProsa(item) {
+    modulosSinalProsa.Estilo.carregar(item.sinalizacoesEstilo || '');
+    modulosSinalProsa.Tema.carregar(item.sinalizacoesTema || '');
+    modulosSinalProsa.Relacao.carregar(item.sinalizacoesRelacao || '');
+    modulosSinalProsa.Sensibilidade.carregar(item.sinalizacoesSensibilidade || '');
+    modulosSinalProsa.Outros.carregar(item.sinalizacoesOutros || '');
+    modulosSinalProsa.Tom.carregar(item.sinalizacoesTom || '');
 }
 
 export function adicionarPessoaProsa(valor = null) {
@@ -815,14 +2592,45 @@ export function adicionarPessoaProsa(valor = null) {
 export function removerPessoaProsa(nome) {
     grupoPessoasProsa.remover(nome);
 }
+export function alternarPapelPessoaProsa(nome, papel, marcado) {
+    grupoPessoasProsa.alternarPapel(nome, papel, marcado);
+}
+export function alternarDropdownPapelPessoaProsa(nome) {
+    grupoPessoasProsa.alternarDropdown(nome);
+}
 export function renderizarPessoasProsa() {
     grupoPessoasProsa.renderizar();
 }
 export function resetPessoasProsa() {
     grupoPessoasProsa.reset();
 }
-export function carregarPessoasProsa(pessoasStr) {
-    grupoPessoasProsa.carregar(pessoasStr);
+export function carregarPessoasProsa(pessoas) {
+    grupoPessoasProsa.carregar(pessoas);
+}
+export function obterPessoasProsa() {
+    return grupoPessoasProsa.obterItens();
+}
+
+export function adicionarAutoriaProsa(valor = null) {
+    grupoAutoriaProsa.adicionar(valor);
+}
+export function removerAutoriaProsa(autorId) {
+    grupoAutoriaProsa.remover(autorId);
+}
+export function alterarPapelAutoriaProsa(autorId, papel) {
+    grupoAutoriaProsa.alterarPapel(autorId, papel);
+}
+export function renderizarAutoriaProsa() {
+    grupoAutoriaProsa.renderizar();
+}
+export function resetAutoriaProsa() {
+    grupoAutoriaProsa.reset();
+}
+export function carregarAutoriaProsa(autoria) {
+    grupoAutoriaProsa.carregar(autoria);
+}
+export function obterAutoriaProsa() {
+    return grupoAutoriaProsa.obterItens();
 }
 
 // ÔöÇÔöÇÔöÇ G├¬nero (Cartas, Di├ílogos, Ensaios, Prosas po├®ticas...) ÔöÇÔöÇÔöÇ
@@ -930,16 +2738,28 @@ export function initEditor() {
         }
     });
 
-    // Enter no input de tags
-    const inputSinal = document.getElementById('p-sinal-input');
-    if (inputSinal) {
-        inputSinal.addEventListener('keydown', (e) => {
-            if (e.key === 'Enter') {
-                e.preventDefault();
-                adicionarTag();
-            }
-        });
-    }
+    // Enter nos 6 inputs de tags de Sinaliza├º├Áes (Poema) ÔÇö um listener
+    // por categoria, j├í que cada uma agora tem seu pr├│prio input/fun├º├úo
+    // de adicionar (ver SINAL_CATEGORIAS acima).
+    const funcoesSinalPoema = {
+        Estilo: adicionarSinalEstilo,
+        Tema: adicionarSinalTema,
+        Relacao: adicionarSinalRelacao,
+        Sensibilidade: adicionarSinalSensibilidade,
+        Tom: adicionarSinalTom,
+        Outros: adicionarSinalOutros,
+    };
+    SINAL_CATEGORIAS.forEach(({ chave }) => {
+        const input = document.getElementById(`p-sinal-${slugDom(chave)}-input`);
+        if (input) {
+            input.addEventListener('keydown', (e) => {
+                if (e.key === 'Enter') {
+                    e.preventDefault();
+                    funcoesSinalPoema[chave]();
+                }
+            });
+        }
+    });
 
     // Enter no input de pessoas
     const inputPessoa = document.getElementById('p-pessoa-input');
@@ -977,16 +2797,29 @@ export function initEditor() {
         });
     }
 
-    // Enter nos inputs de prosa (tags e pessoas)
-    const inputSinalProsa = document.getElementById('pr-sinal-input');
-    if (inputSinalProsa) {
-        inputSinalProsa.addEventListener('keydown', (e) => {
-            if (e.key === 'Enter') {
-                e.preventDefault();
-                adicionarTagProsa();
-            }
-        });
-    }
+    // Enter nos 6 inputs de tags de Sinaliza├º├Áes (Prosa) ÔÇö mesmo padr├úo
+    // do Poema acima, com as fun├º├Áes -Prosa correspondentes.
+    const funcoesSinalProsa = {
+        Estilo: adicionarSinalEstiloProsa,
+        Tema: adicionarSinalTemaProsa,
+        Relacao: adicionarSinalRelacaoProsa,
+        Sensibilidade: adicionarSinalSensibilidadeProsa,
+        Tom: adicionarSinalTomProsa,
+        Outros: adicionarSinalOutrosProsa,
+    };
+    SINAL_CATEGORIAS.forEach(({ chave }) => {
+        const input = document.getElementById(`pr-sinal-${slugDom(chave)}-input`);
+        if (input) {
+            input.addEventListener('keydown', (e) => {
+                if (e.key === 'Enter') {
+                    e.preventDefault();
+                    funcoesSinalProsa[chave]();
+                }
+            });
+        }
+    });
+
+    // Enter nos inputs de prosa (pessoas e g├¬nero)
     const inputPessoaProsa = document.getElementById('pr-pessoa-input');
     if (inputPessoaProsa) {
         inputPessoaProsa.addEventListener('keydown', (e) => {
diff --git a/js/estatisticas.js b/js/estatisticas.js
index d3bab0c..81216fc 100644
--- a/js/estatisticas.js
+++ b/js/estatisticas.js
@@ -4,7 +4,7 @@
 // ============================================================
 
 import { db } from './db.js';
-import { escapeHtml, anoDeDataParcial } from './utils.js';
+import { escapeHtml, anoDeDataParcial, sinalizacoesCombinadas, nomesPessoas } from './utils.js';
 
 const STOPWORDS = new Set([
     'a',
@@ -377,7 +377,10 @@ export function contarPorLivro() {
 export function contarPorTema(top = 12) {
     const contagem = {};
     todosOsTextos().forEach((t) => {
-        listaDeCampo(t.sinalizacoes).forEach((tag) => {
+        // Etiquetas mais frequentes = as 6 categorias de Sinaliza├º├Áes
+        // combinadas (ver sinalizacoesCombinadas em utils.js), igual j├í
+        // era quando isso era um campo ├║nico ÔÇö s├│ mudou onde o dado mora.
+        listaDeCampo(sinalizacoesCombinadas(t)).forEach((tag) => {
             contagem[tag] = (contagem[tag] || 0) + 1;
         });
     });
@@ -390,7 +393,7 @@ export function contarPorTema(top = 12) {
 export function contarPorPessoa(top = 12) {
     const contagem = {};
     todosOsTextos().forEach((t) => {
-        listaDeCampo(t.pessoas).forEach((nome) => {
+        nomesPessoas(t, db.pessoas).forEach((nome) => {
             contagem[nome] = (contagem[nome] || 0) + 1;
         });
     });
diff --git a/js/exportar-md.js b/js/exportar-md.js
index ed5c8c1..376e6ed 100644
--- a/js/exportar-md.js
+++ b/js/exportar-md.js
@@ -12,10 +12,18 @@
 // volta pro acervo.
 // ============================================================
 
-import { formatarDataParcial, formatarEpocaRetratada, estaPublicado } from './utils.js';
+import {
+    formatarDataParcial,
+    formatarEpocaRetratada,
+    estaPublicado,
+    sinalizacoesCombinadas,
+    rotuloElo,
+    paresGrupoPessoa,
+    paresAutoria,
+} from './utils.js';
 import { db } from './db.js';
 
-const INFO_STATUS = {
+export const INFO_STATUS = {
     publicado: { emoji: '­ƒƒó', titulo: 'Publicado' },
     incompleto: { emoji: '­ƒƒí', titulo: 'Incompleto' },
     migrado: { emoji: '­ƒöÁ', titulo: 'Migrado' },
@@ -49,7 +57,7 @@ function corpoParaMarkdown(texto) {
 
 // ÔöÇÔöÇÔöÇ Campos auxiliares ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
 
-function livroSecaoStr(ls) {
+export function livroSecaoStr(ls) {
     if (!ls) return null;
     const partes = [ls.livro, ls.secao].filter(Boolean);
     return partes.length ? partes.join(' / ') : null;
@@ -64,25 +72,156 @@ function blocoTexto(titulo, texto) {
     return t ? `### ${titulo}\n\n${t}\n\n` : '';
 }
 
-// Elos e Refer├¬ncias guardam ids de outros poemas (conceitos.elos/
-// conceitos.referencias) ÔÇö resolve pros t├¡tulos, igual titulosPoemasPorId
-// em render-listas.js, s├│ que em texto puro (sem HTML) pro Markdown.
-function titulosPorIds(ids) {
-    if (!Array.isArray(ids) || !ids.length) return null;
-    const titulos = ids
-        .map((id) => db.poemas.find((p) => p.id == id)?.titulo)
+// Elos guarda { id, relacao, direcao, texto } (redesenho Rela├º├úo+Dire├º├úo,
+// ver rotuloElo em utils.js); Refer├¬ncias guarda { id, tipo, texto }
+// (schema mais simples, sem Dire├º├úo ÔÇö n├úo mudou). Resolve pros t├¡tulos,
+// igual titulosPoemasPorId em render-listas.js, s├│ que em texto puro
+// (sem HTML) pro Markdown. O r├│tulo entra como prefixo quando existe, e
+// a nota livre (texto) vai entre par├¬nteses no fim.
+export function titulosPorIds(lista) {
+    if (!Array.isArray(lista) || !lista.length) return null;
+    const partes = lista
+        .map((entrada) => {
+            // Item 4: o alvo pode ser Poema ou Prosa (ver resolverItemVinculado
+            // em editor.js / resolverTituloPoemaOuProsa em render-listas.js ÔÇö
+            // mesmo crit├®rio aqui: ids nunca colidem entre os dois arrays).
+            const titulo = (
+                db.poemas.find((p) => p.id == entrada.id) ||
+                (db.prosas || []).find((pr) => pr.id == entrada.id)
+            )?.titulo;
+            if (!titulo) return null;
+            const rotulo =
+                entrada.relacao !== undefined
+                    ? rotuloElo(entrada.relacao, entrada.direcao)
+                    : entrada.tipo;
+            let s = rotulo ? `${rotulo}: ${titulo}` : titulo;
+            if (entrada.texto) s += ` (${entrada.texto})`;
+            return s;
+        })
         .filter(Boolean);
-    return titulos.length ? titulos.join(', ') : null;
+    return partes.length ? partes.join(', ') : null;
 }
 
+// ÔöÇÔöÇÔöÇ Quantos campos est├úo preenchidos ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Mesma lista de campos que itemParaMarkdown() considera (mesma ordem,
+// s├│ que aqui cada um vira um true/false em vez de virar texto) ÔÇö usada
+// pela coluna "Campos Preenchidos" (ver colunas.js/render-listas.js) pra
+// identificar rapidamente os textos com estrutura mais rica/complexa.
+// Uma lista s├│, testada e referenciada nos dois lugares (contagem e
+// TOTAL_CAMPOS_CONSIDERADOS), pra n├úo divergir com o tempo de quais
+// campos "contam".
+function verificacoesDeCampos(item) {
+    const ctx = item.contexto || {};
+    return [
+        !!(ctx.livro || ctx.parte || ctx.secao),
+        !!item.status,
+        !!item.dataEscrita,
+        !!item.dataPublicacao,
+        !!item.epocaRetratada,
+        !!textoPessoas(item),
+        !!textoGrupos(item),
+        !!sinalizacoesCombinadas(item),
+        !!item.genero,
+        !!titulosPorIds(item.conceitos?.elos),
+        !!titulosPorIds(item.conceitos?.referencias),
+        !!(item.texto || '').trim(),
+        !!(item.notas || '').trim(),
+        !!(item.descricaoVisual || '').trim(),
+        !!(item.contextoHistorico || '').trim(),
+        !!(item.ocultacao || '').trim(),
+        Array.isArray(item.intertextualidade) && item.intertextualidade.length > 0,
+        Array.isArray(item.anexos) && item.anexos.length > 0,
+        !!(item.anexosNotaGeral || '').trim(),
+        Array.isArray(item.anotacoesMarginais) && item.anotacoesMarginais.length > 0,
+        Array.isArray(item.envios) && item.envios.length > 0,
+        Array.isArray(item.reconhecimentos) && item.reconhecimentos.length > 0,
+        !!(item.conteudoSensivel || '').trim(),
+        !!(item.vocabularioHiperacionante || '').trim(),
+        !!livroSecaoStr(item.cortadoDe),
+        !!livroSecaoStr(item.lancadoEm),
+        !!(item.justificativaMigracao || '').trim(),
+        !!(item.pendencia || '').trim(),
+        !!(item.descarte || '').trim(),
+    ];
+}
+
+export function contarCamposPreenchidos(item) {
+    return verificacoesDeCampos(item).filter(Boolean).length;
+}
+
+// Total de campos considerados na contagem acima ÔÇö calculado a partir de
+// um item vazio (em vez de um n├║mero fixo) pra nunca ficar dessincronizado
+// se verificacoesDeCampos() ganhar/perder alguma verifica├º├úo.
+export const TOTAL_CAMPOS_CONSIDERADOS = verificacoesDeCampos({}).length;
+
 // ÔöÇÔöÇÔöÇ Um item (Poema ou Prosa) ÔåÆ se├º├úo Markdown ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
-function itemParaMarkdown(item, indice) {
+// pessoas ├® array de objeto {pessoaId, papeis} (papeis: array, desde o
+// multi-select ÔÇö ver migrarPapeisPessoa em db.js); o nome n├úo mora mais
+// no item, mora no cadastro central db.pessoas (ver
+// migrarPessoasParaCadastro em db.js) ÔÇö resolve pessoaId ÔåÆ nome antes
+// de montar o texto. Mostra "Nome (Papel1, Papel2)" quando h├í pap├®is
+// marcados, na ordem em que foram marcados no editor, s├│ o nome quando
+// n├úo h├í nenhum (mesmo crit├®rio de exibi├º├úo do resto do app ÔÇö ver
+// badgesPessoas em render-listas.js). pessoaId sem correspond├¬ncia no
+// cadastro (n├úo deveria acontecer, mas dado importado de fora pode vir
+// incompleto) ├® ignorado, n├úo quebra a lista pros demais.
+export function textoPessoas(item) {
+    if (!Array.isArray(item.pessoas)) return null;
+    const partes = item.pessoas
+        .map((p) => ({ nome: db.pessoas.find((x) => x.id == p.pessoaId)?.nome, papeis: p.papeis }))
+        .filter((p) => p.nome)
+        .map((p) => (p.papeis && p.papeis.length ? `${p.nome} (${p.papeis.join(', ')})` : p.nome));
+    return partes.length ? partes.join(', ') : null;
+}
+
+// Grupo a que cada pessoa do item pertence (n├úo ├® o mesmo dado de
+// textoPessoas acima: ali ├® o papel da pessoa NESTE texto ÔÇö Retratado(a)/
+// Dedicat├íria/etc.; aqui ├® o Grupo, caracter├¡stica da pr├│pria Pessoa,
+// constante entre textos ÔÇö ver paresGrupoPessoa em utils.js, mesma
+// resolu├º├úo usada na coluna "Grupos" das tabelas ÔÇö ver badgesGrupos em
+// render-listas.js ÔÇö e no painel do modal ÔÇö ver renderPainelGrupos em
+// editor.js). Formato "Grupo (Pessoa)", n├úo "Pessoa (Grupo)": o pedido
+// original foi por essa ordem, pra n├úo confundir com o par├¬ntese de
+// papel de textoPessoas.
+export function textoGrupos(item) {
+    const pares = paresGrupoPessoa(item, db.pessoas, db.grupos);
+    if (!pares.length) return null;
+    return pares.map(({ grupo, pessoa }) => `${grupo.nome} (${pessoa.nome})`).join(', ');
+}
+
+// Autoria: array {autorId, papel} (ver migrarAutoria em db.js) ÔÇö
+// resolve autorId ÔåÆ nome no cadastro central db.autores (ver
+// paresAutoria em utils.js). Papel aqui ├® sempre ├║nico e sempre
+// preenchido (migra├º├úo garante isso), ent├úo sempre mostra "Nome
+// (Papel)" por extenso ÔÇö diferente de textoPessoas, que s├│ parentiza
+// quando h├í pap├®is marcados.
+export function textoAutoria(item) {
+    const pares = paresAutoria(item, db.autores);
+    if (!pares.length) return null;
+    return pares.map(({ autor, papel }) => `${autor.nome} (${papel})`).join(', ');
+}
+
+// `indice` ├® opcional ÔÇö omitido (ou falsy), o cabe├ºalho sai sem
+// numera├º├úo, ├║til pra gerar o Markdown de um ├║nico item avulso (ex.:
+// modal de Visualiza├º├úo) fora do contexto de uma lista exportada.
+// Dividido em "antes"/"depois" do bloco de Texto (ver itemParaMarkdownPartes
+// logo abaixo) ÔÇö quem s├│ quer o Markdown final (gerarMarkdownExportacao,
+// gerarMarkdownItem) simplesmente concatena os tr├¬s peda├ºos, ent├úo esse
+// refactor n├úo muda o Markdown gerado em nada. O motivo de existir a
+// vers├úo dividida: exportar-pdf.js precisa renderizar o corpo do Texto a
+// partir do HTML/Markdown h├¡brido original (cores, negrito, sublinhado,
+// it├ílico ÔÇö ver corpoParaMarkdown acima, que descarta tudo isso), ent├úo
+// s├│ reaproveita este Markdown pra tudo em volta do Texto, n├úo pro Texto
+// em si.
+function itemParaMarkdownAntesDoTexto(item, indice) {
     const tipoLabel = item.tipo === 'prosa' ? 'Prosa' : 'Poema';
-    let md = `## ${indice}. "${item.titulo || '(sem t├¡tulo)'}" *(${tipoLabel})*\n\n`;
+    const prefixoNumero = indice ? `${indice}. ` : '';
+    let md = `## ${prefixoNumero}"${item.titulo || '(sem t├¡tulo)'}" *(${tipoLabel})*\n\n`;
 
     const ctx = item.contexto || {};
     const caminho = [ctx.livro, ctx.parte, ctx.secao].filter(Boolean).join(' ÔåÆ ');
     md += linhaMeta('Localiza├º├úo', caminho || null);
+    md += linhaMeta('Idioma', item.idioma || null);
 
     if (item.status) {
         const info = INFO_STATUS[item.status] || { emoji: 'ÔÜ¬', titulo: item.status };
@@ -99,17 +238,25 @@ function itemParaMarkdown(item, indice) {
         md += linhaMeta('Primeira publica├º├úo', formatarDataParcial(item.dataPublicacao));
     }
     if (item.epocaRetratada) {
-        md += linhaMeta('├ëpoca retratada', formatarEpocaRetratada(item.epocaRetratada));
+        md += linhaMeta('├ëpoca retratada', formatarEpocaRetratada(item.epocaRetratada, db.epocas));
     }
-    md += linhaMeta('Dedicado a / Sobre quem', item.pessoas || null);
-    md += linhaMeta('Sinaliza├º├Áes', item.sinalizacoes || null);
+    md += linhaMeta('Pessoas', textoPessoas(item));
+    md += linhaMeta('Grupos', textoGrupos(item));
+    md += linhaMeta('Sinaliza├º├Áes', sinalizacoesCombinadas(item) || null);
     if (item.genero) md += linhaMeta('G├¬nero', item.genero);
     md += linhaMeta('Elos', titulosPorIds(item.conceitos?.elos));
     md += linhaMeta('Refer├¬ncias', titulosPorIds(item.conceitos?.referencias));
 
     md += '\n';
     md += blocoTexto('Texto', corpoParaMarkdown(item.texto));
+
+    return md;
+}
+
+function itemParaMarkdownDepoisDoTexto(item) {
+    let md = '';
     md += blocoTexto('Notas', item.notas);
+    md += linhaMeta('Autoria', textoAutoria(item));
     md += blocoTexto('Descri├º├úo Visual', item.descricaoVisual);
     md += blocoTexto('Contexto Hist├│rico/Pessoal', item.contextoHistorico);
     md += blocoTexto('Oculta├º├úo', item.ocultacao);
@@ -145,6 +292,34 @@ function itemParaMarkdown(item, indice) {
         md += '\n';
     }
 
+    if (Array.isArray(item.envios) && item.envios.length) {
+        md += '### Envios e Rea├º├Áes\n\n';
+        item.envios.forEach((e) => {
+            const partes = [
+                e.pessoa,
+                e.meio ? `via ${e.meio}` : '',
+                e.data && formatarDataParcial(e.data) !== 'ÔÇö' ? formatarDataParcial(e.data) : '',
+            ]
+                .filter(Boolean)
+                .join(', ');
+            const prefixo = partes ? `**${partes}:** ` : '';
+            const notas = e.notas ? ` *(${e.notas})*` : '';
+            md += `- ${prefixo}${e.reacao || ''}${notas}\n`;
+        });
+        md += '\n';
+    }
+
+    if (Array.isArray(item.reconhecimentos) && item.reconhecimentos.length) {
+        md += '### Reconhecimentos\n\n';
+        item.reconhecimentos.forEach((r) => {
+            const ano = r.ano || r.ano === 0 ? String(r.ano) : '';
+            const meta = [r.premio, r.posicao, ano].filter(Boolean).join(', ');
+            const prefixo = meta ? `**${meta}:** ` : '';
+            md += `- ${prefixo}${r.texto || ''}\n`;
+        });
+        md += '\n';
+    }
+
     // Conte├║do Sens├¡vel / Vocabul├írio Hiperacionante em destaque (blockquote),
     // j├í que sinalizam algo que quem l├¬ deveria notar antes do texto em si.
     if ((item.conteudoSensivel || '').trim()) {
@@ -162,12 +337,37 @@ function itemParaMarkdown(item, indice) {
         if (lancado) md += `- Lan├ºado em: ${lancado}\n`;
         md += '\n';
     }
+    md += blocoTexto('Justificativa da Migra├º├úo', item.justificativaMigracao);
 
+    md += blocoTexto('Pend├¬ncia', item.pendencia);
     md += blocoTexto('Descarte', item.descarte);
 
     return md;
 }
 
+// { antesDoTexto, depoisDoTexto } ÔÇö ver coment├írio de
+// itemParaMarkdownAntesDoTexto acima. gerarPdfExportacao (exportar-pdf.js)
+// ├® quem usa a vers├úo dividida; itemParaMarkdown (abaixo) ├® s├│ as duas
+// metades coladas, pro resto do app continuar chamando uma fun├º├úo s├│.
+export function itemParaMarkdownPartes(item, indice) {
+    return {
+        antesDoTexto: itemParaMarkdownAntesDoTexto(item, indice),
+        depoisDoTexto: itemParaMarkdownDepoisDoTexto(item),
+    };
+}
+
+export function itemParaMarkdown(item, indice) {
+    const { antesDoTexto, depoisDoTexto } = itemParaMarkdownPartes(item, indice);
+    return antesDoTexto + depoisDoTexto;
+}
+
+// Markdown de um ├║nico item avulso, sem numera├º├úo nem cabe├ºalho de
+// "Exporta├º├úo Po├®tica" ÔÇö usado pelo modal de Visualiza├º├úo (ver
+// visualizar.js) e por baixarMarkdown() quando chamado com 1 item s├│.
+export function gerarMarkdownItem(item) {
+    return itemParaMarkdown(item);
+}
+
 // ÔöÇÔöÇÔöÇ Documento completo ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
 export function gerarMarkdownExportacao(itens) {
     const agora = new Date();
diff --git a/js/exportar.js b/js/exportar.js
index 8137f28..0611bfb 100644
--- a/js/exportar.js
+++ b/js/exportar.js
@@ -14,8 +14,9 @@
 
 import { db } from './db.js';
 import { exportarColetaneaResolvida } from './coletaneas.js';
-import { escapeHtml, mostrarAviso, estaPublicado } from './utils.js';
+import { escapeHtml, mostrarAviso, estaPublicado, sinalizacoesCombinadas, nomesPessoas } from './utils.js';
 import { baixarMarkdown } from './exportar-md.js';
+import { baixarPdf } from './exportar-pdf.js';
 
 function listaDeCampo(valor) {
     if (!valor) return [];
@@ -180,15 +181,19 @@ export function correspondeFiltro(item, opcoes) {
         if (!livroId || !opcoes.livrosIncluir.includes(String(livroId))) return false;
     }
     if (opcoes.pessoasIncluir.length) {
-        const pessoasItem = listaDeCampo(item.pessoas);
+        const pessoasItem = nomesPessoas(item, db.pessoas).map((n) => n.toLowerCase());
         if (!opcoes.pessoasIncluir.some((p) => pessoasItem.includes(p))) return false;
     }
     if (opcoes.temasIncluir.length) {
-        const temasItem = listaDeCampo(item.sinalizacoes);
+        // Filtro de exporta├º├úo por "tema" j├í olhava pro campo de
+        // Sinaliza├º├Áes inteiro (n├úo s├│ a categoria Tema) ÔÇö mantido assim
+        // com sinalizacoesCombinadas() pra n├úo mudar o que a pessoa j├í
+        // espera desse filtro (ver contarPorTema em estatisticas.js).
+        const temasItem = listaDeCampo(sinalizacoesCombinadas(item));
         if (!opcoes.temasIncluir.some((t) => temasItem.includes(t))) return false;
     }
     if (opcoes.temasExcluir.length) {
-        const temasItem = listaDeCampo(item.sinalizacoes);
+        const temasItem = listaDeCampo(sinalizacoesCombinadas(item));
         if (opcoes.temasExcluir.some((t) => temasItem.includes(t))) return false;
     }
     if (!dataEstaNoIntervalo(item.dataEscrita, opcoes.dataDe, opcoes.dataAte)) return false;
@@ -197,6 +202,7 @@ export function correspondeFiltro(item, opcoes) {
     if (opcoes.status === 'completos' && item.status !== 'completo') return false;
     if (opcoes.status === 'incompletos' && item.status !== 'incompleto') return false;
     if (opcoes.status === 'migrados' && item.status !== 'migrado') return false;
+    if (opcoes.status === 'pendentes' && !item.pendencia?.trim()) return false;
     if (opcoes.status === 'descartados' && item.status !== 'descartado') return false;
     return true;
 }
@@ -362,7 +368,7 @@ export function executarExportacaoSeletivaMarkdown() {
 // listagem ├® independente ÔÇö ver selecaoPoemas/selecaoProsas em
 // render-listas.js). Reaproveita montarRegistro (mesmo formato/contexto
 // resolvido da exporta├º├úo seletiva) e baixarMarkdown (mesmo documento .md).
-function itensDaSelecao(tipo, ids) {
+export function itensDaSelecao(tipo, ids) {
     const idsSet = new Set(ids);
     const colecao = tipo === 'prosa' ? db.prosas : db.poemas;
     return colecao.filter((item) => idsSet.has(item.id)).map((item) => montarRegistro(tipo, item));
@@ -402,6 +408,84 @@ export function exportarSelecaoMarkdown(tipo, ids) {
     baixarMarkdown(itens, `selecao_${tipo}s_${Date.now()}.md`);
 }
 
+// Mesmo padr├úo de exportarSelecaoMarkdown acima, s├│ que em PDF ÔÇö usa o
+// try/catch de exportarItem (baixo) porque a lib jsPDF ├® carregada via
+// CDN (ver coment├írio em exportar-pdf.js) e pode n├úo ter chegado a
+// tempo (conex├úo lenta/caiu bem na hora do clique).
+export function exportarSelecaoPdf(tipo, ids) {
+    const itens = itensDaSelecao(tipo, ids);
+    if (itens.length === 0) {
+        mostrarAviso('Nenhum item selecionado.');
+        return;
+    }
+
+    try {
+        baixarPdf(itens, `selecao_${tipo}s_${Date.now()}.pdf`);
+    } catch (err) {
+        mostrarAviso(err.message || 'N├úo foi poss├¡vel gerar o PDF.');
+    }
+}
+
+// Nome de arquivo a partir do t├¡tulo do item ÔÇö sem acentos, min├║sculo,
+// s├│ letras/n├║meros/h├¡fen. Usado pelo download individual abaixo, pra
+// gerar algo como "meu-poema.pdf" em vez de "selecao_poemas_169...json".
+function nomeArquivoSeguro(texto) {
+    const base = (texto || '')
+        .normalize('NFD')
+        .replace(/[\u0300-\u036f]/g, '')
+        .toLowerCase()
+        .replace(/[^a-z0-9]+/g, '-')
+        .replace(/^-+|-+$/g, '');
+    return base || 'sem-titulo';
+}
+
+// ÔöÇÔöÇÔöÇ Download individual (bot├úo "Baixar" da coluna A├º├Áes e modal de
+// Visualiza├º├úo) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// `formato` ├® 'md' | 'pdf' | 'json' ÔÇö na coluna A├º├Áes vem da configura├º├úo
+// salva por tabela (ver getFormatoBaixar em acoes-coluna.js); no modal
+// de Visualiza├º├úo, do bot├úo que a pessoa clicou (os tr├¬s ficam sempre
+// vis├¡veis ali, ver visualizar.js). Reaproveita itensDaSelecao (mesmo
+// registro/contexto resolvido das demais exporta├º├Áes).
+export function exportarItem(tipo, id, formato) {
+    const itens = itensDaSelecao(tipo, [id]);
+    if (itens.length === 0) {
+        mostrarAviso('Item n├úo encontrado.');
+        return;
+    }
+
+    const nomeBase = nomeArquivoSeguro(itens[0].titulo);
+
+    if (formato === 'json') {
+        const saida = { export_format: 'selecao', itens };
+        const blob = new Blob([JSON.stringify(saida, null, 4)], {
+            type: 'application/json;charset=utf-8',
+        });
+        const url = URL.createObjectURL(blob);
+        const a = document.createElement('a');
+        a.style.display = 'none';
+        a.href = url;
+        a.download = `${nomeBase}.json`;
+        document.body.appendChild(a);
+        a.click();
+        setTimeout(() => {
+            document.body.removeChild(a);
+            URL.revokeObjectURL(url);
+        }, 100);
+        return;
+    }
+
+    if (formato === 'pdf') {
+        try {
+            baixarPdf(itens, `${nomeBase}.pdf`);
+        } catch (err) {
+            mostrarAviso(err.message || 'N├úo foi poss├¡vel gerar o PDF.');
+        }
+        return;
+    }
+
+    baixarMarkdown(itens, `${nomeBase}.md`);
+}
+
 // ÔöÇÔöÇÔöÇ Exportar V├írios Livros Completos (dados do livro + conte├║do aninhado) ÔöÇÔöÇ
 // Mesma ideia de exportarLivroCompleto, mas pra todos os Livros marcados nos
 // checkboxes de #exp-livros-checks (os mesmos usados pelo filtro acima) ÔÇö
@@ -550,9 +634,61 @@ export function exportarTudoFlatMarkdown() {
     }
 }
 
+// Mesma sele├º├úo/filtros de executarExportacaoSeletiva, mas em PDF. Colet├óneas
+// ficam de fora por ora ÔÇö mesma decis├úo de executarExportacaoSeletivaMarkdown
+// (ver coment├írio l├í acima); PDF segue a mesma lista de itens que o .md usa.
+// try/catch por causa da mesma corrida com o carregamento do jsPDF via CDN
+// tratada em exportarSelecaoPdf (ver coment├írio l├í).
+export function executarExportacaoSeletivaPdf() {
+    const opcoes = lerFiltrosDoFormulario();
+    const { itens } = gerarExportacaoSeletiva(opcoes);
+
+    if (itens.length === 0) {
+        const span = document.getElementById('exp-resultado');
+        if (span) span.innerText = 'Nenhum item encontrado com esses filtros ÔÇö nada pra baixar.';
+        return;
+    }
+
+    try {
+        baixarPdf(itens, `exportacao_seletiva_${Date.now()}.pdf`);
+    } catch (err) {
+        mostrarAviso(err.message || 'N├úo foi poss├¡vel gerar o PDF.');
+        return;
+    }
+
+    const span = document.getElementById('exp-resultado');
+    if (span) {
+        span.innerText = `${itens.length} item(ns) exportado(s) em PDF.`;
+    }
+}
+
 // Mant├®m os checkboxes de Livros/Colet├óneas atualizados conforme o banco muda
 window.addEventListener('db:saved', popularSelecaoExportacao);
 
+// Colet├óneas ficam de fora do PDF por ora ÔÇö mesma decis├úo de
+// exportarTudoFlatMarkdown (ver coment├írio l├í acima). Mesmo try/catch de
+// executarExportacaoSeletivaPdf, pela mesma corrida com o carregamento do
+// jsPDF via CDN (ver exportarSelecaoPdf).
+export function exportarTudoFlatPdf() {
+    const { itens } = gerarTudoFlat();
+    if (itens.length === 0) {
+        mostrarAviso('Acervo vazio ÔÇö nada pra exportar.');
+        return;
+    }
+
+    try {
+        baixarPdf(itens, `arquivo_poetico_flat_${Date.now()}.pdf`);
+    } catch (err) {
+        mostrarAviso(err.message || 'N├úo foi poss├¡vel gerar o PDF.');
+        return;
+    }
+
+    const span = document.getElementById('exp-resultado');
+    if (span) {
+        span.innerText = `Acervo inteiro exportado em PDF (${itens.length} texto(s)).`;
+    }
+}
+
 // ÔöÇÔöÇÔöÇ Exportar Tudo Aninhado (absorvido do encadeia.html) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
 // Gera a mesma estrutura profunda (Livro ÔåÆ Parte ÔåÆ Se├º├úo ÔåÆ Poema) que o
 // encadeia.html produzia, mas sem precisar sair do app principal.
diff --git a/js/forms.js b/js/forms.js
index 569d252..d9249d3 100644
--- a/js/forms.js
+++ b/js/forms.js
@@ -2,7 +2,15 @@
 // forms.js ÔÇö Submit handlers e fun├º├Áes de edi├º├úo por entidade
 // ============================================================
 
-import { db, save } from './db.js';
+import {
+    db,
+    save,
+    obterOuCriarEpocaPorNome,
+    calcularImpactoExclusaoPessoa,
+    calcularImpactoExclusaoEpoca,
+    mesclarPessoas,
+    mesclarEpocas,
+} from './db.js';
 import {
     reordenarPosicao,
     fecharEspaco,
@@ -16,9 +24,14 @@ import {
     escapeHtml,
     mostrarAviso,
     mostrarAvisoComAcao,
+    abrirModalConfirmacao,
     criarRastreadorDeAlteracoes,
     violaOrdemDeDatas,
-    obterSugestaoEpocaPorNome,
+    obterSugestaoEpocaPorId,
+    nomeEpoca,
+    CORES_GRUPO,
+    CORES_GRUPO_PADRAO,
+    pontoCorGrupo,
 } from './utils.js';
 
 // Rastreadores de altera├º├Áes n├úo salvas dos formul├írios de texto longo
@@ -32,13 +45,35 @@ import { salvarCapa, deletarCapa } from './capas.js';
 import { getColetaneasDeItem } from './coletaneas.js';
 import { toggleModal, garantirModal, renderDropdowns, sincronizarFiltroDestino } from './ui.js';
 import {
-    carregarTags,
     atualizarDatalist,
     carregarPessoas,
-    resetTagsProsa,
-    carregarTagsProsa,
+    obterPessoas,
+    resetSinalizacoes,
+    carregarSinalizacoes,
+    resetPessoas,
+    resetSinalizacoesProsa,
+    carregarSinalizacoesProsa,
     resetPessoasProsa,
     carregarPessoasProsa,
+    obterPessoasProsa,
+    carregarAutoria,
+    obterAutoria,
+    resetAutoria,
+    carregarAutoriaProsa,
+    obterAutoriaProsa,
+    resetAutoriaProsa,
+    carregarEnvios,
+    obterEnvios,
+    resetEnvios,
+    carregarEnviosProsa,
+    obterEnviosProsa,
+    resetEnviosProsa,
+    carregarReconhecimentos,
+    obterReconhecimentos,
+    resetReconhecimentos,
+    carregarReconhecimentosProsa,
+    obterReconhecimentosProsa,
+    resetReconhecimentosProsa,
     resetGeneroProsa,
     carregarGeneroProsa,
     atualizarDatalistProsa,
@@ -48,8 +83,36 @@ import {
     carregarAnexos,
     obterAnotacoes,
     carregarAnotacoes,
+    obterElos,
+    carregarElos,
+    obterReferencias,
+    carregarReferencias,
+    renderPainelElosDerivados,
     atualizarFiltroSecoesMigracao,
+    obterIntertextualidadeProsa,
+    carregarIntertextualidadeProsa,
+    resetIntertextualidadeProsa,
+    obterAnexosProsa,
+    carregarAnexosProsa,
+    resetAnexosProsa,
+    obterElosProsa,
+    carregarElosProsa,
+    resetElosProsa,
+    obterReferenciasProsa,
+    carregarReferenciasProsa,
+    resetReferenciasProsa,
+    renderPainelElosDerivadosProsa,
 } from './editor.js';
+import { renderPessoas, renderEpocas } from './render-listas.js';
+
+// L├¬ o par Livro/Se├º├úo de texto livre dos campos de Migra├º├úo (Cortado
+// de / Lan├ºado em) ÔÇö compartilhado por Poema e Prosa (item 4), por isso
+// vive no escopo do m├│dulo em vez de dentro de um dos dois onsubmit.
+function lerLivroSecao(prefLivro, prefSecao) {
+    const livro = document.getElementById(prefLivro)?.value || '';
+    const secao = document.getElementById(prefSecao)?.value || '';
+    return livro || secao ? { livro, secao } : null;
+}
 
 // ÔöÇÔöÇÔöÇ Preenchimento declarativo de formul├írio de edi├º├úo ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
 // Substitui a lista repetitiva de "document.getElementById(id).value =
@@ -191,6 +254,376 @@ export async function editarLivro(id) {
     toggleModal('modal-livro');
 }
 
+// ÔöÇÔöÇÔöÇ Pessoa ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Cadastro central: quem a pessoa ├ë (nome + grupos que pertence),
+// constante entre poemas. O papel que ela ocupa em CADA texto
+// (Retratado(a)/Dedicat├írio(a)/etc.) n├úo mora aqui ÔÇö mora em
+// item.pessoas, dentro do pr├│prio poema/prosa (ver criarGrupoDePessoas
+// em editor.js). Grupo em si vem de um cadastro ├á parte (ver Grupo,
+// abaixo) ÔÇö aqui s├│ marcamos quais grupos, j├í existentes, esta pessoa
+// pertence, via checkbox (n├úo cria grupo novo por aqui de prop├│sito:
+// grupo cadastr├ível formalmente ├® o ponto de n├úo hardcodar "J├║lias"/
+// "Friends" no c├│digo-fonte, ent├úo criar mora s├│ na aba Grupos).
+
+export function initFormPessoa() {
+    const form = document.getElementById('form-pessoa');
+    if (!form) return;
+
+    form.onsubmit = (e) => {
+        e.preventDefault();
+        const id = document.getElementById('ps-edit-id').value;
+        const nome = document.getElementById('ps-nome').value.trim();
+        const grupoIds = Array.from(
+            document.querySelectorAll('#ps-grupos-container input[type="checkbox"]:checked'),
+        ).map((el) => parseInt(el.value));
+
+        // Grava os dados do form no cadastro (cria se `id` vazio, atualiza
+        // no lugar se n├úo) ÔÇö usado tanto no caminho normal quanto como
+        // primeiro passo de "Mesclar agora" abaixo (o registro precisa
+        // existir em `db.pessoas` antes de virar `origemId` de
+        // mesclarPessoas, que s├│ opera sobre entradas j├í cadastradas).
+        const gravar = () => {
+            const dados = { id: id ? parseInt(id) : gerarId(), nome, grupoIds };
+            if (id) db.pessoas[db.pessoas.findIndex((x) => x.id == id)] = dados;
+            else db.pessoas.push(dados);
+            return dados.id;
+        };
+
+        // Nome duplicado (mesmo crit├®rio de dedup exato de
+        // obterOuCriarPessoaPorNome/migrarPessoasParaCadastro) ÔÇö n├úo
+        // decide sozinho: pode ser a mesma pessoa cadastrada duas vezes
+        // (a├¡ faz sentido mesclar) ou duas pessoas diferentes de
+        // prop├│sito, mesmo nome (ex.: grupos diferentes) ÔÇö a├¡ o Victor
+        // quer mesmo os dois registros separados. Motivado por uma
+        // pergunta dele sobre o mesmo risco em ├ëpoca, ver abaixo.
+        const duplicata = db.pessoas.find((x) => x.nome === nome && x.id != id);
+
+        if (!duplicata) {
+            gravar();
+            save();
+            toggleModal('modal-pessoa');
+            return;
+        }
+
+        abrirModalConfirmacao({
+            titulo: `J├í existe uma Pessoa chamada "${duplicata.nome}"`,
+            rotulo: 'Nome duplicado',
+            mensagem:
+                'Pode ser a mesma pessoa cadastrada duas vezes, ou duas pessoas diferentes de prop├│sito (mesmo nome, grupos diferentes, por exemplo). O que voc├¬ quer fazer?',
+            textoConfirmar: 'Mesclar agora',
+            corConfirmar: '#d97706',
+            acaoSecundaria: {
+                texto: 'Salvar mesmo assim',
+                onClick: () => {
+                    gravar();
+                    save();
+                    toggleModal('modal-pessoa');
+                },
+            },
+            onConfirmar: () => {
+                const origemId = gravar();
+                mesclarPessoas(db, origemId, duplicata.id);
+                save();
+                toggleModal('modal-pessoa');
+                renderPessoas();
+                mostrarAviso(`Mesclado com "${duplicata.nome}"`, 'sucesso');
+            },
+        });
+    };
+}
+
+export async function editarPessoa(id) {
+    const p = db.pessoas.find((x) => x.id == id);
+    if (!p) return;
+    await garantirModal('modal-pessoa');
+    renderDropdowns(); // popula #ps-grupos-container com os grupos existentes
+
+    document.getElementById('ps-edit-id').value = p.id;
+    document.getElementById('ps-nome').value = p.nome;
+    document.querySelectorAll('#ps-grupos-container input[type="checkbox"]').forEach((el) => {
+        el.checked = (p.grupoIds || []).includes(parseInt(el.value));
+    });
+    document.getElementById('modal-pessoa-titulo').innerText = 'Editar Pessoa';
+    toggleModal('modal-pessoa');
+}
+
+// ÔöÇÔöÇÔöÇ Grupo ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Registro dedicado (n├úo tag livre tipo Sinaliza├º├Áes) justamente pra
+// poder criar/editar/renomear formalmente, sem hardcode no c├│digo-fonte
+// (ver conversa que definiu isso) e sem o risco de diverg├¬ncia que uma
+// string solta repetida em cada Pessoa teria.
+
+// Desenha as bolinhas de CORES_GRUPO (fonte ├║nica, ver utils.js) e marca
+// a selecionada com um anel; clique grava em #g-cor (hidden input) e
+// redesenha pra mover o anel. Sem input[type=color] de prop├│sito ÔÇö a
+// paleta ├® curada (ver CORES_GRUPO em utils.js).
+export function renderSeletorCorGrupo(corSelecionada) {
+    const container = document.getElementById('g-cor-container');
+    if (!container) return;
+    container.innerHTML = CORES_GRUPO.map(({ chave, rotulo }) => {
+        const selecionada = chave === corSelecionada;
+        const anel = selecionada
+            ? 'ring-2 ring-offset-2 ring-gray-400 dark:ring-offset-slate-900'
+            : '';
+        return `<button type="button" title="${escapeHtml(rotulo)}" aria-label="${escapeHtml(rotulo)}"
+            onclick="selecionarCorGrupo('${chave}')"
+            class="w-6 h-6 rounded-full ${pontoCorGrupo(chave)} ${anel}"></button>`;
+    }).join('');
+}
+
+window.selecionarCorGrupo = (chave) => {
+    document.getElementById('g-cor').value = chave;
+    renderSeletorCorGrupo(chave);
+};
+
+export function initFormGrupo() {
+    const form = document.getElementById('form-grupo');
+    if (!form) return;
+
+    form.onsubmit = (e) => {
+        e.preventDefault();
+        const id = document.getElementById('g-edit-id').value;
+        const dados = {
+            id: id ? parseInt(id) : gerarId(),
+            nome: document.getElementById('g-nome').value.trim(),
+            cor: document.getElementById('g-cor').value || CORES_GRUPO_PADRAO,
+        };
+
+        if (id) db.grupos[db.grupos.findIndex((x) => x.id == id)] = dados;
+        else db.grupos.push(dados);
+
+        save();
+        toggleModal('modal-grupo');
+    };
+}
+
+export async function editarGrupo(id) {
+    const g = db.grupos.find((x) => x.id == id);
+    if (!g) return;
+    await garantirModal('modal-grupo');
+    document.getElementById('g-edit-id').value = g.id;
+    document.getElementById('g-nome').value = g.nome;
+    document.getElementById('g-cor').value = g.cor || CORES_GRUPO_PADRAO;
+    renderSeletorCorGrupo(g.cor || CORES_GRUPO_PADRAO);
+    document.getElementById('modal-grupo-titulo').innerText = 'Editar Grupo';
+    toggleModal('modal-grupo');
+}
+
+// ÔöÇÔöÇÔöÇ Autor ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Cadastro central (db.autores: { id, nome, sobre }), ├á parte do de
+// Pessoas ÔÇö quem tem responsabilidade autoral sobre um texto (ver
+// migrarAutoria em db.js e AUTORIA_PAPEIS em utils.js). Sem grupos:
+// diferente de Pessoa, Autor n├úo tem uma taxonomia pr├│pria pra
+// agrupar ÔÇö s├│ nome e uma nota livre ("sobre").
+
+export function initFormAutor() {
+    const form = document.getElementById('form-autor');
+    if (!form) return;
+
+    form.onsubmit = (e) => {
+        e.preventDefault();
+        const id = document.getElementById('au-edit-id').value;
+
+        const dados = {
+            id: id ? parseInt(id) : gerarId(),
+            nome: document.getElementById('au-nome').value.trim(),
+            sobre: document.getElementById('au-sobre').value.trim(),
+        };
+
+        if (id) db.autores[db.autores.findIndex((x) => x.id == id)] = dados;
+        else db.autores.push(dados);
+
+        save();
+        toggleModal('modal-autor');
+    };
+}
+
+export async function editarAutor(id) {
+    const a = db.autores.find((x) => x.id == id);
+    if (!a) return;
+    await garantirModal('modal-autor');
+    document.getElementById('au-edit-id').value = a.id;
+    document.getElementById('au-nome').value = a.nome;
+    document.getElementById('au-sobre').value = a.sobre || '';
+    document.getElementById('modal-autor-titulo').innerText = 'Editar Autor';
+    toggleModal('modal-autor');
+}
+
+// ÔöÇÔöÇÔöÇ ├ëpoca ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Cadastro central (db.epocas: { id, nome, contextoRelacao, notas }),
+// item 3 do plano de schema ÔÇö a que per├¡odo um poema se refere (ver
+// migrarEpocas em db.js). Mesmo esp├¡rito de Autor acima: sem grupos,
+// s├│ um cadastro simples referenciado por epocaId. O modal de Poema
+// n├úo abre este modal diretamente ÔÇö resolve/cria por nome no submit
+// (ver obterOuCriarEpocaPorNome em db.js); este modal serve pra edi├º├úo
+// posterior (contextoRelacao/notas) e exclus├úo, na aba de gest├úo.
+
+export function initFormEpoca() {
+    const form = document.getElementById('form-epoca');
+    if (!form) return;
+
+    form.onsubmit = (e) => {
+        e.preventDefault();
+        const id = document.getElementById('ep-edit-id').value;
+        const nome = document.getElementById('ep-nome').value.trim();
+        const contextoRelacao = document.getElementById('ep-contexto').value.trim();
+        const notas = document.getElementById('ep-notas').value.trim();
+
+        // Mesmo racioc├¡nio de initFormPessoa acima: grava primeiro (cria
+        // se `id` vazio, atualiza no lugar se n├úo), pra existir em
+        // `db.epocas` antes de virar `origemId` de mesclarEpocas se for
+        // esse o caminho escolhido.
+        const gravar = () => {
+            const dados = { id: id ? parseInt(id) : gerarId(), nome, contextoRelacao, notas };
+            if (id) db.epocas[db.epocas.findIndex((x) => x.id == id)] = dados;
+            else db.epocas.push(dados);
+            return dados.id;
+        };
+
+        // Nome duplicado (mesmo crit├®rio de dedup exato de
+        // obterOuCriarEpocaPorNome/migrarEpocas) ÔÇö mesma pergunta de
+        // Pessoa acima: pode ser a mesma ├ëpoca cadastrada duas vezes, ou
+        // duas ├ëpocas diferentes de prop├│sito, mesmo nome (ex.: o
+        // relacionamento muda, mas o per├¡odo recebeu o mesmo apelido).
+        const duplicata = db.epocas.find((x) => x.nome === nome && x.id != id);
+
+        if (!duplicata) {
+            gravar();
+            save();
+            toggleModal('modal-epoca');
+            return;
+        }
+
+        abrirModalConfirmacao({
+            titulo: `J├í existe uma ├ëpoca chamada "${duplicata.nome}"`,
+            rotulo: 'Nome duplicado',
+            mensagem:
+                'Pode ser a mesma ├ëpoca cadastrada duas vezes, ou duas ├ëpocas diferentes de prop├│sito, mesmo nome (ex.: o relacionamento mudou, mas o per├¡odo ganhou o mesmo apelido). O que voc├¬ quer fazer?',
+            textoConfirmar: 'Mesclar agora',
+            corConfirmar: '#d97706',
+            acaoSecundaria: {
+                texto: 'Salvar mesmo assim',
+                onClick: () => {
+                    gravar();
+                    save();
+                    toggleModal('modal-epoca');
+                },
+            },
+            onConfirmar: () => {
+                const origemId = gravar();
+                mesclarEpocas(db, origemId, duplicata.id);
+                save();
+                toggleModal('modal-epoca');
+                renderEpocas();
+                mostrarAviso(`Mesclado com "${duplicata.nome}"`, 'sucesso');
+            },
+        });
+    };
+}
+
+export async function editarEpoca(id) {
+    const ep = db.epocas.find((x) => x.id == id);
+    if (!ep) return;
+    await garantirModal('modal-epoca');
+    document.getElementById('ep-edit-id').value = ep.id;
+    document.getElementById('ep-nome').value = ep.nome;
+    document.getElementById('ep-contexto').value = ep.contextoRelacao || '';
+    document.getElementById('ep-notas').value = ep.notas || '';
+    document.getElementById('modal-epoca-titulo').innerText = 'Editar ├ëpoca';
+    toggleModal('modal-epoca');
+}
+
+// ÔöÇÔöÇÔöÇ Mesclar (Pessoa/├ëpoca) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Modal gen├®rico (modal-mesclar.html), reaproveitado pelas duas abas de
+// gest├úo que t├¬m o mesmo risco de duplicata por nome ÔÇö renomear uma
+// Pessoa/├ëpoca pra igual a outra n├úo mescla nada sozinho (ver
+// mesclarPessoas/mesclarEpocas em db.js pra detalhe da mec├ónica e da
+// conversa que motivou isso). `RENDER_MESCLAR` isola a ├║nica parte que
+// muda entre os dois tipos (r├│tulo, como calcular impacto, qual fun├º├úo
+// de mesclagem chamar, qual lista re-renderizar depois) ÔÇö o fluxo do
+// modal em si (abrir, escolher destino, confirmar, executar) ├® id├¬ntico.
+const RENDER_MESCLAR = {
+    pessoas: {
+        rotulo: 'Pessoa',
+        impacto: (id) => calcularImpactoExclusaoPessoa(db, id),
+        mesclar: (origemId, destinoId) => mesclarPessoas(db, origemId, destinoId),
+        rerender: renderPessoas,
+    },
+    epocas: {
+        rotulo: '├ëpoca',
+        impacto: (id) => calcularImpactoExclusaoEpoca(db, id),
+        mesclar: (origemId, destinoId) => mesclarEpocas(db, origemId, destinoId),
+        rerender: renderEpocas,
+    },
+};
+
+export function initFormMesclar() {
+    const form = document.getElementById('form-mesclar');
+    if (!form) return;
+
+    form.onsubmit = (e) => {
+        e.preventDefault();
+        const tipo = document.getElementById('ms-tipo').value;
+        const origemId = parseInt(document.getElementById('ms-origem-id').value);
+        const destinoId = parseInt(document.getElementById('ms-destino-id').value);
+        const cfg = RENDER_MESCLAR[tipo];
+        if (!cfg || !destinoId) return;
+
+        const origem = db[tipo].find((x) => x.id == origemId);
+        const destino = db[tipo].find((x) => x.id == destinoId);
+        if (!origem || !destino) return;
+
+        const { poemasIds, prosasIds } = cfg.impacto(origemId);
+        const total = poemasIds.length + prosasIds.length;
+
+        // Fecha o modal de escolha antes de abrir o de confirma├º├úo ÔÇö os
+        // dois usam a mesma tecla Esc/foco-trap (ver modais.js), ent├úo
+        // empilhar os dois ao mesmo tempo confundiria qual fecha primeiro.
+        toggleModal('modal-mesclar');
+        abrirModalConfirmacao({
+            titulo: `${origem.nome} ÔåÆ ${destino.nome}`,
+            rotulo: `Mesclar ${cfg.rotulo}`,
+            mensagem:
+                total > 0
+                    ? `${total} texto${total !== 1 ? 's' : ''} que ${total !== 1 ? 'referenciam' : 'referencia'} "${origem.nome}" ${total !== 1 ? 'passam' : 'passa'} a referenciar "${destino.nome}". "${origem.nome}" ser├í removido(a) do cadastro. Essa a├º├úo n├úo pode ser desfeita.`
+                    : `"${origem.nome}" ser├í removido(a) do cadastro. Essa a├º├úo n├úo pode ser desfeita.`,
+            textoConfirmar: 'Mesclar',
+            corConfirmar: '#d97706',
+            onConfirmar: () => {
+                cfg.mesclar(origemId, destinoId);
+                save();
+                cfg.rerender();
+                mostrarAviso(`Mesclado: "${origem.nome}" ÔåÆ "${destino.nome}"`, 'sucesso');
+            },
+        });
+    };
+}
+
+export async function abrirModalMesclar(tipo, origemId) {
+    const cfg = RENDER_MESCLAR[tipo];
+    if (!cfg) return;
+    const origem = db[tipo]?.find((x) => x.id == origemId);
+    if (!origem) return;
+
+    const outras = (db[tipo] || [])
+        .filter((x) => x.id != origemId)
+        .sort((a, b) => a.nome.localeCompare(b.nome, 'pt-BR'));
+    if (outras.length === 0) {
+        mostrarAviso(`N├úo h├í outra ${cfg.rotulo.toLowerCase()} cadastrada pra mesclar.`);
+        return;
+    }
+
+    await garantirModal('modal-mesclar');
+    document.getElementById('modal-mesclar-titulo').innerText = `Mesclar ${cfg.rotulo}`;
+    document.getElementById('ms-tipo').value = tipo;
+    document.getElementById('ms-origem-id').value = origemId;
+    document.getElementById('ms-origem-nome').innerText = origem.nome;
+    document.getElementById('ms-destino-id').innerHTML = outras
+        .map((x) => `<option value="${x.id}">${escapeHtml(x.nome)}</option>`)
+        .join('');
+    toggleModal('modal-mesclar');
+}
+
 // ÔöÇÔöÇÔöÇ Parte ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
 
 export function initFormParte() {
@@ -380,12 +813,20 @@ export function toggleCamposEpocaNa() {
 // Aplica a sugest├úo autom├ítica de datas/contexto ao repetir um nome de
 // per├¡odo j├í usado (ver p-epoca-nome em modal-poema.html) ÔÇö s├│ entra
 // nos campos que estiverem vazios (De/At├® de ├ëpoca Retratada e Contexto
-// Hist├│rico/Pessoal), nunca sobrescrevendo o que j├í foi digitado. Ver
-// obterSugestaoEpocaPorNome em utils.js pra crit├®rio de "mais recente".
+// Hist├│rico/Pessoal), nunca sobrescrevendo o que j├í foi digitado. O
+// nome digitado s├│ dispara sugest├úo se j├í existir uma ├ëpoca cadastrada
+// com esse nome exato (db.epocas) ÔÇö nome novo n├úo tem hist├│rico ainda
+// pra sugerir. Ver obterSugestaoEpocaPorId em utils.js pra crit├®rio de
+// "mais recente".
 export function aplicarSugestaoEpoca() {
     const nome = (document.getElementById('p-epoca-nome')?.value || '').trim();
     if (!nome) return;
-    const sugestao = obterSugestaoEpocaPorNome(db.poemas, nome);
+    const epoca = db.epocas.find((e) => e.nome === nome);
+    if (!epoca) return;
+    // Item 4: ├ëpoca ├® cadastro central compartilhado (item 3) ÔÇö busca no
+    // texto mais recente com esse epocaId em Poemas E Prosas, n├úo s├│
+    // Poemas, j├í que uma prosa pode ter documentado o per├¡odo primeiro.
+    const sugestao = obterSugestaoEpocaPorId([...db.poemas, ...(db.prosas || [])], epoca.id);
     if (!sugestao) return;
     preencherDataParcialSeVazio('p-epoca-ini', sugestao.inicio);
     preencherDataParcialSeVazio('p-epoca-fim', sugestao.fim);
@@ -424,15 +865,21 @@ export function initFormPoema() {
         if (dataEscrita) dataEscrita.exata = document.getElementById('p-data-esc-exata').checked;
 
         if (violaOrdemDeDatas(dataEscrita, dataPublicacao)) {
-            return mostrarAviso('A Data de Primeira Publica├º├úo n├úo pode ser anterior ├á Data de Escrita.');
+            return mostrarAviso(
+                'A Data de Primeira Publica├º├úo n├úo pode ser anterior ├á Data de Escrita.',
+            );
         }
 
         // ├ëpoca Retratada ├® um intervalo (n├úo um ponto no tempo), com um
         // terceiro estado al├®m de "preenchido"/"vazio": N/A marcado ├® uma
         // exclus├úo deliberada, distinta de "ainda n├úo categorizado" (o
-        // campo inteiro fica null). Ver formatarEpocaRetratada em utils.js.
+        // campo inteiro fica null). O nome digitado ├® resolvido/criado no
+        // cadastro central db.epocas (mesmo caminho de
+        // obterOuCriarAutorPorNome) ÔÇö ver formatarEpocaRetratada em
+        // utils.js.
         const epocaNa = document.getElementById('p-epoca-na').checked;
         const epocaNome = (document.getElementById('p-epoca-nome')?.value || '').trim();
+        const epocaRecorte = document.getElementById('p-epoca-recorte')?.value || null;
         const epocaInicio = lerDataParcial('p-epoca-ini');
         const epocaFim = lerDataParcial('p-epoca-fim');
 
@@ -440,17 +887,19 @@ export function initFormPoema() {
             return mostrarAviso('O "At├®" da ├ëpoca Retratada n├úo pode ser anterior ao "De".');
         }
 
+        const epocaCadastrada = epocaNome ? obterOuCriarEpocaPorNome(epocaNome) : null;
+
         const epocaRetratada =
-            epocaNa || epocaInicio || epocaFim || epocaNome
-                ? { na: epocaNa, inicio: epocaInicio, fim: epocaFim, nome: epocaNome }
+            epocaNa || epocaInicio || epocaFim || epocaCadastrada
+                ? {
+                      epocaId: epocaCadastrada?.id ?? null,
+                      inicio: epocaInicio,
+                      fim: epocaFim,
+                      recorte: epocaRecorte,
+                      na: epocaNa,
+                  }
                 : null;
 
-        const lerLivroSecao = (prefLivro, prefSecao) => {
-            const livro = document.getElementById(prefLivro)?.value || '';
-            const secao = document.getElementById(prefSecao)?.value || '';
-            return livro || secao ? { livro, secao } : null;
-        };
-
         const dados = {
             id,
             titulo: document.getElementById('p-titulo').value,
@@ -458,6 +907,7 @@ export function initFormPoema() {
             paiTipo,
             paiId,
             sequencia: seqOuNull(document.getElementById('p-sequencia').value),
+            idioma: document.getElementById('p-idioma').value.trim() || 'pt-BR',
             dataEscrita,
             dataPublicacao,
             ano: dataEscrita?.ano || '', // mantido por compatibilidade (ordena├º├úo/estat├¡sticas/exporta├º├úo)
@@ -465,16 +915,20 @@ export function initFormPoema() {
                 parseInt(o.value),
             ),
             conceitos: {
-                elos: Array.from(document.getElementById('p-elos-select').selectedOptions).map(
-                    (o) => parseInt(o.value),
-                ),
-                referencias: Array.from(
-                    document.getElementById('p-refs-select').selectedOptions,
-                ).map((o) => parseInt(o.value)),
+                elos: obterElos(),
+                referencias: obterReferencias(),
             },
             notas: document.getElementById('p-notas').value,
-            sinalizacoes: document.getElementById('p-sinal').value,
-            pessoas: document.getElementById('p-pessoas').value,
+            sinalizacoesEstilo: document.getElementById('p-sinal-estilo').value,
+            sinalizacoesTema: document.getElementById('p-sinal-tema').value,
+            sinalizacoesRelacao: document.getElementById('p-sinal-relacao').value,
+            sinalizacoesSensibilidade: document.getElementById('p-sinal-sensibilidade').value,
+            sinalizacoesTom: document.getElementById('p-sinal-tom').value,
+            sinalizacoesOutros: document.getElementById('p-sinal-outros').value,
+            pessoas: obterPessoas(),
+            autoria: obterAutoria(),
+            envios: obterEnvios(),
+            reconhecimentos: obterReconhecimentos(),
             status: document.getElementById('p-status').value,
             epocaRetratada,
             intertextualidade: obterIntertextualidade(),
@@ -488,14 +942,19 @@ export function initFormPoema() {
             vocabularioHiperacionante: document.getElementById('p-hiperacionante').value,
             cortadoDe: lerLivroSecao('p-cortado-livro', 'p-cortado-secao'),
             lancadoEm: lerLivroSecao('p-lancado-livro', 'p-lancado-secao'),
+            justificativaMigracao: document.getElementById('p-migracao-justificativa').value,
             descarte: document.getElementById('p-descarte').value,
+            pendencia: document.getElementById('p-pendencia').value,
         };
 
         // Aviso n├úo-bloqueante: os campos de Migra├º├úo e Descarte s├│ fazem
         // sentido junto do status correspondente. N├úo impede salvar ÔÇö s├│
         // avisa, pra n├úo travar o fluxo caso o texto seja escrito antes de
         // trocar o status, ou o status mude sem que o campo seja limpo.
-        if ((dados.cortadoDe || dados.lancadoEm) && dados.status !== 'migrado') {
+        if (
+            (dados.cortadoDe || dados.lancadoEm || dados.justificativaMigracao.trim()) &&
+            dados.status !== 'migrado'
+        ) {
             mostrarAvisoComAcao(
                 'Migra├º├úo entre livros preenchida, mas o status n├úo ├® "Migrado".',
                 'Reabrir',
@@ -543,6 +1002,12 @@ export function initFormPoema() {
         save();
         rastreadorPoema.marcarLimpo();
         toggleModal('modal-poema');
+        resetSinalizacoes();
+        resetPessoas();
+        resetAutoria();
+        resetEnvios();
+        resetReconhecimentos();
+        form.reset();
     };
 }
 
@@ -551,6 +1016,7 @@ const MAPA_POEMA = {
     titulo: 'p-titulo',
     texto: 'p-texto',
     sequencia: 'p-sequencia',
+    idioma: ['p-idioma', 'pt-BR'],
     notas: 'p-notas',
     anexosNotaGeral: 'p-anexos-nota-geral',
     status: 'p-status', // select ÔÇö preencherCampos seta .value por j├í n├úo ser checkbox
@@ -560,6 +1026,7 @@ const MAPA_POEMA = {
     conteudoSensivel: 'p-sensivel',
     vocabularioHiperacionante: 'p-hiperacionante',
     descarte: 'p-descarte',
+    pendencia: 'p-pendencia',
 };
 
 export async function editarPoema(id) {
@@ -577,11 +1044,15 @@ export async function editarPoema(id) {
     preencherDataParcial('p-epoca-ini', p.epocaRetratada?.inicio);
     preencherDataParcial('p-epoca-fim', p.epocaRetratada?.fim);
     document.getElementById('p-epoca-na').checked = !!p.epocaRetratada?.na;
-    document.getElementById('p-epoca-nome').value = p.epocaRetratada?.nome || '';
+    document.getElementById('p-epoca-nome').value = nomeEpoca(p.epocaRetratada, db.epocas);
+    document.getElementById('p-epoca-recorte').value = p.epocaRetratada?.recorte || '';
     toggleCamposEpocaNa();
     document.getElementById('p-intertexto-tipo').value = '';
     document.getElementById('p-intertexto-texto').value = '';
     carregarIntertextualidade(p.intertextualidade || []);
+    carregarElos(p.conceitos?.elos || []);
+    carregarReferencias(p.conceitos?.referencias || []);
+    renderPainelElosDerivados(p.id);
     document.getElementById('p-anexo-tipo').value = '';
     document.getElementById('p-anexo-link').value = '';
     carregarAnexos(p.anexos || p.ilustracoes || []);
@@ -593,6 +1064,7 @@ export async function editarPoema(id) {
     document.getElementById('p-cortado-secao').value = p.cortadoDe?.secao || '';
     document.getElementById('p-lancado-livro').value = p.lancadoEm?.livro || '';
     document.getElementById('p-lancado-secao').value = p.lancadoEm?.secao || '';
+    document.getElementById('p-migracao-justificativa').value = p.justificativaMigracao || '';
     // .value= direto n├úo dispara 'input' ÔÇö reaplica o filtro de Se├º├úo
     // manualmente pros 4 campos que acabaram de ser preenchidos.
     atualizarFiltroSecoesMigracao();
@@ -612,17 +1084,50 @@ export async function editarPoema(id) {
     };
 
     setM('p-livros', p.livrosIds || []);
-    setM('p-elos-select', p.conceitos?.elos || []);
-    setM('p-refs-select', p.conceitos?.referencias || []);
 
-    carregarTags(p.sinalizacoes);
+    carregarSinalizacoes(p);
     carregarPessoas(p.pessoas);
+    carregarAutoria(p.autoria);
+    carregarEnvios(p.envios);
+    carregarReconhecimentos(p.reconhecimentos);
     renderColetaneasInfo('p-coletaneas-info', 'poema', p.id);
     document.getElementById('modal-poema-titulo').innerText = 'Editar Poema';
     toggleModal('modal-poema');
 }
 
 // ÔöÇÔöÇÔöÇ Prosa ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Item 4: Prosa ganha os mesmos campos de Poema ÔÇö ver toggleCamposEpocaNa/
+// aplicarSugestaoEpoca acima pro coment├írio completo, mesma l├│gica aqui.
+
+export function toggleCamposEpocaNaProsa() {
+    const na = document.getElementById('pr-epoca-na')?.checked;
+    ['pr-epoca-ini', 'pr-epoca-fim'].forEach((prefixo) => {
+        ['dia', 'mes', 'ano'].forEach((parte) => {
+            const el = document.getElementById(`${prefixo}-${parte}`);
+            if (el) el.disabled = !!na;
+        });
+    });
+}
+
+export function aplicarSugestaoEpocaProsa() {
+    const nome = (document.getElementById('pr-epoca-nome')?.value || '').trim();
+    if (!nome) return;
+    const epoca = db.epocas.find((e) => e.nome === nome);
+    if (!epoca) return;
+    // ├ëpoca ├® cadastro central compartilhado (item 3) ÔÇö a sugest├úo de
+    // datas/contexto busca no texto mais recente com esse epocaId em
+    // QUALQUER um dos dois arrays (n├úo s├│ Prosa), j├í que um poema pode
+    // ter documentado o per├¡odo primeiro. `obterSugestaoEpocaPorId`
+    // ordena por id, e ids nunca colidem entre os dois tipos.
+    const sugestao = obterSugestaoEpocaPorId([...db.poemas, ...(db.prosas || [])], epoca.id);
+    if (!sugestao) return;
+    preencherDataParcialSeVazio('pr-epoca-ini', sugestao.inicio);
+    preencherDataParcialSeVazio('pr-epoca-fim', sugestao.fim);
+    const contextoEl = document.getElementById('pr-contexto');
+    if (contextoEl && !contextoEl.value.trim() && sugestao.contextoHistorico) {
+        contextoEl.value = sugestao.contextoHistorico;
+    }
+}
 
 export function initFormProsa() {
     const form = document.getElementById('form-prosa');
@@ -650,26 +1155,102 @@ export function initFormProsa() {
         if (dataEscrita) dataEscrita.exata = document.getElementById('pr-data-esc-exata').checked;
 
         if (violaOrdemDeDatas(dataEscrita, dataPublicacao)) {
-            return mostrarAviso('A Data de Primeira Publica├º├úo n├úo pode ser anterior ├á Data de Escrita.');
+            return mostrarAviso(
+                'A Data de Primeira Publica├º├úo n├úo pode ser anterior ├á Data de Escrita.',
+            );
+        }
+
+        // ├ëpoca Retratada ÔÇö mesmo formato de Poema (ver initFormPoema
+        // acima pro coment├írio completo).
+        const epocaNa = document.getElementById('pr-epoca-na').checked;
+        const epocaNome = (document.getElementById('pr-epoca-nome')?.value || '').trim();
+        const epocaRecorte = document.getElementById('pr-epoca-recorte')?.value || null;
+        const epocaInicio = lerDataParcial('pr-epoca-ini');
+        const epocaFim = lerDataParcial('pr-epoca-fim');
+
+        if (violaOrdemDeDatas(epocaInicio, epocaFim)) {
+            return mostrarAviso('O "At├®" da ├ëpoca Retratada n├úo pode ser anterior ao "De".');
         }
 
+        const epocaCadastrada = epocaNome ? obterOuCriarEpocaPorNome(epocaNome) : null;
+
+        const epocaRetratada =
+            epocaNa || epocaInicio || epocaFim || epocaCadastrada
+                ? {
+                      epocaId: epocaCadastrada?.id ?? null,
+                      inicio: epocaInicio,
+                      fim: epocaFim,
+                      recorte: epocaRecorte,
+                      na: epocaNa,
+                  }
+                : null;
+
         const dados = {
             id,
             titulo: document.getElementById('pr-titulo').value,
             texto: document.getElementById('pr-texto').value,
             sequencia: seqOuNull(document.getElementById('pr-sequencia').value),
+            idioma: document.getElementById('pr-idioma').value.trim() || 'pt-BR',
             dataEscrita,
             dataPublicacao,
             ano: dataEscrita?.ano || '', // mantido por compatibilidade (ordena├º├úo/estat├¡sticas/exporta├º├úo)
             paiTipo,
             paiId,
+            livrosIds: Array.from(document.getElementById('pr-livros').selectedOptions).map((o) =>
+                parseInt(o.value),
+            ),
+            conceitos: {
+                elos: obterElosProsa(),
+                referencias: obterReferenciasProsa(),
+            },
             notas: document.getElementById('pr-notas').value,
-            sinalizacoes: document.getElementById('pr-sinal').value,
-            pessoas: document.getElementById('pr-pessoas').value,
+            sinalizacoesEstilo: document.getElementById('pr-sinal-estilo').value,
+            sinalizacoesTema: document.getElementById('pr-sinal-tema').value,
+            sinalizacoesRelacao: document.getElementById('pr-sinal-relacao').value,
+            sinalizacoesSensibilidade: document.getElementById('pr-sinal-sensibilidade').value,
+            sinalizacoesTom: document.getElementById('pr-sinal-tom').value,
+            sinalizacoesOutros: document.getElementById('pr-sinal-outros').value,
+            pessoas: obterPessoasProsa(),
+            autoria: obterAutoriaProsa(),
+            envios: obterEnviosProsa(),
+            reconhecimentos: obterReconhecimentosProsa(),
             genero: document.getElementById('pr-genero').value,
             publicado: document.getElementById('pr-pub').checked,
+            status: document.getElementById('pr-status').value,
+            epocaRetratada,
+            intertextualidade: obterIntertextualidadeProsa(),
+            anexos: obterAnexosProsa(),
+            anexosNotaGeral: document.getElementById('pr-anexos-nota-geral').value,
+            contextoHistorico: document.getElementById('pr-contexto').value,
+            ocultacao: document.getElementById('pr-ocultacao').value,
+            conteudoSensivel: document.getElementById('pr-sensivel').value,
+            vocabularioHiperacionante: document.getElementById('pr-hiperacionante').value,
+            cortadoDe: lerLivroSecao('pr-cortado-livro', 'pr-cortado-secao'),
+            lancadoEm: lerLivroSecao('pr-lancado-livro', 'pr-lancado-secao'),
+            justificativaMigracao: document.getElementById('pr-migracao-justificativa').value,
+            descarte: document.getElementById('pr-descarte').value,
+            pendencia: document.getElementById('pr-pendencia').value,
         };
 
+        // Avisos n├úo-bloqueantes ÔÇö mesmo crit├®rio de initFormPoema.
+        if (
+            (dados.cortadoDe || dados.lancadoEm || dados.justificativaMigracao.trim()) &&
+            dados.status !== 'migrado'
+        ) {
+            mostrarAvisoComAcao(
+                'Migra├º├úo entre livros preenchida, mas o status n├úo ├® "Migrado".',
+                'Reabrir',
+                () => editarProsa(dados.id),
+            );
+        }
+        if (dados.descarte.trim() && dados.status !== 'descartado') {
+            mostrarAvisoComAcao(
+                'Descarte preenchido, mas o status n├úo ├® "Descartado".',
+                'Reabrir',
+                () => editarProsa(dados.id),
+            );
+        }
+
         const anterior = idInput ? db.prosas.find((x) => x.id == id) : null;
         const mesmoEscopo =
             anterior &&
@@ -703,9 +1284,16 @@ export function initFormProsa() {
         save();
         rastreadorProsa.marcarLimpo();
         toggleModal('modal-prosa');
-        resetTagsProsa();
+        resetSinalizacoesProsa();
         resetPessoasProsa();
+        resetAutoriaProsa();
+        resetEnviosProsa();
+        resetReconhecimentosProsa();
         resetGeneroProsa();
+        resetIntertextualidadeProsa();
+        resetAnexosProsa();
+        resetElosProsa();
+        resetReferenciasProsa();
         form.reset();
     };
 }
@@ -715,8 +1303,16 @@ const MAPA_PROSA = {
     titulo: 'pr-titulo',
     texto: 'pr-texto',
     sequencia: ['pr-sequencia', 0],
+    idioma: ['pr-idioma', 'pt-BR'],
     notas: 'pr-notas',
     publicado: 'pr-pub',
+    anexosNotaGeral: 'pr-anexos-nota-geral',
+    contextoHistorico: 'pr-contexto',
+    ocultacao: 'pr-ocultacao',
+    conteudoSensivel: 'pr-sensivel',
+    vocabularioHiperacionante: 'pr-hiperacionante',
+    descarte: 'pr-descarte',
+    pendencia: 'pr-pendencia',
 };
 
 export async function editarProsa(id) {
@@ -729,6 +1325,48 @@ export async function editarProsa(id) {
     preencherDataParcial('pr-data-esc', pr.dataEscrita);
     preencherDataParcial('pr-data-pub', pr.dataPublicacao);
     document.getElementById('pr-data-esc-exata').checked = !!pr.dataEscrita?.exata;
+    preencherDataParcial('pr-epoca-ini', pr.epocaRetratada?.inicio);
+    preencherDataParcial('pr-epoca-fim', pr.epocaRetratada?.fim);
+    document.getElementById('pr-epoca-na').checked = !!pr.epocaRetratada?.na;
+    document.getElementById('pr-epoca-nome').value = nomeEpoca(pr.epocaRetratada, db.epocas);
+    document.getElementById('pr-epoca-recorte').value = pr.epocaRetratada?.recorte || '';
+    toggleCamposEpocaNaProsa();
+    document.getElementById('pr-intertexto-tipo').value = '';
+    document.getElementById('pr-intertexto-texto').value = '';
+    carregarIntertextualidadeProsa(pr.intertextualidade || []);
+    carregarElosProsa(pr.conceitos?.elos || []);
+    carregarReferenciasProsa(pr.conceitos?.referencias || []);
+    renderPainelElosDerivadosProsa(pr.id);
+    document.getElementById('pr-anexo-tipo').value = '';
+    document.getElementById('pr-anexo-link').value = '';
+    carregarAnexosProsa(pr.anexos || []);
+    document.getElementById('pr-cortado-livro').value = pr.cortadoDe?.livro || '';
+    document.getElementById('pr-cortado-secao').value = pr.cortadoDe?.secao || '';
+    document.getElementById('pr-lancado-livro').value = pr.lancadoEm?.livro || '';
+    document.getElementById('pr-lancado-secao').value = pr.lancadoEm?.secao || '';
+    document.getElementById('pr-migracao-justificativa').value = pr.justificativaMigracao || '';
+    // .value= direto n├úo dispara 'input' ÔÇö reaplica o filtro de Se├º├úo
+    // manualmente pros 4 campos que acabaram de ser preenchidos (mesmo
+    // motivo de atualizarFiltroSecoesMigracao em editarPoema).
+    atualizarFiltroSecoesMigracao();
+
+    // Status: campo novo (item 4) ÔÇö prosas existentes s├│ tinham o
+    // checkbox `publicado`, sem no├º├úo de Incompleto/Migrado/Descartado.
+    // Default inteligente no carregamento pra n├úo perder essa distin├º├úo
+    // ao reabrir uma prosa antiga pra editar ÔÇö sem migra├º├úo de dado (s├│
+    // afeta o que aparece pr├®-selecionado no `<select>`; se a pessoa n├úo
+    // mexer nele, o valor gravado no submit passa a ser expl├¡cito).
+    document.getElementById('pr-status').value = pr.status || (pr.publicado ? 'publicado' : 'completo');
+
+    const setM = (elId, vals) => {
+        const el = document.getElementById(elId);
+        if (!el) return;
+        const arr = Array.isArray(vals) ? vals.map(String) : [];
+        Array.from(el.options).forEach((opt) => {
+            opt.selected = arr.includes(String(opt.value));
+        });
+    };
+    setM('pr-livros', pr.livrosIds || []);
 
     const destinoStr =
         pr.paiTipo && pr.paiId
@@ -738,8 +1376,11 @@ export async function editarProsa(id) {
               : ''; // compatibilidade: prosas salvas antes da remo├º├úo do campo legado
     sincronizarFiltroDestino('pr-destino-filtro', 'pr-destino', destinoStr);
 
-    carregarTagsProsa(pr.sinalizacoes);
+    carregarSinalizacoesProsa(pr);
     carregarPessoasProsa(pr.pessoas);
+    carregarAutoriaProsa(pr.autoria);
+    carregarEnviosProsa(pr.envios);
+    carregarReconhecimentosProsa(pr.reconhecimentos);
     carregarGeneroProsa(pr.genero);
     atualizarDatalistProsa();
     renderColetaneasInfo('pr-coletaneas-info', 'prosa', pr.id);
diff --git a/js/main.js b/js/main.js
index 91dcccc..d304e85 100644
--- a/js/main.js
+++ b/js/main.js
@@ -15,6 +15,7 @@ import { mostrarAviso, avisarSalvo, debounce } from './utils.js';
 import { listarSnapshots, baixarSnapshot } from './autobackup.js';
 import {
     openTab,
+    abrirAba,
     toggleModal,
     prepararNovo,
     toggleCamposIntroducao,
@@ -23,9 +24,25 @@ import {
     filtrarDestinoProsa,
     autoPreencherDataPublicacao,
     togglePainel,
+    toggleMenuMobile,
+    initNav,
 } from './ui.js';
 import { registrarModal } from './modais.js';
-import { toggleColuna, moverColuna, selecionarTodasColunas, desmarcarTodasColunas } from './colunas.js';
+import {
+    toggleColuna,
+    moverColuna,
+    selecionarTodasColunas,
+    desmarcarTodasColunas,
+    resetarColunas,
+} from './colunas.js';
+import {
+    toggleAcaoColuna,
+    setFormatoBaixarColuna,
+    resetarAcoesColuna,
+    getFormatoBaixar,
+} from './acoes-coluna.js';
+import { exportarItem } from './exportar.js';
+import { abrirVisualizacao, baixarDoModalVisualizacao } from './visualizar.js';
 import { initTema, setTema } from './theme.js';
 import { renderLists } from './render.js';
 import {
@@ -39,6 +56,7 @@ import {
     getCombinadorBuscaProsas,
     setFiltroLivroPoemas,
     ordenarPoemasPor,
+    buscarPorPrefixo,
     setStatusPoemas,
     setItensPorPagina,
     setPaginaPoemas,
@@ -49,10 +67,12 @@ import {
     excluirSelecaoPoemas,
     exportarSelecaoPoemasJson,
     exportarSelecaoPoemasMarkdown,
+    exportarSelecaoPoemasPdf,
     aplicarPessoaEmMassa,
     removerPessoaEmMassa,
     aplicarSinalEmMassa,
     removerSinalEmMassa,
+    atualizarListaSinalBulk,
     aplicarDataEmMassa,
     limparDataEmMassa,
     toggleSelecaoProsa,
@@ -61,6 +81,7 @@ import {
     excluirSelecaoProsas,
     exportarSelecaoProsasJson,
     exportarSelecaoProsasMarkdown,
+    exportarSelecaoProsasPdf,
     aplicarPessoaEmMassaProsa,
     removerPessoaEmMassaProsa,
     aplicarSinalEmMassaProsa,
@@ -100,35 +121,94 @@ import {
     previsualizarExportacaoSeletiva,
     executarExportacaoSeletiva,
     executarExportacaoSeletivaMarkdown,
+    executarExportacaoSeletivaPdf,
     popularSelecaoExportacao,
     exportarTudoAninhado,
     exportarLivroCompleto,
     exportarLivrosCompletos,
     exportarTudoFlatJson,
     exportarTudoFlatMarkdown,
+    exportarTudoFlatPdf,
 } from './exportar.js';
 import { renderEstatisticas } from './estatisticas.js';
+import { renderConexoes, baixarDiagramaReferencias } from './render-conexoes.js';
 import {
     initEditor,
-    adicionarTag,
-    removerTag,
+    adicionarSinalEstilo,
+    removerSinalEstilo,
+    adicionarSinalTema,
+    removerSinalTema,
+    adicionarSinalRelacao,
+    removerSinalRelacao,
+    adicionarSinalSensibilidade,
+    removerSinalSensibilidade,
+    adicionarSinalTom,
+    removerSinalTom,
+    adicionarSinalOutros,
+    removerSinalOutros,
     applyStyle,
     wrapText,
     setAlign,
     adicionarPessoa,
     removerPessoa,
+    alternarPapelPessoa,
+    alternarDropdownPapelPessoa,
+    adicionarAutoria,
+    removerAutoria,
+    alterarPapelAutoria,
+    adicionarEnvio,
+    editarEnvio,
+    cancelarEdicaoEnvio,
+    removerEnvio,
+    adicionarReconhecimento,
+    editarReconhecimento,
+    cancelarEdicaoReconhecimento,
+    removerReconhecimento,
     atualizarDatalist,
     atualizarDatalistProsa,
-    adicionarTagProsa,
-    removerTagProsa,
+    adicionarSinalEstiloProsa,
+    removerSinalEstiloProsa,
+    adicionarSinalTemaProsa,
+    removerSinalTemaProsa,
+    adicionarSinalRelacaoProsa,
+    removerSinalRelacaoProsa,
+    adicionarSinalSensibilidadeProsa,
+    removerSinalSensibilidadeProsa,
+    adicionarSinalTomProsa,
+    removerSinalTomProsa,
+    adicionarSinalOutrosProsa,
+    removerSinalOutrosProsa,
     adicionarPessoaProsa,
     removerPessoaProsa,
+    alternarPapelPessoaProsa,
+    alternarDropdownPapelPessoaProsa,
+    adicionarAutoriaProsa,
+    removerAutoriaProsa,
+    alterarPapelAutoriaProsa,
+    adicionarEnvioProsa,
+    editarEnvioProsa,
+    cancelarEdicaoEnvioProsa,
+    removerEnvioProsa,
+    adicionarReconhecimentoProsa,
+    editarReconhecimentoProsa,
+    cancelarEdicaoReconhecimentoProsa,
+    removerReconhecimentoProsa,
     adicionarGeneroProsa,
     removerGeneroProsa,
     adicionarIntertexto,
     removerIntertexto,
     editarIntertexto,
     cancelarEdicaoIntertexto,
+    adicionarElo,
+    removerElo,
+    editarElo,
+    cancelarEdicaoElo,
+    onRelacaoEloAlterada,
+    selecionarDirecaoElo,
+    adicionarReferencia,
+    removerReferencia,
+    editarReferencia,
+    cancelarEdicaoReferencia,
     adicionarAnexo,
     removerAnexo,
     editarAnexo,
@@ -137,6 +217,24 @@ import {
     removerAnotacao,
     editarAnotacao,
     cancelarEdicaoAnotacao,
+    adicionarEloProsa,
+    removerEloProsa,
+    editarEloProsa,
+    cancelarEdicaoEloProsa,
+    onRelacaoEloAlteradaProsa,
+    selecionarDirecaoEloProsa,
+    adicionarReferenciaProsa,
+    removerReferenciaProsa,
+    editarReferenciaProsa,
+    cancelarEdicaoReferenciaProsa,
+    adicionarIntertextoProsa,
+    removerIntertextoProsa,
+    editarIntertextoProsa,
+    cancelarEdicaoIntertextoProsa,
+    adicionarAnexoProsa,
+    removerAnexoProsa,
+    editarAnexoProsa,
+    cancelarEdicaoAnexoProsa,
 } from './editor.js';
 import {
     initFormLivro,
@@ -153,6 +251,16 @@ import {
     editarProsa,
     initFormElemento,
     editarElemento,
+    initFormPessoa,
+    editarPessoa,
+    initFormGrupo,
+    editarGrupo,
+    initFormAutor,
+    editarAutor,
+    initFormEpoca,
+    editarEpoca,
+    initFormMesclar,
+    abrirModalMesclar,
     rastreadorPoema,
     rastreadorProsa,
 } from './forms.js';
@@ -193,8 +301,17 @@ registrarModal(
 );
 registrarModal('modal-prosa', 'modal-prosa.html', initFormProsa, rastreadorProsa);
 registrarModal('modal-elemento', 'modal-elemento.html', initFormElemento);
+registrarModal('modal-pessoa', 'modal-pessoa.html', initFormPessoa);
+registrarModal('modal-grupo', 'modal-grupo.html', initFormGrupo);
+registrarModal('modal-autor', 'modal-autor.html', initFormAutor);
+registrarModal('modal-epoca', 'modal-epoca.html', initFormEpoca);
+registrarModal('modal-mesclar', 'modal-mesclar.html', initFormMesclar);
 registrarModal('modal-col-parte', 'modal-col-parte.html', initFormColParte);
 registrarModal('modal-col-item', 'modal-col-item.html', initFormColItem);
+// Sem form/init pr├│prio ÔÇö o conte├║do ├® preenchido a cada abertura por
+// abrirVisualizacao() (visualizar.js), n├úo h├í nada pra inicializar uma
+// vez s├│ (ver coment├írio em registrarModal, acima).
+registrarModal('modal-visualizar', 'modal-visualizar.html', () => {});
 
 // ÔöÇÔöÇÔöÇ Listener delegado para as listas (render-listas.js) ÔöÇÔöÇÔöÇÔöÇÔöÇ
 // render-listas.js gera bot├Áes/checkboxes com data-action + data-id/
@@ -214,21 +331,30 @@ const ACOES_LISTA = {
     'editar-poema': (el) => editarPoema(Number(el.dataset.id)),
     'editar-prosa': (el) => editarProsa(Number(el.dataset.id)),
     'editar-elemento': (el) => editarElemento(Number(el.dataset.id)),
+    'editar-pessoa': (el) => editarPessoa(Number(el.dataset.id)),
+    'editar-grupo': (el) => editarGrupo(Number(el.dataset.id)),
+    'editar-autor': (el) => editarAutor(Number(el.dataset.id)),
+    'editar-epoca': (el) => editarEpoca(Number(el.dataset.id)),
+    'mesclar-item': (el) => abrirModalMesclar(el.dataset.tipo, Number(el.dataset.id)),
     'excluir-item': (el) => deleteItem(el.dataset.tipo, Number(el.dataset.id)),
+    'ver-item': (el) => abrirVisualizacao(el.dataset.tipo, Number(el.dataset.id)),
+    'baixar-item': (el) =>
+        exportarItem(el.dataset.tipo, Number(el.dataset.id), getFormatoBaixar(el.dataset.tabela)),
     'mover-livro': (el) => moverLivro(Number(el.dataset.id), el.dataset.dir),
     'pagina-poemas': (el) => setPaginaPoemas(Number(el.dataset.pagina)),
     'pagina-prosas': (el) => setPaginaProsas(Number(el.dataset.pagina)),
-    'toggle-poema': (el) => toggleSelecaoPoema(el.checked, Number(el.dataset.id)),
-    'toggle-prosa': (el) => toggleSelecaoProsa(el.checked, Number(el.dataset.id)),
+    'toggle-poema': (el, e) => toggleSelecaoPoema(el.checked, Number(el.dataset.id), e?.shiftKey),
+    'toggle-prosa': (el, e) => toggleSelecaoProsa(el.checked, Number(el.dataset.id), e?.shiftKey),
     'toggle-todos-poemas': (el) => toggleSelecaoTodosPoemas(el.checked),
     'toggle-todos-prosas': (el) => toggleSelecaoTodosProsas(el.checked),
+    'baixar-diagrama-referencias': (el) => baixarDiagramaReferencias(el),
 };
 
 document.querySelector('main')?.addEventListener('click', (e) => {
     const alvo = e.target.closest('[data-action]');
     if (!alvo) return;
     const acao = ACOES_LISTA[alvo.dataset.action];
-    if (acao) acao(alvo);
+    if (acao) acao(alvo, e);
 });
 
 // ÔöÇÔöÇÔöÇ Inicializa├º├úo ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
@@ -243,6 +369,7 @@ document.addEventListener('DOMContentLoaded', async () => {
     await migrarImagensLegadasParaIndexedDB();
 
     initTema();
+    initNav();
     renderColetaneas();
     renderLists();
     atualizarDatalist();
@@ -420,6 +547,8 @@ window.importarJSON = function (event) {
 // no escopo global (window). Com ES Modules isso ├® expl├¡cito.
 
 window.openTab = openTab;
+window.abrirAba = abrirAba;
+window.toggleMenuMobile = toggleMenuMobile;
 window.toggleModal = toggleModal;
 window.prepararNovo = prepararNovo;
 window.sugerirSequencia = sugerirSequencia;
@@ -432,20 +561,76 @@ window.autoPreencherDataPublicacao = autoPreencherDataPublicacao;
 // render-listas.js, que agora usa data-action + o listener delegado
 // acima. Continuam importados normalmente (uso local no ACOES_LISTA).
 
-window.adicionarTag = adicionarTag;
-window.removerTag = removerTag;
+window.adicionarSinalEstilo = adicionarSinalEstilo;
+window.removerSinalEstilo = removerSinalEstilo;
+window.adicionarSinalTema = adicionarSinalTema;
+window.removerSinalTema = removerSinalTema;
+window.adicionarSinalRelacao = adicionarSinalRelacao;
+window.removerSinalRelacao = removerSinalRelacao;
+window.adicionarSinalSensibilidade = adicionarSinalSensibilidade;
+window.removerSinalSensibilidade = removerSinalSensibilidade;
+window.adicionarSinalTom = adicionarSinalTom;
+window.removerSinalTom = removerSinalTom;
+window.adicionarSinalOutros = adicionarSinalOutros;
+window.removerSinalOutros = removerSinalOutros;
 window.adicionarPessoa = adicionarPessoa;
 window.removerPessoa = removerPessoa;
-window.adicionarTagProsa = adicionarTagProsa;
-window.removerTagProsa = removerTagProsa;
+window.alternarPapelPessoa = alternarPapelPessoa;
+window.alternarDropdownPapelPessoa = alternarDropdownPapelPessoa;
+window.adicionarAutoria = adicionarAutoria;
+window.removerAutoria = removerAutoria;
+window.alterarPapelAutoria = alterarPapelAutoria;
+window.adicionarEnvio = adicionarEnvio;
+window.editarEnvio = editarEnvio;
+window.cancelarEdicaoEnvio = cancelarEdicaoEnvio;
+window.removerEnvio = removerEnvio;
+window.adicionarReconhecimento = adicionarReconhecimento;
+window.editarReconhecimento = editarReconhecimento;
+window.cancelarEdicaoReconhecimento = cancelarEdicaoReconhecimento;
+window.removerReconhecimento = removerReconhecimento;
+window.adicionarSinalEstiloProsa = adicionarSinalEstiloProsa;
+window.removerSinalEstiloProsa = removerSinalEstiloProsa;
+window.adicionarSinalTemaProsa = adicionarSinalTemaProsa;
+window.removerSinalTemaProsa = removerSinalTemaProsa;
+window.adicionarSinalRelacaoProsa = adicionarSinalRelacaoProsa;
+window.removerSinalRelacaoProsa = removerSinalRelacaoProsa;
+window.adicionarSinalSensibilidadeProsa = adicionarSinalSensibilidadeProsa;
+window.removerSinalSensibilidadeProsa = removerSinalSensibilidadeProsa;
+window.adicionarSinalTomProsa = adicionarSinalTomProsa;
+window.removerSinalTomProsa = removerSinalTomProsa;
+window.adicionarSinalOutrosProsa = adicionarSinalOutrosProsa;
+window.removerSinalOutrosProsa = removerSinalOutrosProsa;
 window.adicionarPessoaProsa = adicionarPessoaProsa;
 window.removerPessoaProsa = removerPessoaProsa;
+window.alternarPapelPessoaProsa = alternarPapelPessoaProsa;
+window.alternarDropdownPapelPessoaProsa = alternarDropdownPapelPessoaProsa;
+window.adicionarAutoriaProsa = adicionarAutoriaProsa;
+window.removerAutoriaProsa = removerAutoriaProsa;
+window.alterarPapelAutoriaProsa = alterarPapelAutoriaProsa;
+window.adicionarEnvioProsa = adicionarEnvioProsa;
+window.editarEnvioProsa = editarEnvioProsa;
+window.cancelarEdicaoEnvioProsa = cancelarEdicaoEnvioProsa;
+window.removerEnvioProsa = removerEnvioProsa;
+window.adicionarReconhecimentoProsa = adicionarReconhecimentoProsa;
+window.editarReconhecimentoProsa = editarReconhecimentoProsa;
+window.cancelarEdicaoReconhecimentoProsa = cancelarEdicaoReconhecimentoProsa;
+window.removerReconhecimentoProsa = removerReconhecimentoProsa;
 window.adicionarGeneroProsa = adicionarGeneroProsa;
 window.removerGeneroProsa = removerGeneroProsa;
 window.adicionarIntertexto = adicionarIntertexto;
 window.removerIntertexto = removerIntertexto;
 window.editarIntertexto = editarIntertexto;
 window.cancelarEdicaoIntertexto = cancelarEdicaoIntertexto;
+window.adicionarElo = adicionarElo;
+window.removerElo = removerElo;
+window.editarElo = editarElo;
+window.cancelarEdicaoElo = cancelarEdicaoElo;
+window.onRelacaoEloAlterada = onRelacaoEloAlterada;
+window.selecionarDirecaoElo = selecionarDirecaoElo;
+window.adicionarReferencia = adicionarReferencia;
+window.removerReferencia = removerReferencia;
+window.editarReferencia = editarReferencia;
+window.cancelarEdicaoReferencia = cancelarEdicaoReferencia;
 window.adicionarAnexo = adicionarAnexo;
 window.removerAnexo = removerAnexo;
 window.editarAnexo = editarAnexo;
@@ -454,6 +639,24 @@ window.adicionarAnotacao = adicionarAnotacao;
 window.removerAnotacao = removerAnotacao;
 window.editarAnotacao = editarAnotacao;
 window.cancelarEdicaoAnotacao = cancelarEdicaoAnotacao;
+window.adicionarEloProsa = adicionarEloProsa;
+window.removerEloProsa = removerEloProsa;
+window.editarEloProsa = editarEloProsa;
+window.cancelarEdicaoEloProsa = cancelarEdicaoEloProsa;
+window.onRelacaoEloAlteradaProsa = onRelacaoEloAlteradaProsa;
+window.selecionarDirecaoEloProsa = selecionarDirecaoEloProsa;
+window.adicionarReferenciaProsa = adicionarReferenciaProsa;
+window.removerReferenciaProsa = removerReferenciaProsa;
+window.editarReferenciaProsa = editarReferenciaProsa;
+window.cancelarEdicaoReferenciaProsa = cancelarEdicaoReferenciaProsa;
+window.adicionarIntertextoProsa = adicionarIntertextoProsa;
+window.removerIntertextoProsa = removerIntertextoProsa;
+window.editarIntertextoProsa = editarIntertextoProsa;
+window.cancelarEdicaoIntertextoProsa = cancelarEdicaoIntertextoProsa;
+window.adicionarAnexoProsa = adicionarAnexoProsa;
+window.removerAnexoProsa = removerAnexoProsa;
+window.editarAnexoProsa = editarAnexoProsa;
+window.cancelarEdicaoAnexoProsa = cancelarEdicaoAnexoProsa;
 window.applyStyle = applyStyle;
 window.wrapText = wrapText;
 // Debounce de 200ms: cada tecla digitada dispara um renderPoemas()/
@@ -487,6 +690,7 @@ function atualizarBotaoCombinador(btn, valor) {
 window.setFiltroLivroProsa = setFiltroLivroProsa;
 window.setFiltroLivroPoemas = setFiltroLivroPoemas;
 window.ordenarPoemasPor = ordenarPoemasPor;
+window.buscarPorPrefixo = buscarPorPrefixo;
 window.setStatusPoemas = setStatusPoemas;
 window.setItensPorPagina = setItensPorPagina;
 window.togglePainel = togglePainel;
@@ -494,6 +698,11 @@ window.toggleColuna = toggleColuna;
 window.moverColuna = moverColuna;
 window.selecionarTodasColunas = selecionarTodasColunas;
 window.desmarcarTodasColunas = desmarcarTodasColunas;
+window.resetarColunas = resetarColunas;
+window.toggleAcaoColuna = toggleAcaoColuna;
+window.setFormatoBaixarColuna = setFormatoBaixarColuna;
+window.resetarAcoesColuna = resetarAcoesColuna;
+window.baixarDoModalVisualizacao = baixarDoModalVisualizacao;
 window.setTema = setTema;
 window.setFiltroDataEscritaPoemas = setFiltroDataEscritaPoemas;
 window.setFiltroDataPublicacaoPoemas = setFiltroDataPublicacaoPoemas;
@@ -516,10 +725,12 @@ window.limparSelecaoPoemas = limparSelecaoPoemas;
 window.excluirSelecaoPoemas = excluirSelecaoPoemas;
 window.exportarSelecaoPoemasJson = exportarSelecaoPoemasJson;
 window.exportarSelecaoPoemasMarkdown = exportarSelecaoPoemasMarkdown;
+window.exportarSelecaoPoemasPdf = exportarSelecaoPoemasPdf;
 window.aplicarPessoaEmMassa = aplicarPessoaEmMassa;
 window.removerPessoaEmMassa = removerPessoaEmMassa;
 window.aplicarSinalEmMassa = aplicarSinalEmMassa;
 window.removerSinalEmMassa = removerSinalEmMassa;
+window.atualizarListaSinalBulk = atualizarListaSinalBulk;
 window.aplicarDataEmMassa = aplicarDataEmMassa;
 window.limparDataEmMassa = limparDataEmMassa;
 // idem: toggleSelecaoTodosProsas segue em window pelo mesmo motivo.
@@ -528,6 +739,7 @@ window.limparSelecaoProsas = limparSelecaoProsas;
 window.excluirSelecaoProsas = excluirSelecaoProsas;
 window.exportarSelecaoProsasJson = exportarSelecaoProsasJson;
 window.exportarSelecaoProsasMarkdown = exportarSelecaoProsasMarkdown;
+window.exportarSelecaoProsasPdf = exportarSelecaoProsasPdf;
 window.aplicarPessoaEmMassaProsa = aplicarPessoaEmMassaProsa;
 window.removerPessoaEmMassaProsa = removerPessoaEmMassaProsa;
 window.aplicarSinalEmMassaProsa = aplicarSinalEmMassaProsa;
@@ -546,12 +758,15 @@ window.setFiltroLivroElementos = setFiltroLivroElementos;
 window.previsualizarExportacaoSeletiva = previsualizarExportacaoSeletiva;
 window.executarExportacaoSeletiva = executarExportacaoSeletiva;
 window.executarExportacaoSeletivaMarkdown = executarExportacaoSeletivaMarkdown;
+window.executarExportacaoSeletivaPdf = executarExportacaoSeletivaPdf;
 window.renderEstatisticas = renderEstatisticas;
+window.renderConexoes = renderConexoes;
 window.exportarTudoAninhado = exportarTudoAninhado;
 window.exportarLivroCompleto = exportarLivroCompleto;
 window.exportarLivrosCompletos = exportarLivrosCompletos;
 window.exportarTudoFlatJson = exportarTudoFlatJson;
 window.exportarTudoFlatMarkdown = exportarTudoFlatMarkdown;
+window.exportarTudoFlatPdf = exportarTudoFlatPdf;
 
 window.toggleSelecaoEstrutura = toggleSelecaoEstrutura;
 window.marcarTodosEstrutura = marcarTodosEstrutura;
diff --git a/js/render-listas.js b/js/render-listas.js
index a3d419b..925225b 100644
--- a/js/render-listas.js
+++ b/js/render-listas.js
@@ -8,7 +8,16 @@
 // via preencherCapas).
 // ============================================================
 
-import { db, save, deleteItemsEmMassa } from './db.js';
+import {
+    db,
+    save,
+    deleteItemsEmMassa,
+    calcularImpactoExclusaoPessoa,
+    calcularImpactoExclusaoGrupo,
+    calcularImpactoExclusaoAutor,
+    calcularImpactoExclusaoEpoca,
+    obterOuCriarPessoaPorNome,
+} from './db.js';
 import {
     getElementHierarchy,
     getPosicaoElemento,
@@ -16,6 +25,8 @@ import {
     filtrarPorConteudo,
     formatarDataParcial,
     formatarIntervaloEpocaRetratada,
+    nomeEpoca,
+    ROTULOS_RECORTE_EPOCA,
     escapeHtml,
     sanitizarTextoRico,
     abrirModalConfirmacao,
@@ -25,10 +36,23 @@ import {
     parseFiltroDataRapido,
     itemBateFiltroEpoca,
     itemFaltaEpocaParaFiltro,
+    sinalizacoesCombinadas,
+    SINALIZACOES_CATEGORIAS,
+    PREFIXOS_CANONICOS_POR_CAMPO,
+    rotuloElo,
+    nomesPessoas,
+    iniciaisPapeisPessoa,
+    paresGrupoPessoa,
+    classesCorGrupo,
+    pontoCorGrupo,
+    paresAutoria,
+    estaPublicado,
 } from './utils.js';
 import { preencherCapas } from './render-lightbox.js';
 import { DEFINICAO_COLUNAS, getColunasAtivas, renderSeletorColunas } from './colunas.js';
-import { exportarSelecaoJson, exportarSelecaoMarkdown } from './exportar.js';
+import { getAcoesAtivas, renderSeletorAcoes } from './acoes-coluna.js';
+import { exportarSelecaoJson, exportarSelecaoMarkdown, exportarSelecaoPdf } from './exportar.js';
+import { contarCamposPreenchidos, TOTAL_CAMPOS_CONSIDERADOS } from './exportar-md.js';
 
 // Sempre que uma coluna ├® ligada/desligada (ver colunas.js) a tabela
 // correspondente precisa recalcular cabe├ºalho + linhas.
@@ -37,11 +61,55 @@ window.addEventListener('colunas:alteradas', (ev) => {
     if (ev.detail?.tabela === 'prosas') renderProsas();
 });
 
+// O mesmo vale pra coluna A├º├Áes (ver acoes-coluna.js) ÔÇö trocar quais
+// bot├Áes aparecem, ou o formato do Baixar, tamb├®m exige recalcular a
+// linha inteira (a c├®lula de A├º├Áes ├® montada junto no template).
+window.addEventListener('acoes-coluna:alteradas', (ev) => {
+    if (ev.detail?.tabela === 'poemas') renderPoemas();
+    if (ev.detail?.tabela === 'prosas') renderProsas();
+});
+
 // ├ìcones dos bot├Áes Editar/Excluir dos cards e tabelas abaixo. Ficam como
 // string pronta (em vez de gerar via DOM) porque entram direto nas
 // template strings dos cards, junto com o resto do HTML.
 const ICONE_EDITAR = `<svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round" class="w-3.5 h-3.5 inline-block" aria-hidden="true"><path d="M12.5 3.5l4 4L6.5 17.5H2.5v-4L12.5 3.5Z"/><path d="M10.5 5.5l4 4"/></svg>`;
 const ICONE_EXCLUIR = `<svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round" class="w-3.5 h-3.5 inline-block" aria-hidden="true"><path d="M4 6h12"/><path d="M8 6V4.5a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1V6"/><path d="M5.5 6l.6 9.5a1.5 1.5 0 0 0 1.5 1.4h4.8a1.5 1.5 0 0 0 1.5-1.4L14.5 6"/><path d="M8.5 9v5"/><path d="M11.5 9v5"/></svg>`;
+// Duas linhas convergindo num ponto s├│ ÔÇö mesmo esp├¡rito visual de
+// "mesclar" em apps de versionamento (git merge), usado no bot├úo de
+// Mesclar de Pessoas/├ëpocas (ver renderPessoas/renderEpocas abaixo).
+const ICONE_MESCLAR = `<svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round" class="w-3.5 h-3.5 inline-block" aria-hidden="true"><path d="M4 3v5a4 4 0 0 0 4 4h0"/><path d="M16 3v5a4 4 0 0 1-4 4h0"/><path d="M10 12v5"/><path d="M7.5 14.5l2.5 2.5 2.5-2.5"/></svg>`;
+// Ver = olho; Baixar = seta pra baixo com bandeja ÔÇö s├│ usados nas linhas
+// de Poemas/Prosas (ver acoes-coluna.js), por isso ficam ao lado dos
+// outros dois em vez de nos outros CELULAS_* deste arquivo.
+const ICONE_VER = `<svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round" class="w-3.5 h-3.5 inline-block" aria-hidden="true"><path d="M1.5 10S4.5 4 10 4s8.5 6 8.5 6-3 6-8.5 6-8.5-6-8.5-6Z"/><circle cx="10" cy="10" r="2.25"/></svg>`;
+const ICONE_BAIXAR = `<svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round" class="w-3.5 h-3.5 inline-block" aria-hidden="true"><path d="M10 3v9.5"/><path d="M6 9l4 4 4-4"/><path d="M3.5 15.5h13"/></svg>`;
+
+// Bot├Áes da coluna A├º├Áes de Poemas/Prosas (Ver/Baixar/Editar/Excluir,
+// ver acoes-coluna.js), montados conforme a configura├º├úo salva de cada
+// tabela ÔÇö s├│ entram os habilitados, na ordem fixa de DEFINICAO_ACOES.
+// `tipo` ├® 'poema'/'prosa' (usado por ver-item/baixar-item em
+// main.js e por deleteItem, que espera 'poemas'/'prosas').
+function celulaAcoesItem(tabela, tipo, tipoPlural, id) {
+    const ativas = new Set(getAcoesAtivas(tabela));
+    const botoes = [];
+    if (ativas.has('ver'))
+        botoes.push(
+            `<button data-action="ver-item" data-tipo="${tipo}" data-id="${id}" title="Ver" aria-label="Ver" class="inline-flex items-center justify-center p-1.5 rounded text-gray-500 dark:text-slate-400 hover:bg-gray-100 dark:hover:bg-slate-700">${ICONE_VER}</button>`,
+        );
+    if (ativas.has('baixar'))
+        botoes.push(
+            `<button data-action="baixar-item" data-tabela="${tabela}" data-tipo="${tipo}" data-id="${id}" title="Baixar" aria-label="Baixar" class="inline-flex items-center justify-center p-1.5 rounded text-gray-500 dark:text-slate-400 hover:bg-gray-100 dark:hover:bg-slate-700">${ICONE_BAIXAR}</button>`,
+        );
+    if (ativas.has('editar'))
+        botoes.push(
+            `<button data-action="editar-${tipo}" data-id="${id}" title="Editar" aria-label="Editar" class="inline-flex items-center justify-center bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 p-1.5 rounded hover:bg-blue-200 dark:hover:bg-blue-800">${ICONE_EDITAR}</button>`,
+        );
+    if (ativas.has('excluir'))
+        botoes.push(
+            `<button data-action="excluir-item" data-tipo="${tipoPlural}" data-id="${id}" title="Excluir" aria-label="Excluir" class="inline-flex items-center justify-center p-1.5 rounded text-red-400 hover:text-red-600 dark:hover:text-red-400 hover:bg-red-100 dark:hover:bg-red-900/40">${ICONE_EXCLUIR}</button>`,
+        );
+    return botoes.join('');
+}
 
 let filtroPoemas = '';
 let filtroProsas = '';
@@ -69,6 +137,13 @@ let ordenacaoPoemas = { campo: 'estrutura', direcao: 'asc' };
 let statusPoemas = 'todos';
 let selecaoPoemas = new Set();
 let selecaoProsas = new Set();
+// ├éncora do ├║ltimo checkbox marcado/desmarcado em cada aba ÔÇö usada pro
+// shift-click estender a sele├º├úo pro intervalo entre ele e o anterior
+// (ver toggleSelecaoPoema/toggleSelecaoProsa). null = nenhum clique
+// ainda nesta sess├úo, ou o ├║ltimo clique n├úo fez parte do intervalo
+// vis├¡vel atual (filtro/ordena├º├úo mudou no meio do caminho).
+let ultimoCheckPoema = null;
+let ultimoCheckProsa = null;
 let filtroLivroPartes = '';
 let filtroLivroSecoes = '';
 let filtroParteSecoes = '';
@@ -229,6 +304,26 @@ export function setFiltroProsas(valor) {
     renderProsas();
 }
 
+// Atalho de clique no cabe├ºalho da coluna (ver thOrdenavel/thComLupa):
+// joga o prefixo "campo:" correspondente pronto no campo de busca de
+// metadados (de Poemas ou de Prosas, conforme `tabela`) e foca nele ÔÇö
+// pra quem n├úo lembra a sintaxe de prefixo n├úo precisar decorar nada, s├│
+// clicar na coluna que j├í est├í olhando. Se j├í houver algo digitado, o
+// prefixo ├® acrescentado ao final (separado por espa├ºo) em vez de
+// substituir, pra permitir combinar com outros termos.
+export function buscarPorPrefixo(tabela, campoItem) {
+    const prefixo = PREFIXOS_CANONICOS_POR_CAMPO[campoItem];
+    if (!prefixo) return;
+    const input = document.getElementById(`busca-${tabela}`);
+    if (!input) return;
+    const atual = input.value.trim();
+    const novo = atual ? `${atual} ${prefixo}:` : `${prefixo}:`;
+    input.value = novo;
+    (tabela === 'prosas' ? setFiltroProsas : setFiltroPoemas)(novo);
+    input.focus();
+    input.setSelectionRange(novo.length, novo.length);
+}
+
 export function setFiltroConteudoPoemas(valor) {
     filtroConteudoPoemas = valor;
     paginaPoemas = 1;
@@ -523,11 +618,16 @@ function decorarCamposBusca(item, extraLivros = '') {
             ? item.intertextualidade.map((it) => `${it.tipo || ''} ${it.texto || ''}`).join(' ')
             : '',
         _buscaAnexos: Array.isArray(item.anexos)
-            ? item.anexos.map((it) => `${it.tipo || ''} ${it.texto || ''} ${it.link || ''}`).join(' ')
+            ? item.anexos
+                  .map((it) => `${it.tipo || ''} ${it.texto || ''} ${it.link || ''}`)
+                  .join(' ')
             : '',
         _buscaAnotacoes: Array.isArray(item.anotacoesMarginais)
             ? item.anotacoesMarginais
-                  .map((it) => `${it.trecho || ''} ${it.posicao || ''} ${it.fonte || ''} ${it.texto || ''}`)
+                  .map(
+                      (it) =>
+                          `${it.trecho || ''} ${it.posicao || ''} ${it.fonte || ''} ${it.texto || ''}`,
+                  )
                   .join(' ')
             : '',
         _buscaCortadoDe: item.cortadoDe
@@ -536,6 +636,103 @@ function decorarCamposBusca(item, extraLivros = '') {
         _buscaLancadoEm: item.lancadoEm
             ? `${item.lancadoEm.livro || ''} ${item.lancadoEm.secao || ''}`.trim()
             : '',
+        // Elos/Refer├¬ncias apontam pra "outro texto do acervo (poema ou
+        // prosa)" ÔÇö ids s├úo gerados por um contador global ├║nico
+        // (gerarId()), ent├úo um id de poema nunca colide com um id de
+        // prosa; resolver o t├¡tulo ├® s├│ checar os dois arrays (ver
+        // resolverTituloPoemaOuProsa acima).
+        _buscaElos: Array.isArray(item.conceitos?.elos)
+            ? item.conceitos.elos
+                  .map(
+                      (it) =>
+                          `${rotuloElo(it.relacao, it.direcao)} ${it.relacao || ''} ${it.texto || ''} ${resolverTituloPoemaOuProsa(it.id)}`,
+                  )
+                  .join(' ')
+            : '',
+        _buscaReferencias: Array.isArray(item.conceitos?.referencias)
+            ? item.conceitos.referencias
+                  .map(
+                      (it) =>
+                          `${it.tipo || ''} ${it.texto || ''} ${resolverTituloPoemaOuProsa(it.id)}`,
+                  )
+                  .join(' ')
+            : '',
+        // Sinaliza├º├Áes combinadas das 5 categorias ÔÇö s├│ pra busca geral
+        // "etiqueta:"; quem quer restringir por categoria usa
+        // estilo:/tema:/relacao:/sensibilidade:/tom: direto (ver
+        // CAMPOS_ATRIBUTO em utils.js).
+        _buscaSinalizacoes: sinalizacoesCombinadas(item),
+        // pessoas ├® array de objeto {pessoaId, papeis} (nome mora no
+        // cadastro central db.pessoas desde migrarPessoasParaCadastro ÔÇö
+        // ver db.js; papeis: array, desde o multi-select, ver
+        // migrarPapeisPessoa) ÔÇö nome e pap├®is entram na busca geral e no
+        // prefixo "pessoa:" (ver CAMPOS_ATRIBUTO em utils.js), assim
+        // "pessoa:dedicat├íria" tamb├®m acha algu├®m por qualquer um dos
+        // pap├®is marcados, n├úo s├│ pelo nome.
+        _buscaPessoas: Array.isArray(item.pessoas)
+            ? item.pessoas
+                  .map((p) => {
+                      const nome = db.pessoas.find((x) => x.id == p.pessoaId)?.nome || '';
+                      return `${nome} ${(p.papeis || []).join(' ')}`;
+                  })
+                  .join(' ')
+            : '',
+        // S├│ os pap├®is (sem nome) ÔÇö pro prefixo "papel:" (ver
+        // CAMPOS_ATRIBUTO em utils.js), pra quem quer restringir por
+        // papel especificamente, sem risco de um termo bater s├│ porque ├®
+        // parecido com o nome de algu├®m marcado no texto.
+        _buscaPapeis: Array.isArray(item.pessoas)
+            ? item.pessoas.flatMap((p) => p.papeis || []).join(' ')
+            : '',
+        // Grupo ├® caracter├¡stica da Pessoa (constante entre poemas), n├úo
+        // do v├¡nculo poemaÔåöpessoa ÔÇö ver coment├írio de nomesGrupos/
+        // paresGrupoPessoa em utils.js. Resolvido aqui pro prefixo
+        // "grupo:", que acha o texto pelo grupo de algu├®m mencionado
+        // (ex.: "grupo:fam├¡lia"), mesmo sem citar o nome da pessoa.
+        _buscaGrupos: [
+            ...new Set(paresGrupoPessoa(item, db.pessoas, db.grupos).map((par) => par.grupo.nome)),
+        ].join(' '),
+        // autoria ├® array {autorId, papel} (nome mora no cadastro
+        // central db.autores ÔÇö ver migrarAutoria em db.js); nome e papel
+        // entram na busca geral e no prefixo "autor:" (ver
+        // CAMPOS_ATRIBUTO em utils.js), mesmo padr├úo de _buscaPessoas.
+        _buscaAutoria: paresAutoria(item, db.autores)
+            .map(({ autor, papel }) => `${autor.nome} ${papel || ''}`)
+            .join(' '),
+        // epocaRetratada guarda s├│ epocaId (nome mora no cadastro central
+        // db.epocas ÔÇö ver migrarEpocas em db.js); nome e recorte entram na
+        // busca geral e no prefixo "epoca:" (ver CAMPOS_ATRIBUTO em
+        // utils.js), mesmo padr├úo de _buscaAutoria acima.
+        _buscaEpoca: item.epocaRetratada
+            ? [
+                  nomeEpoca(item.epocaRetratada, db.epocas),
+                  ROTULOS_RECORTE_EPOCA[item.epocaRetratada.recorte] || '',
+              ]
+                  .filter(Boolean)
+                  .join(' ')
+            : '',
+        // envios ├® array {pessoa, data, meio, reacao, notas} ÔÇö pessoa/meio
+        // s├úo texto livre (n├úo v├¡nculo por id), entram na busca geral e
+        // no prefixo "envio:" junto de rea├º├úo/notas (ver CAMPOS_ATRIBUTO
+        // em utils.js), mesmo padr├úo de _buscaAnotacoes acima.
+        _buscaEnvios: Array.isArray(item.envios)
+            ? item.envios
+                  .map(
+                      (e) => `${e.pessoa || ''} ${e.meio || ''} ${e.reacao || ''} ${e.notas || ''}`,
+                  )
+                  .join(' ')
+            : '',
+        // reconhecimentos ├® array {premio, posicao, ano, texto} ÔÇö premio ├®
+        // texto livre (n├úo v├¡nculo por id), entram na busca geral e no
+        // prefixo "reconhecimento:"/"reconhecimentos:" (ver CAMPOS_ATRIBUTO
+        // em utils.js), mesmo padr├úo de _buscaEnvios acima.
+        _buscaReconhecimentos: Array.isArray(item.reconhecimentos)
+            ? item.reconhecimentos
+                  .map(
+                      (r) => `${r.premio || ''} ${r.posicao || ''} ${r.ano || ''} ${r.texto || ''}`,
+                  )
+                  .join(' ')
+            : '',
     };
 }
 
@@ -561,12 +758,178 @@ function badgesEtiquetas(
     );
 }
 
-// T├¡tulos dos poemas referenciados por uma lista de IDs (Elos/Refer├¬ncias)
-function titulosPoemasPorId(ids) {
-    if (!ids || !ids.length) return '<span class="text-gray-300 dark:text-slate-600">ÔÇö</span>';
-    const titulos = ids.map((id) => db.poemas.find((p) => p.id == id)?.titulo).filter(Boolean);
-    if (!titulos.length) return '<span class="text-gray-300 dark:text-slate-600">ÔÇö</span>';
-    return titulos.map((t) => escapeHtml(t)).join(', ');
+// Coluna de Pessoas: pessoas ├® array de objeto {pessoaId, papeis} ÔÇö
+// nome vem do cadastro central db.pessoas (ver migrarPessoasParaCadastro
+// em db.js); papeis ├® array, desde o multi-select (ver migrarPapeisPessoa)
+// ÔÇö cada chip mostra o nome e, quando h├í pap├®is marcados, as iniciais
+// deles (R/I/D/M/A ÔÇö ver iniciaisPapeisPessoa em utils.js) separadas por
+// "┬À", na ordem em que foram marcados no editor (n├úo ├® hierarquia fixa
+// por categoria ÔÇö ver alternarPapel em editor.js). Iniciais em vez do
+// nome por extenso pra caber na coluna sem poluir; passar o mouse por
+// cima do chip mostra "Nome (Papel1, Papel2)" por extenso via `title`
+// (mesmo padr├úo da bolinha de status/pend├¬ncia, ver DEFINICAO_COLUNAS
+// mais abaixo). Sem papel marcado, o title mostra s├│ o nome ÔÇö n├úo faz
+// sentido escrever "(sem papel)" ali. Exporta├º├úo pra MD tamb├®m mant├®m os
+// pap├®is por extenso (ver exportar-md.js). pessoaId sem correspond├¬ncia
+// no cadastro (n├úo deveria acontecer) n├úo gera chip, em vez de mostrar
+// "undefined".
+function badgesPessoas(pessoas) {
+    if (!Array.isArray(pessoas) || !pessoas.length)
+        return '<span class="text-gray-300 dark:text-slate-600">ÔÇö</span>';
+    return pessoas
+        .map((p) => {
+            const nome = db.pessoas.find((x) => x.id == p.pessoaId)?.nome;
+            if (!nome) return '';
+            const papeis = Array.isArray(p.papeis) ? p.papeis.filter(Boolean) : [];
+            const iniciais = escapeHtml(iniciaisPapeisPessoa(papeis));
+            const title = papeis.length ? `${nome} (${papeis.join(', ')})` : nome;
+            return `<span title="${escapeHtml(title)}" class="text-[9px] bg-rose-100 dark:bg-rose-900 text-rose-600 dark:text-rose-400 px-1.5 py-0.5 rounded mr-1 mb-1 inline-block">${escapeHtml(nome)}${iniciais ? ` <span class="opacity-70">${iniciais}</span>` : ''}</span>`;
+        })
+        .join('');
+}
+
+// Coluna de Grupos: um badge por par (Grupo, Pessoa) ÔÇö ver paresGrupoPessoa
+// em utils.js (mesma resolu├º├úo usada no painel do modal ÔÇö ver
+// renderPainelGruposDoChip em editor.js ÔÇö e na exporta├º├úo em Markdown ÔÇö
+// ver exportar-md.js). Cada badge usa a cor pr├│pria daquele grupo
+// (classesCorGrupo) e mostra "Grupo (Pessoa)" pra n├úo perder de quem ├®
+// o v├¡nculo quando o item tem mais de uma pessoa em grupos diferentes.
+function badgesGrupos(item) {
+    const pares = paresGrupoPessoa(item, db.pessoas, db.grupos);
+    if (!pares.length) return '<span class="text-gray-300 dark:text-slate-600">ÔÇö</span>';
+    return pares
+        .map(
+            ({ grupo, pessoa }) =>
+                `<span class="text-[9px] ${classesCorGrupo(grupo.cor)} px-1.5 py-0.5 rounded mr-1 mb-1 inline-block">${escapeHtml(grupo.nome)} <span class="opacity-70">(${escapeHtml(pessoa.nome)})</span></span>`,
+        )
+        .join('');
+}
+
+// Coluna de Autoria: um badge por par (Autor, papel) ÔÇö ver paresAutoria
+// em utils.js. Diferente de badgesPessoas, papel aqui ├® sempre ├║nico e
+// sempre marcado (todo item.autoria vem preenchido pela migra├º├úo ÔÇö ver
+// migrarAutoria em db.js), ent├úo o badge sempre mostra "Nome (Papel)"
+// por extenso ÔÇö n├úo precisa reduzir a iniciais como em Pessoas, j├í que
+// n├úo acumula mais de um papel por autor no mesmo texto.
+function badgesAutoria(item) {
+    const pares = paresAutoria(item, db.autores);
+    if (!pares.length) return '<span class="text-gray-300 dark:text-slate-600">ÔÇö</span>';
+    return pares
+        .map(
+            ({ autor, papel }) =>
+                `<span class="text-[9px] bg-indigo-100 dark:bg-indigo-900 text-indigo-600 dark:text-indigo-400 px-1.5 py-0.5 rounded mr-1 mb-1 inline-block">${escapeHtml(autor.nome)} <span class="opacity-70">(${escapeHtml(papel)})</span></span>`,
+        )
+        .join('');
+}
+
+// Coluna de Envios: um badge por envio, "pessoa ┬À data" no chip (com
+// meio no title, hover) ÔÇö igual esp├¡rito de badgesAutoria, mas sem
+// cadastro central por tr├ís (pessoa/meio s├úo texto livre ÔÇö ver
+// coment├írio em criarListaDeEntradas/utils.js). A rea├º├úo em si n├úo
+// cabe no chip (pode ser longa); fica s├│ no modal/exporta├º├úo ÔÇö aqui ├®
+// s├│ "pra quem e quando", suficiente pra escanear a tabela.
+function badgesEnvios(item) {
+    if (!Array.isArray(item.envios) || !item.envios.length) {
+        return '<span class="text-gray-300 dark:text-slate-600">ÔÇö</span>';
+    }
+    return item.envios
+        .map((e) => {
+            const rotulo = [e.pessoa, formatarDataParcial(e.data)]
+                .filter((v) => v && v !== 'ÔÇö')
+                .map(escapeHtml)
+                .join(' ┬À ');
+            const title = e.meio ? ` title="via ${escapeHtml(e.meio)}"` : '';
+            return `<span class="text-[9px] bg-teal-100 dark:bg-teal-900 text-teal-700 dark:text-teal-300 px-1.5 py-0.5 rounded mr-1 mb-1 inline-block"${title}>${rotulo || '(sem dados)'}</span>`;
+        })
+        .join('');
+}
+
+// Coluna de Reconhecimentos: um badge por pr├¬mio/men├º├úo, "pr├¬mio ┬À
+// posi├º├úo ┬À ano" no chip ÔÇö mesmo esp├¡rito de badgesEnvios (sem
+// cadastro central, premio/posicao s├úo texto livre). O texto/nota em
+// si n├úo cabe no chip; fica s├│ no modal/exporta├º├úo.
+function badgesReconhecimentos(item) {
+    if (!Array.isArray(item.reconhecimentos) || !item.reconhecimentos.length) {
+        return '<span class="text-gray-300 dark:text-slate-600">ÔÇö</span>';
+    }
+    return item.reconhecimentos
+        .map((r) => {
+            const rotulo = [r.premio, r.posicao, r.ano]
+                .filter((v) => v || v === 0)
+                .map((v) => escapeHtml(String(v)))
+                .join(' ┬À ');
+            return `<span class="text-[9px] bg-amber-100 dark:bg-amber-900 text-amber-700 dark:text-amber-300 px-1.5 py-0.5 rounded mr-1 mb-1 inline-block">${rotulo || '(sem dados)'}</span>`;
+        })
+        .join('');
+}
+
+// Badge do nome de ├ëpoca na coluna ├ëpoca Retratada (Poemas e Prosas ÔÇö
+// mesmo helper pras duas tabelas, ver CELULAS_POEMAS/CELULAS_PROSAS
+// abaixo). Pedido do Victor: o "e p├│s" do recorte "repercuss├úo" (ver
+// ROTULOS_RECORTE_EPOCA em utils.js) precisa aparecer direto no badge,
+// n├úo s├│ na exporta├º├úo ÔÇö mesmo texto que formatarEpocaRetratada j├í
+// usa ("Nome e p├│s"). O Contexto do relacionamento (`contextoRelacao`,
+// campo do cadastro central db.epocas ÔÇö ver modal-epoca.html) n├úo cabe
+// no espa├ºo do badge, ent├úo entra como `title` (hover), mesmo padr├úo
+// j├í usado em badgesPessoas/badgesEnvios pra informa├º├úo secund├íria que
+// n├úo precisa estar sempre vis├¡vel.
+function badgeEpocaRetratada(epoca) {
+    const nome = nomeEpoca(epoca, db.epocas);
+    if (!nome) return '';
+    const posRepercussao = epoca?.recorte === 'repercuss├úo' ? ' e p├│s' : '';
+    const contexto = epoca?.epocaId
+        ? db.epocas.find((e) => e.id == epoca.epocaId)?.contextoRelacao
+        : '';
+    const title = contexto ? ` title="${escapeHtml(contexto)}"` : '';
+    return `<span class="inline-block px-1.5 py-0.5 mr-1 rounded bg-teal-100 dark:bg-teal-900 text-teal-700 dark:text-teal-300 text-[10px] font-bold align-middle"${title}>${escapeHtml(nome)}${posRepercussao}</span>`;
+}
+
+// Resolve o t├¡tulo de um Elo/Refer├¬ncia-alvo, que pode ser um poema OU
+// uma prosa ÔÇö ids s├úo gerados por um contador global ├║nico (gerarId()
+// em utils.js), ent├úo nunca colidem entre os dois arrays; basta checar
+// os dois. Usado por titulosPoemasPorId/textoTitulosPoemasPorId
+// (tabela) e por _buscaElos/_buscaReferencias (decorarCamposBusca).
+function resolverTituloPoemaOuProsa(id) {
+    return db.poemas.find((p) => p.id == id)?.titulo || db.prosas.find((pr) => pr.id == id)?.titulo;
+}
+
+// T├¡tulos dos poemas referenciados por uma lista de Elos/Refer├¬ncias.
+// Elos guarda { id, relacao, direcao, texto } (ver migrarElosParaRelacaoDirecao
+// em db.js ÔÇö redesenho Rela├º├úo+Dire├º├úo); Refer├¬ncias guarda { id, tipo, texto }
+// (schema mais simples, n├úo mudou). `resolverRotulo` isola essa diferen├ºa:
+// cada chamador passa a fun├º├úo certa pra extrair o r├│tulo de exibi├º├úo de
+// uma entrada. O r├│tulo vira uma badge (mesmo padr├úo de Intertextualidade/
+// Anexos logo abaixo), uma linha por v├¡nculo ÔÇö assim o tipo da rela├º├úo n├úo
+// se confunde com o t├¡tulo do poema s├│ de bater o olho na coluna.
+// `corClasse` deixa Elos e Refer├¬ncias com uma cor de badge pr├│pria cada.
+function titulosPoemasPorId(
+    lista,
+    resolverRotulo,
+    corClasse = 'bg-cyan-100 dark:bg-cyan-900 text-cyan-700 dark:text-cyan-300',
+) {
+    if (!lista || !lista.length) return '<span class="text-gray-300 dark:text-slate-600">ÔÇö</span>';
+    const linhas = lista
+        .map((entrada) => {
+            const titulo = resolverTituloPoemaOuProsa(entrada.id);
+            if (!titulo) return null;
+            const rotulo = resolverRotulo(entrada);
+            const badge = rotulo
+                ? `<span class="inline-block px-1.5 py-0.5 mr-1 rounded ${corClasse} text-[10px] font-bold uppercase align-middle">${escapeHtml(rotulo)}</span>`
+                : '';
+            return `<div>${badge}${escapeHtml(titulo)}</div>`;
+        })
+        .filter(Boolean);
+    if (!linhas.length) return '<span class="text-gray-300 dark:text-slate-600">ÔÇö</span>';
+    return linhas.join('');
+}
+
+// Resolvedores de r├│tulo pra cada natureza de entrada ÔÇö ver
+// titulosPoemasPorId acima e textoTitulosPoemasPorId abaixo.
+function rotuloEntradaElo(entrada) {
+    return entrada.relacao ? rotuloElo(entrada.relacao, entrada.direcao) : '';
+}
+function rotuloEntradaReferencia(entrada) {
+    return entrada.tipo || '';
 }
 
 function trechoNota(notas) {
@@ -576,6 +939,23 @@ function trechoNota(notas) {
     return `<span title="${escapeHtml(limpo)}">${escapeHtml(trecho)}</span>`;
 }
 
+// C├®lula da coluna "Campos Preenchidos" (ver contarCamposPreenchidos em
+// exportar-md.js) ÔÇö compartilhada entre Poemas e Prosas. Mostra "N/TOTAL"
+// mais uma barrinha de preenchimento, pra bater o olho e comparar a
+// riqueza/complexidade estrutural entre os textos sem abrir cada um.
+function celulaCamposPreenchidos(item) {
+    const preenchidos = contarCamposPreenchidos(item);
+    const proporcao = Math.round((preenchidos / TOTAL_CAMPOS_CONSIDERADOS) * 100);
+    return `<td class="p-4 text-xs text-gray-500 dark:text-slate-400" title="${preenchidos} de ${TOTAL_CAMPOS_CONSIDERADOS} campos preenchidos">
+        <div class="flex items-center gap-2">
+            <span class="font-mono">${preenchidos}/${TOTAL_CAMPOS_CONSIDERADOS}</span>
+            <span class="w-10 h-1.5 rounded-full bg-gray-200 dark:bg-slate-700 overflow-hidden">
+                <span class="block h-full bg-indigo-400 dark:bg-indigo-500" style="width: ${proporcao}%"></span>
+            </span>
+        </div>
+    </td>`;
+}
+
 // Monta o <thead> de Poemas ou Prosas de acordo com as colunas ativas.
 // `celulaCheck`/`celulaTitulo`/`celulaAcoes` s├úo o HTML fixo de in├¡cio/fim
 // (checkbox, t├¡tulo e A├º├Áes), que n├úo passam pelo seletor de colunas.
@@ -628,50 +1008,163 @@ function iconeOrdenacao(ativo, direcao) {
     return `<span class="inline-block w-3 text-blue-600 dark:text-blue-400">${direcao === 'asc' ? 'Ôû▓' : 'Ôû╝'}</span>`;
 }
 
+// Coluna ÔåÆ campo do item decorado usado pela busca (ver decorarCamposBusca
+// acima e CAMPOS_ATRIBUTO em utils.js), uma por tabela. S├│ entram aqui as
+// colunas que t├¬m um prefixo "campo:" correspondente ÔÇö em Poemas, Status
+// e Datas ficam de fora de prop├│sito: j├í t├¬m filtro estruturado pr├│prio
+// (dropdown/painel de data). Elos/Refer├¬ncias (item 1 do schema ainda
+// pendente) ficam de fora porque ainda n├úo t├¬m texto decorado pra
+// buscar. Em Prosas, Datas e V├¡nculo (posi├º├úo estrutural, n├úo um campo
+// de texto ├║nico) ficam de fora pelo mesmo motivo das Datas de Poemas.
+// ├ëpoca Retratada entrou (item 3) apesar de ter filtro estruturado de
+// data pr├│prio ÔÇö mesmo assim, com nome agora resolvido via cadastro
+// central (_buscaEpoca), faz sentido ter a lupa de atalho pro prefixo
+// "epoca:", igual autoria ÔÇö a data continua s├│ pelo painel dedicado.
+// pessoas aponta pro campo decorado "_buscaPessoas" (n├úo pro nome cru da
+// coluna) ÔÇö mesma conven├º├úo de todas as outras entradas aqui; sem isso a
+// lupa da coluna Pessoas n├úo teria prefixo correspondente em
+// PREFIXOS_CANONICOS_POR_CAMPO (utils.js) e n├úo apareceria.
+const COLUNA_CAMPO_BUSCA = {
+    poemas: {
+        titulo: 'titulo',
+        pessoas: '_buscaPessoas',
+        grupos: '_buscaGrupos',
+        intertextualidade: '_buscaIntertexto',
+        anexos: '_buscaAnexos',
+        anexosNotaGeral: 'anexosNotaGeral',
+        anotacoesMarginais: '_buscaAnotacoes',
+        descricaoVisual: 'descricaoVisual',
+        contextoHistorico: 'contextoHistorico',
+        etiquetas: '_buscaSinalizacoes',
+        notas: 'notas',
+        ocultacao: 'ocultacao',
+        conteudoSensivel: 'conteudoSensivel',
+        vocabularioHiperacionante: 'vocabularioHiperacionante',
+        descarte: 'descarte',
+        pendencia: 'pendencia',
+        cortadoDe: '_buscaCortadoDe',
+        lancadoEm: '_buscaLancadoEm',
+        justificativaMigracao: 'justificativaMigracao',
+        elos: '_buscaElos',
+        referencias: '_buscaReferencias',
+        autoria: '_buscaAutoria',
+        envios: '_buscaEnvios',
+        reconhecimentos: '_buscaReconhecimentos',
+        epocaRetratada: '_buscaEpoca',
+    },
+    prosas: {
+        titulo: 'titulo',
+        pessoas: '_buscaPessoas',
+        grupos: '_buscaGrupos',
+        genero: 'genero',
+        etiquetas: '_buscaSinalizacoes',
+        notas: 'notas',
+        autoria: '_buscaAutoria',
+        envios: '_buscaEnvios',
+        reconhecimentos: '_buscaReconhecimentos',
+        // Item 4: mesmas entradas de Poemas para os campos que Prosa
+        // acabou de ganhar (ver decorarCamposBusca acima ÔÇö j├í gen├®rico,
+        // roda igual pras duas tabelas, ent├úo os campos decorados
+        // _busca* j├í existem pra Prosa desde sempre).
+        intertextualidade: '_buscaIntertexto',
+        anexos: '_buscaAnexos',
+        anexosNotaGeral: 'anexosNotaGeral',
+        contextoHistorico: 'contextoHistorico',
+        ocultacao: 'ocultacao',
+        conteudoSensivel: 'conteudoSensivel',
+        vocabularioHiperacionante: 'vocabularioHiperacionante',
+        descarte: 'descarte',
+        pendencia: 'pendencia',
+        cortadoDe: '_buscaCortadoDe',
+        lancadoEm: '_buscaLancadoEm',
+        justificativaMigracao: 'justificativaMigracao',
+        elos: '_buscaElos',
+        referencias: '_buscaReferencias',
+        epocaRetratada: '_buscaEpoca',
+    },
+};
+
+// ├ìcone de atalho pra busca por essa coluna (ver buscarPorPrefixo) ÔÇö s├│
+// aparece quando a coluna tem um prefixo "campo:" correspondente.
+// mousedown+preventDefault (n├úo click) pelo mesmo motivo do resto da UI de
+// busca: evita que o bot├úo roube o foco antes do input.focus() dentro da
+// pr├│pria fun├º├úo.
+function iconeBuscaColuna(tabela, campoItem) {
+    if (!campoItem || !PREFIXOS_CANONICOS_POR_CAMPO[campoItem]) return '';
+    const prefixo = PREFIXOS_CANONICOS_POR_CAMPO[campoItem];
+    return `<span onmousedown="event.preventDefault(); event.stopPropagation(); buscarPorPrefixo('${tabela}', '${campoItem}')"
+        class="text-gray-300 dark:text-slate-600 hover:text-blue-600 dark:hover:text-blue-400 cursor-pointer"
+        title="Buscar por ${prefixo}:">­ƒöì</span>`;
+}
+
 // <th> clic├ível: alterna a ordena├º├úo da tabela de Poemas pra essa coluna
 // (ver ordenarPoemasPor). `campo` ├® a key usada em COMPARADORES_ORDENACAO_POEMAS
-// (ou 'titulo'/'estrutura', os dois casos especiais).
+// (ou 'titulo'/'estrutura', os dois casos especiais). `campoItem`, quando
+// informado, ├® o campo decorado usado pela busca (ver COLUNA_CAMPO_BUSCA)
+// ÔÇö controla se a lupa de atalho aparece.
 // `sticky top-0` vai direto em cada <th> (n├úo no <thead>, nem no <tr>) ÔÇö
 // em v├írios navegadores, sticky em table-row-group/table-row ├® ignorado ou
 // inconsistente, ainda mais combinado com border-collapse; em <th>
 // (table-cell) funciona de forma confi├ível, ent├úo ├® a├¡ que a regra mora.
-function thOrdenavel(campo, label, estado, classeExtra = '') {
+function thOrdenavel(campo, label, estado, classeExtra = '', campoItem = null) {
     const ativo = estado.campo === campo;
     return `<th class="p-4 border-b border-gray-200 dark:border-slate-700 sticky top-0 z-20 bg-gray-100 dark:bg-slate-700 ${classeExtra}">
-        <button type="button" onclick="ordenarPoemasPor('${campo}')"
-            class="flex items-center gap-1 font-semibold hover:text-blue-600 dark:hover:text-blue-400 select-none"
-            title="Ordenar por ${escapeHtml(label)}">
-            <span>${label}</span>
-            ${iconeOrdenacao(ativo, estado.direcao)}
-        </button>
+        <span class="flex items-center gap-1">
+            <button type="button" onclick="ordenarPoemasPor('${campo}')"
+                class="flex items-center gap-1 font-semibold hover:text-blue-600 dark:hover:text-blue-400 select-none"
+                title="Ordenar por ${escapeHtml(label)}">
+                <span>${label}</span>
+                ${iconeOrdenacao(ativo, estado.direcao)}
+            </button>
+            ${iconeBuscaColuna('poemas', campoItem)}
+        </span>
     </th>`;
 }
 
-function montarCabecalho(tabela, celulaCheck, celulaTitulo, celulaAcoes) {
+// <th> n├úo-orden├ível (Prosas n├úo tem cabe├ºalho clic├ível pra ordena├º├úo ÔÇö
+// ver coment├írio em montarCabecalho), mas que ainda ganha a lupa de
+// atalho quando a coluna tem prefixo de busca correspondente (ver
+// COLUNA_CAMPO_BUSCA).
+function thComLupa(label, campoItem, classeExtra = '') {
+    return `<th class="p-4 border-b border-gray-200 dark:border-slate-700 sticky top-0 z-20 bg-gray-100 dark:bg-slate-700 ${classeExtra}">
+        <span class="flex items-center gap-1">
+            <span class="font-semibold">${label}</span>
+            ${iconeBuscaColuna('prosas', campoItem)}
+        </span>
+    </th>`;
+}
+
+function montarCabecalho(tabela, celulaCheck, celulaAcoes) {
     const ativas = getColunasAtivas(tabela);
     const def = DEFINICAO_COLUNAS[tabela];
+    const camposBusca = COLUNA_CAMPO_BUSCA[tabela] || {};
 
-    // S├│ Poemas tem cabe├ºalho clic├ível por enquanto (ver DEFINICAO_COLUNAS ÔÇö
-    // ├® a ├║nica tabela cujas colunas t├¬m sortType definido).
+    // S├│ Poemas tem cabe├ºalho orden├ível por enquanto (ver DEFINICAO_COLUNAS ÔÇö
+    // ├® a ├║nica tabela cujas colunas t├¬m sortType definido). Prosas ainda
+    // ganha a lupa de atalho por coluna (ver thComLupa), s├│ n├úo a ordena├º├úo.
     if (tabela === 'poemas') {
-        const tituloOrdenavel = thOrdenavel('titulo', 'ID / T├¡tulo', ordenacaoPoemas, 'left-8');
+        const tituloOrdenavel = thOrdenavel(
+            'titulo',
+            'ID / T├¡tulo',
+            ordenacaoPoemas,
+            'left-8',
+            camposBusca.titulo,
+        );
         const meio = ativas
             .map((key) => def.find((c) => c.key === key))
             .filter(Boolean)
-            .map((c) => thOrdenavel(c.key, c.label, ordenacaoPoemas))
+            .map((c) => thOrdenavel(c.key, c.label, ordenacaoPoemas, '', camposBusca[c.key]))
             .join('');
         return celulaCheck + tituloOrdenavel + meio + celulaAcoes;
     }
 
+    const tituloComLupa = thComLupa('T├¡tulo', camposBusca.titulo, 'sticky left-8');
     const meio = ativas
         .map((key) => def.find((c) => c.key === key))
         .filter(Boolean)
-        .map(
-            (c) =>
-                `<th class="p-4 border-b border-gray-200 dark:border-slate-700 sticky top-0 z-20 bg-gray-100 dark:bg-slate-700">${c.label}</th>`,
-        )
+        .map((c) => thComLupa(c.label, camposBusca[c.key]))
         .join('');
-    return celulaCheck + celulaTitulo + meio + celulaAcoes;
+    return celulaCheck + tituloComLupa + meio + celulaAcoes;
 }
 
 function atualizarPainelColunas(tabela, painelId) {
@@ -679,6 +1172,11 @@ function atualizarPainelColunas(tabela, painelId) {
     if (painel) painel.innerHTML = renderSeletorColunas(tabela);
 }
 
+function atualizarPainelAcoes(tabela, painelId) {
+    const painel = document.getElementById(painelId);
+    if (painel) painel.innerHTML = renderSeletorAcoes(tabela);
+}
+
 // ÔöÇÔöÇÔöÇ Sele├º├úo m├║ltipla de Poemas (a├º├Áes em massa) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
 
 // Retorna a lista de poemas atualmente vis├¡vel, j├í com status, busca
@@ -691,6 +1189,7 @@ function getListaVisivelPoemas() {
     else if (statusPoemas === 'completos') base = base.filter((p) => p.status === 'completo');
     else if (statusPoemas === 'incompletos') base = base.filter((p) => p.status === 'incompleto');
     else if (statusPoemas === 'migrados') base = base.filter((p) => p.status === 'migrado');
+    else if (statusPoemas === 'pendentes') base = base.filter((p) => p.pendencia?.trim());
     else if (statusPoemas === 'descartados') base = base.filter((p) => p.status === 'descartado');
 
     if (filtroLivroPoemas) {
@@ -816,11 +1315,17 @@ function compararPorStatus(a, b, asc) {
 
 // Texto puro (sem HTML) dos t├¡tulos ligados por Elos/Refer├¬ncias ÔÇö usado
 // s├│ pra ordena├º├úo; a c├®lula em si (titulosPoemasPorId) escapa e formata
-// ├á parte.
-function textoTitulosPoemasPorId(ids) {
-    if (!ids || !ids.length) return '';
-    return ids
-        .map((id) => db.poemas.find((p) => p.id == id)?.titulo)
+// ├á parte. `resolverRotulo` mesma fun├º├úo passada pra titulosPoemasPorId,
+// pra ordenar pelo mesmo texto que aparece na coluna.
+function textoTitulosPoemasPorId(lista, resolverRotulo) {
+    if (!lista || !lista.length) return '';
+    return lista
+        .map((entrada) => {
+            const titulo = resolverTituloPoemaOuProsa(entrada.id);
+            if (!titulo) return null;
+            const rotulo = resolverRotulo(entrada);
+            return rotulo ? `${rotulo}: ${titulo}` : titulo;
+        })
         .filter(Boolean)
         .join(', ');
 }
@@ -850,13 +1355,34 @@ function compararPorEpocaRetratada(a, b, asc) {
 
 const COMPARADORES_ORDENACAO_POEMAS = {
     titulo: compararPorTexto((p) => p.titulo),
+    idioma: compararPorTexto((p) => p.idioma),
     dataEscrita: compararPorData((p) => p.dataEscrita),
     dataPublicacao: compararPorData((p) => p.dataPublicacao),
     epocaRetratada: compararPorEpocaRetratada,
     status: compararPorStatus,
-    pessoas: compararPorTexto((p) => p.pessoas),
-    elos: compararPorTexto((p) => textoTitulosPoemasPorId(p.conceitos?.elos)),
-    referencias: compararPorTexto((p) => textoTitulosPoemasPorId(p.conceitos?.referencias)),
+    pessoas: compararPorTexto((p) => nomesPessoas(p, db.pessoas).join(', ')),
+    grupos: compararPorTexto((p) =>
+        paresGrupoPessoa(p, db.pessoas, db.grupos)
+            .map(({ grupo }) => grupo.nome)
+            .join(', '),
+    ),
+    autoria: compararPorTexto((p) =>
+        paresAutoria(p, db.autores)
+            .map(({ autor }) => autor.nome)
+            .join(', '),
+    ),
+    envios: compararPorTexto((p) =>
+        Array.isArray(p.envios) ? p.envios.map((e) => e.pessoa || '').join(', ') : '',
+    ),
+    reconhecimentos: compararPorTexto((p) =>
+        Array.isArray(p.reconhecimentos)
+            ? p.reconhecimentos.map((r) => r.premio || '').join(', ')
+            : '',
+    ),
+    elos: compararPorTexto((p) => textoTitulosPoemasPorId(p.conceitos?.elos, rotuloEntradaElo)),
+    referencias: compararPorTexto((p) =>
+        textoTitulosPoemasPorId(p.conceitos?.referencias, rotuloEntradaReferencia),
+    ),
     intertextualidade: compararPorTexto((p) =>
         Array.isArray(p.intertextualidade)
             ? p.intertextualidade.map((it) => it.texto).join(' ')
@@ -867,23 +1393,58 @@ const COMPARADORES_ORDENACAO_POEMAS = {
     ),
     anexosNotaGeral: compararPorTexto((p) => p.anexosNotaGeral),
     anotacoesMarginais: compararPorTexto((p) =>
-        Array.isArray(p.anotacoesMarginais) ? p.anotacoesMarginais.map((it) => it.texto).join(' ') : '',
+        Array.isArray(p.anotacoesMarginais)
+            ? p.anotacoesMarginais.map((it) => it.texto).join(' ')
+            : '',
     ),
     descricaoVisual: compararPorTexto((p) => p.descricaoVisual),
     contextoHistorico: compararPorTexto((p) => p.contextoHistorico),
-    etiquetas: compararPorTexto((p) => p.sinalizacoes),
+    etiquetas: compararPorTexto((p) => sinalizacoesCombinadas(p)),
     notas: compararPorTexto((p) => p.notas),
     ocultacao: compararPorTexto((p) => p.ocultacao),
     conteudoSensivel: compararPorTexto((p) => p.conteudoSensivel),
     vocabularioHiperacionante: compararPorTexto((p) => p.vocabularioHiperacionante),
-    cortadoDe: compararPorTexto((p) => [p.cortadoDe?.livro, p.cortadoDe?.secao].filter(Boolean).join(' ')),
-    lancadoEm: compararPorTexto((p) => [p.lancadoEm?.livro, p.lancadoEm?.secao].filter(Boolean).join(' ')),
+    cortadoDe: compararPorTexto((p) =>
+        [p.cortadoDe?.livro, p.cortadoDe?.secao].filter(Boolean).join(' '),
+    ),
+    lancadoEm: compararPorTexto((p) =>
+        [p.lancadoEm?.livro, p.lancadoEm?.secao].filter(Boolean).join(' '),
+    ),
+    justificativaMigracao: compararPorTexto((p) => p.justificativaMigracao),
     descarte: compararPorTexto((p) => p.descarte),
+    pendencia: compararPorTexto((p) => p.pendencia),
+    camposPreenchidos: (a, b, asc) => {
+        const diff = contarCamposPreenchidos(a) - contarCamposPreenchidos(b);
+        return asc ? diff : -diff;
+    },
 };
 
-export function toggleSelecaoPoema(checked, id) {
+// shiftKey estende a sele├º├úo pro intervalo entre este checkbox e o
+// ├║ltimo clicado (ordem da lista filtrada/ordenada atual, n├úo a ordem
+// de estrutura fixa ÔÇö o intervalo ├® sempre "o que est├í entre as duas
+// linhas na tela"). Marca ou desmarca o intervalo inteiro com o mesmo
+// `checked` do checkbox que disparou o shift-click, replicando o
+// padr├úo do Gmail/Finder. Se a ├óncora n├úo estiver mais na lista vis├¡vel
+// (filtro mudou, ou ├® o primeiro clique da sess├úo), cai pro
+// comportamento normal de um clique s├│.
+export function toggleSelecaoPoema(checked, id, shiftKey) {
+    if (shiftKey && ultimoCheckPoema !== null && ultimoCheckPoema !== id) {
+        const visiveis = getListaVisivelPoemas().map((p) => p.id);
+        const iAncora = visiveis.indexOf(ultimoCheckPoema);
+        const iAtual = visiveis.indexOf(id);
+        if (iAncora !== -1 && iAtual !== -1) {
+            const [ini, fim] = iAncora < iAtual ? [iAncora, iAtual] : [iAtual, iAncora];
+            const intervalo = visiveis.slice(ini, fim + 1);
+            if (checked) intervalo.forEach((pid) => selecaoPoemas.add(pid));
+            else intervalo.forEach((pid) => selecaoPoemas.delete(pid));
+            ultimoCheckPoema = id;
+            renderPoemas();
+            return;
+        }
+    }
     if (checked) selecaoPoemas.add(id);
     else selecaoPoemas.delete(id);
+    ultimoCheckPoema = id;
     atualizarBarraSelecao();
 }
 
@@ -931,6 +1492,24 @@ function removerValorDeCampo(poema, campo, valor) {
     poema[campo] = atuais.filter((v) => v !== valor).join(', ');
 }
 
+// Variantes pra Pessoas (array de objeto {pessoaId, papeis} desde a
+// virada pro cadastro central ÔÇö as duas fun├º├Áes gen├®ricas acima
+// continuam servindo Sinaliza├º├Áes e G├¬nero, que n├úo mudaram de
+// formato). Adicionar via edi├º├úo em massa sempre entra com papeis: []
+// (nenhum marcado) ÔÇö o bulk-edit s├│ pede o nome, n├úo os pap├®is; quem
+// quiser categorizar edita no modal do item, onde o chip j├í tem os
+// checkboxes.
+function adicionarPessoaEmCampo(item, pessoaId) {
+    const atuais = Array.isArray(item.pessoas) ? item.pessoas : [];
+    if (!atuais.some((p) => p.pessoaId === pessoaId)) atuais.push({ pessoaId, papeis: [] });
+    item.pessoas = atuais;
+}
+
+function removerPessoaEmCampo(item, pessoaId) {
+    if (!Array.isArray(item.pessoas)) return;
+    item.pessoas = item.pessoas.filter((p) => p.pessoaId !== pessoaId);
+}
+
 export function aplicarPessoaEmMassa() {
     const input = document.getElementById('bulk-pessoa-input');
     const nome = (input?.value || '').trim();
@@ -944,8 +1523,9 @@ export function aplicarPessoaEmMassa() {
         textoConfirmar: 'Aplicar',
         corConfirmar: '#e11d48',
         onConfirmar: () => {
+            const pessoa = obterOuCriarPessoaPorNome(nome);
             db.poemas.forEach((p) => {
-                if (selecaoPoemas.has(p.id)) adicionarValorEmCampo(p, 'pessoas', nome);
+                if (selecaoPoemas.has(p.id)) adicionarPessoaEmCampo(p, pessoa.id);
             });
             if (input) input.value = '';
             selecaoPoemas.clear();
@@ -959,6 +1539,15 @@ export function removerPessoaEmMassa() {
     const nome = (input?.value || '').trim();
     if (!nome || selecaoPoemas.size === 0) return;
 
+    const pessoa = db.pessoas.find((p) => p.nome === nome);
+    if (!pessoa) {
+        // Nome n├úo bate com nenhuma pessoa cadastrada ÔÇö nada a remover
+        // (diferente de "adicionar", aqui n├úo faz sentido criar s├│ pra
+        // desvincular em seguida).
+        if (input) input.value = '';
+        return;
+    }
+
     const n = selecaoPoemas.size;
     abrirModalConfirmacao({
         titulo: `Remover "${nome}"`,
@@ -968,7 +1557,7 @@ export function removerPessoaEmMassa() {
         corConfirmar: '#dc2626',
         onConfirmar: () => {
             db.poemas.forEach((p) => {
-                if (selecaoPoemas.has(p.id)) removerValorDeCampo(p, 'pessoas', nome);
+                if (selecaoPoemas.has(p.id)) removerPessoaEmCampo(p, pessoa.id);
             });
             if (input) input.value = '';
             selecaoPoemas.clear();
@@ -977,8 +1566,24 @@ export function removerPessoaEmMassa() {
     });
 }
 
+// Mapa campoÔåÆslug de DOM, pra achar o datalist certo
+// (sugestoes-sinais-{slug}-bulk[-prosa]) quando a pessoa troca a
+// categoria no seletor da edi├º├úo em massa ÔÇö ver SINALIZACOES_CATEGORIAS
+// em utils.js e SINAL_CATEGORIAS em editor.js (mesmo slug nos dois).
+const SLUG_POR_CAMPO_SINAL = Object.fromEntries(
+    Object.entries(SINALIZACOES_CATEGORIAS).map(([slug, campo]) => [campo, slug]),
+);
+
+export function atualizarListaSinalBulk(selectEl, inputId, sufixo = '') {
+    const input = document.getElementById(inputId);
+    if (!input || !selectEl) return;
+    const slug = SLUG_POR_CAMPO_SINAL[selectEl.value] || 'estilo';
+    input.setAttribute('list', `sugestoes-sinais-${slug}-bulk${sufixo}`);
+}
+
 export function aplicarSinalEmMassa() {
     const input = document.getElementById('bulk-sinal-input');
+    const campo = document.getElementById('bulk-sinal-categoria')?.value || 'sinalizacoesEstilo';
     const tag = (input?.value || '').trim();
     if (!tag || selecaoPoemas.size === 0) return;
 
@@ -991,7 +1596,7 @@ export function aplicarSinalEmMassa() {
         corConfirmar: '#2563eb',
         onConfirmar: () => {
             db.poemas.forEach((p) => {
-                if (selecaoPoemas.has(p.id)) adicionarValorEmCampo(p, 'sinalizacoes', tag);
+                if (selecaoPoemas.has(p.id)) adicionarValorEmCampo(p, campo, tag);
             });
             if (input) input.value = '';
             selecaoPoemas.clear();
@@ -1002,6 +1607,7 @@ export function aplicarSinalEmMassa() {
 
 export function removerSinalEmMassa() {
     const input = document.getElementById('bulk-sinal-input');
+    const campo = document.getElementById('bulk-sinal-categoria')?.value || 'sinalizacoesEstilo';
     const tag = (input?.value || '').trim();
     if (!tag || selecaoPoemas.size === 0) return;
 
@@ -1014,7 +1620,7 @@ export function removerSinalEmMassa() {
         corConfirmar: '#dc2626',
         onConfirmar: () => {
             db.poemas.forEach((p) => {
-                if (selecaoPoemas.has(p.id)) removerValorDeCampo(p, 'sinalizacoes', tag);
+                if (selecaoPoemas.has(p.id)) removerValorDeCampo(p, campo, tag);
             });
             if (input) input.value = '';
             selecaoPoemas.clear();
@@ -1056,6 +1662,9 @@ export function exportarSelecaoPoemasJson() {
 export function exportarSelecaoPoemasMarkdown() {
     exportarSelecaoMarkdown('poema', [...selecaoPoemas]);
 }
+export function exportarSelecaoPoemasPdf() {
+    exportarSelecaoPdf('poema', [...selecaoPoemas]);
+}
 
 // ÔöÇÔöÇÔöÇ Datas em massa (Poemas): Escrita / Publica├º├úo ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
 // Mesma mec├ónica da vers├úo de Prosas logo abaixo ÔÇö ver os coment├írios l├í.
@@ -1171,9 +1780,25 @@ function getListaVisivelProsas() {
     return lista;
 }
 
-export function toggleSelecaoProsa(checked, id) {
+// Ver toggleSelecaoPoema ÔÇö mesma l├│gica de shift-click, espelhada pra Prosas.
+export function toggleSelecaoProsa(checked, id, shiftKey) {
+    if (shiftKey && ultimoCheckProsa !== null && ultimoCheckProsa !== id) {
+        const visiveis = getListaVisivelProsas().map((pr) => pr.id);
+        const iAncora = visiveis.indexOf(ultimoCheckProsa);
+        const iAtual = visiveis.indexOf(id);
+        if (iAncora !== -1 && iAtual !== -1) {
+            const [ini, fim] = iAncora < iAtual ? [iAncora, iAtual] : [iAtual, iAncora];
+            const intervalo = visiveis.slice(ini, fim + 1);
+            if (checked) intervalo.forEach((pid) => selecaoProsas.add(pid));
+            else intervalo.forEach((pid) => selecaoProsas.delete(pid));
+            ultimoCheckProsa = id;
+            renderProsas();
+            return;
+        }
+    }
     if (checked) selecaoProsas.add(id);
     else selecaoProsas.delete(id);
+    ultimoCheckProsa = id;
     atualizarBarraSelecaoProsas();
 }
 
@@ -1214,8 +1839,9 @@ export function aplicarPessoaEmMassaProsa() {
         textoConfirmar: 'Aplicar',
         corConfirmar: '#e11d48',
         onConfirmar: () => {
+            const pessoa = obterOuCriarPessoaPorNome(nome);
             db.prosas.forEach((pr) => {
-                if (selecaoProsas.has(pr.id)) adicionarValorEmCampo(pr, 'pessoas', nome);
+                if (selecaoProsas.has(pr.id)) adicionarPessoaEmCampo(pr, pessoa.id);
             });
             if (input) input.value = '';
             selecaoProsas.clear();
@@ -1229,6 +1855,12 @@ export function removerPessoaEmMassaProsa() {
     const nome = (input?.value || '').trim();
     if (!nome || selecaoProsas.size === 0) return;
 
+    const pessoa = db.pessoas.find((p) => p.nome === nome);
+    if (!pessoa) {
+        if (input) input.value = '';
+        return;
+    }
+
     const n = selecaoProsas.size;
     abrirModalConfirmacao({
         titulo: `Remover "${nome}"`,
@@ -1238,7 +1870,7 @@ export function removerPessoaEmMassaProsa() {
         corConfirmar: '#dc2626',
         onConfirmar: () => {
             db.prosas.forEach((pr) => {
-                if (selecaoProsas.has(pr.id)) removerValorDeCampo(pr, 'pessoas', nome);
+                if (selecaoProsas.has(pr.id)) removerPessoaEmCampo(pr, pessoa.id);
             });
             if (input) input.value = '';
             selecaoProsas.clear();
@@ -1249,6 +1881,8 @@ export function removerPessoaEmMassaProsa() {
 
 export function aplicarSinalEmMassaProsa() {
     const input = document.getElementById('bulk-sinal-input-prosa');
+    const campo =
+        document.getElementById('bulk-sinal-categoria-prosa')?.value || 'sinalizacoesEstilo';
     const tag = (input?.value || '').trim();
     if (!tag || selecaoProsas.size === 0) return;
 
@@ -1261,7 +1895,7 @@ export function aplicarSinalEmMassaProsa() {
         corConfirmar: '#2563eb',
         onConfirmar: () => {
             db.prosas.forEach((pr) => {
-                if (selecaoProsas.has(pr.id)) adicionarValorEmCampo(pr, 'sinalizacoes', tag);
+                if (selecaoProsas.has(pr.id)) adicionarValorEmCampo(pr, campo, tag);
             });
             if (input) input.value = '';
             selecaoProsas.clear();
@@ -1272,6 +1906,8 @@ export function aplicarSinalEmMassaProsa() {
 
 export function removerSinalEmMassaProsa() {
     const input = document.getElementById('bulk-sinal-input-prosa');
+    const campo =
+        document.getElementById('bulk-sinal-categoria-prosa')?.value || 'sinalizacoesEstilo';
     const tag = (input?.value || '').trim();
     if (!tag || selecaoProsas.size === 0) return;
 
@@ -1284,7 +1920,7 @@ export function removerSinalEmMassaProsa() {
         corConfirmar: '#dc2626',
         onConfirmar: () => {
             db.prosas.forEach((pr) => {
-                if (selecaoProsas.has(pr.id)) removerValorDeCampo(pr, 'sinalizacoes', tag);
+                if (selecaoProsas.has(pr.id)) removerValorDeCampo(pr, campo, tag);
             });
             if (input) input.value = '';
             selecaoProsas.clear();
@@ -1444,6 +2080,9 @@ export function exportarSelecaoProsasJson() {
 export function exportarSelecaoProsasMarkdown() {
     exportarSelecaoMarkdown('prosa', [...selecaoProsas]);
 }
+export function exportarSelecaoProsasPdf() {
+    exportarSelecaoPdf('prosa', [...selecaoProsas]);
+}
 
 // ÔöÇÔöÇÔöÇ Livros ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
 
@@ -1671,13 +2310,13 @@ export function renderPoemas() {
 
     const colunasAtivas = getColunasAtivas('poemas');
     atualizarPainelColunas('poemas', 'painel-colunas-poemas');
+    atualizarPainelAcoes('poemas', 'painel-acoes-poemas');
 
     const cabecalho = document.getElementById('cabecalho-poemas');
     if (cabecalho) {
         cabecalho.innerHTML = montarCabecalho(
             'poemas',
             `<th class="p-4 border-b w-8 border-gray-200 dark:border-slate-700 sticky left-0 top-0 z-20 bg-gray-100 dark:bg-slate-700"><input type="checkbox" id="check-todos-poemas" data-action="toggle-todos-poemas"></th>`,
-            `<th class="p-4 border-b border-gray-200 dark:border-slate-700">ID / T├¡tulo</th>`,
             `<th class="p-4 border-b text-right border-gray-200 dark:border-slate-700 sticky top-0 z-20 bg-gray-100 dark:bg-slate-700">A├º├Áes</th>`,
         );
         // O checkbox mestre ├® recriado a cada render do cabe├ºalho ÔÇö reaplica o estado
@@ -1720,6 +2359,8 @@ export function renderPoemas() {
         );
 
     const CELULAS_POEMAS = {
+        idioma: (p) =>
+            `<td class="p-4 text-xs text-gray-400 dark:text-slate-500 font-mono">${escapeHtml(p.idioma || 'pt-BR')}</td>`,
         dataEscrita: (p) => {
             const aproximada = !!(p.dataEscrita && !p.dataEscrita.exata);
             const dicas = [];
@@ -1751,30 +2392,38 @@ export function renderPoemas() {
                 descartado: { emoji: '­ƒö┤', titulo: 'Descartado' },
             };
             const { emoji, titulo } = INFO_STATUS[p.status] || { emoji: 'ÔÜ¬', titulo: 'Completo' };
-            return `<td class="p-4" title="${titulo}">${emoji}</td>`;
+            // Pend├¬ncia agora ├® um campo independente do status ÔÇö o ­ƒƒá ├®
+            // exibido junto do c├¡rculo de status normal (n├úo no lugar dele)
+            // sempre que houver texto preenchido em item.pendencia.
+            const temPendencia = !!p.pendencia?.trim();
+            const badgePendencia = temPendencia
+                ? `<span title="Pendente: ${escapeHtml(p.pendencia.trim())}">­ƒƒá</span>`
+                : '';
+            return `<td class="p-4" title="${titulo}"><span class="inline-flex items-center gap-1">${emoji}${badgePendencia}</span></td>`;
         },
         dataPublicacao: (p) =>
             `<td class="p-4 text-xs text-gray-400 dark:text-slate-500 font-mono">${p.dataPublicacao ? formatarDataParcial(p.dataPublicacao) : 'ÔÇö'}</td>`,
-        pessoas: (p) =>
-            `<td class="p-4">${badgesEtiquetas(p.pessoas, 'bg-rose-100 dark:bg-rose-900 text-rose-600 dark:text-rose-400')}</td>`,
+        pessoas: (p) => `<td class="p-4">${badgesPessoas(p.pessoas)}</td>`,
+        grupos: (p) => `<td class="p-4">${badgesGrupos(p)}</td>`,
+        autoria: (p) => `<td class="p-4">${badgesAutoria(p)}</td>`,
+        envios: (p) => `<td class="p-4">${badgesEnvios(p)}</td>`,
+        reconhecimentos: (p) => `<td class="p-4">${badgesReconhecimentos(p)}</td>`,
         elos: (p) =>
-            `<td class="p-4 text-xs text-gray-500 dark:text-slate-400">${titulosPoemasPorId(p.conceitos?.elos)}</td>`,
+            `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${titulosPoemasPorId(p.conceitos?.elos, rotuloEntradaElo, 'bg-cyan-100 dark:bg-cyan-900 text-cyan-700 dark:text-cyan-300')}</td>`,
         referencias: (p) =>
-            `<td class="p-4 text-xs text-gray-500 dark:text-slate-400">${titulosPoemasPorId(p.conceitos?.referencias)}</td>`,
-        etiquetas: (p) => `<td class="p-4">${badgesEtiquetas(p.sinalizacoes)}</td>`,
+            `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${titulosPoemasPorId(p.conceitos?.referencias, rotuloEntradaReferencia, 'bg-fuchsia-100 dark:bg-fuchsia-900 text-fuchsia-700 dark:text-fuchsia-300')}</td>`,
+        etiquetas: (p) => `<td class="p-4">${badgesEtiquetas(sinalizacoesCombinadas(p))}</td>`,
         notas: (p) =>
             `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${trechoNota(p.notas)}</td>`,
         epocaRetratada: (p) => {
             const epoca = p.epocaRetratada;
             const na = epoca?.na;
-            const nomeBadge = epoca?.nome
-                ? `<span class="inline-block px-1.5 py-0.5 mr-1 rounded bg-teal-100 dark:bg-teal-900 text-teal-700 dark:text-teal-300 text-[10px] font-bold align-middle">${escapeHtml(epoca.nome)}</span>`
-                : '';
-            return `<td class="p-4 text-xs ${na ? 'text-gray-300 dark:text-slate-600 italic' : 'text-gray-400 dark:text-slate-500'}">${nomeBadge}<span class="font-mono">${formatarIntervaloEpocaRetratada(epoca)}</span></td>`;
+            return `<td class="p-4 text-xs ${na ? 'text-gray-300 dark:text-slate-600 italic' : 'text-gray-400 dark:text-slate-500'}">${badgeEpocaRetratada(epoca)}<span class="font-mono">${formatarIntervaloEpocaRetratada(epoca)}</span></td>`;
         },
         intertextualidade: (p) => {
             const lista = Array.isArray(p.intertextualidade) ? p.intertextualidade : [];
-            if (!lista.length) return `<td class="p-4 text-xs text-gray-300 dark:text-slate-600">ÔÇö</td>`;
+            if (!lista.length)
+                return `<td class="p-4 text-xs text-gray-300 dark:text-slate-600">ÔÇö</td>`;
             const html = lista
                 .map((it) => {
                     const badge = it.tipo
@@ -1787,7 +2436,8 @@ export function renderPoemas() {
         },
         anexos: (p) => {
             const lista = Array.isArray(p.anexos) ? p.anexos : [];
-            if (!lista.length) return `<td class="p-4 text-xs text-gray-300 dark:text-slate-600">ÔÇö</td>`;
+            if (!lista.length)
+                return `<td class="p-4 text-xs text-gray-300 dark:text-slate-600">ÔÇö</td>`;
             const html = lista
                 .map((it) => {
                     const badge = it.tipo
@@ -1802,7 +2452,8 @@ export function renderPoemas() {
             `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${trechoNota(p.anexosNotaGeral)}</td>`,
         anotacoesMarginais: (p) => {
             const lista = Array.isArray(p.anotacoesMarginais) ? p.anotacoesMarginais : [];
-            if (!lista.length) return `<td class="p-4 text-xs text-gray-300 dark:text-slate-600">ÔÇö</td>`;
+            if (!lista.length)
+                return `<td class="p-4 text-xs text-gray-300 dark:text-slate-600">ÔÇö</td>`;
             const html = lista
                 .map((it) => {
                     const meta = [it.posicao, it.fonte].filter(Boolean).join(' ┬À ');
@@ -1834,8 +2485,13 @@ export function renderPoemas() {
                 return `<td class="p-4 text-xs text-gray-300 dark:text-slate-600">ÔÇö</td>`;
             return `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${escapeHtml([p.lancadoEm.livro, p.lancadoEm.secao].filter(Boolean).join(' / '))}</td>`;
         },
+        justificativaMigracao: (p) =>
+            `<td class="p-4 text-xs max-w-xs ${p.justificativaMigracao ? 'text-blue-700 dark:text-blue-400 border-l-2 border-blue-300 dark:border-blue-700' : 'text-gray-300 dark:text-slate-600'}">${p.justificativaMigracao ? trechoNota(p.justificativaMigracao) : 'ÔÇö'}</td>`,
         descarte: (p) =>
             `<td class="p-4 text-xs max-w-xs ${p.descarte ? 'text-amber-700 dark:text-amber-400 border-l-2 border-amber-300 dark:border-amber-700' : 'text-gray-300 dark:text-slate-600'}">${p.descarte ? trechoNota(p.descarte) : 'ÔÇö'}</td>`,
+        pendencia: (p) =>
+            `<td class="p-4 text-xs max-w-xs ${p.pendencia ? 'text-orange-700 dark:text-orange-400 border-l-2 border-orange-300 dark:border-orange-700' : 'text-gray-300 dark:text-slate-600'}">${p.pendencia ? trechoNota(p.pendencia) : 'ÔÇö'}</td>`,
+        camposPreenchidos: (p) => celulaCamposPreenchidos(p),
     };
 
     container.innerHTML = listaPagina
@@ -1856,8 +2512,7 @@ export function renderPoemas() {
             </td>
             ${celulasMeio}
             <td class="p-4 text-right space-x-2">
-                <button data-action="editar-poema" data-id="${p.id}" title="Editar" aria-label="Editar" class="inline-flex items-center justify-center bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 p-1.5 rounded hover:bg-blue-200 dark:hover:bg-blue-800">${ICONE_EDITAR}</button>
-                <button data-action="excluir-item" data-tipo="poemas" data-id="${p.id}" title="Excluir" aria-label="Excluir" class="inline-flex items-center justify-center p-1.5 rounded text-red-400 hover:text-red-600 dark:hover:text-red-400 hover:bg-red-100 dark:hover:bg-red-900/40">${ICONE_EXCLUIR}</button>
+                ${celulaAcoesItem('poemas', 'poema', 'poemas', p.id)}
             </td>
         </tr>`;
         })
@@ -1902,13 +2557,13 @@ export function renderProsas() {
 
     const colunasAtivas = getColunasAtivas('prosas');
     atualizarPainelColunas('prosas', 'painel-colunas-prosas');
+    atualizarPainelAcoes('prosas', 'painel-acoes-prosas');
 
     const cabecalho = document.getElementById('cabecalho-prosas');
     if (cabecalho) {
         cabecalho.innerHTML = montarCabecalho(
             'prosas',
             `<th class="p-4 border-b w-8 border-gray-200 dark:border-slate-700 sticky left-0 top-0 z-20 bg-gray-100 dark:bg-slate-700"><input type="checkbox" id="check-todos-prosas" data-action="toggle-todos-prosas"></th>`,
-            `<th class="p-4 border-b border-gray-200 dark:border-slate-700 sticky left-8 top-0 z-20 bg-gray-100 dark:bg-slate-700">T├¡tulo</th>`,
             `<th class="p-4 border-b text-right border-gray-200 dark:border-slate-700 sticky top-0 z-20 bg-gray-100 dark:bg-slate-700">A├º├Áes</th>`,
         );
         const novoMaster = document.getElementById('check-todos-prosas');
@@ -1948,6 +2603,8 @@ export function renderProsas() {
         );
 
     const CELULAS_PROSAS = {
+        idioma: (pr) =>
+            `<td class="p-4 text-xs text-gray-400 dark:text-slate-500 font-mono">${escapeHtml(pr.idioma || 'pt-BR')}</td>`,
         dataEscrita: (pr) => {
             const aproximada = !!(pr.dataEscrita && !pr.dataEscrita.exata);
             const dicas = [];
@@ -1974,20 +2631,98 @@ export function renderProsas() {
                 : 'Sem v├¡nculo';
             return `<td class="p-4 text-xs text-gray-400 dark:text-slate-500">${infoVinc}</td>`;
         },
+        // Item 4: Prosa ganha o mesmo c├¡rculo de status de Poema (ver
+        // CELULAS_POEMAS.status acima) ÔÇö badgePendencia id├¬ntico, mesmo
+        // crit├®rio (texto em pr.pendencia, independente do status).
+        status: (pr) => {
+            const INFO_STATUS = {
+                publicado: { emoji: '­ƒƒó', titulo: 'Publicado' },
+                incompleto: { emoji: '­ƒƒí', titulo: 'Incompleto' },
+                migrado: { emoji: '­ƒöÁ', titulo: 'Migrado' },
+                descartado: { emoji: '­ƒö┤', titulo: 'Descartado' },
+            };
+            const { emoji, titulo } = INFO_STATUS[pr.status] || { emoji: 'ÔÜ¬', titulo: 'Completo' };
+            const temPendencia = !!pr.pendencia?.trim();
+            const badgePendencia = temPendencia
+                ? `<span title="Pendente: ${escapeHtml(pr.pendencia.trim())}">­ƒƒá</span>`
+                : '';
+            return `<td class="p-4" title="${titulo}"><span class="inline-flex items-center gap-1">${emoji}${badgePendencia}</span></td>`;
+        },
         dataPublicacao: (pr) =>
             `<td class="p-4 text-xs text-gray-400 dark:text-slate-500 font-mono">${pr.dataPublicacao ? formatarDataParcial(pr.dataPublicacao) : 'ÔÇö'}</td>`,
-        pessoas: (pr) =>
-            `<td class="p-4">${badgesEtiquetas(pr.pessoas, 'bg-rose-100 dark:bg-rose-900 text-rose-600 dark:text-rose-400')}</td>`,
-        etiquetas: (pr) => `<td class="p-4">${badgesEtiquetas(pr.sinalizacoes)}</td>`,
+        pessoas: (pr) => `<td class="p-4">${badgesPessoas(pr.pessoas)}</td>`,
+        grupos: (pr) => `<td class="p-4">${badgesGrupos(pr)}</td>`,
+        autoria: (pr) => `<td class="p-4">${badgesAutoria(pr)}</td>`,
+        envios: (pr) => `<td class="p-4">${badgesEnvios(pr)}</td>`,
+        reconhecimentos: (pr) => `<td class="p-4">${badgesReconhecimentos(pr)}</td>`,
+        etiquetas: (pr) => `<td class="p-4">${badgesEtiquetas(sinalizacoesCombinadas(pr))}</td>`,
         genero: (pr) =>
             `<td class="p-4">${badgesEtiquetas(pr.genero, 'bg-amber-100 dark:bg-amber-900 text-amber-700 dark:text-amber-400')}</td>`,
         notas: (pr) =>
             `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${trechoNota(pr.notas)}</td>`,
+        epocaRetratada: (pr) => {
+            const epoca = pr.epocaRetratada;
+            const na = epoca?.na;
+            return `<td class="p-4 text-xs ${na ? 'text-gray-300 dark:text-slate-600 italic' : 'text-gray-400 dark:text-slate-500'}">${badgeEpocaRetratada(epoca)}<span class="font-mono">${formatarIntervaloEpocaRetratada(epoca)}</span></td>`;
+        },
+        contextoHistorico: (pr) =>
+            `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${trechoNota(pr.contextoHistorico)}</td>`,
+        elos: (pr) =>
+            `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${titulosPoemasPorId(pr.conceitos?.elos, rotuloEntradaElo, 'bg-cyan-100 dark:bg-cyan-900 text-cyan-700 dark:text-cyan-300')}</td>`,
+        referencias: (pr) =>
+            `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${titulosPoemasPorId(pr.conceitos?.referencias, rotuloEntradaReferencia, 'bg-fuchsia-100 dark:bg-fuchsia-900 text-fuchsia-700 dark:text-fuchsia-300')}</td>`,
+        intertextualidade: (pr) => {
+            const lista = Array.isArray(pr.intertextualidade) ? pr.intertextualidade : [];
+            if (!lista.length)
+                return `<td class="p-4 text-xs text-gray-300 dark:text-slate-600">ÔÇö</td>`;
+            const html = lista
+                .map((it) => {
+                    const badge = it.tipo
+                        ? `<span class="inline-block px-1.5 py-0.5 mr-1 rounded bg-purple-100 dark:bg-purple-900 text-purple-700 dark:text-purple-300 text-[10px] font-bold uppercase align-middle">${escapeHtml(it.tipo)}</span>`
+                        : '';
+                    return `<div>${badge}${trechoNota(it.texto)}</div>`;
+                })
+                .join('');
+            return `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${html}</td>`;
+        },
+        anexos: (pr) => {
+            const lista = Array.isArray(pr.anexos) ? pr.anexos : [];
+            if (!lista.length)
+                return `<td class="p-4 text-xs text-gray-300 dark:text-slate-600">ÔÇö</td>`;
+            const html = lista
+                .map((it) => {
+                    const badge = it.tipo
+                        ? `<span class="inline-block px-1.5 py-0.5 mr-1 rounded bg-indigo-100 dark:bg-indigo-900 text-indigo-700 dark:text-indigo-300 text-[10px] font-bold uppercase align-middle">${escapeHtml(it.tipo)}</span>`
+                        : '';
+                    return `<div>${badge}${trechoNota(it.texto)}</div>`;
+                })
+                .join('');
+            return `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${html}</td>`;
+        },
+        anexosNotaGeral: (pr) =>
+            `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${trechoNota(pr.anexosNotaGeral)}</td>`,
+        ocultacao: (pr) =>
+            `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${trechoNota(pr.ocultacao)}</td>`,
+        conteudoSensivel: (pr) =>
+            `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${trechoNota(pr.conteudoSensivel)}</td>`,
+        vocabularioHiperacionante: (pr) =>
+            `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${trechoNota(pr.vocabularioHiperacionante)}</td>`,
+        cortadoDe: (pr) =>
+            `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${pr.cortadoDe ? escapeHtml(`${pr.cortadoDe.livro || ''} ${pr.cortadoDe.secao || ''}`.trim()) : 'ÔÇö'}</td>`,
+        lancadoEm: (pr) =>
+            `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${pr.lancadoEm ? escapeHtml(`${pr.lancadoEm.livro || ''} ${pr.lancadoEm.secao || ''}`.trim()) : 'ÔÇö'}</td>`,
+        justificativaMigracao: (pr) =>
+            `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${trechoNota(pr.justificativaMigracao)}</td>`,
+        pendencia: (pr) =>
+            `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${trechoNota(pr.pendencia)}</td>`,
+        descarte: (pr) =>
+            `<td class="p-4 text-xs text-gray-500 dark:text-slate-400 max-w-xs">${trechoNota(pr.descarte)}</td>`,
+        camposPreenchidos: (pr) => celulaCamposPreenchidos(pr),
     };
 
     container.innerHTML = listaPaginaPr
         .map((pr) => {
-            const pubBadge = pr.publicado
+            const pubBadge = estaPublicado(pr)
                 ? `<span class="text-[9px] bg-emerald-100 dark:bg-emerald-900 text-emerald-700 dark:text-emerald-300 px-1.5 py-0.5 rounded font-bold uppercase">pub</span>`
                 : '';
             const celulasMeio = colunasAtivas
@@ -2005,8 +2740,7 @@ export function renderProsas() {
             </td>
             ${celulasMeio}
             <td class="p-4 text-right space-x-2">
-                <button data-action="editar-prosa" data-id="${pr.id}" title="Editar" aria-label="Editar" class="inline-flex items-center justify-center bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 p-1.5 rounded hover:bg-blue-200 dark:hover:bg-blue-800">${ICONE_EDITAR}</button>
-                <button data-action="excluir-item" data-tipo="prosas" data-id="${pr.id}" title="Excluir" aria-label="Excluir" class="inline-flex items-center justify-center p-1.5 rounded text-red-400 hover:text-red-600 dark:hover:text-red-400 hover:bg-red-100 dark:hover:bg-red-900/40">${ICONE_EXCLUIR}</button>
+                ${celulaAcoesItem('prosas', 'prosa', 'prosas', pr.id)}
             </td>
         </tr>`;
         })
@@ -2078,3 +2812,190 @@ export function renderElementos() {
         .join('');
     preencherCapas(container);
 }
+
+// ÔöÇÔöÇÔöÇ Pessoas ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Cadastro central (ver migrarPessoasParaCadastro em db.js). Ordenada
+// alfabeticamente ÔÇö diferente de Livros/Partes, n├úo tem uma "ordem
+// editorial" pr├│pria pra Pessoa, ent├úo n├úo h├í bot├Áes de mover.
+
+export function renderPessoas() {
+    const container = document.getElementById('lista-pessoas');
+    if (!container) return;
+
+    const ordenadas = [...db.pessoas].sort((a, b) => a.nome.localeCompare(b.nome, 'pt-BR'));
+
+    if (ordenadas.length === 0) {
+        container.innerHTML = `<div class="col-span-full text-center text-gray-400 dark:text-slate-500 text-sm py-6">Nenhuma pessoa cadastrada ainda.</div>`;
+        return;
+    }
+
+    container.innerHTML = ordenadas
+        .map((p) => {
+            // Objetos de Grupo (n├úo s├│ nome, ver nomesGrupos em utils.js)
+            // pra cada badge poder usar a cor pr├│pria daquele grupo ÔÇö
+            // ver classesCorGrupo/CORES_GRUPO em utils.js.
+            const grupos = (p.grupoIds || [])
+                .map((id) => db.grupos.find((g) => g.id === id))
+                .filter(Boolean);
+            const { poemasIds, prosasIds } = calcularImpactoExclusaoPessoa(db, p.id);
+            const totalTextos = poemasIds.length + prosasIds.length;
+            return `
+        <div class="bg-white dark:bg-slate-900 p-4 rounded-lg border border-gray-200 dark:border-slate-700 shadow-sm flex justify-between items-center">
+            <div class="flex-1 min-w-0">
+                <h4 class="font-bold text-gray-800 dark:text-slate-100">${escapeHtml(p.nome)}</h4>
+                <div class="flex flex-wrap gap-1 mt-1">
+                    ${
+                        grupos.length
+                            ? grupos
+                                  .map(
+                                      (g) =>
+                                          `<span class="text-[9px] ${classesCorGrupo(g.cor)} px-1.5 py-0.5 rounded">${escapeHtml(g.nome)}</span>`,
+                                  )
+                                  .join('')
+                            : `<span class="text-[10px] text-gray-300 dark:text-slate-600 italic">sem grupo</span>`
+                    }
+                </div>
+                <p class="text-[10px] text-gray-400 dark:text-slate-500 font-mono mt-1">
+                    aparece em ${totalTextos} texto${totalTextos !== 1 ? 's' : ''}
+                </p>
+            </div>
+            <div class="flex gap-3 flex-shrink-0">
+                <button data-action="editar-pessoa" data-id="${p.id}" title="Editar" aria-label="Editar" class="inline-flex items-center justify-center p-1.5 rounded text-blue-600 dark:text-blue-400 hover:bg-blue-100 dark:hover:bg-blue-900/40">${ICONE_EDITAR}</button>
+                <button data-action="mesclar-item" data-tipo="pessoas" data-id="${p.id}" title="Mesclar com outra pessoa" aria-label="Mesclar" class="inline-flex items-center justify-center p-1.5 rounded text-amber-600 dark:text-amber-400 hover:bg-amber-100 dark:hover:bg-amber-900/40">${ICONE_MESCLAR}</button>
+                <button data-action="excluir-item" data-tipo="pessoas" data-id="${p.id}" title="Excluir" aria-label="Excluir" class="inline-flex items-center justify-center p-1.5 rounded text-red-400 hover:text-red-600 dark:hover:text-red-400 hover:bg-red-100 dark:hover:bg-red-900/40">${ICONE_EXCLUIR}</button>
+            </div>
+        </div>`;
+        })
+        .join('');
+}
+
+// ÔöÇÔöÇÔöÇ Grupos ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Registro dedicado (ver conversa que definiu isso: cadastr├ível de
+// verdade, n├úo tag livre) ÔÇö cria/edita/renomeia formalmente, sem
+// hardcode no c├│digo-fonte. Mostra quantas pessoas pertencem a cada
+// grupo, pra dar uma no├º├úo de impacto antes de excluir.
+
+export function renderGrupos() {
+    const container = document.getElementById('lista-grupos');
+    if (!container) return;
+
+    const ordenados = [...db.grupos].sort((a, b) => a.nome.localeCompare(b.nome, 'pt-BR'));
+
+    if (ordenados.length === 0) {
+        container.innerHTML = `<div class="col-span-full text-center text-gray-400 dark:text-slate-500 text-sm py-6">Nenhum grupo cadastrado ainda.</div>`;
+        return;
+    }
+
+    container.innerHTML = ordenados
+        .map((g) => {
+            const { pessoasIds } = calcularImpactoExclusaoGrupo(db, g.id);
+            return `
+        <div class="bg-white dark:bg-slate-900 p-4 rounded-lg border border-gray-200 dark:border-slate-700 shadow-sm flex justify-between items-center">
+            <div class="flex-1 min-w-0">
+                <h4 class="font-bold text-gray-800 dark:text-slate-100 flex items-center gap-2">
+                    <span class="inline-block w-2.5 h-2.5 rounded-full flex-shrink-0 ${pontoCorGrupo(g.cor)}" title="Cor do grupo" aria-hidden="true"></span>
+                    ${escapeHtml(g.nome)}
+                </h4>
+                <p class="text-[10px] text-gray-400 dark:text-slate-500 font-mono mt-1">
+                    ${pessoasIds.length} pessoa${pessoasIds.length !== 1 ? 's' : ''}
+                </p>
+            </div>
+            <div class="flex gap-3 flex-shrink-0">
+                <button data-action="editar-grupo" data-id="${g.id}" title="Editar" aria-label="Editar" class="inline-flex items-center justify-center p-1.5 rounded text-blue-600 dark:text-blue-400 hover:bg-blue-100 dark:hover:bg-blue-900/40">${ICONE_EDITAR}</button>
+                <button data-action="excluir-item" data-tipo="grupos" data-id="${g.id}" title="Excluir" aria-label="Excluir" class="inline-flex items-center justify-center p-1.5 rounded text-red-400 hover:text-red-600 dark:hover:text-red-400 hover:bg-red-100 dark:hover:bg-red-900/40">${ICONE_EXCLUIR}</button>
+            </div>
+        </div>`;
+        })
+        .join('');
+}
+
+// ÔöÇÔöÇÔöÇ Autores ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Cadastro central (ver migrarAutoria em db.js), ├á parte de Pessoas ÔÇö
+// mesmo esp├¡rito de renderPessoas acima, mas sem grupos (Autor n├úo tem
+// taxonomia pr├│pria) e mostrando "sobre" em vez de badges.
+
+export function renderAutores() {
+    const container = document.getElementById('lista-autores');
+    if (!container) return;
+
+    const ordenados = [...db.autores].sort((a, b) => a.nome.localeCompare(b.nome, 'pt-BR'));
+
+    if (ordenados.length === 0) {
+        container.innerHTML = `<div class="col-span-full text-center text-gray-400 dark:text-slate-500 text-sm py-6">Nenhum autor cadastrado ainda.</div>`;
+        return;
+    }
+
+    container.innerHTML = ordenados
+        .map((a) => {
+            const { poemasIds, prosasIds } = calcularImpactoExclusaoAutor(db, a.id);
+            const totalTextos = poemasIds.length + prosasIds.length;
+            return `
+        <div class="bg-white dark:bg-slate-900 p-4 rounded-lg border border-gray-200 dark:border-slate-700 shadow-sm flex justify-between items-center">
+            <div class="flex-1 min-w-0">
+                <h4 class="font-bold text-gray-800 dark:text-slate-100">${escapeHtml(a.nome)}</h4>
+                ${
+                    a.sobre
+                        ? `<p class="text-[10px] text-gray-400 dark:text-slate-500 mt-1 line-clamp-2">${escapeHtml(a.sobre)}</p>`
+                        : ''
+                }
+                <p class="text-[10px] text-gray-400 dark:text-slate-500 font-mono mt-1">
+                    aparece em ${totalTextos} texto${totalTextos !== 1 ? 's' : ''}
+                </p>
+            </div>
+            <div class="flex gap-3 flex-shrink-0">
+                <button data-action="editar-autor" data-id="${a.id}" title="Editar" aria-label="Editar" class="inline-flex items-center justify-center p-1.5 rounded text-blue-600 dark:text-blue-400 hover:bg-blue-100 dark:hover:bg-blue-900/40">${ICONE_EDITAR}</button>
+                <button data-action="excluir-item" data-tipo="autores" data-id="${a.id}" title="Excluir" aria-label="Excluir" class="inline-flex items-center justify-center p-1.5 rounded text-red-400 hover:text-red-600 dark:hover:text-red-400 hover:bg-red-100 dark:hover:bg-red-900/40">${ICONE_EXCLUIR}</button>
+            </div>
+        </div>`;
+        })
+        .join('');
+}
+
+// ÔöÇÔöÇÔöÇ ├ëpocas ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Cadastro central (ver migrarEpocas em db.js), item 3 do plano de
+// schema ÔÇö mesmo esp├¡rito de renderAutores acima: sem grupos/badges
+// pr├│prios, mostrando o "contextoRelacao" (ex.: "Parceiro A e Parceiro
+// B") em vez de "sobre". Contagem considera Poema e Prosa (corrigido
+// numa sess├úo posterior ÔÇö ver coment├írio de calcularImpactoExclusaoEpoca
+// em db.js; coment├írio antigo aqui dizia "s├│ Poema" por resqu├¡cio do
+// mesmo gap). Bot├úo "Mesclar" (ver renderPessoas acima e Mesclar
+// (Pessoa/├ëpoca) em forms.js) ÔÇö mesma lacuna de duplicata por rename
+// existe pra ├ëpoca.
+
+export function renderEpocas() {
+    const container = document.getElementById('lista-epocas');
+    if (!container) return;
+
+    const ordenadas = [...db.epocas].sort((a, b) => a.nome.localeCompare(b.nome, 'pt-BR'));
+
+    if (ordenadas.length === 0) {
+        container.innerHTML = `<div class="col-span-full text-center text-gray-400 dark:text-slate-500 text-sm py-6">Nenhuma ├®poca cadastrada ainda.</div>`;
+        return;
+    }
+
+    container.innerHTML = ordenadas
+        .map((ep) => {
+            const { poemasIds, prosasIds } = calcularImpactoExclusaoEpoca(db, ep.id);
+            const total = poemasIds.length + prosasIds.length;
+            return `
+        <div class="bg-white dark:bg-slate-900 p-4 rounded-lg border border-gray-200 dark:border-slate-700 shadow-sm flex justify-between items-center">
+            <div class="flex-1 min-w-0">
+                <h4 class="font-bold text-gray-800 dark:text-slate-100">${escapeHtml(ep.nome)}</h4>
+                ${
+                    ep.contextoRelacao
+                        ? `<p class="text-[10px] text-gray-400 dark:text-slate-500 mt-1 line-clamp-2">${escapeHtml(ep.contextoRelacao)}</p>`
+                        : ''
+                }
+                <p class="text-[10px] text-gray-400 dark:text-slate-500 font-mono mt-1">
+                    aparece em ${total} texto${total !== 1 ? 's' : ''}
+                </p>
+            </div>
+            <div class="flex gap-3 flex-shrink-0">
+                <button data-action="editar-epoca" data-id="${ep.id}" title="Editar" aria-label="Editar" class="inline-flex items-center justify-center p-1.5 rounded text-blue-600 dark:text-blue-400 hover:bg-blue-100 dark:hover:bg-blue-900/40">${ICONE_EDITAR}</button>
+                <button data-action="mesclar-item" data-tipo="epocas" data-id="${ep.id}" title="Mesclar com outra ├®poca" aria-label="Mesclar" class="inline-flex items-center justify-center p-1.5 rounded text-amber-600 dark:text-amber-400 hover:bg-amber-100 dark:hover:bg-amber-900/40">${ICONE_MESCLAR}</button>
+                <button data-action="excluir-item" data-tipo="epocas" data-id="${ep.id}" title="Excluir" aria-label="Excluir" class="inline-flex items-center justify-center p-1.5 rounded text-red-400 hover:text-red-600 dark:hover:text-red-400 hover:bg-red-100 dark:hover:bg-red-900/40">${ICONE_EXCLUIR}</button>
+            </div>
+        </div>`;
+        })
+        .join('');
+}
diff --git a/js/render.js b/js/render.js
index 2e20b0b..646cd1f 100644
--- a/js/render.js
+++ b/js/render.js
@@ -20,6 +20,10 @@ import {
     renderPoemas,
     renderProsas,
     renderElementos,
+    renderPessoas,
+    renderGrupos,
+    renderAutores,
+    renderEpocas,
 } from './render-listas.js';
 import { popularSeletorEstrutura, renderEstrutura } from './render-estrutura.js';
 
@@ -27,6 +31,10 @@ export function renderLists() {
     renderLivros();
     renderPartes();
     renderSecoes();
+    renderPessoas();
+    renderGrupos();
+    renderAutores();
+    renderEpocas();
     renderPoemas();
     renderProsas();
     renderElementos();
diff --git a/js/ui.js b/js/ui.js
index c3a6996..a2962ea 100644
--- a/js/ui.js
+++ b/js/ui.js
@@ -5,15 +5,31 @@
 
 import { db } from './db.js';
 import {
-    resetTags,
+    resetSinalizacoes,
     resetPessoas,
+    resetAutoria,
+    resetEnvios,
+    resetReconhecimentos,
     atualizarDatalist,
     resetIntertextualidade,
     resetAnexos,
     resetAnotacoes,
+    resetElos,
+    resetReferencias,
+    renderPainelElosDerivados,
+    atualizarRotulosDirecaoElo,
+    atualizarRotulosDirecaoEloProsa,
 } from './editor.js';
-import { extrairFasesUnicas, escapeHtml } from './utils.js';
+import {
+    extrairFasesUnicas,
+    escapeHtml,
+    RELACOES_ELO,
+    TIPOS_REFERENCIA,
+    CORES_GRUPO_PADRAO,
+} from './utils.js';
 import { toggleModal, garantirModal } from './modais.js';
+import { renderEstatisticas } from './estatisticas.js';
+import { renderConexoes } from './render-conexoes.js';
 
 // Reexportados pra quem j├í importava toggleModal a partir de ui.js
 // (forms.js, coletaneas.js, main.js) n├úo precisar trocar o caminho.
@@ -28,6 +44,197 @@ export function openTab(tabName) {
     if (targetBtn) targetBtn.classList.add('active-tab');
 }
 
+// ÔöÇÔöÇÔöÇ Navega├º├úo agrupada: dropdowns no desktop + menu hamb├║rguer (mobile) ÔöÇ
+// As 11 abas, organizadas nos 4 grupos combinados com Victor: Estrutura
+// do acervo, Conte├║do, An├ílise, e Exporta├º├úo sozinha. ├Ünica fonte de
+// verdade tanto pro painel mobile quanto pro destaque do grupo ativo na
+// nav desktop ÔÇö a nav desktop em si (os dropdowns, que abrem no hover
+// via CSS puro, ver .nav-dropdown em style.css) continua est├ítica no
+// HTML, s├│ o "qual bot├úo de grupo fica destacado" ├® calculado daqui.
+export const GRUPOS_NAV = [
+    {
+        id: 'estrutura-acervo',
+        rotulo: 'Estrutura do acervo',
+        abas: [
+            { id: 'livros', rotulo: 'Livros' },
+            { id: 'coletaneas', rotulo: 'Colet├óneas' },
+            { id: 'partes', rotulo: 'Partes' },
+            { id: 'secoes', rotulo: 'Se├º├Áes' },
+            { id: 'pessoas', rotulo: 'Pessoas' },
+            { id: 'grupos', rotulo: 'Grupos' },
+            { id: 'autores', rotulo: 'Autores' },
+            { id: 'epocas', rotulo: '├ëpocas' },
+        ],
+    },
+    {
+        id: 'conteudo',
+        rotulo: 'Conte├║do',
+        abas: [
+            { id: 'poemas', rotulo: 'Poemas' },
+            { id: 'prosas', rotulo: 'Prosas' },
+            { id: 'elementos', rotulo: 'Elementos Textuais' },
+        ],
+    },
+    {
+        id: 'analise',
+        rotulo: 'An├ílise',
+        abas: [
+            { id: 'estrutura', rotulo: 'Estrutura' },
+            { id: 'conexoes', rotulo: 'Conex├Áes' },
+            { id: 'estatisticas', rotulo: 'Estat├¡sticas' },
+        ],
+    },
+    {
+        id: 'exportacao',
+        rotulo: 'Exporta├º├úo',
+        abas: [{ id: 'exportar-filtrado', rotulo: 'Exporta├º├úo' }],
+    },
+];
+
+// Grupo "Ferramentas" (Localizar e Substituir, Vers├Áes Alternativas): fora
+// do GRUPOS_NAV porque s├úo links pra p├íginas separadas, n├úo abas trocadas
+// via abrirAba/openTab dentro desta SPA ÔÇö por isso ficam de fora de
+// TODAS_ABAS e de atualizarGrupoAtivoNavDesktop (n├úo h├í "aba ativa" pra
+// destacar aqui). Ainda assim precisam aparecer no menu mobile ÔÇö ver
+// renderMenuMobile logo abaixo ÔÇö sen├úo ficam inacess├¡veis abaixo do
+// breakpoint md, j├í que a nav desktop (onde moram como dropdown por CSS
+// puro) some inteira no mobile.
+const FERRAMENTAS_NAV = [
+    { href: 'localizar-substituir.html', rotulo: 'Localizar e Substituir' },
+    { href: 'filtrar.html', rotulo: 'Vers├Áes Alternativas' },
+];
+
+const TODAS_ABAS = GRUPOS_NAV.flatMap((g) => g.abas);
+
+// Ponto de entrada ├║nico pra trocar de aba (chamado tanto pelos
+// dropdowns da nav desktop quanto pelo painel mobile): faz o que
+// openTab j├í fazia, mais o rec├ílculo sob demanda que Conex├Áes/
+// Estat├¡sticas precisam (antes vivia espalhado em onclick inline no
+// HTML), atualiza o destaque do grupo ativo e fecha o painel mobile
+// se estiver aberto, j├í que selecionar uma aba ├® o sinal claro de que
+// o usu├írio terminou de navegar.
+export function abrirAba(tabName) {
+    openTab(tabName);
+    if (tabName === 'conexoes') renderConexoes();
+    if (tabName === 'estatisticas') renderEstatisticas();
+    atualizarRotuloAbaAtual(tabName);
+    atualizarGrupoAtivoNavDesktop(tabName);
+    fecharDropdownNavDesktop(tabName);
+    fecharMenuMobile();
+}
+
+function atualizarRotuloAbaAtual(tabName) {
+    const rotuloEl = document.getElementById('rotulo-aba-atual');
+    if (!rotuloEl) return;
+    const aba = TODAS_ABAS.find((a) => a.id === tabName);
+    rotuloEl.textContent = aba ? aba.rotulo : '';
+}
+
+// Destaca (borda azul embaixo, mesmo visual do .active-tab) o bot├úo do
+// grupo que cont├®m a aba aberta ÔÇö s├│ existe bot├úo de grupo pros 3 com
+// dropdown; Exporta├º├úo ├® uma aba solta, j├í destacada pelo openTab.
+function atualizarGrupoAtivoNavDesktop(tabName) {
+    document
+        .querySelectorAll('.grupo-nav-btn')
+        .forEach((b) => b.classList.remove('nav-grupo-ativo'));
+    const grupo = GRUPOS_NAV.find((g) => g.abas.some((a) => a.id === tabName));
+    if (!grupo) return;
+    const btn = document.getElementById(`grupo-${grupo.id}-btn`);
+    if (btn) btn.classList.add('nav-grupo-ativo');
+}
+
+// Clicar num item do dropdown n├úo tira o mouse de cima da ├írea ÔÇö sem
+// isso, o :hover em style.css manteria o dropdown do grupo aberto por
+// cima da aba rec├®m-aberta. Fecha na hora via classe pr├│pria
+// (.dropdown-fechado-manual, ver style.css) e s├│ devolve o dropdown ao
+// controle normal do :hover quando o mouse sai de fato (mouseleave),
+// pra n├úo precisar ficar rastreando estado depois disso. Tamb├®m tira o
+// foco do bot├úo clicado, sen├úo o :focus-within reabriria o dropdown j├í
+// no primeiro mouseleave (antes do usu├írio voltar a interagir com ele).
+function fecharDropdownNavDesktop(tabName) {
+    document.activeElement?.blur();
+    const btn = document.getElementById('btn-' + tabName);
+    const wrap = btn?.closest('.nav-dropdown-wrap');
+    if (!wrap) return;
+    wrap.classList.add('dropdown-fechado-manual');
+    wrap.addEventListener('mouseleave', () => wrap.classList.remove('dropdown-fechado-manual'), {
+        once: true,
+    });
+}
+
+function renderMenuMobile() {
+    const painel = document.getElementById('painel-menu-mobile');
+    if (!painel) return;
+    const atual = document.querySelector('.tab-content.active')?.id;
+    painel.innerHTML = GRUPOS_NAV.map(
+        (grupo) => `
+        <div class="mb-3 last:mb-0">
+            <div class="text-[10px] font-bold uppercase text-gray-400 dark:text-slate-500 px-2 mb-1">
+                ${escapeHtml(grupo.rotulo)}
+            </div>
+            ${grupo.abas
+                .map(
+                    (aba) => `
+                <button type="button" onclick="abrirAba('${aba.id}')"
+                    class="w-full text-left text-sm py-2 px-2 rounded-md hover:bg-gray-50 dark:hover:bg-slate-700 dark:text-slate-200 ${
+                        aba.id === atual
+                            ? 'bg-blue-50 dark:bg-blue-950 text-blue-700 dark:text-blue-300 font-bold'
+                            : ''
+                    }">
+                    ${escapeHtml(aba.rotulo)}
+                </button>`,
+                )
+                .join('')}
+        </div>`,
+    ).join('');
+    painel.innerHTML += `
+        <div class="mb-3 last:mb-0">
+            <div class="text-[10px] font-bold uppercase text-gray-400 dark:text-slate-500 px-2 mb-1">
+                Ferramentas
+            </div>
+            ${FERRAMENTAS_NAV.map(
+                (item) => `
+                <a href="${item.href}"
+                    class="block w-full text-left text-sm py-2 px-2 rounded-md hover:bg-gray-50 dark:hover:bg-slate-700 dark:text-slate-200">
+                    ${escapeHtml(item.rotulo)}
+                </a>`,
+            ).join('')}
+        </div>`;
+}
+
+// Abre/fecha o painel mobile (dropdown abaixo do bot├úo hamb├║rguer,
+// mesmo princ├¡pio de togglePainel ÔÇö s├│ que com conte├║do montado na
+// hora, pra sempre refletir a aba ativa quando reaberto) e alterna o
+// ├¡cone entre Ôÿ░ e Ô£ò.
+export function toggleMenuMobile() {
+    const painel = document.getElementById('painel-menu-mobile');
+    const icone = document.getElementById('icone-menu-mobile');
+    if (!painel) return;
+    const vaiAbrir = painel.classList.contains('hidden');
+    if (vaiAbrir) renderMenuMobile();
+    painel.classList.toggle('hidden');
+    if (icone) icone.textContent = vaiAbrir ? 'Ô£ò' : 'Ôÿ░';
+}
+
+function fecharMenuMobile() {
+    const painel = document.getElementById('painel-menu-mobile');
+    const icone = document.getElementById('icone-menu-mobile');
+    if (!painel || painel.classList.contains('hidden')) return;
+    painel.classList.add('hidden');
+    if (icone) icone.textContent = 'Ôÿ░';
+}
+
+// Chamada no load (main.js) pra sincronizar o r├│tulo do bot├úo
+// hamb├║rguer e o destaque de grupo na nav desktop com a aba que j├í
+// vem ativa no HTML (Livros).
+export function initNav() {
+    const atual = document.querySelector('.tab-content.active')?.id;
+    if (atual) {
+        atualizarRotuloAbaAtual(atual);
+        atualizarGrupoAtivoNavDesktop(atual);
+    }
+}
+
 // ÔöÇÔöÇÔöÇ Helpers de hierarquia (pra saber a qual Livro algo pertence) ÔöÇÔöÇ
 
 function livroIdDaParte(parteId) {
@@ -289,22 +496,121 @@ export function renderDropdowns() {
             .map((l) => `<option value="${l.id}">${escapeHtml(l.titulo)}</option>`)
             .join('');
 
-    // 6. Elos e Refer├¬ncias entre poemas
-    const sElos = document.getElementById('p-elos-select');
-    const sRefs = document.getElementById('p-refs-select');
-    if (sElos && sRefs) {
-        const opcoes = [...db.poemas]
-            .sort((a, b) => a.titulo.localeCompare(b.titulo))
-            .map((p) => `<option value="${p.id}">${escapeHtml(p.titulo)}</option>`)
+    // 6. Elos e Refer├¬ncias entre poemas ÔÇö selects de poema-alvo (single),
+    //    usados pela linha de "adicionar" de cada lista (ver editor.js:
+    //    adicionarElo/adicionarReferencia). Repovoados a cada render pra
+    //    refletir poemas criados/renomeados depois que o modal abriu.
+    const sEloPoema = document.getElementById('p-elo-poema');
+    const sRefPoema = document.getElementById('p-ref-poema');
+    if (sEloPoema || sRefPoema) {
+        const opcoes =
+            '<option value="">Selecione um poema...</option>' +
+            [...db.poemas]
+                .sort((a, b) => a.titulo.localeCompare(b.titulo))
+                .map((p) => `<option value="${p.id}">${escapeHtml(p.titulo)}</option>`)
+                .join('');
+        if (sEloPoema) sEloPoema.innerHTML = opcoes;
+        if (sRefPoema) sRefPoema.innerHTML = opcoes;
+    }
+    // Rela├º├úo de Elos / Tipo de Refer├¬ncias ÔÇö Elos usa as 8 rela├º├Áes
+    // fechadas (RELACOES_ELO em utils.js, ver redesenho Rela├º├úo+Dire├º├úo);
+    // Refer├¬ncias continua com sua lista fechada simples de tipos
+    // (TIPOS_REFERENCIA), sem Dire├º├úo. Populadas aqui, e n├úo hardcoded
+    // no HTML, pra ter uma ├║nica fonte de verdade caso a lista mude.
+    const sEloRelacao = document.getElementById('p-elo-relacao');
+    const sRefTipo = document.getElementById('p-ref-tipo');
+    if (sEloRelacao) {
+        sEloRelacao.innerHTML =
+            '<option value="">Rela├º├úo...</option>' +
+            RELACOES_ELO.map(
+                (r) => `<option value="${escapeHtml(r)}">${escapeHtml(r)}</option>`,
+            ).join('');
+        atualizarRotulosDirecaoElo();
+    }
+    if (sRefTipo) {
+        sRefTipo.innerHTML =
+            '<option value="">Tipo...</option>' +
+            TIPOS_REFERENCIA.map(
+                (t) => `<option value="${escapeHtml(t)}">${escapeHtml(t)}</option>`,
+            ).join('');
+    }
+
+    // 6b. Livros m├║ltiplos no Modal de Prosas (item 4 ÔÇö mesmo campo de
+    //     Poema, etiqueta editorial paralela, colet├óneas exclu├¡das).
+    const sProsaLivros = document.getElementById('pr-livros');
+    if (sProsaLivros)
+        sProsaLivros.innerHTML = db.livros
+            .filter((l) => l.tipo !== 'Colet├ónea')
+            .map((l) => `<option value="${l.id}">${escapeHtml(l.titulo)}</option>`)
             .join('');
-        sElos.innerHTML = opcoes;
-        sRefs.innerHTML = opcoes;
+
+    // 6c. Elos e Refer├¬ncias no Modal de Prosas (item 4) ÔÇö selects de
+    //     texto-alvo (single), mas agora com Poemas E Prosas juntos num
+    //     optgroup por tipo (um texto do acervo pode se ligar tanto a um
+    //     poema quanto a outra prosa) ÔÇö diferente do Modal de Poemas
+    //     acima, que por ora s├│ oferece outros Poemas como alvo.
+    const sEloProsa = document.getElementById('pr-elo-poema');
+    const sRefProsa = document.getElementById('pr-ref-poema');
+    if (sEloProsa || sRefProsa) {
+        const opcoesProsa =
+            '<option value="">Selecione um texto...</option>' +
+            '<optgroup label="Poemas">' +
+            [...db.poemas]
+                .sort((a, b) => a.titulo.localeCompare(b.titulo))
+                .map((p) => `<option value="${p.id}">${escapeHtml(p.titulo)}</option>`)
+                .join('') +
+            '</optgroup>' +
+            '<optgroup label="Prosas">' +
+            [...db.prosas]
+                .sort((a, b) => a.titulo.localeCompare(b.titulo))
+                .map((pr) => `<option value="${pr.id}">${escapeHtml(pr.titulo)}</option>`)
+                .join('') +
+            '</optgroup>';
+        if (sEloProsa) sEloProsa.innerHTML = opcoesProsa;
+        if (sRefProsa) sRefProsa.innerHTML = opcoesProsa;
+    }
+    const sEloRelacaoProsa = document.getElementById('pr-elo-relacao');
+    const sRefTipoProsa = document.getElementById('pr-ref-tipo');
+    if (sEloRelacaoProsa) {
+        sEloRelacaoProsa.innerHTML =
+            '<option value="">Rela├º├úo...</option>' +
+            RELACOES_ELO.map(
+                (r) => `<option value="${escapeHtml(r)}">${escapeHtml(r)}</option>`,
+            ).join('');
+        atualizarRotulosDirecaoEloProsa();
+    }
+    if (sRefTipoProsa) {
+        sRefTipoProsa.innerHTML =
+            '<option value="">Tipo...</option>' +
+            TIPOS_REFERENCIA.map(
+                (t) => `<option value="${escapeHtml(t)}">${escapeHtml(t)}</option>`,
+            ).join('');
     }
 
     // 7. Destino de Prosas (Livro, Parte ou Se├º├úo) ÔÇö sem filtro por padr├úo
     const destinoProsa = document.getElementById('pr-destino');
     if (destinoProsa) destinoProsa.innerHTML = construirOptionsDestino('', false);
     popularFiltroLivro('pr-destino-filtro');
+
+    // 8. Grupos ÔåÆ checkboxes do Modal de Pessoa. Sempre reconstru├¡do
+    // (e portanto sempre desmarcado) ÔÇö editarPessoa() marca de volta os
+    // que a pessoa j├í pertence logo em seguida, depois desta chamada.
+    const cGrupos = document.getElementById('ps-grupos-container');
+    if (cGrupos) {
+        const ordenados = [...db.grupos].sort((a, b) => a.nome.localeCompare(b.nome, 'pt-BR'));
+        cGrupos.innerHTML = ordenados.length
+            ? ordenados
+                  .map(
+                      (g) => `
+                <label class="flex items-center gap-1.5 text-sm cursor-pointer">
+                    <input type="checkbox" value="${g.id}"
+                        class="rounded border-gray-300 dark:border-slate-600 text-rose-500 focus:ring-rose-400" />
+                    ${escapeHtml(g.nome)}
+                </label>`,
+                  )
+                  .join('')
+            : `<p class="text-xs text-gray-400 dark:text-slate-500">Nenhum grupo cadastrado ainda ÔÇö use "+ Criar novo grupo" acima.</p>`;
+    }
 }
 
 // Quando o destino (Livro/Parte/Se├º├úo) escolhido pertence a um Livro que
@@ -349,30 +655,61 @@ export async function prepararNovo(tipo) {
         prosa: 'pr-edit-id',
         elemento: 'el-edit-id',
         coletanea: 'col-edit-id',
+        pessoa: 'ps-edit-id',
+        grupo: 'g-edit-id',
+        autor: 'au-edit-id',
+        epoca: 'ep-edit-id',
     };
 
     const idField = document.getElementById(idFields[tipo]);
     if (idField) idField.value = '';
 
     if (tipo === 'poema') {
-        resetTags();
+        resetSinalizacoes();
         resetPessoas();
+        resetAutoria();
+        resetEnvios();
+        resetReconhecimentos();
         resetIntertextualidade();
         resetAnexos();
         resetAnotacoes();
+        resetElos();
+        resetReferencias();
+        renderPainelElosDerivados(null);
         atualizarDatalist();
         const infoP = document.getElementById('p-coletaneas-info');
         if (infoP) infoP.innerHTML = '';
     }
 
     if (tipo === 'prosa') {
-        // resetTagsProsa e resetPessoasProsa s├úo importadas dinamicamente
-        // pra evitar ciclo de importa├º├úo (editor ÔåÆ ui n├úo existe)
+        // resetSinalizacoesProsa e resetPessoasProsa s├úo importadas
+        // dinamicamente pra evitar ciclo de importa├º├úo (editor ÔåÆ ui n├úo existe)
         import('./editor.js').then(
-            ({ resetTagsProsa, resetPessoasProsa, resetGeneroProsa, atualizarDatalistProsa }) => {
-                resetTagsProsa();
+            ({
+                resetSinalizacoesProsa,
+                resetPessoasProsa,
+                resetAutoriaProsa,
+                resetEnviosProsa,
+                resetReconhecimentosProsa,
+                resetGeneroProsa,
+                atualizarDatalistProsa,
+                resetIntertextualidadeProsa,
+                resetAnexosProsa,
+                resetElosProsa,
+                resetReferenciasProsa,
+                renderPainelElosDerivadosProsa,
+            }) => {
+                resetSinalizacoesProsa();
                 resetPessoasProsa();
+                resetAutoriaProsa();
+                resetEnviosProsa();
+                resetReconhecimentosProsa();
                 resetGeneroProsa();
+                resetIntertextualidadeProsa();
+                resetAnexosProsa();
+                resetElosProsa();
+                resetReferenciasProsa();
+                renderPainelElosDerivadosProsa(null);
                 atualizarDatalistProsa();
             },
         );
@@ -380,6 +717,18 @@ export async function prepararNovo(tipo) {
         if (infoPr) infoPr.innerHTML = '';
     }
 
+    if (tipo === 'grupo') {
+        // Import din├ómico pra evitar ciclo (forms ÔåÆ ui j├í existe, ver
+        // resetPessoasProsa acima pelo mesmo motivo). Sem cor marcada
+        // ainda no form novo: seleciona a primeira da paleta por padr├úo
+        // (mesmo padr├úo do fallback em classesCorGrupo, ver utils.js).
+        import('./forms.js').then(({ renderSeletorCorGrupo }) => {
+            const corField = document.getElementById('g-cor');
+            if (corField) corField.value = CORES_GRUPO_PADRAO;
+            renderSeletorCorGrupo(CORES_GRUPO_PADRAO);
+        });
+    }
+
     const title = document.getElementById(`modal-${tipo}-titulo`);
     if (title) title.innerText = `Novo(a) ${tipo.charAt(0).toUpperCase() + tipo.slice(1)}`;
 
diff --git a/js/utils.js b/js/utils.js
index ec3fede..12482ba 100644
--- a/js/utils.js
+++ b/js/utils.js
@@ -339,6 +339,13 @@ export function getPosicaoElemento(el, db) {
 // render-listas.js). Vive aqui por ser uma utilidade gen├®rica de UI
 // sem depend├¬ncia de estado interno ÔÇö qualquer m├│dulo pode importar.
 //
+// `acaoSecundaria` ├® opcional ÔÇö usado quando a decis├úo n├úo ├® um
+// simples sim/n├úo, mas tem uma terceira sa├¡da al├®m de "Confirmar" e
+// "Cancelar" (ex.: nome duplicado ao editar Pessoa/├ëpoca, ver
+// initFormPessoa/initFormEpoca em forms.js: "Mesclar agora" vs "Salvar
+// mesmo assim" vs "Cancelar"). Fica escondido quando n├úo informado ÔÇö
+// quem j├í chama abrirModalConfirmacao sem essa op├º├úo n├úo muda nada.
+//
 // Uso:
 //   abrirModalConfirmacao({
 //       titulo: 'T├¡tulo do item',
@@ -346,15 +353,17 @@ export function getPosicaoElemento(el, db) {
 //       mensagem: 'Descri├º├úo do que vai acontecer.',
 //       textoConfirmar: 'Confirmar',
 //       corConfirmar: '#dc2626',
+//       acaoSecundaria: { texto: 'Fazer outra coisa', onClick: () => {} },
 //       onConfirmar: () => { /* executa a a├º├úo */ }
 //   });
 
 export function abrirModalConfirmacao({
     titulo,
     rotulo,
-    mensagem = 'Esta a├º├úo ├® permanente e n├úo pode ser desfeita.',
+    mensagem = 'Voc├¬ ter├í alguns segundos pra desfazer depois de confirmar ÔÇö passado esse tempo, a exclus├úo ├® permanente.',
     textoConfirmar = 'Confirmar',
     corConfirmar = '#dc2626',
+    acaoSecundaria = null,
     onConfirmar,
 }) {
     let overlay = document.getElementById('modal-confirmar-exclusao');
@@ -385,13 +394,18 @@ export function abrirModalConfirmacao({
                 id="excl-titulo"></h3>
             <p style="margin:0 0 24px; font-size:13px; color:#6b7280;"
                id="excl-mensagem"></p>
-            <div style="display:flex; gap:10px; justify-content:flex-end;">
+            <div style="display:flex; gap:10px; justify-content:flex-end; flex-wrap:wrap;">
                 <button id="excl-cancelar"
                     style="padding:8px 18px; border-radius:8px; border:1px solid #e5e7eb;
                            background:#fff; color:#374151; font-size:13px; font-weight:600;
                            cursor:pointer;">
                     Cancelar
                 </button>
+                <button id="excl-secundaria"
+                    style="display:none; padding:8px 18px; border-radius:8px; border:1px solid #e5e7eb;
+                           background:#fff; color:#374151; font-size:13px; font-weight:600;
+                           cursor:pointer;">
+                </button>
                 <button id="excl-confirmar"
                     style="padding:8px 18px; border-radius:8px; border:none;
                            color:#fff; font-size:13px; font-weight:600;
@@ -414,6 +428,7 @@ export function abrirModalConfirmacao({
     document.getElementById('excl-mensagem').textContent = mensagem;
 
     const btnCancelar = document.getElementById('excl-cancelar');
+    const btnSecundaria = document.getElementById('excl-secundaria');
     const btnConfirmar = document.getElementById('excl-confirmar');
     btnCancelar.onclick = _fecharModalExclusao;
     btnConfirmar.onclick = () => {
@@ -423,6 +438,18 @@ export function abrirModalConfirmacao({
     btnConfirmar.textContent = textoConfirmar;
     btnConfirmar.style.background = corConfirmar;
 
+    if (acaoSecundaria) {
+        btnSecundaria.style.display = '';
+        btnSecundaria.textContent = acaoSecundaria.texto;
+        btnSecundaria.onclick = () => {
+            _fecharModalExclusao();
+            acaoSecundaria.onClick();
+        };
+    } else {
+        btnSecundaria.style.display = 'none';
+        btnSecundaria.onclick = null;
+    }
+
     overlay.style.display = 'flex';
     setTimeout(() => btnCancelar.focus(), 0);
 }
@@ -765,9 +792,57 @@ export function violaOrdemDeDatas(anterior, posterior) {
 // terceiro estado al├®m de "preenchido"/"vazio": `na: true` significa
 // "n├úo se aplica" (marcado deliberadamente), distinto de "ainda n├úo
 // categorizado" (o campo inteiro null). Ver epocaRetratada em forms.js.
-export function formatarEpocaRetratada(epoca) {
+//
+// Item 3 do plano de schema: ├ëpoca deixou de guardar o nome como texto
+// livre (`epocaRetratada.nome`) e passou a referenciar por ID um
+// cadastro central pr├│prio (`db.epocas`, `{id, nome, contextoRelacao,
+// notas}` ÔÇö ver migrarEpocas/obterOuCriarEpocaPorNome em db.js), mesmo
+// padr├úo j├í validado em Pessoas/Autores. `epocaRetratada` guarda s├│
+// `{ epocaId, inicio, fim, recorte, na }` ÔÇö quem a ├®poca ├ë (nome,
+// contexto do relacionamento, notas) mora uma vez s├│ no cadastro; o
+// recorte de "momento vs. repercuss├úo" (ver RECORTES_EPOCA abaixo)
+// continua por item, j├í que o mesmo per├¡odo pode ser retratado como
+// evento pontual num poema e como efeito posterior noutro.
+export const RECORTES_EPOCA = ['momento', 'repercuss├úo'];
+
+export const ROTULOS_RECORTE_EPOCA = {
+    momento: 'Momento',
+    repercuss├úo: 'Momento e Repercuss├úo',
+};
+
+// Resolve epocaRetratada.epocaId pro nome cadastrado ÔÇö dado ainda n├úo
+// migrado (schema antigo, `.nome` cru) cai no fallback direto, mesmo
+// crit├®rio defensivo j├í usado em nomesPessoas/paresAutoria pra dado que
+// ainda n├úo passou pela migra├º├úo.
+export function nomeEpoca(epoca, epocasCadastro = []) {
+    if (!epoca) return '';
+    if (epoca.epocaId) {
+        return epocasCadastro.find((e) => e.id == epoca.epocaId)?.nome || '';
+    }
+    return epoca.nome || '';
+}
+
+// Resolve epocaRetratada.epocaId pro Contexto do relacionamento
+// cadastrado (db.epocas.contextoRelacao, ex. "Pedro e Victor" ÔÇö ver
+// modal-epoca.html). Dado ainda n├úo migrado (schema antigo, sem
+// epocaId) n├úo tem cadastro pra resolver, ent├úo n├úo tem contexto.
+// Mesmo crit├®rio defensivo de nomeEpoca.
+export function contextoRelacaoEpoca(epoca, epocasCadastro = []) {
+    if (!epoca?.epocaId) return '';
+    return epocasCadastro.find((e) => e.id == epoca.epocaId)?.contextoRelacao || '';
+}
+
+export function formatarEpocaRetratada(epoca, epocasCadastro = []) {
     if (!epoca) return 'ÔÇö';
-    if (epoca.na) return epoca.nome ? `${epoca.nome} (N/A)` : 'N/A';
+    const nome = nomeEpoca(epoca, epocasCadastro);
+    // Contexto do relacionamento entra na frente, separado por "ÔÇó"
+    // (mesmo padr├úo pedido pro badge da coluna, s├│ que aqui sempre
+    // vis├¡vel ÔÇö na exporta├º├úo/Visualiza├º├úo n├úo h├í espa├ºo de tabela
+    // pra restringir, nem hover pra esconder atr├ís). Ex.: "Pedro e
+    // Victor ÔÇó Namoro (2019 ÔÇô 2021)".
+    const contexto = contextoRelacaoEpoca(epoca, epocasCadastro);
+    const prefixo = contexto ? `${contexto} ÔÇó ` : '';
+    if (epoca.na) return nome ? `${prefixo}${nome} (N/A)` : 'N/A';
     const ini = formatarDataParcial(epoca.inicio);
     const fim = formatarDataParcial(epoca.fim);
     const intervalo =
@@ -778,8 +853,34 @@ export function formatarEpocaRetratada(epoca) {
               : fim !== 'ÔÇö'
                 ? `At├® ${fim}`
                 : '';
-    if (epoca.nome) return intervalo ? `${epoca.nome} (${intervalo})` : epoca.nome;
-    return intervalo || 'ÔÇö';
+    // "repercuss├úo" (r├│tulo "Momento e Repercuss├úo") ├® marcado inline,
+    // logo depois do nome e antes do par├¬nteses de datas ("Nome e p├│s
+    // (datas)") ÔÇö n├úo mais num colchete solto no fim ("Nome (datas)
+    // [Repercuss├úo (e P├│s)]"), que lia como se o colchete falasse da
+    // repercuss├úo em si e de um "p├│s-repercuss├úo", em vez de "o
+    // momento e o que veio depois dele". "momento" (s├│ o evento) segue
+    // marcado em colchete no fim, j├í que n├úo tem ambiguidade de leitura.
+    const posRepercussao = epoca.recorte === 'repercuss├úo' ? ' e p├│s' : '';
+    const recorte =
+        epoca.recorte && epoca.recorte !== 'repercuss├úo'
+            ? ` [${ROTULOS_RECORTE_EPOCA[epoca.recorte] || epoca.recorte}]`
+            : '';
+    if (nome) {
+        return (
+            prefixo +
+            (intervalo ? `${nome}${posRepercussao} (${intervalo})` : `${nome}${posRepercussao}`) +
+            recorte
+        );
+    }
+    // Sem nome, o contexto (se houver) fica sozinho ÔÇö caso raro, ├®poca
+    // sem nome s├│ existe em dado legado n├úo migrado, que tamb├®m n├úo
+    // teria epocaId pra resolver contexto, ent├úo `prefixo` ├® sempre ''
+    // aqui na pr├ítica; mantido por simetria caso o schema mude.
+    return (
+        prefixo +
+        (intervalo ? `${intervalo}${posRepercussao}` : posRepercussao ? 'e p├│s' : 'ÔÇö') +
+        recorte
+    );
 }
 
 // S├│ o intervalo textual de ├ëpoca Retratada, sem o nome do per├¡odo ÔÇö
@@ -798,33 +899,21 @@ export function formatarIntervaloEpocaRetratada(epoca) {
     return 'ÔÇö';
 }
 
-// Nomes de per├¡odo j├í usados em ├ëpoca Retratada, pra autocompletar o
-// campo "Nome do per├¡odo" (ver p-epoca-nome em modal-poema.html) ÔÇö mesma
-// l├│gica de extrairValoresUnicosDeIntertextualidade, s├│ que sobre
-// epocaRetratada.nome.
-export function extrairNomesEpocaUnicos(poemas) {
-    const nomes = new Set();
-    poemas.forEach((p) => {
-        if (p.epocaRetratada?.nome) nomes.add(p.epocaRetratada.nome);
-    });
-    return Array.from(nomes).sort();
-}
-
-// Sugest├úo autom├ítica de datas e contexto pra um nome de per├¡odo j├í
-// usado (ver aplicarSugestaoEpoca em forms.js): busca o poema mais
-// recente (maior id ÔÇö id ├® timestamp de cria├º├úo, ver gerarId) que tenha
-// esse mesmo nome em epocaRetratada.nome, e devolve as datas e o
-// contexto hist├│rico/pessoal daquele poema como sugest├úo.
+// Sugest├úo autom├ítica de datas e contexto pra uma ├ëpoca j├í cadastrada
+// (ver aplicarSugestaoEpoca em forms.js): entre os poemas que j├í
+// referenciam essa mesma `epocaId`, busca o mais recente (maior id ÔÇö id
+// ├® timestamp de cria├º├úo, ver gerarId) e devolve as datas e o contexto
+// hist├│rico/pessoal dele como sugest├úo.
 //
 // ├ë s├│ um ponto de partida, nunca uma imposi├º├úo ÔÇö quem chama s├│ aplica
-// nos campos que estiverem vazios (preencherDataParcialSeVazio). Um
-// mesmo nome de per├¡odo pode, de prop├│sito, ter datas diferentes de um
-// poema pro outro ÔÇö ex.: "Luto" pode valer at├® um ponto num poema e se
-// estender mais adiante em outro.
-export function obterSugestaoEpocaPorNome(poemas, nome) {
-    if (!nome) return null;
+// nos campos que estiverem vazios (preencherDataParcialSeVazio). Uma
+// mesma ├ëpoca pode, de prop├│sito, ter datas diferentes de um poema pro
+// outro ÔÇö ex.: "Luto" pode valer at├® um ponto num poema e se estender
+// mais adiante em outro.
+export function obterSugestaoEpocaPorId(poemas, epocaId) {
+    if (!epocaId) return null;
     const candidatos = poemas
-        .filter((p) => p.epocaRetratada?.nome === nome)
+        .filter((p) => p.epocaRetratada?.epocaId == epocaId)
         .sort((a, b) => b.id - a.id);
     if (!candidatos.length) return null;
     const maisRecente = candidatos[0];
@@ -1021,37 +1110,190 @@ export function estaPublicado(item) {
     return item.status ? item.status === 'publicado' : !!item.publicado;
 }
 
-// Recebe o array db.poemas e retorna todos os nomes de pessoas ├║nicos ordenados
-export function extrairPessoasUnicas(poemas) {
-    const nomes = new Set();
-    poemas.forEach((p) => {
-        if (p.pessoas) {
-            const lista = Array.isArray(p.pessoas)
-                ? p.pessoas
-                : p.pessoas.split(',').map((s) => s.trim());
-            lista.forEach((n) => {
-                if (n) nomes.add(n);
-            });
-        }
+// Nomes de pessoas de um item (poema/prosa), resolvidos via o cadastro
+// central `db.pessoas` ÔÇö desde a migra├º├úo pra Pessoa como entidade
+// pr├│pria (ver migrarPessoasParaCadastro em db.js), item.pessoas guarda
+// `{ pessoaId, papeis }`, n├úo mais `{ nome, papeis }`; quem sabe o nome
+// agora ├® s├│ o cadastro. Recebe `pessoasCadastro` (normalmente
+// `db.pessoas`) em vez de fechar sobre `db` pra continuar test├ível com
+// dados de mentira, mesmo padr├úo do resto deste arquivo. pessoaId sem
+// correspond├¬ncia no cadastro (n├úo deveria acontecer, mas dado
+// importado de fora pode vir incompleto) ├® ignorado, n├úo quebra a
+// lista pros demais.
+export function nomesPessoas(item, pessoasCadastro = []) {
+    if (!Array.isArray(item.pessoas)) return [];
+    const porId = new Map(pessoasCadastro.map((p) => [p.id, p.nome]));
+    return item.pessoas.map((p) => porId.get(p.pessoaId)).filter(Boolean);
+}
+
+// Nomes dos grupos que uma Pessoa pertence, resolvidos via o cadastro
+// central `db.grupos` ÔÇö mesmo padr├úo de nomesPessoas acima, mas um
+// n├¡vel abaixo: Grupo ├® caracter├¡stica da Pessoa (constante entre
+// poemas), n├úo do v├¡nculo poemaÔåöpessoa (esse ├® o papel, que continua
+// em item.pessoas ÔÇö ver coment├írio ali).
+export function nomesGrupos(pessoa, gruposCadastro = []) {
+    if (!pessoa || !Array.isArray(pessoa.grupoIds)) return [];
+    const porId = new Map(gruposCadastro.map((g) => [g.id, g.nome]));
+    return pessoa.grupoIds.map((id) => porId.get(id)).filter(Boolean);
+}
+
+// Pares (Grupo, Pessoa) de um item (poema/prosa) ÔÇö resolve
+// item.pessoas ÔåÆ cada pessoa ÔåÆ cada grupo que ela pertence, achatando
+// tudo numa lista ├║nica. Usado tanto pela exporta├º├úo em Markdown
+// ("Grupos: Namorado (Dalton), Ex-namorado (Pedro)") quanto pela
+// coluna "Grupos" das tabelas e pelo painel somente-leitura do modal
+// (ver renderPainelGruposDoChip em editor.js) ÔÇö uma pessoa em mais de
+// um grupo gera um par por grupo, n├úo uma linha combinada. pessoaId
+// ou grupoId sem correspond├¬ncia no cadastro ├® ignorado, mesmo
+// crit├®rio de nomesPessoas/nomesGrupos acima.
+export function paresGrupoPessoa(item, pessoasCadastro = [], gruposCadastro = []) {
+    if (!Array.isArray(item.pessoas)) return [];
+    const porPessoaId = new Map(pessoasCadastro.map((p) => [p.id, p]));
+    const porGrupoId = new Map(gruposCadastro.map((g) => [g.id, g]));
+    const pares = [];
+    item.pessoas.forEach((p) => {
+        const pessoa = porPessoaId.get(p.pessoaId);
+        if (!pessoa || !Array.isArray(pessoa.grupoIds)) return;
+        pessoa.grupoIds.forEach((grupoId) => {
+            const grupo = porGrupoId.get(grupoId);
+            if (grupo) pares.push({ grupo, pessoa });
+        });
     });
-    return Array.from(nomes).sort();
+    return pares;
+}
+
+// ÔöÇÔöÇÔöÇ Cor de Grupo ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// Paleta curada (n├úo cor livre via input[type=color]): cada Grupo
+// escolhe uma destas chaves, salva em `grupo.cor`. Curada em vez de
+// hex livre por dois motivos ÔÇö cada entrada j├í garante contraste
+// correto claro/escuro (par bg-100/900 + text-600/400, mesmo padr├úo
+// de badge do resto do app), e evita repetir a cor que j├í identifica
+// "isto ├® uma Pessoa" nas tabelas (rose, ver badgesPessoas em
+// render-listas.js) ÔÇö por isso rose n├úo faz parte da paleta de Grupo,
+// pra badge de Pessoa e badge de Grupo nunca se confundirem visualmente
+// s├│ de bater o olho. `CORES_GRUPO` ├® a fonte ├║nica (paleta do seletor
+// no modal + resolu├º├úo de classes); grupo sem `cor` (dado de antes
+// dessa feature) cai no `PADRAO`.
+export const CORES_GRUPO = [
+    { chave: 'blue', rotulo: 'Azul' },
+    { chave: 'emerald', rotulo: 'Verde' },
+    { chave: 'amber', rotulo: '├émbar' },
+    { chave: 'violet', rotulo: 'Violeta' },
+    { chave: 'cyan', rotulo: 'Ciano' },
+    { chave: 'fuchsia', rotulo: 'F├║csia' },
+    { chave: 'orange', rotulo: 'Laranja' },
+    { chave: 'teal', rotulo: 'Verde-azulado' },
+    { chave: 'indigo', rotulo: '├ìndigo' },
+    { chave: 'slate', rotulo: 'Cinza' },
+];
+export const CORES_GRUPO_PADRAO = 'blue';
+
+// Classes completas por cor, escritas por extenso (n├úo constru├¡das com
+// template string tipo `bg-${chave}-100`) ÔÇö o Tailwind Play CDN deste
+// projeto escaneia o DOM renderizado atr├ís de nomes de classe
+// literais; string montada em runtime some do JS-fonte antes de
+// chegar l├í, ent├úo cada combina├º├úo precisa existir escrita por
+// inteiro em algum lugar que o scanner veja. Mesmo padr├úo que o resto
+// do app j├í segue (`corClasse: 'bg-rose-500'`, sempre literal, nunca
+// interpolado).
+const CLASSES_POR_COR_GRUPO = {
+    blue: 'bg-blue-100 dark:bg-blue-900 text-blue-600 dark:text-blue-400',
+    emerald: 'bg-emerald-100 dark:bg-emerald-900 text-emerald-600 dark:text-emerald-400',
+    amber: 'bg-amber-100 dark:bg-amber-900 text-amber-600 dark:text-amber-400',
+    violet: 'bg-violet-100 dark:bg-violet-900 text-violet-600 dark:text-violet-400',
+    cyan: 'bg-cyan-100 dark:bg-cyan-900 text-cyan-600 dark:text-cyan-400',
+    fuchsia: 'bg-fuchsia-100 dark:bg-fuchsia-900 text-fuchsia-600 dark:text-fuchsia-400',
+    orange: 'bg-orange-100 dark:bg-orange-900 text-orange-600 dark:text-orange-400',
+    teal: 'bg-teal-100 dark:bg-teal-900 text-teal-600 dark:text-teal-400',
+    indigo: 'bg-indigo-100 dark:bg-indigo-900 text-indigo-600 dark:text-indigo-400',
+    slate: 'bg-slate-100 dark:bg-slate-900 text-slate-600 dark:text-slate-400',
+};
+
+// Classes Tailwind (bg/text, claro+escuro) pra badge de um Grupo. Cor
+// desconhecida ou ausente (dado de antes da feature de cor) cai no
+// padr├úo, sem quebrar a renderiza├º├úo.
+export function classesCorGrupo(cor) {
+    return CLASSES_POR_COR_GRUPO[cor] || CLASSES_POR_COR_GRUPO[CORES_GRUPO_PADRAO];
+}
+
+// Bolinha s├│lida (dot) por cor ÔÇö usada tanto no seletor de cor do
+// formul├írio de Grupo (ver renderSeletorCorGrupo em forms.js) quanto no
+// card da pr├│pria aba Grupos (ver renderGrupos em render-listas.js), pra
+// bater o olho na cor sem precisar abrir "Editar". Mapa ├á parte de
+// CLASSES_POR_COR_GRUPO (que ├® bg claro + texto, pensado pra badge com
+// nome dentro) porque aqui o preenchimento ├® s├│lido (-500), sem par
+// claro/escuro ÔÇö ├® s├│ uma bolinha, n├úo precisa de contraste de texto.
+// Escrito por extenso pelo mesmo motivo de CLASSES_POR_COR_GRUPO acima
+// (Tailwind Play CDN escaneia classe literal no DOM).
+const PONTO_POR_COR_GRUPO = {
+    blue: 'bg-blue-500',
+    emerald: 'bg-emerald-500',
+    amber: 'bg-amber-500',
+    violet: 'bg-violet-500',
+    cyan: 'bg-cyan-500',
+    fuchsia: 'bg-fuchsia-500',
+    orange: 'bg-orange-500',
+    teal: 'bg-teal-500',
+    indigo: 'bg-indigo-500',
+    slate: 'bg-slate-500',
+};
+
+export function pontoCorGrupo(cor) {
+    return PONTO_POR_COR_GRUPO[cor] || PONTO_POR_COR_GRUPO[CORES_GRUPO_PADRAO];
+}
+
+// Remove acentos e caixa alta pra compara├º├úo de busca (t├¡tulo, texto do
+// prefixo "campo:" e valores dos campos). Usada em vez de um simples
+// toLowerCase() pra que "arvore" ache "├írvore", "coracao" ache "cora├º├úo",
+// etc. ÔÇö mesma t├®cnica (NFD + strip de diacr├¡ticos) j├í usada em
+// exportarLivroCompleto (exportar.js) pra gerar nome de arquivo, s├│ que
+// aplicada aqui ├á busca em vez de a um nome de arquivo.
+export function normalizarBusca(s) {
+    if (s == null) return '';
+    return String(s)
+        .toLowerCase()
+        .normalize('NFD')
+        .replace(/[\u0300-\u036f]/g, '');
 }
 
 // Nomes de atributo aceitos no prefixo "campo:valor" (ver filtrarTextos
 // abaixo) ÔåÆ chave correspondente no item j├í decorado. livro/parte/secao
 // s├úo preenchidos por decorarCamposBusca() em render-listas.js a partir
 // do v├¡nculo estrutural (paiTipo/paiId) do poema/prosa.
+// Como o nome do prefixo agora passa por normalizarBusca() antes do
+// lookup (ver parseConsultaBusca), n├úo precisa mais duplicar cada chave
+// com/sem acento (t├¡tulo/titulo, rela├º├úo/relacao, etc.) ÔÇö um ├║nico nome
+// sem acento j├í casa com as duas formas digitadas.
 const CAMPOS_ATRIBUTO = {
     titulo: 'titulo',
-    t├¡tulo: 'titulo',
     texto: 'texto',
-    etiqueta: 'sinalizacoes',
-    pessoa: 'pessoas',
+    etiqueta: '_buscaSinalizacoes',
+    estilo: 'sinalizacoesEstilo',
+    tema: 'sinalizacoesTema',
+    relacao: 'sinalizacoesRelacao',
+    tom: 'sinalizacoesTom',
+    // Diferente de sensivel: (abaixo), que busca no par├ígrafo descritivo
+    // de conteudoSensivel ÔÇö aqui ├® a tag solta de Sensibilidade
+    // (ex.: "Linguagem obscena").
+    sensibilidade: 'sinalizacoesSensibilidade',
+    // Balde tempor├írio de tags migradas sem categoria pr├│pria ainda
+    // (ver SINAL_CATEGORIAS em editor.js) ÔÇö n├úo ├® uma categoria de
+    // verdade, s├│ d├í pra buscar nela enquanto ela existir.
+    outros: 'sinalizacoesOutros',
+    pessoa: '_buscaPessoas',
+    papel: '_buscaPapeis',
+    grupo: '_buscaGrupos',
+    // S├│ se aplica a Prosa (ver extrairGenerosUnicos) ÔÇö em Poema, o
+    // campo simplesmente n├úo existe, ent├úo o prefixo n├úo acha nada l├í.
+    genero: 'genero',
+    idioma: 'idioma',
+    epoca: '_buscaEpoca',
+    autor: '_buscaAutoria',
+    envio: '_buscaEnvios',
     nota: 'notas',
     livro: '_buscaLivro',
     parte: '_buscaParte',
     secao: '_buscaSecao',
-    se├º├úo: '_buscaSecao',
     visual: 'descricaoVisual',
     contexto: 'contextoHistorico',
     intertexto: '_buscaIntertexto',
@@ -1059,18 +1301,64 @@ const CAMPOS_ATRIBUTO = {
     anexos: '_buscaAnexos',
     notaanexos: 'anexosNotaGeral',
     anotacao: '_buscaAnotacoes',
-    anota├º├úo: '_buscaAnotacoes',
     anotacoes: '_buscaAnotacoes',
-    anota├º├Áes: '_buscaAnotacoes',
     ocultacao: 'ocultacao',
-    oculta├º├úo: 'ocultacao',
     sensivel: 'conteudoSensivel',
-    sens├¡vel: 'conteudoSensivel',
     hiperacionante: 'vocabularioHiperacionante',
     cortado: '_buscaCortadoDe',
     lancado: '_buscaLancadoEm',
-    lan├ºado: '_buscaLancadoEm',
     descarte: 'descarte',
+    pendencia: 'pendencia',
+    elo: '_buscaElos',
+    elos: '_buscaElos',
+    referencia: '_buscaReferencias',
+    referencias: '_buscaReferencias',
+    reconhecimento: '_buscaReconhecimentos',
+    reconhecimentos: '_buscaReconhecimentos',
+};
+
+// Lista, pra uso da UI (legenda de ajuda, atalho de clique no cabe├ºalho
+// da coluna ÔÇö ver render-listas.js), dos prefixos "can├┤nicos" ÔÇö um por
+// campo de destino, preferindo a forma mais curta/comum quando um campo
+// tem mais de um nome aceito (ex.: "anexo" em vez de "anexos").
+export const PREFIXOS_CANONICOS_POR_CAMPO = {
+    titulo: 'titulo',
+    texto: 'texto',
+    _buscaSinalizacoes: 'etiqueta',
+    sinalizacoesEstilo: 'estilo',
+    sinalizacoesTema: 'tema',
+    sinalizacoesRelacao: 'relacao',
+    sinalizacoesTom: 'tom',
+    sinalizacoesSensibilidade: 'sensibilidade',
+    sinalizacoesOutros: 'outros',
+    _buscaPessoas: 'pessoa',
+    _buscaPapeis: 'papel',
+    _buscaGrupos: 'grupo',
+    genero: 'genero',
+    idioma: 'idioma',
+    _buscaEpoca: 'epoca',
+    _buscaAutoria: 'autor',
+    _buscaEnvios: 'envio',
+    notas: 'nota',
+    _buscaLivro: 'livro',
+    _buscaParte: 'parte',
+    _buscaSecao: 'secao',
+    descricaoVisual: 'visual',
+    contextoHistorico: 'contexto',
+    _buscaIntertexto: 'intertexto',
+    _buscaAnexos: 'anexo',
+    anexosNotaGeral: 'notaanexos',
+    _buscaAnotacoes: 'anotacao',
+    ocultacao: 'ocultacao',
+    conteudoSensivel: 'sensivel',
+    vocabularioHiperacionante: 'hiperacionante',
+    _buscaCortadoDe: 'cortado',
+    _buscaLancadoEm: 'lancado',
+    descarte: 'descarte',
+    pendencia: 'pendencia',
+    _buscaElos: 'elo',
+    _buscaReferencias: 'referencia',
+    _buscaReconhecimentos: 'reconhecimento',
 };
 
 // Interpreta uma consulta de busca no estilo Google e devolve os grupos de
@@ -1087,13 +1375,25 @@ const CAMPOS_ATRIBUTO = {
 //     o lado do "ou" em que est├í
 //   - um prefixo "campo:" restringe o termo a um atributo (ver
 //     CAMPOS_ATRIBUTO acima); sem prefixo, busca nos campos gerais
+//   - "campo:*" (asterisco sozinho, s├│ faz sentido com prefixo de campo)
+//     n├úo busca o caractere "*" ÔÇö significa "esse campo est├í preenchido,
+//     n├úo importa com o qu├¬". "-campo:*" inverte: "esse campo est├í
+//     vazio". Sem prefixo, um "*" solto ├® tratado como termo literal
+//     (n├úo tem campo ├║nico pra checar presen├ºa).
+//   - acentos n├úo importam nem no termo nem no nome do prefixo (ver
+//     normalizarBusca acima): "arvore"/"├írvore" e "sensivel:"/"sens├¡vel:"
+//     casam do mesmo jeito.
 // Ex.: Dalton -rascunho            ÔåÆ menciona Dalton, mas n├úo a tag "rascunho"
 //      Dalton ou Gabriela          ÔåÆ menciona Dalton OU Gabriela
 //      -2023                       ÔåÆ tudo, exceto o que tiver "2023"
 //      "beira do mar"              ÔåÆ s├│ o que tiver essa sequ├¬ncia exata
-//      pessoa:Dalton               ÔåÆ s├│ onde "Dalton" aparece em Pessoas
+//      pessoa:Dalton               ÔåÆ s├│ onde "Dalton" aparece em Pessoas (nome OU papel)
+//      papel:Melhor Amiga          ÔåÆ s├│ quem tem algu├®m marcado com esse papel espec├¡fico
+//      grupo:Fam├¡lia               ÔåÆ s├│ quem menciona algu├®m que pertence a esse Grupo
 //      -etiqueta:rascunho          ÔåÆ exclui quem tem a etiqueta "rascunho"
 //      secao:"Fragmentos do Fim"   ÔåÆ s├│ quem est├í dentro dessa se├º├úo
+//      sensivel:*                  ÔåÆ s├│ quem tem Conte├║do Sens├¡vel preenchido
+//      -sensivel:*                 ÔåÆ s├│ quem N├âO tem Conte├║do Sens├¡vel preenchido
 function parseConsultaBusca(query) {
     // Cada match ├®, opcionalmente, um prefixo "campo:" seguido de uma
     // frase entre aspas ou uma palavra solta, com "-" opcional na frente
@@ -1119,8 +1419,8 @@ function parseConsultaBusca(query) {
 
         let campo = null;
         const casouCampo = resto.match(/^([a-zA-Z├á-├║├Ç-├Ü]+):([\s\S]*)$/);
-        if (casouCampo && CAMPOS_ATRIBUTO[casouCampo[1].toLowerCase()]) {
-            campo = CAMPOS_ATRIBUTO[casouCampo[1].toLowerCase()];
+        if (casouCampo && CAMPOS_ATRIBUTO[normalizarBusca(casouCampo[1])]) {
+            campo = CAMPOS_ATRIBUTO[normalizarBusca(casouCampo[1])];
             resto = casouCampo[2];
         }
 
@@ -1128,10 +1428,16 @@ function parseConsultaBusca(query) {
         if (termo.startsWith('"') && termo.endsWith('"') && termo.length >= 2) {
             termo = termo.slice(1, -1); // tira as aspas, mant├®m os espa├ºos de dentro
         }
-        termo = termo.trim().toLowerCase();
+        termo = termo.trim();
         if (!termo) return;
 
-        (excluir ? termosExcluir : grupoAtual).push({ campo, termo });
+        // Presen├ºa de campo ("campo:*"): s├│ faz sentido com um campo
+        // restrito e o asterisco sozinho ÔÇö "campo:*algo*" ou um "*" sem
+        // prefixo continuam sendo termo literal, n├úo presen├ºa.
+        const presenca = campo && termo === '*';
+        termo = presenca ? '*' : normalizarBusca(termo);
+
+        (excluir ? termosExcluir : grupoAtual).push({ campo, termo, presenca });
     });
     if (grupoAtual.length) gruposIncluir.push(grupoAtual);
 
@@ -1139,50 +1445,61 @@ function parseConsultaBusca(query) {
 }
 
 // Filtra uma lista de textos (poemas/prosas) por uma busca livre que
-// procura em t├¡tulo, ano, sinaliza├º├Áes, pessoas, livros, descri├º├úo
-// visual, contexto hist├│rico/pessoal e intertextualidade ao mesmo tempo
-// ÔÇö ou, opcionalmente, restrita a um atributo espec├¡fico. Ver
+// procura em t├¡tulo, ano, sinaliza├º├Áes, pessoas, autoria, grupos,
+// pap├®is, ├®poca retratada, livros, descri├º├úo visual, contexto
+// hist├│rico/pessoal e intertextualidade ao mesmo tempo ÔÇö ou,
+// opcionalmente, restrita a um atributo espec├¡fico. Ver
 // parseConsultaBusca acima pra sintaxe completa.
 export function filtrarTextos(lista, query) {
     if (!query || !query.trim()) return lista;
     const { gruposIncluir, termosExcluir } = parseConsultaBusca(query);
 
     return lista.filter((item) => {
-        const camposGerais = [
-            item.titulo,
-            item.ano,
-            item.sinalizacoes,
-            item.pessoas,
-            item.genero,
-            item.notas,
-            item._livros,
-            item.descricaoVisual,
-            item.contextoHistorico,
-            item._buscaIntertexto,
-            item._buscaAnexos,
-            item.anexosNotaGeral,
-            item._buscaAnotacoes,
-            item.ocultacao,
-            item.conteudoSensivel,
-            item.vocabularioHiperacionante,
-            item._buscaCortadoDe,
-            item._buscaLancadoEm,
-            item.descarte,
-        ]
-            .filter(Boolean)
-            .join(' ')
-            .toLowerCase();
+        const camposGerais = normalizarBusca(
+            [
+                item.titulo,
+                item.ano,
+                item._buscaSinalizacoes,
+                item._buscaPessoas,
+                item._buscaAutoria,
+                item._buscaGrupos,
+                item._buscaPapeis,
+                item._buscaEpoca,
+                item.genero,
+                item.notas,
+                item._livros,
+                item.descricaoVisual,
+                item.contextoHistorico,
+                item._buscaIntertexto,
+                item._buscaAnexos,
+                item.anexosNotaGeral,
+                item._buscaAnotacoes,
+                item.ocultacao,
+                item.conteudoSensivel,
+                item.vocabularioHiperacionante,
+                item._buscaCortadoDe,
+                item._buscaLancadoEm,
+                item.descarte,
+                item.pendencia,
+            ]
+                .filter(Boolean)
+                .join(' '),
+        );
 
         const valorDoTermo = (t) => {
             if (!t.campo) return camposGerais;
             const v = item[t.campo];
-            return v == null ? '' : String(v).toLowerCase();
+            return v == null ? '' : normalizarBusca(String(v));
         };
 
+        // "campo:*" checa presen├ºa (valor n├úo-vazio), ignorando o texto
+        // do campo ÔÇö o resto continua sendo substring normal.
+        const bateTermo = (t) =>
+            t.presenca ? valorDoTermo(t) !== '' : valorDoTermo(t).includes(t.termo);
+
         const combinaInclusao =
-            gruposIncluir.length === 0 ||
-            gruposIncluir.some((grupo) => grupo.every((t) => valorDoTermo(t).includes(t.termo)));
-        const combinaExclusao = termosExcluir.some((t) => valorDoTermo(t).includes(t.termo));
+            gruposIncluir.length === 0 || gruposIncluir.some((grupo) => grupo.every(bateTermo));
+        const combinaExclusao = termosExcluir.some(bateTermo);
 
         return combinaInclusao && !combinaExclusao;
     });
@@ -1196,12 +1513,13 @@ export function filtrarPorConteudo(lista, query) {
     const { gruposIncluir, termosExcluir } = parseConsultaBusca(query);
 
     return lista.filter((item) => {
-        const texto = (item.texto == null ? '' : String(item.texto)).toLowerCase();
+        const texto = normalizarBusca(item.texto == null ? '' : String(item.texto));
+
+        const bateTermo = (t) => (t.presenca ? texto !== '' : texto.includes(t.termo));
 
         const combinaInclusao =
-            gruposIncluir.length === 0 ||
-            gruposIncluir.some((grupo) => grupo.every((t) => texto.includes(t.termo)));
-        const combinaExclusao = termosExcluir.some((t) => texto.includes(t.termo));
+            gruposIncluir.length === 0 || gruposIncluir.some((grupo) => grupo.every(bateTermo));
+        const combinaExclusao = termosExcluir.some(bateTermo);
 
         return combinaInclusao && !combinaExclusao;
     });
@@ -1226,13 +1544,18 @@ export function extrairGenerosUnicos(prosas) {
     return Array.from(generos).sort();
 }
 
-export function extrairSinalizacoesUnicas(poemas) {
+// Sinaliza├º├Áes viraram 5 campos por categoria (Estilo/Tema/Rela├º├úo/
+// Sensibilidade/Tom ÔÇö ver SINALIZACOES_CATEGORIAS) em vez de um campo
+// ├║nico misturando tudo (ver migrarSinalizacoes em db.js). `campo`
+// aceita qualquer um desses nomes de campo ÔÇö a fun├º├úo ├® gen├®rica, n├úo
+// hardcoded pra sinalizacoesEstilo especificamente ÔÇö pra poder ser
+// reaproveitada por cada datalist de categoria em editor.js.
+export function extrairSinalizacoesUnicas(itens, campo = 'sinalizacoesTema') {
     const sinais = new Set();
-    poemas.forEach((p) => {
-        if (p.sinalizacoes) {
-            const lista = Array.isArray(p.sinalizacoes)
-                ? p.sinalizacoes
-                : p.sinalizacoes.split(',').map((s) => s.trim());
+    itens.forEach((p) => {
+        const valor = p[campo];
+        if (valor) {
+            const lista = Array.isArray(valor) ? valor : valor.split(',').map((s) => s.trim());
             lista.forEach((s) => {
                 if (s) sinais.add(s);
             });
@@ -1241,6 +1564,274 @@ export function extrairSinalizacoesUnicas(poemas) {
     return Array.from(sinais).sort();
 }
 
+// Nomes de campo das categorias de Sinaliza├º├Áes, na ordem em que
+// aparecem no modal. "Conte├║do sens├¡vel" n├úo ├® mais um valor dentro de
+// Sensibilidade ÔÇö passou a ser derivado da presen├ºa do campo
+// `conteudoSensivel` (ver badgeConteudoSensivel em render-listas.js).
+// "outros" ├® o balde tempor├írio de tags migradas sem categoria pr├│pria
+// ainda (Premiados/Tradu├º├úo/Varia├º├Áes ÔÇö ver editor.js e o plano em
+// An├ílise de estrutura e metadados po├®ticos); n├úo ├® uma 6┬¬ categoria
+// definitiva, ├® onde elas ficam vis├¡veis at├® virar Reconhecimentos e
+// Elos tipados de Deriva├º├úo.
+export const SINALIZACOES_CATEGORIAS = {
+    estilo: 'sinalizacoesEstilo',
+    tema: 'sinalizacoesTema',
+    relacao: 'sinalizacoesRelacao',
+    sensibilidade: 'sinalizacoesSensibilidade',
+    tom: 'sinalizacoesTom',
+    outros: 'sinalizacoesOutros',
+};
+
+// Uma ├║nica string combinando as categorias acima ÔÇö pra busca geral, export
+// em .md (linha resumo) e estat├¡sticas, que n├úo precisam saber de
+// categoria, s├│ "quais tags esse item tem". Cada consumidor que SE
+// importa com categoria (o modal de edi├º├úo, o filtro por prefixo
+// estilo:/tema:/etc.) usa os campos individuais direto.
+export function sinalizacoesCombinadas(item) {
+    return Object.values(SINALIZACOES_CATEGORIAS)
+        .map((campo) => item[campo])
+        .filter(Boolean)
+        .join(', ');
+}
+
+// ÔöÇÔöÇÔöÇ Elos / Refer├¬ncias tipados (item 1 do plano de schema) ÔöÇÔöÇÔöÇÔöÇ
+// Duas listas fechadas separadas ÔÇö porque Elos e Refer├¬ncias t├¬m
+// natureza diferente ÔÇö e, desde o redesenho Rela├º├úo+Dire├º├úo, Elos usa
+// um schema ├á parte de Refer├¬ncias (ver mais abaixo).
+//
+// PAPEIS_PESSOA: papel de cada pessoa vinculada a um texto (item 2 do
+// plano de schema ÔÇö "Pedro, Dani" comma-string vira array de objeto
+// { nome, papel }). Fechado (n├úo texto livre) pra dar consist├¬ncia de
+// filtro/estat├¡stica; "" (vazio) ├® o valor de "n├úo especificado" ÔÇö todo
+// nome migrado da string antiga cai aqui (ver migrarPessoas em db.js),
+// e continua sendo uma op├º├úo v├ílida pra quem n├úo quiser categorizar.
+export const PAPEIS_PESSOA = [
+    'Retratado(a)',
+    'Inspirado(a) por',
+    'Dedicat├írio(a)',
+    'Mencionado(a)',
+    'Aludido(a)',
+];
+
+// Iniciais de PAPEIS_PESSOA pra exibi├º├úo compacta (modal e coluna da
+// tabela ÔÇö ver badgesPessoas em render-listas.js e o chip de pap├®is em
+// editor.js). As 5 iniciais n├úo colidem (R/I/D/M/A), ent├úo n├úo precisa de
+// abrevia├º├úo tipo "Re/In/De/Me". Mant├®m a ordem em que os pap├®is foram
+// marcados (n├úo ├® hierarquia fixa por categoria ÔÇö ver alternarPapel em
+// editor.js), s├│ troca o nome por extenso pela inicial; "┬À" (ponto
+// m├®dio) como separador entre elas, escolhido por ser mais leve que "-"
+// ou espa├ºo sem ficar amb├¡guo tipo "RDIM" grudado. Exporta├º├úo pra MD
+// mant├®m os pap├®is por extenso (ver exportar-md.js) ÔÇö a abrevia├º├úo ├® s├│
+// pra UI, onde passar o mouse por cima do chip j├í mostra o papel por
+// extenso via `title` (ver badgesPessoas em render-listas.js).
+export function iniciaisPapeisPessoa(papeis) {
+    if (!Array.isArray(papeis) || !papeis.length) return '';
+    return papeis.map((p) => p.charAt(0)).join('┬À');
+}
+
+// ÔöÇÔöÇÔöÇ Autoria ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// AUTORIA_PAPEIS: papel de cada Autor vinculado a um texto ÔÇö lista
+// fechada pr├│pria (n├úo reaproveita PAPEIS_PESSOA acima), j├í que
+// Autor/Coautor ├® um v├¡nculo single-role por texto (um autor s├│ pode
+// estar marcado como UM dos dois num mesmo poema/prosa), diferente de
+// Pessoa, onde os pap├®is se acumulam. `item.autoria` ├® um array de
+// `{ autorId, papel }`, resolvido contra o cadastro central
+// `db.autores` (nome + sobre ÔÇö ver criarGrupoDeAutoria em editor.js e
+// migrarAutoria/obterOuCriarAutorPorNome em db.js).
+export const AUTORIA_PAPEIS = ['Autor', 'Coautor'];
+
+// Pares (Autor, papel) de um item (poema/prosa) ÔÇö resolve
+// item.autoria ÔåÆ cada v├¡nculo { autorId, papel } ÔåÆ o Autor
+// correspondente no cadastro central, mesmo padr├úo de paresGrupoPessoa
+// acima. Usado pela exporta├º├úo em Markdown (ver textoAutoria em
+// exportar-md.js), pela coluna "Autoria" das tabelas (ver
+// badgesAutoria em render-listas.js) e por `_buscaAutoria` (nome +
+// papel, alimenta o prefixo de busca `autor:`). autorId sem
+// correspond├¬ncia no cadastro (autor exclu├¡do) ├® ignorado, mesmo
+// crit├®rio de nomesPessoas/paresGrupoPessoa; `item.autoria` ausente ou
+// n├úo-array (dado ainda n├úo migrado) devolve lista vazia sem quebrar.
+export function paresAutoria(item, autoresCadastro = []) {
+    if (!Array.isArray(item.autoria)) return [];
+    const porId = new Map(autoresCadastro.map((a) => [a.id, a]));
+    const pares = [];
+    item.autoria.forEach((v) => {
+        const autor = porId.get(v.autorId);
+        if (autor) pares.push({ autor, papel: v.papel });
+    });
+    return pares;
+}
+
+// IDIOMA: campo simples em Poema/Prosa (item 9 do plano de schema),
+// padr├úo "pt-BR" ÔÇö complementa a Rela├º├úo "Tradu├º├úo" do item 1 (sem ele
+// n├úo dava pra saber em que l├¡ngua um texto traduzido est├í). Texto
+// livre com autocomplete (n├úo select fechado ÔÇö um texto pode estar em
+// qualquer idioma, inclusive misto/dialeto, n├úo faz sentido fechar a
+// lista), ent├úo n├úo ├® uma constante fechada como PAPEIS_PESSOA. C├│digos
+// BCP-47 (pt-BR, en, es...) s├│ por conven├º├úo de concis├úo; texto livre
+// aceita qualquer valor.
+//
+// IDIOMAS_SUGERIDOS ├® s├│ a semente inicial do datalist ÔÇö antes de
+// qualquer poema/prosa ter um idioma diferente de pt-BR salvo, o campo
+// ainda precisa sugerir algo al├®m do pr├│prio valor padr├úo. Ver
+// extrairIdiomasUnicos abaixo, que soma isso ao que j├í est├í salvo no
+// acervo (mesmo padr├úo de atualizarDatalistEpoca em editor.js).
+export const IDIOMAS_SUGERIDOS = ['pt-BR', 'en', 'es', 'fr', 'it', 'de'];
+
+// Idiomas j├í usados no acervo (Poemas + Prosas juntos, mesmo motivo de
+// atualizarDatalistEpoca somar as duas fontes: um idioma digitado numa
+// prosa deve sugerir numa pr├│xima edi├º├úo de poema e vice-versa), somado
+// ├á semente de IDIOMAS_SUGERIDOS pra n├úo come├ºar vazio.
+export function extrairIdiomasUnicos(itens) {
+    const idiomas = new Set(IDIOMAS_SUGERIDOS);
+    itens.forEach((item) => {
+        if (item.idioma) idiomas.add(item.idioma);
+    });
+    return Array.from(idiomas).sort();
+}
+
+// ÔöÇÔöÇÔöÇ Envios e Rea├º├Áes ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// ENVIOS: item 7 do plano de schema. Registro de quando um texto foi
+// enviado/mostrado pra algu├®m e como essa pessoa reagiu ÔÇö lista (n├úo
+// campo ├║nico), porque o mesmo poema pode ter sido enviado mais de uma
+// vez, pra pessoas diferentes, em momentos diferentes. Poema + Prosa
+// desde j├í (mesma antecipa├º├úo do item 9/Idioma e do campo Autoria).
+//
+// `item.envios` ├® array de `{ pessoa, data, meio, reacao, notas }`:
+//   - `pessoa`: texto livre (n├úo refer├¬ncia a `db.pessoas` por id) ÔÇö
+//     reaproveita s├│ o *datalist* de nomes j├í cadastrados como sugest├úo
+//     de digita├º├úo (mesmo padr├úo de `meio` abaixo), sem virar v├¡nculo
+//     estrutural; n├úo faz sentido for├ºar cadastro central s├│ pra
+//     registrar "mandei pro Instagram da Dani".
+//   - `data`: data parcial (dia/m├¬s/ano ÔÇö ver lerDataParcial/
+//     preencherDataParcial/formatarDataParcial acima), o dia do envio,
+//     n├úo de escrita/publica├º├úo.
+//   - `meio`: texto livre com autocomplete (WhatsApp, Instagram,
+//     presencial...) ÔÇö ver extrairMeiosEnviosUnicos abaixo, mesmo
+//     motivo de IDIOMAS_SUGERIDOS n├úo fechar a lista: o meio de envio
+//     n├úo ├® uma categoria fixa e finita.
+//   - `reacao`/`notas`: texto livre ÔÇö o que a pessoa disse/fez, e uma
+//     nota pr├│pria ├á parte (contexto adicional, n├úo a rea├º├úo em si).
+//
+// Meios j├í usados no acervo (Poemas + Prosas juntos, mesmo motivo de
+// extrairIdiomasUnicos somar as duas fontes), sem semente fixa ÔÇö ao
+// contr├írio de IDIOMAS_SUGERIDOS, n├úo h├í um punhado ├│bvio de meios pra
+// sugerir antes do primeiro envio salvo (WhatsApp/Instagram/presencial
+// variam demais por acervo).
+export function extrairMeiosEnviosUnicos(itens) {
+    const meios = new Set();
+    itens.forEach((item) => {
+        if (Array.isArray(item.envios)) {
+            item.envios.forEach((e) => {
+                if (e && e.meio) meios.add(e.meio);
+            });
+        }
+    });
+    return Array.from(meios).sort();
+}
+
+// ÔöÇÔöÇÔöÇ Reconhecimentos ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+// RECONHECIMENTOS: item 8 do plano de schema. Entidade tipo lista,
+// separada de tag solta (a tag "Premiados" que hoje ficava num balde
+// tempor├írio em sinalizacoesOutros ÔÇö ver MAPA_MIGRACAO_SINALIZACOES em
+// db.js ÔÇö migra pra c├í, ver migrarReconhecimentos em db.js). Poema +
+// Prosa desde j├í (mesma antecipa├º├úo de Idioma/Autoria/Envios).
+//
+// `item.reconhecimentos` ├® array de `{ premio, posicao, ano, texto }`:
+//   - `premio`: texto livre (nome do concurso/pr├¬mio/men├º├úo) ÔÇö reaproveita
+//     s├│ o *datalist* dos nomes j├í cadastrados como sugest├úo de digita├º├úo
+//     (ver extrairPremiosUnicos abaixo), mesmo padr├úo de `meio` em Envios:
+//     n├úo h├í uma lista fechada de pr├¬mios poss├¡veis.
+//   - `posicao`: texto livre (ex.: "1┬║ lugar", "Men├º├úo honrosa") ÔÇö n├úo ├®
+//     um valor fechado, pr├¬mios diferentes nomeiam coloca├º├úo de formas
+//     diferentes.
+//   - `ano`: n├║mero (ano da premia├º├úo), pode ficar em branco.
+//   - `texto`: nota livre opcional (contexto adicional sobre o pr├¬mio).
+//
+// Pr├¬mios j├í usados no acervo (Poemas + Prosas juntos, mesmo motivo de
+// extrairMeiosEnviosUnicos acima), sem semente fixa.
+export function extrairPremiosUnicos(itens) {
+    const premios = new Set();
+    itens.forEach((item) => {
+        if (Array.isArray(item.reconhecimentos)) {
+            item.reconhecimentos.forEach((r) => {
+                if (r && r.premio) premios.add(r.premio);
+            });
+        }
+    });
+    return Array.from(premios).sort();
+}
+
+// TIPOS_REFERENCIA: rela├º├Áes sempre UNIDIRECIONAIS (mais novo ÔåÆ mais
+// antigo), sem par estrutural fechado ÔÇö por isso sem painel derivado
+// (n├úo h├í "outro lado" a inferir). "Imagem central compartilhada" ├® um
+// motivo recorrendo ao longo do corpus sem rela├º├úo de par fechado
+// (diferente de Elos, que ├® bilateral). "Aceno a" ├® um gesto mais
+// solto, sem exigir reciprocidade nem pertencer a um tipo mais
+// espec├¡fico.
+export const TIPOS_REFERENCIA = [
+    'Personagem em comum',
+    'Imagem central compartilhada',
+    'Aceno a',
+    'Outro',
+];
+
+// RELACOES_ELO: rela├º├Áes BILATERAIS entre dois poemas (par estrutural ÔÇö
+// reescrita, tradu├º├úo, resposta...), redesenhadas pra separar a
+// Rela├º├úo em si (8 valores fixos) da Dire├º├úo (Origem/Destino) de cada
+// lado ÔÇö em vez do schema antigo (`TIPOS_ELO` fixo com 11 r├│tulos, um
+// por lado poss├¡vel, e um mapa `TIPO_INVERSO_ELO` s├│ pros 3 pares que
+// tinham nome pros dois lados). Com Rela├º├úo+Dire├º├úo, todo par vira
+// automaticamente nomeado nos dois sentidos (ver ROTULOS_RELACAO_ELO
+// logo abaixo), sem precisar de mapa de inverso.
+export const RELACOES_ELO = [
+    'Reescrita',
+    'Continuidade',
+    'Tradu├º├úo',
+    'Varia├º├úo',
+    'Vers├úo',
+    'Resposta',
+    'D├¡ptico',
+    'Outro',
+];
+
+// R├│tulo de exibi├º├úo por Rela├º├úo e lado (Origem = texto mais
+// antigo/base, Destino = texto derivado/mais novo). Rela├º├Áes
+// assim├®tricas (Reescrita, Continuidade, Tradu├º├úo, Varia├º├úo, Vers├úo,
+// Resposta) t├¬m um r├│tulo diferente por lado; D├¡ptico e Outro n├úo t├¬m
+// uma dire├º├úo real, ent├úo usam o mesmo r├│tulo dos dois lados. "Vers├úo"
+// n├úo carrega mais "descartada" no r├│tulo (antes "Vers├úo anterior
+// (descartada) de") ÔÇö decis├úo de simplificar o texto.
+export const ROTULOS_RELACAO_ELO = {
+    Reescrita: { origem: 'Reescrito em', destino: 'Reescrita de' },
+    Continuidade: { origem: 'Continuado em', destino: 'Continua├º├úo de' },
+    Tradu├º├úo: { origem: 'Traduzido para', destino: 'Tradu├º├úo de' },
+    Varia├º├úo: { origem: 'Variado em', destino: 'Varia├º├úo de' },
+    Vers├úo: { origem: 'Vers├úo anterior de', destino: 'Vers├úo oficial de' },
+    Resposta: { origem: 'Respondido em', destino: 'Resposta a' },
+    D├¡ptico: { origem: 'D├¡ptico com', destino: 'D├¡ptico com' },
+    Outro: { origem: 'Outro', destino: 'Outro' },
+};
+
+// R├│tulo mostrado pra um elo, dada sua Rela├º├úo e Dire├º├úo ÔÇö usado no
+// modal (bot├Áes de dire├º├úo, ver atualizarRotulosDirecaoElo em
+// editor.js), na coluna da tabela (render-listas.js) e na exporta├º├úo
+// (exportar-md.js). Rela├º├úo vazia/desconhecida (elo legado sem
+// migra├º├úo aplicada, ou dado corrompido) devolve string vazia em vez
+// de inventar um r├│tulo.
+export function rotuloElo(relacao, direcao) {
+    return ROTULOS_RELACAO_ELO[relacao]?.[direcao] || '';
+}
+
+// Dire├º├úo oposta ÔÇö usada pelo painel derivado (elosDerivados em
+// editor.js) pra mostrar o r├│tulo certo do lado que ainda n├úo foi
+// cadastrado manualmente. Valores vazios/desconhecidos passam direto,
+// sem inventar uma dire├º├úo que n├úo existe.
+export function direcaoInversa(direcao) {
+    if (direcao === 'origem') return 'destino';
+    if (direcao === 'destino') return 'origem';
+    return direcao;
+}
+
 // Sugest├Áes de autocompletar pra Intertextualidade (campo Texto):
 // v├írias entradas de intertexto ÔÇö de poemas diferentes, ├ás vezes tipos
 // diferentes ÔÇö costumam repetir a mesma refer├¬ncia (a mesma conversa, o
diff --git a/modais/modal-poema.html b/modais/modal-poema.html
index 5a1184b..22ca35b 100644
--- a/modais/modal-poema.html
+++ b/modais/modal-poema.html
@@ -64,6 +64,10 @@
                     <label class="form-label">Sequ├¬ncia</label>
                     <input type="number" id="p-sequencia" />
 
+                    <label class="form-label">Idioma</label>
+                    <input type="text" id="p-idioma" list="sugestoes-idioma" value="pt-BR" />
+                    <datalist id="sugestoes-idioma"></datalist>
+
                     <details class="campo-grupo" open>
                         <summary>
                             Data de escrita, publica├º├úo, ├®poca e contexto
@@ -130,147 +134,161 @@
                             </div>
                             <label class="form-label mb-2 mt-3">Data de Primeira Publica├º├úo</label>
 
-                        <div class="grid grid-cols-3 gap-1">
-                            <input
-                                type="number"
-                                id="p-data-pub-dia"
-                                placeholder="Dia"
-                                min="1"
-                                max="31"
-                                title="Dia"
-                            />
-                            <input
-                                type="number"
-                                id="p-data-pub-mes"
-                                placeholder="M├¬s"
-                                min="1"
-                                max="12"
-                                title="M├¬s"
-                            />
-                            <input
-                                type="number"
-                                id="p-data-pub-ano"
-                                placeholder="Ano"
-                                min="1900"
-                                max="2100"
-                                title="Ano"
-                            />
-                        </div>
-                        <p class="text-[10px] text-gray-400 dark:text-slate-500 mt-1">
-                            Preencha s├│ o que souber ÔÇö qualquer campo pode ficar em branco. O ano de
-                            publica├º├úo ├® sugerido automaticamente se o Destino escolhido pertencer a
-                            um livro j├í publicado.
-                        </p>
-
-                        <div class="flex items-center justify-between mt-3">
-                            <label class="form-label mb-2"
-                                >├ëpoca Retratada
-                                <span
-                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
-                                    >(a que ├®poca o poema se refere ÔÇö n├úo quando foi escrito)</span
-                                ></label
-                            >
-                            <label
-                                class="flex items-center gap-1 text-xs text-gray-500 dark:text-slate-400 cursor-pointer mb-2 whitespace-nowrap"
-                                title="Marque quando o poema n├úo se referir a uma ├®poca espec├¡fica ÔÇö diferente de deixar em branco, que significa 'ainda n├úo categorizado'."
-                            >
+                            <div class="grid grid-cols-3 gap-1">
                                 <input
-                                    type="checkbox"
-                                    id="p-epoca-na"
-                                    onchange="toggleCamposEpocaNa()"
+                                    type="number"
+                                    id="p-data-pub-dia"
+                                    placeholder="Dia"
+                                    min="1"
+                                    max="31"
+                                    title="Dia"
+                                />
+                                <input
+                                    type="number"
+                                    id="p-data-pub-mes"
+                                    placeholder="M├¬s"
+                                    min="1"
+                                    max="12"
+                                    title="M├¬s"
+                                />
+                                <input
+                                    type="number"
+                                    id="p-data-pub-ano"
+                                    placeholder="Ano"
+                                    min="1900"
+                                    max="2100"
+                                    title="Ano"
                                 />
-                                N├úo se aplica
-                            </label>
-                        </div>
-                        <input
-                            type="text"
-                            id="p-epoca-nome"
-                            list="sugestoes-epoca-nome"
-                            class="text-sm mb-2"
-                            placeholder="Nome do per├¡odo (opcional) ÔÇö ex.: Namoro com Fulano, Luto, Pandemia..."
-                            onchange="aplicarSugestaoEpoca()"
-                        />
-                        <datalist id="sugestoes-epoca-nome"></datalist>
-                        <p class="text-[10px] text-gray-400 dark:text-slate-500 -mt-1 mb-2">
-                            Repetir um nome j├í usado sugere as datas e o contexto do poema mais
-                            recente com esse nome ÔÇö s├│ nos campos que estiverem vazios, sem
-                            sobrescrever o que voc├¬ j├í preencheu.
-                        </p>
-                        <div class="grid grid-cols-2 gap-2">
-                            <div>
-                                <span class="text-[10px] text-gray-400 dark:text-slate-500"
-                                    >De</span
-                                >
-                                <div class="grid grid-cols-3 gap-1">
-                                    <input
-                                        type="number"
-                                        id="p-epoca-ini-dia"
-                                        placeholder="Dia"
-                                        min="1"
-                                        max="31"
-                                        title="Dia"
-                                    />
-                                    <input
-                                        type="number"
-                                        id="p-epoca-ini-mes"
-                                        placeholder="M├¬s"
-                                        min="1"
-                                        max="12"
-                                        title="M├¬s"
-                                    />
-                                    <input
-                                        type="number"
-                                        id="p-epoca-ini-ano"
-                                        placeholder="Ano"
-                                        min="1900"
-                                        max="2100"
-                                        title="Ano"
-                                    />
-                                </div>
                             </div>
-                            <div>
-                                <span class="text-[10px] text-gray-400 dark:text-slate-500"
-                                    >At├®</span
+                            <p class="text-[10px] text-gray-400 dark:text-slate-500 mt-1">
+                                Preencha s├│ o que souber ÔÇö qualquer campo pode ficar em branco. O
+                                ano de publica├º├úo ├® sugerido automaticamente se o Destino escolhido
+                                pertencer a um livro j├í publicado.
+                            </p>
+
+                            <div class="flex items-center justify-between mt-3">
+                                <label class="form-label mb-2"
+                                    >├ëpoca Retratada
+                                    <span
+                                        class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                        >(a que ├®poca o poema se refere ÔÇö n├úo quando foi
+                                        escrito)</span
+                                    ></label
+                                >
+                                <label
+                                    class="flex items-center gap-1 text-xs text-gray-500 dark:text-slate-400 cursor-pointer mb-2 whitespace-nowrap"
+                                    title="Marque quando o poema n├úo se referir a uma ├®poca espec├¡fica ÔÇö diferente de deixar em branco, que significa 'ainda n├úo categorizado'."
                                 >
-                                <div class="grid grid-cols-3 gap-1">
-                                    <input
-                                        type="number"
-                                        id="p-epoca-fim-dia"
-                                        placeholder="Dia"
-                                        min="1"
-                                        max="31"
-                                        title="Dia"
-                                    />
-                                    <input
-                                        type="number"
-                                        id="p-epoca-fim-mes"
-                                        placeholder="M├¬s"
-                                        min="1"
-                                        max="12"
-                                        title="M├¬s"
-                                    />
                                     <input
-                                        type="number"
-                                        id="p-epoca-fim-ano"
-                                        placeholder="Ano"
-                                        min="1900"
-                                        max="2100"
-                                        title="Ano"
+                                        type="checkbox"
+                                        id="p-epoca-na"
+                                        onchange="toggleCamposEpocaNa()"
                                     />
+                                    N├úo se aplica
+                                </label>
+                            </div>
+                            <input
+                                type="text"
+                                id="p-epoca-nome"
+                                list="sugestoes-epoca-nome"
+                                class="text-sm mb-2"
+                                placeholder="Nome do per├¡odo (opcional) ÔÇö ex.: Namoro com Fulano, Luto, Pandemia..."
+                                onchange="aplicarSugestaoEpoca()"
+                            />
+                            <datalist id="sugestoes-epoca-nome"></datalist>
+                            <p class="text-[10px] text-gray-400 dark:text-slate-500 -mt-1 mb-2">
+                                Repetir um nome j├í usado sugere as datas e o contexto do poema mais
+                                recente com esse nome ÔÇö s├│ nos campos que estiverem vazios, sem
+                                sobrescrever o que voc├¬ j├í preencheu.
+                            </p>
+                            <div class="grid grid-cols-2 gap-2">
+                                <div>
+                                    <span class="text-[10px] text-gray-400 dark:text-slate-500"
+                                        >De</span
+                                    >
+                                    <div class="grid grid-cols-3 gap-1">
+                                        <input
+                                            type="number"
+                                            id="p-epoca-ini-dia"
+                                            placeholder="Dia"
+                                            min="1"
+                                            max="31"
+                                            title="Dia"
+                                        />
+                                        <input
+                                            type="number"
+                                            id="p-epoca-ini-mes"
+                                            placeholder="M├¬s"
+                                            min="1"
+                                            max="12"
+                                            title="M├¬s"
+                                        />
+                                        <input
+                                            type="number"
+                                            id="p-epoca-ini-ano"
+                                            placeholder="Ano"
+                                            min="1900"
+                                            max="2100"
+                                            title="Ano"
+                                        />
+                                    </div>
+                                </div>
+                                <div>
+                                    <span class="text-[10px] text-gray-400 dark:text-slate-500"
+                                        >At├®</span
+                                    >
+                                    <div class="grid grid-cols-3 gap-1">
+                                        <input
+                                            type="number"
+                                            id="p-epoca-fim-dia"
+                                            placeholder="Dia"
+                                            min="1"
+                                            max="31"
+                                            title="Dia"
+                                        />
+                                        <input
+                                            type="number"
+                                            id="p-epoca-fim-mes"
+                                            placeholder="M├¬s"
+                                            min="1"
+                                            max="12"
+                                            title="M├¬s"
+                                        />
+                                        <input
+                                            type="number"
+                                            id="p-epoca-fim-ano"
+                                            placeholder="Ano"
+                                            min="1900"
+                                            max="2100"
+                                            title="Ano"
+                                        />
+                                    </div>
                                 </div>
                             </div>
-                        </div>
 
-                        <label class="form-label mt-3">Contexto Hist├│rico/Pessoal</label>
-                        <textarea
-                            id="p-contexto"
-                            class="text-sm h-20"
-                            placeholder="O que inspirou o texto ÔÇö acontecimentos, eventos, vida pessoal..."
-                        ></textarea>
+                            <label
+                                class="text-[10px] text-gray-400 dark:text-slate-500 mt-2 block"
+                                for="p-epoca-recorte"
+                                >Recorte</label
+                            >
+                            <select id="p-epoca-recorte" class="text-sm">
+                                <option value="">N├úo especificado</option>
+                                <option value="momento">Momento</option>
+                                <option value="repercuss├úo">Momento e Repercuss├úo</option>
+                            </select>
+                            <p class="text-[10px] text-gray-400 dark:text-slate-500 mb-2">
+                                O poema retrata o evento em si, ou o efeito dele depois?
+                            </p>
+
+                            <label class="form-label mt-3">Contexto Hist├│rico/Pessoal</label>
+                            <textarea
+                                id="p-contexto"
+                                class="text-sm h-20"
+                                placeholder="O que inspirou o texto ÔÇö acontecimentos, eventos, vida pessoal..."
+                            ></textarea>
                         </div>
                     </details>
 
-
                     <label class="form-label">Texto</label>
                     <div
                         class="flex flex-wrap items-center gap-2 mb-1 p-2 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-t-md"
@@ -366,408 +384,832 @@
 
                     <label class="form-label mt-3">Notas</label>
                     <textarea id="p-notas" class="text-sm h-20"></textarea>
-                </div>
-
-                <div
-                    class="space-y-4 bg-gray-50 dark:bg-slate-800 p-4 rounded-lg border border-gray-300 dark:border-slate-600"
-                >
-                    <details class="campo-grupo" open>
-                        <summary>Livros e destino</summary>
-                        <div class="campo-grupo-corpo">
-                    <label class="form-label">Livros</label>
-                    <select id="p-livros" multiple class="h-24 text-xs"></select>
-                    <label
-                        class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
-                        >Filtrar por Livro (opcional)</label
-                    >
-                    <select
-                        id="p-destino-filtro"
-                        onchange="filtrarDestinoPoema(this.value)"
-                        class="w-full p-3 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl text-xs mb-2"
-                    >
-                        <option value="">-- Todos os livros --</option>
-                    </select>
-                    <label
-                        class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
-                        >Destino (Parte ou Se├º├úo)</label
-                    >
-                    <select
-                        id="p-destino"
-                        onchange="
-                            sugerirSequencia();
-                            autoPreencherDataPublicacao(this.value, 'p');
-                        "
-                        class="w-full p-3 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl text-xs focus:ring-2 focus:ring-blue-500 outline-none"
-                    ></select>
-                    <div id="p-coletaneas-info"></div>
-                        </div>
-                    </details>
 
-                    <details class="campo-grupo">
-                        <summary>
-                            Elos, refer├¬ncias e intertextualidade
-                        </summary>
-                        <div class="campo-grupo-corpo">
-                    <label class="form-label">Elos</label>
-                    <select
-                        id="p-elos-select"
-                        multiple
-                        class="h-24 text-xs bg-blue-50 dark:bg-blue-950"
-                    ></select>
-                    <label class="form-label">Refer├¬ncias</label>
-                    <select
-                        id="p-refs-select"
-                        multiple
-                        class="h-24 text-xs bg-blue-50 dark:bg-blue-950"
-                    ></select>
-                    <label class="form-label"
-                        >Intertextualidade
-                        <span class="font-normal normal-case text-gray-400 dark:text-slate-500"
-                            >(di├ílogo com algo externo ao arquivo ÔÇö m├║sica, livro, s├®rie... pode ter
-                            mais de uma refer├¬ncia, de tipos diferentes)</span
-                        ></label
-                    >
+                    <label class="form-label mt-3">Autoria</label>
                     <div class="flex gap-2 mb-2">
-                        <select id="p-intertexto-tipo" class="text-xs w-28 flex-shrink-0">
-                            <option value="">Tipo...</option>
-                            <option value="Livro">Livro</option>
-                            <option value="Texto">Texto</option>
-                            <option value="M├║sica">M├║sica</option>
-                            <option value="Filme/S├®rie">Filme/S├®rie</option>
-                            <option value="V├¡deo">V├¡deo</option>
-                            <option value="Cita├º├úo">Cita├º├úo</option>
-                            <option value="Conversa">Conversa</option>
-                            <option value="Outro">Outro</option>
-                        </select>
                         <input
                             type="text"
-                            id="p-intertexto-texto"
-                            list="sugestoes-intertexto"
+                            id="p-autor-input"
+                            list="sugestoes-autores"
                             class="text-sm flex-1"
-                            placeholder="Ex.: '├üguas de Mar├ºo', Tom Jobim"
+                            placeholder="Nome do autor..."
                         />
                         <button
                             type="button"
-                            id="p-intertexto-btn-add"
-                            onclick="adicionarIntertexto()"
-                            class="bg-purple-100 dark:bg-purple-900 text-purple-700 dark:text-purple-300 px-3 rounded text-xs font-bold flex-shrink-0"
+                            onclick="adicionarAutoria()"
+                            class="bg-indigo-100 dark:bg-indigo-900 text-indigo-700 dark:text-indigo-300 px-3 rounded text-xs font-bold"
                         >
                             +
                         </button>
-                        <button
-                            type="button"
-                            id="p-intertexto-btn-cancelar"
-                            onclick="cancelarEdicaoIntertexto()"
-                            class="hidden text-gray-500 dark:text-slate-400 hover:text-gray-700 dark:hover:text-slate-200 px-2 text-xs flex-shrink-0"
-                        >
-                            Cancelar
-                        </button>
                     </div>
-                    <datalist id="sugestoes-intertexto"></datalist>
                     <div
-                        id="p-intertexto-lista"
-                        class="space-y-1 mb-2 min-h-[12px]"
+                        id="p-autoria-container"
+                        class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
                     ></div>
+                </div>
+
+                <div
+                    class="space-y-4 bg-gray-50 dark:bg-slate-800 p-4 rounded-lg border border-gray-300 dark:border-slate-600"
+                >
+                    <details class="campo-grupo" open>
+                        <summary>Livros e destino</summary>
+                        <div class="campo-grupo-corpo">
+                            <label class="form-label">Livros</label>
+                            <select id="p-livros" multiple class="h-24 text-xs"></select>
+                            <label
+                                class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
+                                >Filtrar por Livro (opcional)</label
+                            >
+                            <select
+                                id="p-destino-filtro"
+                                onchange="filtrarDestinoPoema(this.value)"
+                                class="w-full p-3 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl text-xs mb-2"
+                            >
+                                <option value="">-- Todos os livros --</option>
+                            </select>
+                            <label
+                                class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
+                                >Destino (Parte ou Se├º├úo)</label
+                            >
+                            <select
+                                id="p-destino"
+                                onchange="
+                                    sugerirSequencia();
+                                    autoPreencherDataPublicacao(this.value, 'p');
+                                "
+                                class="w-full p-3 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl text-xs focus:ring-2 focus:ring-blue-500 outline-none"
+                            ></select>
+                            <div id="p-coletaneas-info"></div>
+                        </div>
+                    </details>
+
+                    <details class="campo-grupo">
+                        <summary>Elos, refer├¬ncias e intertextualidade</summary>
+                        <div class="campo-grupo-corpo">
+                            <label class="form-label"
+                                >Elos
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(liga├º├úo estrutural com outro poema do acervo ÔÇö reescrita,
+                                    tradu├º├úo, continua├º├úo...)</span
+                                ></label
+                            >
+                            <div class="flex gap-2 mb-1">
+                                <select
+                                    id="p-elo-relacao"
+                                    class="text-xs w-28 flex-shrink-0"
+                                    onchange="onRelacaoEloAlterada()"
+                                ></select>
+                                <select id="p-elo-poema" class="text-xs flex-1"></select>
+                                <button
+                                    type="button"
+                                    id="p-elo-btn-add"
+                                    onclick="adicionarElo()"
+                                    class="bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 px-3 rounded text-xs font-bold flex-shrink-0"
+                                >
+                                    +
+                                </button>
+                                <button
+                                    type="button"
+                                    id="p-elo-btn-cancelar"
+                                    onclick="cancelarEdicaoElo()"
+                                    class="hidden text-gray-500 dark:text-slate-400 hover:text-gray-700 dark:hover:text-slate-200 px-2 text-xs flex-shrink-0"
+                                >
+                                    Cancelar
+                                </button>
+                            </div>
+                            <div class="flex gap-2 mb-2 items-center">
+                                <span
+                                    class="text-[10px] text-gray-400 dark:text-slate-500 flex-shrink-0"
+                                    >Este poema ├®:</span
+                                >
+                                <button
+                                    type="button"
+                                    id="p-elo-direcao-origem"
+                                    onclick="selecionarDirecaoElo('origem')"
+                                    class="elo-direcao-btn text-xs flex-1 px-2 py-1 rounded border border-slate-200 dark:border-slate-700 text-gray-500 dark:text-slate-400"
+                                >
+                                    Origem
+                                </button>
+                                <button
+                                    type="button"
+                                    id="p-elo-direcao-destino"
+                                    onclick="selecionarDirecaoElo('destino')"
+                                    class="elo-direcao-btn text-xs flex-1 px-2 py-1 rounded border border-slate-200 dark:border-slate-700 text-gray-500 dark:text-slate-400"
+                                >
+                                    Destino
+                                </button>
+                                <input type="hidden" id="p-elo-direcao" value="" />
+                            </div>
+                            <input
+                                type="text"
+                                id="p-elo-texto"
+                                class="text-sm w-full mb-2"
+                                placeholder="Nota opcional sobre esse elo..."
+                            />
+                            <div id="p-elos-lista" class="space-y-1 mb-2 min-h-[12px]"></div>
+                            <div id="p-elos-derivados" class="space-y-1 mb-2 hidden"></div>
+
+                            <label class="form-label"
+                                >Refer├¬ncias
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(liga├º├úo mais solta com outro poema ÔÇö personagem em comum,
+                                    imagem compartilhada...)</span
+                                ></label
+                            >
+                            <div class="flex gap-2 mb-2">
+                                <select id="p-ref-tipo" class="text-xs w-28 flex-shrink-0"></select>
+                                <select id="p-ref-poema" class="text-xs flex-1"></select>
+                                <button
+                                    type="button"
+                                    id="p-ref-btn-add"
+                                    onclick="adicionarReferencia()"
+                                    class="bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 px-3 rounded text-xs font-bold flex-shrink-0"
+                                >
+                                    +
+                                </button>
+                                <button
+                                    type="button"
+                                    id="p-ref-btn-cancelar"
+                                    onclick="cancelarEdicaoReferencia()"
+                                    class="hidden text-gray-500 dark:text-slate-400 hover:text-gray-700 dark:hover:text-slate-200 px-2 text-xs flex-shrink-0"
+                                >
+                                    Cancelar
+                                </button>
+                            </div>
+                            <input
+                                type="text"
+                                id="p-ref-texto"
+                                class="text-sm w-full mb-2"
+                                placeholder="Nota opcional sobre essa refer├¬ncia..."
+                            />
+                            <div id="p-refs-lista" class="space-y-1 mb-2 min-h-[12px]"></div>
+                            <label class="form-label"
+                                >Intertextualidade
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(di├ílogo com algo externo ao arquivo ÔÇö m├║sica, livro, s├®rie...
+                                    pode ter mais de uma refer├¬ncia, de tipos diferentes)</span
+                                ></label
+                            >
+                            <div class="flex gap-2 mb-2">
+                                <select id="p-intertexto-tipo" class="text-xs w-28 flex-shrink-0">
+                                    <option value="">Tipo...</option>
+                                    <option value="Livro">Livro</option>
+                                    <option value="Texto">Texto</option>
+                                    <option value="M├║sica">M├║sica</option>
+                                    <option value="Filme/S├®rie">Filme/S├®rie</option>
+                                    <option value="V├¡deo">V├¡deo</option>
+                                    <option value="Cita├º├úo">Cita├º├úo</option>
+                                    <option value="Conversa">Conversa</option>
+                                    <option value="Outro">Outro</option>
+                                </select>
+                                <input
+                                    type="text"
+                                    id="p-intertexto-texto"
+                                    list="sugestoes-intertexto"
+                                    class="text-sm flex-1"
+                                    placeholder="Ex.: '├üguas de Mar├ºo', Tom Jobim"
+                                />
+                                <button
+                                    type="button"
+                                    id="p-intertexto-btn-add"
+                                    onclick="adicionarIntertexto()"
+                                    class="bg-purple-100 dark:bg-purple-900 text-purple-700 dark:text-purple-300 px-3 rounded text-xs font-bold flex-shrink-0"
+                                >
+                                    +
+                                </button>
+                                <button
+                                    type="button"
+                                    id="p-intertexto-btn-cancelar"
+                                    onclick="cancelarEdicaoIntertexto()"
+                                    class="hidden text-gray-500 dark:text-slate-400 hover:text-gray-700 dark:hover:text-slate-200 px-2 text-xs flex-shrink-0"
+                                >
+                                    Cancelar
+                                </button>
+                            </div>
+                            <datalist id="sugestoes-intertexto"></datalist>
+                            <div id="p-intertexto-lista" class="space-y-1 mb-2 min-h-[12px]"></div>
                         </div>
                     </details>
 
                     <details class="campo-grupo">
                         <summary>Anexos</summary>
                         <div class="campo-grupo-corpo">
-                    <label class="form-label"
-                        >Anexos
-                        <span class="font-normal normal-case text-gray-400 dark:text-slate-500"
-                            >(imagens, letterings, v├¡deos... que acompanham o texto ÔÇö uma ou v├írios, de tipos diferentes)</span
-                        ></label
-                    >
-                    <div class="flex gap-2 mb-2">
-                        <select id="p-anexo-tipo" class="text-xs w-36 flex-shrink-0">
-                            <option value="">Tipo...</option>
-                            <option value="Ilustra├º├úo">Ilustra├º├úo</option>
-                            <option value="Foto">Foto</option>
-                            <option value="Lettering">Lettering</option>
-                            <option value="Declama├º├úo em v├¡deo">Declama├º├úo em v├¡deo</option>
-                            <option value="Coment├írios em v├¡deo">Coment├írios em v├¡deo</option>
-                            <option value="Outro">Outro</option>
-                        </select>
-                        <input
-                            type="text"
-                            id="p-anexo-link"
-                            class="text-sm flex-1"
-                            placeholder="Link (obrigat├│rio para v├¡deo)"
-                        />
-                    </div>
-                    <div class="flex flex-col gap-2 mb-2">
-                        <textarea
-                            id="p-anexo-input"
-                            class="text-sm h-20"
-                            placeholder="Descri├º├úo do anexo..."
-                        ></textarea>
-                        <button
-                            type="button"
-                            id="p-anexo-btn-add"
-                            onclick="adicionarAnexo()"
-                            class="bg-indigo-100 dark:bg-indigo-900 text-indigo-700 dark:text-indigo-300 px-3 py-1.5 rounded text-xs font-bold self-end"
-                        >
-                            + Adicionar anexo
-                        </button>
-                        <button
-                            type="button"
-                            id="p-anexo-btn-cancelar"
-                            onclick="cancelarEdicaoAnexo()"
-                            class="hidden text-gray-500 dark:text-slate-400 hover:text-gray-700 dark:hover:text-slate-200 text-xs self-end px-1"
-                        >
-                            Cancelar edi├º├úo
-                        </button>
-                    </div>
-                    <div
-                        id="p-anexos-lista"
-                        class="space-y-1 mb-2 min-h-[12px]"
-                    ></div>
-                    <label class="form-label"
-                        >Nota sobre o conjunto
-                        <span class="font-normal normal-case text-gray-400 dark:text-slate-500"
-                            >(quando os anexos se relacionam entre si ÔÇö tema, estilo, unidade ÔÇö
-                            n├úo cada um isoladamente)</span
-                        ></label
-                    >
-                    <textarea id="p-anexos-nota-geral" class="text-sm h-20"></textarea>
+                            <label class="form-label"
+                                >Anexos
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(imagens, letterings, v├¡deos... que acompanham o texto ÔÇö uma ou
+                                    v├írios, de tipos diferentes)</span
+                                ></label
+                            >
+                            <div class="flex gap-2 mb-2">
+                                <select id="p-anexo-tipo" class="text-xs w-36 flex-shrink-0">
+                                    <option value="">Tipo...</option>
+                                    <option value="Ilustra├º├úo">Ilustra├º├úo</option>
+                                    <option value="Foto">Foto</option>
+                                    <option value="Lettering">Lettering</option>
+                                    <option value="Declama├º├úo em v├¡deo">Declama├º├úo em v├¡deo</option>
+                                    <option value="Coment├írios em v├¡deo">
+                                        Coment├írios em v├¡deo
+                                    </option>
+                                    <option value="Outro">Outro</option>
+                                </select>
+                                <input
+                                    type="text"
+                                    id="p-anexo-link"
+                                    class="text-sm flex-1"
+                                    placeholder="Link (obrigat├│rio para v├¡deo)"
+                                />
+                            </div>
+                            <div class="flex flex-col gap-2 mb-2">
+                                <textarea
+                                    id="p-anexo-input"
+                                    class="text-sm h-20"
+                                    placeholder="Descri├º├úo do anexo..."
+                                ></textarea>
+                                <button
+                                    type="button"
+                                    id="p-anexo-btn-add"
+                                    onclick="adicionarAnexo()"
+                                    class="bg-indigo-100 dark:bg-indigo-900 text-indigo-700 dark:text-indigo-300 px-3 py-1.5 rounded text-xs font-bold self-end"
+                                >
+                                    + Adicionar anexo
+                                </button>
+                                <button
+                                    type="button"
+                                    id="p-anexo-btn-cancelar"
+                                    onclick="cancelarEdicaoAnexo()"
+                                    class="hidden text-gray-500 dark:text-slate-400 hover:text-gray-700 dark:hover:text-slate-200 text-xs self-end px-1"
+                                >
+                                    Cancelar edi├º├úo
+                                </button>
+                            </div>
+                            <div id="p-anexos-lista" class="space-y-1 mb-2 min-h-[12px]"></div>
+                            <label class="form-label"
+                                >Nota sobre o conjunto
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(quando os anexos se relacionam entre si ÔÇö tema, estilo,
+                                    unidade ÔÇö n├úo cada um isoladamente)</span
+                                ></label
+                            >
+                            <textarea id="p-anexos-nota-geral" class="text-sm h-20"></textarea>
                         </div>
                     </details>
 
                     <details class="campo-grupo">
                         <summary>Anota├º├Áes marginais e descri├º├úo visual</summary>
                         <div class="campo-grupo-corpo">
-                    <label class="form-label"
-                        >Anota├º├Áes Marginais
-                        <span class="font-normal normal-case text-gray-400 dark:text-slate-500"
-                            >(coment├írio de outra voz escrito por cima do texto, associado a um
-                            verso ou estrofe ÔÇö diferente de Intertextualidade, que dialoga com algo
-                            de fora do arquivo)</span
-                        ></label
-                    >
-                    <div class="flex gap-2 mb-2">
-                        <input
-                            type="text"
-                            id="p-anotacao-trecho"
-                            class="text-sm flex-1"
-                            placeholder="Verso ou estrofe de refer├¬ncia..."
-                        />
-                    </div>
-                    <div class="flex gap-2 mb-2">
-                        <input
-                            type="text"
-                            id="p-anotacao-posicao"
-                            list="sugestoes-posicoes-anotacoes"
-                            class="text-xs flex-1"
-                            placeholder="Posi├º├úo (ex.: ├á direita, abaixo e ├á esquerda...)"
-                        />
-                        <input
-                            type="text"
-                            id="p-anotacao-fonte"
-                            list="sugestoes-fontes-anotacoes"
-                            class="text-xs flex-1"
-                            placeholder="Fonte (ex.: Baguet Script)"
-                        />
-                    </div>
-                    <datalist id="sugestoes-posicoes-anotacoes"></datalist>
-                    <datalist id="sugestoes-fontes-anotacoes"></datalist>
-                    <div class="flex flex-col gap-2 mb-2">
-                        <textarea
-                            id="p-anotacao-texto"
-                            class="text-sm h-20"
-                            placeholder="Texto da anota├º├úo..."
-                        ></textarea>
-                        <button
-                            type="button"
-                            id="p-anotacao-btn-add"
-                            onclick="adicionarAnotacao()"
-                            class="bg-amber-100 dark:bg-amber-900 text-amber-700 dark:text-amber-300 px-3 py-1.5 rounded text-xs font-bold self-end"
-                        >
-                            + Adicionar anota├º├úo
-                        </button>
-                        <button
-                            type="button"
-                            id="p-anotacao-btn-cancelar"
-                            onclick="cancelarEdicaoAnotacao()"
-                            class="hidden text-gray-500 dark:text-slate-400 hover:text-gray-700 dark:hover:text-slate-200 text-xs self-end px-1"
-                        >
-                            Cancelar edi├º├úo
-                        </button>
-                    </div>
-                    <div
-                        id="p-anotacoes-lista"
-                        class="space-y-1 mb-2 min-h-[12px]"
-                    ></div>
+                            <label class="form-label"
+                                >Anota├º├Áes Marginais
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(coment├írio de outra voz escrito por cima do texto, associado a
+                                    um verso ou estrofe ÔÇö diferente de Intertextualidade, que
+                                    dialoga com algo de fora do arquivo)</span
+                                ></label
+                            >
+                            <div class="flex gap-2 mb-2">
+                                <input
+                                    type="text"
+                                    id="p-anotacao-trecho"
+                                    class="text-sm flex-1"
+                                    placeholder="Verso ou estrofe de refer├¬ncia..."
+                                />
+                            </div>
+                            <div class="flex gap-2 mb-2">
+                                <input
+                                    type="text"
+                                    id="p-anotacao-posicao"
+                                    list="sugestoes-posicoes-anotacoes"
+                                    class="text-xs flex-1"
+                                    placeholder="Posi├º├úo (ex.: ├á direita, abaixo e ├á esquerda...)"
+                                />
+                                <input
+                                    type="text"
+                                    id="p-anotacao-fonte"
+                                    list="sugestoes-fontes-anotacoes"
+                                    class="text-xs flex-1"
+                                    placeholder="Fonte (ex.: Baguet Script)"
+                                />
+                            </div>
+                            <datalist id="sugestoes-posicoes-anotacoes"></datalist>
+                            <datalist id="sugestoes-fontes-anotacoes"></datalist>
+                            <div class="flex flex-col gap-2 mb-2">
+                                <textarea
+                                    id="p-anotacao-texto"
+                                    class="text-sm h-20"
+                                    placeholder="Texto da anota├º├úo..."
+                                ></textarea>
+                                <button
+                                    type="button"
+                                    id="p-anotacao-btn-add"
+                                    onclick="adicionarAnotacao()"
+                                    class="bg-amber-100 dark:bg-amber-900 text-amber-700 dark:text-amber-300 px-3 py-1.5 rounded text-xs font-bold self-end"
+                                >
+                                    + Adicionar anota├º├úo
+                                </button>
+                                <button
+                                    type="button"
+                                    id="p-anotacao-btn-cancelar"
+                                    onclick="cancelarEdicaoAnotacao()"
+                                    class="hidden text-gray-500 dark:text-slate-400 hover:text-gray-700 dark:hover:text-slate-200 text-xs self-end px-1"
+                                >
+                                    Cancelar edi├º├úo
+                                </button>
+                            </div>
+                            <div id="p-anotacoes-lista" class="space-y-1 mb-2 min-h-[12px]"></div>
 
-                    <label class="form-label">Descri├º├úo Visual</label>
-                    <textarea
-                        id="p-visual"
-                        class="text-sm h-20"
-                        placeholder="Disposi├º├úo espacial, forma, elementos concretistas..."
-                    ></textarea>
+                            <label class="form-label">Descri├º├úo Visual</label>
+                            <textarea
+                                id="p-visual"
+                                class="text-sm h-20"
+                                placeholder="Disposi├º├úo espacial, forma, elementos concretistas..."
+                            ></textarea>
                         </div>
                     </details>
 
                     <details class="campo-grupo">
                         <summary>Oculta├º├úo e conte├║do sens├¡vel</summary>
                         <div class="campo-grupo-corpo">
-                    <label class="form-label"
-                        >Oculta├º├úo
-                        <span class="font-normal normal-case text-gray-400 dark:text-slate-500"
-                            >(notas sobre oculta├º├úo de dados)</span
-                        ></label
-                    >
-                    <textarea id="p-ocultacao" class="text-sm h-20"></textarea>
-                    <label class="form-label"
-                        >Conte├║do Sens├¡vel
-                        <span class="font-normal normal-case text-gray-400 dark:text-slate-500"
-                            >(preencher marca o texto para revis├úo em Vers├Áes Alternativas)</span
-                        ></label
-                    >
-                    <textarea
-                        id="p-sensivel"
-                        class="text-sm h-20 border-amber-300 dark:border-amber-700"
-                    ></textarea>
-                    <label class="form-label"
-                        >Vocabul├írio Hiperacionante
-                        <span class="font-normal normal-case text-gray-400 dark:text-slate-500"
-                            >(preencher marca o texto para revis├úo em Vers├Áes Alternativas)</span
-                        ></label
-                    >
-                    <textarea
-                        id="p-hiperacionante"
-                        class="text-sm h-20 border-amber-300 dark:border-amber-700"
-                    ></textarea>
+                            <label class="form-label"
+                                >Oculta├º├úo
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(notas sobre oculta├º├úo de dados)</span
+                                ></label
+                            >
+                            <textarea id="p-ocultacao" class="text-sm h-20"></textarea>
+                            <label class="form-label"
+                                >Conte├║do Sens├¡vel
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(preencher marca o texto para revis├úo em Vers├Áes
+                                    Alternativas)</span
+                                ></label
+                            >
+                            <textarea
+                                id="p-sensivel"
+                                class="text-sm h-20 border-amber-300 dark:border-amber-700"
+                            ></textarea>
+                            <label class="form-label"
+                                >Vocabul├írio Hiperacionante
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(preencher marca o texto para revis├úo em Vers├Áes
+                                    Alternativas)</span
+                                ></label
+                            >
+                            <textarea
+                                id="p-hiperacionante"
+                                class="text-sm h-20 border-amber-300 dark:border-amber-700"
+                            ></textarea>
                         </div>
                     </details>
 
                     <details class="campo-grupo">
                         <summary>Sinaliza├º├Áes e dedicat├│ria</summary>
                         <div class="campo-grupo-corpo">
-                    <label class="form-label">Sinaliza├º├Áes (Etiquetas)</label>
-                    <div class="flex gap-2 mb-2">
-                        <input
-                            type="text"
-                            id="p-sinal-input"
-                            list="sugestoes-sinais"
-                            class="text-sm flex-1"
-                            placeholder="Nova etiqueta..."
-                        />
-                        <button
-                            type="button"
-                            onclick="adicionarTag()"
-                            class="bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 px-3 rounded text-xs font-bold"
-                        >
-                            +
-                        </button>
-                    </div>
-                    <div
-                        id="p-tags-container"
-                        class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
-                    ></div>
-                    <input type="hidden" id="p-sinal" />
-                    <label class="form-label">Dedicado a / Sobre quem (Pessoas)</label>
-                    <div class="flex gap-2 mb-2">
-                        <input
-                            type="text"
-                            id="p-pessoa-input"
-                            list="sugestoes-pessoas"
-                            class="text-sm flex-1"
-                            placeholder="Nome da pessoa..."
-                        />
-                        <button
-                            type="button"
-                            onclick="adicionarPessoa()"
-                            class="bg-rose-100 dark:bg-rose-900 text-rose-700 dark:text-rose-300 px-3 rounded text-xs font-bold"
-                        >
-                            +
-                        </button>
-                    </div>
-                    <div
-                        id="p-pessoas-container"
-                        class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
-                    ></div>
-                    <input type="hidden" id="p-pessoas" />
+                            <label class="form-label">Sinaliza├º├Áes ÔÇö Estilo</label>
+                            <div class="flex gap-2 mb-2">
+                                <input
+                                    type="text"
+                                    id="p-sinal-estilo-input"
+                                    list="sugestoes-sinais-estilo"
+                                    class="text-sm flex-1"
+                                    placeholder="Nova etiqueta de estilo..."
+                                />
+                                <button
+                                    type="button"
+                                    onclick="adicionarSinalEstilo()"
+                                    class="bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 px-3 rounded text-xs font-bold"
+                                >
+                                    +
+                                </button>
+                            </div>
+                            <div
+                                id="p-sinal-estilo-container"
+                                class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
+                            ></div>
+                            <input type="hidden" id="p-sinal-estilo" />
+
+                            <label class="form-label">Sinaliza├º├Áes ÔÇö Tema</label>
+                            <div class="flex gap-2 mb-2">
+                                <input
+                                    type="text"
+                                    id="p-sinal-tema-input"
+                                    list="sugestoes-sinais-tema"
+                                    class="text-sm flex-1"
+                                    placeholder="Nova etiqueta de tema..."
+                                />
+                                <button
+                                    type="button"
+                                    onclick="adicionarSinalTema()"
+                                    class="bg-emerald-100 dark:bg-emerald-900 text-emerald-700 dark:text-emerald-300 px-3 rounded text-xs font-bold"
+                                >
+                                    +
+                                </button>
+                            </div>
+                            <div
+                                id="p-sinal-tema-container"
+                                class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
+                            ></div>
+                            <input type="hidden" id="p-sinal-tema" />
+
+                            <label class="form-label">Sinaliza├º├Áes ÔÇö Rela├º├úo</label>
+                            <div class="flex gap-2 mb-2">
+                                <input
+                                    type="text"
+                                    id="p-sinal-relacao-input"
+                                    list="sugestoes-sinais-relacao"
+                                    class="text-sm flex-1"
+                                    placeholder="Nova etiqueta de rela├º├úo..."
+                                />
+                                <button
+                                    type="button"
+                                    onclick="adicionarSinalRelacao()"
+                                    class="bg-purple-100 dark:bg-purple-900 text-purple-700 dark:text-purple-300 px-3 rounded text-xs font-bold"
+                                >
+                                    +
+                                </button>
+                            </div>
+                            <div
+                                id="p-sinal-relacao-container"
+                                class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
+                            ></div>
+                            <input type="hidden" id="p-sinal-relacao" />
+
+                            <label class="form-label">Sinaliza├º├Áes ÔÇö Sensibilidade</label>
+                            <div class="flex gap-2 mb-2">
+                                <input
+                                    type="text"
+                                    id="p-sinal-sensibilidade-input"
+                                    list="sugestoes-sinais-sensibilidade"
+                                    class="text-sm flex-1"
+                                    placeholder="Nova etiqueta de sensibilidade..."
+                                />
+                                <button
+                                    type="button"
+                                    onclick="adicionarSinalSensibilidade()"
+                                    class="bg-amber-100 dark:bg-amber-900 text-amber-700 dark:text-amber-300 px-3 rounded text-xs font-bold"
+                                >
+                                    +
+                                </button>
+                            </div>
+                            <div
+                                id="p-sinal-sensibilidade-container"
+                                class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
+                            ></div>
+                            <input type="hidden" id="p-sinal-sensibilidade" />
+
+                            <label class="form-label">Sinaliza├º├Áes ÔÇö Tom</label>
+                            <div class="flex gap-2 mb-2">
+                                <input
+                                    type="text"
+                                    id="p-sinal-tom-input"
+                                    list="sugestoes-sinais-tom"
+                                    class="text-sm flex-1"
+                                    placeholder="Nova etiqueta de tom..."
+                                />
+                                <button
+                                    type="button"
+                                    onclick="adicionarSinalTom()"
+                                    class="bg-pink-100 dark:bg-pink-900 text-pink-700 dark:text-pink-300 px-3 rounded text-xs font-bold"
+                                >
+                                    +
+                                </button>
+                            </div>
+                            <div
+                                id="p-sinal-tom-container"
+                                class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
+                            ></div>
+                            <input type="hidden" id="p-sinal-tom" />
+
+                            <label class="form-label"
+                                >Sinaliza├º├Áes ÔÇö Outros
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(balde tempor├írio; ver "Reconhecimentos" e Elos de
+                                    Deriva├º├úo)</span
+                                ></label
+                            >
+                            <div class="flex gap-2 mb-2">
+                                <input
+                                    type="text"
+                                    id="p-sinal-outros-input"
+                                    list="sugestoes-sinais-outros"
+                                    class="text-sm flex-1"
+                                    placeholder="Etiqueta ainda sem categoria..."
+                                />
+                                <button
+                                    type="button"
+                                    onclick="adicionarSinalOutros()"
+                                    class="bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300 px-3 rounded text-xs font-bold"
+                                >
+                                    +
+                                </button>
+                            </div>
+                            <div
+                                id="p-sinal-outros-container"
+                                class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
+                            ></div>
+                            <input type="hidden" id="p-sinal-outros" />
+
+                            <label class="form-label">Pessoas</label>
+                            <div class="flex gap-2 mb-2">
+                                <input
+                                    type="text"
+                                    id="p-pessoa-input"
+                                    list="sugestoes-pessoas"
+                                    class="text-sm flex-1"
+                                    placeholder="Nome da pessoa..."
+                                />
+                                <button
+                                    type="button"
+                                    onclick="adicionarPessoa()"
+                                    class="bg-rose-100 dark:bg-rose-900 text-rose-700 dark:text-rose-300 px-3 rounded text-xs font-bold"
+                                >
+                                    +
+                                </button>
+                            </div>
+                            <div
+                                id="p-pessoas-container"
+                                class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
+                            ></div>
+                            <div
+                                id="p-pessoas-grupos-info"
+                                class="text-[11px] text-gray-400 dark:text-slate-500 mt-1.5"
+                            ></div>
                         </div>
                     </details>
 
                     <details class="campo-grupo">
-                        <summary>Status, migra├º├úo e descarte</summary>
+                        <summary>Envios e Rea├º├Áes</summary>
                         <div class="campo-grupo-corpo">
-                    <div>
-                        <label class="text-sm font-bold block mb-1">Status</label>
-                        <select id="p-status">
-                            <option value="incompleto">­ƒƒí Incompleto</option>
-                            <option value="completo" selected>ÔÜ¬ Completo</option>
-                            <option value="publicado">­ƒƒó Publicado</option>
-                            <option value="migrado">­ƒöÁ Migrado</option>
-                            <option value="descartado">­ƒö┤ Descartado</option>
-                        </select>
-                    </div>
-
-                    <div
-                        class="p-3 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-lg"
-                    >
-                        <label class="form-label mb-2"
-                            >Migra├º├úo entre livros
-                            <span class="font-normal normal-case text-gray-400 dark:text-slate-500"
-                                >(preencha para textos ­ƒöÁ Migrados de um livro/se├º├úo para
-                                outro)</span
-                            ></label
-                        >
-                        <span class="text-[10px] text-gray-400 dark:text-slate-500">Cortado de</span>
-                        <div class="grid grid-cols-2 gap-2 mb-2">
-                            <input
-                                type="text"
-                                id="p-cortado-livro"
-                                list="sugestoes-livros-migracao"
-                                placeholder="Livro de origem"
-                            />
-                            <input
-                                type="text"
-                                id="p-cortado-secao"
-                                list="sugestoes-secoes-migracao-cortado"
-                                placeholder="Parte/Se├º├úo de origem"
-                            />
+                            <label class="form-label"
+                                >Envios
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(quando o texto foi enviado/mostrado pra algu├®m, e como essa
+                                    pessoa reagiu ÔÇö pode haver mais de um envio pro mesmo
+                                    texto)</span
+                                ></label
+                            >
+                            <div class="flex gap-2 mb-2">
+                                <input
+                                    type="text"
+                                    id="p-envio-pessoa"
+                                    list="sugestoes-pessoas"
+                                    class="text-sm flex-1"
+                                    placeholder="Pra quem..."
+                                />
+                                <input
+                                    type="text"
+                                    id="p-envio-meio"
+                                    list="sugestoes-meios-envio"
+                                    class="text-sm flex-1"
+                                    placeholder="Meio (ex.: WhatsApp)"
+                                />
+                            </div>
+                            <div class="grid grid-cols-3 gap-1 mb-2">
+                                <input
+                                    type="number"
+                                    id="p-envio-dia"
+                                    placeholder="Dia"
+                                    min="1"
+                                    max="31"
+                                    title="Dia"
+                                />
+                                <input
+                                    type="number"
+                                    id="p-envio-mes"
+                                    placeholder="M├¬s"
+                                    min="1"
+                                    max="12"
+                                    title="M├¬s"
+                                />
+                                <input
+                                    type="number"
+                                    id="p-envio-ano"
+                                    placeholder="Ano"
+                                    min="1900"
+                                    max="2100"
+                                    title="Ano"
+                                />
+                            </div>
+                            <datalist id="sugestoes-meios-envio"></datalist>
+                            <div class="flex flex-col gap-2 mb-2">
+                                <textarea
+                                    id="p-envio-reacao"
+                                    class="text-sm h-16"
+                                    placeholder="Rea├º├úo..."
+                                ></textarea>
+                                <textarea
+                                    id="p-envio-notas"
+                                    class="text-xs h-14"
+                                    placeholder="Notas (opcional)..."
+                                ></textarea>
+                                <div class="flex items-center gap-2 self-end">
+                                    <button
+                                        type="button"
+                                        id="p-envio-btn-cancelar"
+                                        onclick="cancelarEdicaoEnvio()"
+                                        class="hidden text-gray-500 dark:text-slate-400 hover:text-gray-700 dark:hover:text-slate-200 text-xs px-1"
+                                    >
+                                        Cancelar edi├º├úo
+                                    </button>
+                                    <button
+                                        type="button"
+                                        id="p-envio-btn-add"
+                                        onclick="adicionarEnvio()"
+                                        class="bg-teal-100 dark:bg-teal-900 text-teal-700 dark:text-teal-300 px-3 py-1.5 rounded text-xs font-bold"
+                                    >
+                                        + Adicionar envio
+                                    </button>
+                                </div>
+                            </div>
+                            <div id="p-envios-lista" class="space-y-1 mb-2 min-h-[12px]"></div>
                         </div>
-                        <span class="text-[10px] text-gray-400 dark:text-slate-500">Lan├ºado em</span>
-                        <div class="grid grid-cols-2 gap-2">
-                            <input
-                                type="text"
-                                id="p-lancado-livro"
-                                list="sugestoes-livros-migracao"
-                                placeholder="Livro de destino"
-                            />
-                            <input
-                                type="text"
-                                id="p-lancado-secao"
-                                list="sugestoes-secoes-migracao-lancado"
-                                placeholder="Parte/Se├º├úo de destino"
-                            />
+                    </details>
+
+                    <details class="campo-grupo">
+                        <summary>Reconhecimentos</summary>
+                        <div class="campo-grupo-corpo">
+                            <label class="form-label"
+                                >Pr├¬mios e men├º├Áes
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(concursos, pr├¬mios ou men├º├Áes que este texto recebeu)</span
+                                ></label
+                            >
+                            <div class="flex gap-2 mb-2">
+                                <input
+                                    type="text"
+                                    id="p-reconhecimento-premio"
+                                    list="sugestoes-premios"
+                                    class="text-sm flex-1"
+                                    placeholder="Pr├¬mio/concurso..."
+                                />
+                                <input
+                                    type="text"
+                                    id="p-reconhecimento-posicao"
+                                    class="text-sm flex-1"
+                                    placeholder="Posi├º├úo (ex.: 1┬║ lugar)"
+                                />
+                                <input
+                                    type="number"
+                                    id="p-reconhecimento-ano"
+                                    class="text-sm w-24"
+                                    placeholder="Ano"
+                                    min="1900"
+                                    max="2100"
+                                    title="Ano"
+                                />
+                            </div>
+                            <datalist id="sugestoes-premios"></datalist>
+                            <div class="flex flex-col gap-2 mb-2">
+                                <textarea
+                                    id="p-reconhecimento-texto"
+                                    class="text-xs h-14"
+                                    placeholder="Notas (opcional)..."
+                                ></textarea>
+                                <div class="flex items-center gap-2 self-end">
+                                    <button
+                                        type="button"
+                                        id="p-reconhecimento-btn-cancelar"
+                                        onclick="cancelarEdicaoReconhecimento()"
+                                        class="hidden text-gray-500 dark:text-slate-400 hover:text-gray-700 dark:hover:text-slate-200 text-xs px-1"
+                                    >
+                                        Cancelar edi├º├úo
+                                    </button>
+                                    <button
+                                        type="button"
+                                        id="p-reconhecimento-btn-add"
+                                        onclick="adicionarReconhecimento()"
+                                        class="bg-amber-100 dark:bg-amber-900 text-amber-700 dark:text-amber-300 px-3 py-1.5 rounded text-xs font-bold"
+                                    >
+                                        + Adicionar reconhecimento
+                                    </button>
+                                </div>
+                            </div>
+                            <div
+                                id="p-reconhecimentos-lista"
+                                class="space-y-1 mb-2 min-h-[12px]"
+                            ></div>
                         </div>
-                        <p class="text-[10px] text-gray-400 dark:text-slate-500 mt-1">
-                            Texto livre ÔÇö aceita sugest├Áes de livros/partes/se├º├Áes j├í cadastrados,
-                            mas tamb├®m aceita algo que n├úo exista no arquivo (ex.: um livro de
-                            origem nunca cadastrado aqui). Escolher uma Se├º├úo j├í cadastrada
-                            preenche o Livro sozinho; escolher o Livro filtra as Se├º├Áes sugeridas
-                            pra s├│ as daquele livro.
-                        </p>
-                        <datalist id="sugestoes-livros-migracao"></datalist>
-                        <datalist id="sugestoes-secoes-migracao-cortado"></datalist>
-                        <datalist id="sugestoes-secoes-migracao-lancado"></datalist>
-                    </div>
+                    </details>
 
-                    <label class="form-label"
-                        >Descarte
-                        <span class="font-normal normal-case text-gray-400 dark:text-slate-500"
-                            >(preencha a justificativa para textos ­ƒö┤ Descartados)</span
-                        ></label
-                    >
-                    <textarea
-                        id="p-descarte"
-                        class="text-sm h-20 border-amber-300 dark:border-amber-700"
-                    ></textarea>
+                    <details class="campo-grupo">
+                        <summary>Status e Pend├¬ncias</summary>
+                        <div class="campo-grupo-corpo">
+                            <div>
+                                <label class="text-sm font-bold block mb-1">Status</label>
+                                <select id="p-status">
+                                    <option value="incompleto">­ƒƒí Incompleto</option>
+                                    <option value="completo" selected>ÔÜ¬ Completo</option>
+                                    <option value="publicado">­ƒƒó Publicado</option>
+                                    <option value="migrado">­ƒöÁ Migrado</option>
+                                    <option value="descartado">­ƒö┤ Descartado</option>
+                                </select>
+                            </div>
+
+                            <div
+                                class="p-3 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-lg"
+                            >
+                                <label class="form-label mb-2"
+                                    >Migra├º├úo entre livros
+                                    <span
+                                        class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                        >(preencha para textos ­ƒöÁ Migrados de um livro/se├º├úo para
+                                        outro)</span
+                                    ></label
+                                >
+                                <span class="text-[10px] text-gray-400 dark:text-slate-500"
+                                    >Cortado de</span
+                                >
+                                <div class="grid grid-cols-2 gap-2 mb-2">
+                                    <input
+                                        type="text"
+                                        id="p-cortado-livro"
+                                        list="sugestoes-livros-migracao"
+                                        placeholder="Livro de origem"
+                                    />
+                                    <input
+                                        type="text"
+                                        id="p-cortado-secao"
+                                        list="sugestoes-secoes-migracao-cortado"
+                                        placeholder="Parte/Se├º├úo de origem"
+                                    />
+                                </div>
+                                <span class="text-[10px] text-gray-400 dark:text-slate-500"
+                                    >Lan├ºado em</span
+                                >
+                                <div class="grid grid-cols-2 gap-2">
+                                    <input
+                                        type="text"
+                                        id="p-lancado-livro"
+                                        list="sugestoes-livros-migracao"
+                                        placeholder="Livro de destino"
+                                    />
+                                    <input
+                                        type="text"
+                                        id="p-lancado-secao"
+                                        list="sugestoes-secoes-migracao-lancado"
+                                        placeholder="Parte/Se├º├úo de destino"
+                                    />
+                                </div>
+                                <p class="text-[10px] text-gray-400 dark:text-slate-500 mt-1">
+                                    Texto livre ÔÇö aceita sugest├Áes de livros/partes/se├º├Áes j├í
+                                    cadastrados, mas tamb├®m aceita algo que n├úo exista no arquivo
+                                    (ex.: um livro de origem nunca cadastrado aqui). Escolher uma
+                                    Se├º├úo j├í cadastrada preenche o Livro sozinho; escolher o Livro
+                                    filtra as Se├º├Áes sugeridas pra s├│ as daquele livro.
+                                </p>
+                                <datalist id="sugestoes-livros-migracao"></datalist>
+                                <datalist id="sugestoes-secoes-migracao-cortado"></datalist>
+                                <datalist id="sugestoes-secoes-migracao-lancado"></datalist>
+
+                                <label
+                                    class="text-[10px] text-gray-400 dark:text-slate-500 block mt-2"
+                                    >Justificativa
+                                    <span class="normal-case"
+                                        >(raz├úo do arquivamento original, da migra├º├úo/recupera├º├úo,
+                                        ou ambos)</span
+                                    ></label
+                                >
+                                <textarea
+                                    id="p-migracao-justificativa"
+                                    class="text-sm h-20 border-blue-300 dark:border-blue-700"
+                                ></textarea>
+                            </div>
+
+                            <label class="form-label"
+                                >Pend├¬ncia
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(preencha o que falta resolver ÔÇö o ­ƒƒá aparece sozinho ao lado
+                                    do status enquanto houver texto aqui)</span
+                                ></label
+                            >
+                            <textarea
+                                id="p-pendencia"
+                                class="text-sm h-20 border-orange-300 dark:border-orange-700"
+                            ></textarea>
+
+                            <label class="form-label"
+                                >Descarte
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(preencha a justificativa para textos ­ƒö┤ Descartados)</span
+                                ></label
+                            >
+                            <textarea
+                                id="p-descarte"
+                                class="text-sm h-20 border-amber-300 dark:border-amber-700"
+                            ></textarea>
                         </div>
                     </details>
                 </div>
diff --git a/modais/modal-prosa.html b/modais/modal-prosa.html
index d896e5d..0e62316 100644
--- a/modais/modal-prosa.html
+++ b/modais/modal-prosa.html
@@ -59,7 +59,7 @@
                 <input type="hidden" id="pr-edit-id" />
 
                 <div class="md:col-span-2 space-y-4">
-                    <div class="grid grid-cols-3 gap-4">
+                    <div class="grid grid-cols-4 gap-4">
                         <div class="col-span-2">
                             <label class="form-label">T├¡tulo</label>
                             <input type="text" id="pr-titulo" required />
@@ -68,6 +68,16 @@
                             <label class="form-label">Sequ├¬ncia</label>
                             <input type="number" id="pr-sequencia" placeholder="Ex: 1" />
                         </div>
+                        <div>
+                            <label class="form-label">Idioma</label>
+                            <input
+                                type="text"
+                                id="pr-idioma"
+                                list="sugestoes-idioma-prosa"
+                                value="pt-BR"
+                            />
+                            <datalist id="sugestoes-idioma-prosa"></datalist>
+                        </div>
                     </div>
 
                     <div
@@ -162,35 +172,206 @@
                         </p>
                     </div>
 
+                    <details class="campo-grupo">
+                        <summary>
+                            ├ëpoca retratada e contexto
+                            <span class="campo-grupo-dica">(├®poca retratada, contexto)</span>
+                        </summary>
+                        <div class="campo-grupo-corpo">
+                            <div class="flex items-center justify-between mt-3">
+                                <label class="form-label mb-2"
+                                    >├ëpoca Retratada
+                                    <span
+                                        class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                        >(a que ├®poca a prosa se refere ÔÇö n├úo quando foi
+                                        escrita)</span
+                                    ></label
+                                >
+                                <label
+                                    class="flex items-center gap-1 text-xs text-gray-500 dark:text-slate-400 cursor-pointer mb-2 whitespace-nowrap"
+                                    title="Marque quando a prosa n├úo se referir a uma ├®poca espec├¡fica ÔÇö diferente de deixar em branco, que significa 'ainda n├úo categorizado'."
+                                >
+                                    <input
+                                        type="checkbox"
+                                        id="pr-epoca-na"
+                                        onchange="toggleCamposEpocaNaProsa()"
+                                    />
+                                    N├úo se aplica
+                                </label>
+                            </div>
+                            <input
+                                type="text"
+                                id="pr-epoca-nome"
+                                list="sugestoes-epoca-nome-prosa"
+                                class="text-sm mb-2"
+                                placeholder="Nome do per├¡odo (opcional) ÔÇö ex.: Namoro com Fulano, Luto, Pandemia..."
+                                onchange="aplicarSugestaoEpocaProsa()"
+                            />
+                            <datalist id="sugestoes-epoca-nome-prosa"></datalist>
+                            <p class="text-[10px] text-gray-400 dark:text-slate-500 -mt-1 mb-2">
+                                Repetir um nome j├í usado sugere as datas e o contexto do texto mais
+                                recente com esse nome ÔÇö s├│ nos campos que estiverem vazios, sem
+                                sobrescrever o que voc├¬ j├í preencheu.
+                            </p>
+                            <div class="grid grid-cols-2 gap-2">
+                                <div>
+                                    <span class="text-[10px] text-gray-400 dark:text-slate-500"
+                                        >De</span
+                                    >
+                                    <div class="grid grid-cols-3 gap-1">
+                                        <input
+                                            type="number"
+                                            id="pr-epoca-ini-dia"
+                                            placeholder="Dia"
+                                            min="1"
+                                            max="31"
+                                            title="Dia"
+                                        />
+                                        <input
+                                            type="number"
+                                            id="pr-epoca-ini-mes"
+                                            placeholder="M├¬s"
+                                            min="1"
+                                            max="12"
+                                            title="M├¬s"
+                                        />
+                                        <input
+                                            type="number"
+                                            id="pr-epoca-ini-ano"
+                                            placeholder="Ano"
+                                            min="1900"
+                                            max="2100"
+                                            title="Ano"
+                                        />
+                                    </div>
+                                </div>
+                                <div>
+                                    <span class="text-[10px] text-gray-400 dark:text-slate-500"
+                                        >At├®</span
+                                    >
+                                    <div class="grid grid-cols-3 gap-1">
+                                        <input
+                                            type="number"
+                                            id="pr-epoca-fim-dia"
+                                            placeholder="Dia"
+                                            min="1"
+                                            max="31"
+                                            title="Dia"
+                                        />
+                                        <input
+                                            type="number"
+                                            id="pr-epoca-fim-mes"
+                                            placeholder="M├¬s"
+                                            min="1"
+                                            max="12"
+                                            title="M├¬s"
+                                        />
+                                        <input
+                                            type="number"
+                                            id="pr-epoca-fim-ano"
+                                            placeholder="Ano"
+                                            min="1900"
+                                            max="2100"
+                                            title="Ano"
+                                        />
+                                    </div>
+                                </div>
+                            </div>
+
+                            <label
+                                class="text-[10px] text-gray-400 dark:text-slate-500 mt-2 block"
+                                for="pr-epoca-recorte"
+                                >Recorte</label
+                            >
+                            <select id="pr-epoca-recorte" class="text-sm">
+                                <option value="">N├úo especificado</option>
+                                <option value="momento">Momento</option>
+                                <option value="repercuss├úo">Repercuss├úo (e P├│s)</option>
+                            </select>
+                            <p class="text-[10px] text-gray-400 dark:text-slate-500 mb-2">
+                                A prosa retrata o evento em si, ou o efeito dele depois?
+                            </p>
+
+                            <label class="form-label mt-3">Contexto Hist├│rico/Pessoal</label>
+                            <textarea
+                                id="pr-contexto"
+                                class="text-sm h-20"
+                                placeholder="O que inspirou o texto ÔÇö acontecimentos, eventos, vida pessoal..."
+                            ></textarea>
+                        </div>
+                    </details>
+
                     <div>
                         <label class="form-label">Conte├║do da Prosa</label>
                         <textarea id="pr-texto" class="h-64 leading-relaxed"></textarea>
                     </div>
+
+                    <label class="form-label mt-3">Notas</label>
+                    <textarea id="pr-notas" class="text-sm h-20"></textarea>
+
+                    <label class="form-label mt-3">Autoria</label>
+                    <div class="flex gap-2 mb-2">
+                        <input
+                            type="text"
+                            id="pr-autor-input"
+                            list="sugestoes-autores-prosa"
+                            class="text-sm flex-1"
+                            placeholder="Nome do autor..."
+                        />
+                        <button
+                            type="button"
+                            onclick="adicionarAutoriaProsa()"
+                            class="bg-indigo-100 dark:bg-indigo-900 text-indigo-700 dark:text-indigo-300 px-3 rounded text-xs font-bold"
+                        >
+                            +
+                        </button>
+                    </div>
+                    <div
+                        id="pr-autoria-container"
+                        class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
+                    ></div>
+                    <datalist id="sugestoes-autores-prosa"></datalist>
                 </div>
 
                 <div
                     class="space-y-4 bg-gray-50 dark:bg-slate-800 p-4 rounded-lg border border-gray-300 dark:border-slate-600"
                 >
-                    <label class="form-label">Filtrar por Livro (opcional)</label>
-                    <select
-                        id="pr-destino-filtro"
-                        onchange="filtrarDestinoProsa(this.value)"
-                        class="w-full p-3 bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-xl text-xs mb-2"
-                    >
-                        <option value="">-- Todos os livros --</option>
-                    </select>
-                    <label
-                        class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
-                        >Destino (Vincular a:)</label
-                    >
-                    <select
-                        id="pr-destino"
-                        onchange="autoPreencherDataPublicacao(this.value, 'pr')"
-                        class="w-full p-3 bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-xl text-xs mb-4 focus:ring-2 focus:ring-emerald-500"
-                    >
-                        <option value="">-- Sem v├¡nculo (Avulso) --</option>
-                    </select>
-                    <div id="pr-coletaneas-info" class="mb-4"></div>
+                    <details class="campo-grupo" open>
+                        <summary>Livros e destino</summary>
+                        <div class="campo-grupo-corpo">
+                            <label class="form-label"
+                                >Livros
+                                <span class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(etiqueta editorial paralela ÔÇö n├úo confundir com Destino, abaixo)</span
+                                ></label
+                            >
+                            <select id="pr-livros" multiple class="h-24 text-xs mb-2"></select>
+                            <label
+                                class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
+                                >Filtrar por Livro (opcional)</label
+                            >
+                            <select
+                                id="pr-destino-filtro"
+                                onchange="filtrarDestinoProsa(this.value)"
+                                class="w-full p-3 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl text-xs mb-2"
+                            >
+                                <option value="">-- Todos os livros --</option>
+                            </select>
+                            <label
+                                class="block text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase mb-1"
+                                >Destino (Vincular a:)</label
+                            >
+                            <select
+                                id="pr-destino"
+                                onchange="autoPreencherDataPublicacao(this.value, 'pr')"
+                                class="w-full p-3 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl text-xs focus:ring-2 focus:ring-emerald-500 outline-none"
+                            >
+                                <option value="">-- Sem v├¡nculo (Avulso) --</option>
+                            </select>
+                            <div id="pr-coletaneas-info"></div>
+                        </div>
+                    </details>
+
                     <label class="form-label">G├¬nero</label>
                     <div class="flex gap-2 mb-2">
                         <input
@@ -214,30 +395,160 @@
                     ></div>
                     <datalist id="sugestoes-genero-prosa"></datalist>
                     <input type="hidden" id="pr-genero" />
-                    <label class="form-label">Sinaliza├º├Áes (Etiquetas)</label>
+
+                    <details class="campo-grupo">
+                        <summary>Sinaliza├º├Áes e dedicat├│ria</summary>
+                        <div class="campo-grupo-corpo">
+                    <label class="form-label">Sinaliza├º├Áes ÔÇö Estilo</label>
                     <div class="flex gap-2 mb-2">
                         <input
                             type="text"
-                            id="pr-sinal-input"
-                            list="sugestoes-sinais-prosa"
+                            id="pr-sinal-estilo-input"
+                            list="sugestoes-sinais-estilo-prosa"
                             class="text-sm flex-1"
-                            placeholder="Nova etiqueta..."
+                            placeholder="Nova etiqueta de estilo..."
                         />
                         <button
                             type="button"
-                            onclick="adicionarTagProsa()"
+                            onclick="adicionarSinalEstiloProsa()"
                             class="bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 px-3 rounded text-xs font-bold"
                         >
                             +
                         </button>
                     </div>
                     <div
-                        id="pr-tags-container"
+                        id="pr-sinal-estilo-container"
+                        class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
+                    ></div>
+                    <datalist id="sugestoes-sinais-estilo-prosa"></datalist>
+                    <input type="hidden" id="pr-sinal-estilo" />
+
+                    <label class="form-label">Sinaliza├º├Áes ÔÇö Tema</label>
+                    <div class="flex gap-2 mb-2">
+                        <input
+                            type="text"
+                            id="pr-sinal-tema-input"
+                            list="sugestoes-sinais-tema-prosa"
+                            class="text-sm flex-1"
+                            placeholder="Nova etiqueta de tema..."
+                        />
+                        <button
+                            type="button"
+                            onclick="adicionarSinalTemaProsa()"
+                            class="bg-emerald-100 dark:bg-emerald-900 text-emerald-700 dark:text-emerald-300 px-3 rounded text-xs font-bold"
+                        >
+                            +
+                        </button>
+                    </div>
+                    <div
+                        id="pr-sinal-tema-container"
+                        class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
+                    ></div>
+                    <datalist id="sugestoes-sinais-tema-prosa"></datalist>
+                    <input type="hidden" id="pr-sinal-tema" />
+
+                    <label class="form-label">Sinaliza├º├Áes ÔÇö Rela├º├úo</label>
+                    <div class="flex gap-2 mb-2">
+                        <input
+                            type="text"
+                            id="pr-sinal-relacao-input"
+                            list="sugestoes-sinais-relacao-prosa"
+                            class="text-sm flex-1"
+                            placeholder="Nova etiqueta de rela├º├úo..."
+                        />
+                        <button
+                            type="button"
+                            onclick="adicionarSinalRelacaoProsa()"
+                            class="bg-purple-100 dark:bg-purple-900 text-purple-700 dark:text-purple-300 px-3 rounded text-xs font-bold"
+                        >
+                            +
+                        </button>
+                    </div>
+                    <div
+                        id="pr-sinal-relacao-container"
+                        class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
+                    ></div>
+                    <datalist id="sugestoes-sinais-relacao-prosa"></datalist>
+                    <input type="hidden" id="pr-sinal-relacao" />
+
+                    <label class="form-label">Sinaliza├º├Áes ÔÇö Sensibilidade</label>
+                    <div class="flex gap-2 mb-2">
+                        <input
+                            type="text"
+                            id="pr-sinal-sensibilidade-input"
+                            list="sugestoes-sinais-sensibilidade-prosa"
+                            class="text-sm flex-1"
+                            placeholder="Nova etiqueta de sensibilidade..."
+                        />
+                        <button
+                            type="button"
+                            onclick="adicionarSinalSensibilidadeProsa()"
+                            class="bg-amber-100 dark:bg-amber-900 text-amber-700 dark:text-amber-300 px-3 rounded text-xs font-bold"
+                        >
+                            +
+                        </button>
+                    </div>
+                    <div
+                        id="pr-sinal-sensibilidade-container"
+                        class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
+                    ></div>
+                    <datalist id="sugestoes-sinais-sensibilidade-prosa"></datalist>
+                    <input type="hidden" id="pr-sinal-sensibilidade" />
+
+                    <label class="form-label">Sinaliza├º├Áes ÔÇö Tom</label>
+                    <div class="flex gap-2 mb-2">
+                        <input
+                            type="text"
+                            id="pr-sinal-tom-input"
+                            list="sugestoes-sinais-tom-prosa"
+                            class="text-sm flex-1"
+                            placeholder="Nova etiqueta de tom..."
+                        />
+                        <button
+                            type="button"
+                            onclick="adicionarSinalTomProsa()"
+                            class="bg-pink-100 dark:bg-pink-900 text-pink-700 dark:text-pink-300 px-3 rounded text-xs font-bold"
+                        >
+                            +
+                        </button>
+                    </div>
+                    <div
+                        id="pr-sinal-tom-container"
+                        class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
+                    ></div>
+                    <datalist id="sugestoes-sinais-tom-prosa"></datalist>
+                    <input type="hidden" id="pr-sinal-tom" />
+
+                    <label class="form-label"
+                        >Sinaliza├º├Áes ÔÇö Outros
+                        <span class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                            >(balde tempor├írio; ver "Reconhecimentos" e Elos de Deriva├º├úo)</span
+                        ></label
+                    >
+                    <div class="flex gap-2 mb-2">
+                        <input
+                            type="text"
+                            id="pr-sinal-outros-input"
+                            list="sugestoes-sinais-outros-prosa"
+                            class="text-sm flex-1"
+                            placeholder="Etiqueta ainda sem categoria..."
+                        />
+                        <button
+                            type="button"
+                            onclick="adicionarSinalOutrosProsa()"
+                            class="bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300 px-3 rounded text-xs font-bold"
+                        >
+                            +
+                        </button>
+                    </div>
+                    <div
+                        id="pr-sinal-outros-container"
                         class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
                     ></div>
-                    <datalist id="sugestoes-sinais-prosa"></datalist>
-                    <input type="hidden" id="pr-sinal" />
-                    <label class="form-label">Dedicado a / Sobre quem (Pessoas)</label>
+                    <datalist id="sugestoes-sinais-outros-prosa"></datalist>
+                    <input type="hidden" id="pr-sinal-outros" />
+
+                    <label class="form-label">Pessoas</label>
                     <div class="flex gap-2 mb-2">
                         <input
                             type="text"
@@ -258,10 +569,535 @@
                         id="pr-pessoas-container"
                         class="flex flex-wrap gap-1 min-h-[30px] p-2 bg-white dark:bg-slate-900 border rounded border-gray-300 dark:border-slate-600"
                     ></div>
+                    <div
+                        id="pr-pessoas-grupos-info"
+                        class="text-[11px] text-gray-400 dark:text-slate-500 mt-1.5"
+                    ></div>
                     <datalist id="sugestoes-pessoas-prosa"></datalist>
-                    <input type="hidden" id="pr-pessoas" />
-                    <label class="form-label">Notas</label>
-                    <textarea id="pr-notas" class="text-sm h-20"></textarea>
+                        </div>
+                    </details>
+
+                    <details class="campo-grupo">
+                        <summary>Envios e Rea├º├Áes</summary>
+                        <div class="campo-grupo-corpo">
+                    <label class="form-label"
+                        >Envios
+                        <span
+                            class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                            >(quando o texto foi enviado/mostrado pra algu├®m, e como essa
+                            pessoa reagiu ÔÇö pode haver mais de um envio pro mesmo
+                            texto)</span
+                        ></label
+                    >
+                    <div class="flex gap-2 mb-2">
+                        <input
+                            type="text"
+                            id="pr-envio-pessoa"
+                            list="sugestoes-pessoas-prosa"
+                            class="text-sm flex-1"
+                            placeholder="Pra quem..."
+                        />
+                        <input
+                            type="text"
+                            id="pr-envio-meio"
+                            list="sugestoes-meios-envio-prosa"
+                            class="text-sm flex-1"
+                            placeholder="Meio (ex.: WhatsApp)"
+                        />
+                    </div>
+                    <div class="grid grid-cols-3 gap-1 mb-2">
+                        <input
+                            type="number"
+                            id="pr-envio-dia"
+                            placeholder="Dia"
+                            min="1"
+                            max="31"
+                            title="Dia"
+                        />
+                        <input
+                            type="number"
+                            id="pr-envio-mes"
+                            placeholder="M├¬s"
+                            min="1"
+                            max="12"
+                            title="M├¬s"
+                        />
+                        <input
+                            type="number"
+                            id="pr-envio-ano"
+                            placeholder="Ano"
+                            min="1900"
+                            max="2100"
+                            title="Ano"
+                        />
+                    </div>
+                    <datalist id="sugestoes-meios-envio-prosa"></datalist>
+                    <div class="flex flex-col gap-2 mb-2">
+                        <textarea
+                            id="pr-envio-reacao"
+                            class="text-sm h-16"
+                            placeholder="Rea├º├úo..."
+                        ></textarea>
+                        <textarea
+                            id="pr-envio-notas"
+                            class="text-xs h-14"
+                            placeholder="Notas (opcional)..."
+                        ></textarea>
+                        <div class="flex items-center gap-2 self-end">
+                            <button
+                                type="button"
+                                id="pr-envio-btn-cancelar"
+                                onclick="cancelarEdicaoEnvioProsa()"
+                                class="hidden text-gray-500 dark:text-slate-400 hover:text-gray-700 dark:hover:text-slate-200 text-xs px-1"
+                            >
+                                Cancelar edi├º├úo
+                            </button>
+                            <button
+                                type="button"
+                                id="pr-envio-btn-add"
+                                onclick="adicionarEnvioProsa()"
+                                class="bg-teal-100 dark:bg-teal-900 text-teal-700 dark:text-teal-300 px-3 py-1.5 rounded text-xs font-bold"
+                            >
+                                + Adicionar envio
+                            </button>
+                        </div>
+                    </div>
+                    <div id="pr-envios-lista" class="space-y-1 mb-2 min-h-[12px]"></div>
+                        </div>
+                    </details>
+
+                    <details class="campo-grupo">
+                        <summary>Reconhecimentos</summary>
+                        <div class="campo-grupo-corpo">
+                    <label class="form-label"
+                        >Pr├¬mios e men├º├Áes
+                        <span
+                            class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                            >(concursos, pr├¬mios ou men├º├Áes que este texto recebeu)</span
+                        ></label
+                    >
+                    <div class="flex gap-2 mb-2">
+                        <input
+                            type="text"
+                            id="pr-reconhecimento-premio"
+                            list="sugestoes-premios-prosa"
+                            class="text-sm flex-1"
+                            placeholder="Pr├¬mio/concurso..."
+                        />
+                        <input
+                            type="text"
+                            id="pr-reconhecimento-posicao"
+                            class="text-sm flex-1"
+                            placeholder="Posi├º├úo (ex.: 1┬║ lugar)"
+                        />
+                        <input
+                            type="number"
+                            id="pr-reconhecimento-ano"
+                            class="text-sm w-24"
+                            placeholder="Ano"
+                            min="1900"
+                            max="2100"
+                            title="Ano"
+                        />
+                    </div>
+                    <datalist id="sugestoes-premios-prosa"></datalist>
+                    <div class="flex flex-col gap-2 mb-2">
+                        <textarea
+                            id="pr-reconhecimento-texto"
+                            class="text-xs h-14"
+                            placeholder="Notas (opcional)..."
+                        ></textarea>
+                        <div class="flex items-center gap-2 self-end">
+                            <button
+                                type="button"
+                                id="pr-reconhecimento-btn-cancelar"
+                                onclick="cancelarEdicaoReconhecimentoProsa()"
+                                class="hidden text-gray-500 dark:text-slate-400 hover:text-gray-700 dark:hover:text-slate-200 text-xs px-1"
+                            >
+                                Cancelar edi├º├úo
+                            </button>
+                            <button
+                                type="button"
+                                id="pr-reconhecimento-btn-add"
+                                onclick="adicionarReconhecimentoProsa()"
+                                class="bg-amber-100 dark:bg-amber-900 text-amber-700 dark:text-amber-300 px-3 py-1.5 rounded text-xs font-bold"
+                            >
+                                + Adicionar reconhecimento
+                            </button>
+                        </div>
+                    </div>
+                    <div id="pr-reconhecimentos-lista" class="space-y-1 mb-2 min-h-[12px]"></div>
+                        </div>
+                    </details>
+
+                    <details class="campo-grupo">
+                        <summary>Elos, refer├¬ncias e intertextualidade</summary>
+                        <div class="campo-grupo-corpo">
+                            <label class="form-label"
+                                >Elos
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(liga├º├úo estrutural com outro texto do acervo (poema ou prosa) ÔÇö reescrita,
+                                    tradu├º├úo, continua├º├úo...)</span
+                                ></label
+                            >
+                            <div class="flex gap-2 mb-1">
+                                <select
+                                    id="pr-elo-relacao"
+                                    class="text-xs w-28 flex-shrink-0"
+                                    onchange="onRelacaoEloAlteradaProsa()"
+                                ></select>
+                                <select id="pr-elo-poema" class="text-xs flex-1"></select>
+                                <button
+                                    type="button"
+                                    id="pr-elo-btn-add"
+                                    onclick="adicionarEloProsa()"
+                                    class="bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 px-3 rounded text-xs font-bold flex-shrink-0"
+                                >
+                                    +
+                                </button>
+                                <button
+                                    type="button"
+                                    id="pr-elo-btn-cancelar"
+                                    onclick="cancelarEdicaoEloProsa()"
+                                    class="hidden text-gray-500 dark:text-slate-400 hover:text-gray-700 dark:hover:text-slate-200 px-2 text-xs flex-shrink-0"
+                                >
+                                    Cancelar
+                                </button>
+                            </div>
+                            <div class="flex gap-2 mb-2 items-center">
+                                <span
+                                    class="text-[10px] text-gray-400 dark:text-slate-500 flex-shrink-0"
+                                    >Este poema ├®:</span
+                                >
+                                <button
+                                    type="button"
+                                    id="pr-elo-direcao-origem"
+                                    onclick="selecionarDirecaoEloProsa('origem')"
+                                    class="elo-direcao-btn text-xs flex-1 px-2 py-1 rounded border border-slate-200 dark:border-slate-700 text-gray-500 dark:text-slate-400"
+                                >
+                                    Origem
+                                </button>
+                                <button
+                                    type="button"
+                                    id="pr-elo-direcao-destino"
+                                    onclick="selecionarDirecaoEloProsa('destino')"
+                                    class="elo-direcao-btn text-xs flex-1 px-2 py-1 rounded border border-slate-200 dark:border-slate-700 text-gray-500 dark:text-slate-400"
+                                >
+                                    Destino
+                                </button>
+                                <input type="hidden" id="pr-elo-direcao" value="" />
+                            </div>
+                            <input
+                                type="text"
+                                id="pr-elo-texto"
+                                class="text-sm w-full mb-2"
+                                placeholder="Nota opcional sobre esse elo..."
+                            />
+                            <div id="pr-elos-lista" class="space-y-1 mb-2 min-h-[12px]"></div>
+                            <div id="pr-elos-derivados" class="space-y-1 mb-2 hidden"></div>
+
+                            <label class="form-label"
+                                >Refer├¬ncias
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(liga├º├úo mais solta com outro texto ÔÇö personagem em comum,
+                                    imagem compartilhada...)</span
+                                ></label
+                            >
+                            <div class="flex gap-2 mb-2">
+                                <select id="pr-ref-tipo" class="text-xs w-28 flex-shrink-0"></select>
+                                <select id="pr-ref-poema" class="text-xs flex-1"></select>
+                                <button
+                                    type="button"
+                                    id="pr-ref-btn-add"
+                                    onclick="adicionarReferenciaProsa()"
+                                    class="bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300 px-3 rounded text-xs font-bold flex-shrink-0"
+                                >
+                                    +
+                                </button>
+                                <button
+                                    type="button"
+                                    id="pr-ref-btn-cancelar"
+                                    onclick="cancelarEdicaoReferenciaProsa()"
+                                    class="hidden text-gray-500 dark:text-slate-400 hover:text-gray-700 dark:hover:text-slate-200 px-2 text-xs flex-shrink-0"
+                                >
+                                    Cancelar
+                                </button>
+                            </div>
+                            <input
+                                type="text"
+                                id="pr-ref-texto"
+                                class="text-sm w-full mb-2"
+                                placeholder="Nota opcional sobre essa refer├¬ncia..."
+                            />
+                            <div id="pr-refs-lista" class="space-y-1 mb-2 min-h-[12px]"></div>
+                            <label class="form-label"
+                                >Intertextualidade
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(di├ílogo com algo externo ao arquivo ÔÇö m├║sica, livro, s├®rie...
+                                    pode ter mais de uma refer├¬ncia, de tipos diferentes)</span
+                                ></label
+                            >
+                            <div class="flex gap-2 mb-2">
+                                <select id="pr-intertexto-tipo" class="text-xs w-28 flex-shrink-0">
+                                    <option value="">Tipo...</option>
+                                    <option value="Livro">Livro</option>
+                                    <option value="Texto">Texto</option>
+                                    <option value="M├║sica">M├║sica</option>
+                                    <option value="Filme/S├®rie">Filme/S├®rie</option>
+                                    <option value="V├¡deo">V├¡deo</option>
+                                    <option value="Cita├º├úo">Cita├º├úo</option>
+                                    <option value="Conversa">Conversa</option>
+                                    <option value="Outro">Outro</option>
+                                </select>
+                                <input
+                                    type="text"
+                                    id="pr-intertexto-texto"
+                                    list="sugestoes-intertexto-prosa"
+                                    class="text-sm flex-1"
+                                    placeholder="Ex.: '├üguas de Mar├ºo', Tom Jobim"
+                                />
+                                <button
+                                    type="button"
+                                    id="pr-intertexto-btn-add"
+                                    onclick="adicionarIntertextoProsa()"
+                                    class="bg-purple-100 dark:bg-purple-900 text-purple-700 dark:text-purple-300 px-3 rounded text-xs font-bold flex-shrink-0"
+                                >
+                                    +
+                                </button>
+                                <button
+                                    type="button"
+                                    id="pr-intertexto-btn-cancelar"
+                                    onclick="cancelarEdicaoIntertextoProsa()"
+                                    class="hidden text-gray-500 dark:text-slate-400 hover:text-gray-700 dark:hover:text-slate-200 px-2 text-xs flex-shrink-0"
+                                >
+                                    Cancelar
+                                </button>
+                            </div>
+                            <datalist id="sugestoes-intertexto-prosa"></datalist>
+                            <div id="pr-intertexto-lista" class="space-y-1 mb-2 min-h-[12px]"></div>
+                        </div>
+                    </details>
+
+                    <details class="campo-grupo">
+                        <summary>Anexos</summary>
+                        <div class="campo-grupo-corpo">
+                            <label class="form-label"
+                                >Anexos
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(imagens, letterings, v├¡deos... que acompanham o texto ÔÇö uma ou
+                                    v├írios, de tipos diferentes)</span
+                                ></label
+                            >
+                            <div class="flex gap-2 mb-2">
+                                <select id="pr-anexo-tipo" class="text-xs w-36 flex-shrink-0">
+                                    <option value="">Tipo...</option>
+                                    <option value="Ilustra├º├úo">Ilustra├º├úo</option>
+                                    <option value="Foto">Foto</option>
+                                    <option value="Lettering">Lettering</option>
+                                    <option value="Declama├º├úo em v├¡deo">Declama├º├úo em v├¡deo</option>
+                                    <option value="Coment├írios em v├¡deo">
+                                        Coment├írios em v├¡deo
+                                    </option>
+                                    <option value="Outro">Outro</option>
+                                </select>
+                                <input
+                                    type="text"
+                                    id="pr-anexo-link"
+                                    class="text-sm flex-1"
+                                    placeholder="Link (obrigat├│rio para v├¡deo)"
+                                />
+                            </div>
+                            <div class="flex flex-col gap-2 mb-2">
+                                <textarea
+                                    id="pr-anexo-input"
+                                    class="text-sm h-20"
+                                    placeholder="Descri├º├úo do anexo..."
+                                ></textarea>
+                                <button
+                                    type="button"
+                                    id="pr-anexo-btn-add"
+                                    onclick="adicionarAnexoProsa()"
+                                    class="bg-indigo-100 dark:bg-indigo-900 text-indigo-700 dark:text-indigo-300 px-3 py-1.5 rounded text-xs font-bold self-end"
+                                >
+                                    + Adicionar anexo
+                                </button>
+                                <button
+                                    type="button"
+                                    id="pr-anexo-btn-cancelar"
+                                    onclick="cancelarEdicaoAnexoProsa()"
+                                    class="hidden text-gray-500 dark:text-slate-400 hover:text-gray-700 dark:hover:text-slate-200 text-xs self-end px-1"
+                                >
+                                    Cancelar edi├º├úo
+                                </button>
+                            </div>
+                            <div id="pr-anexos-lista" class="space-y-1 mb-2 min-h-[12px]"></div>
+                            <label class="form-label"
+                                >Nota sobre o conjunto
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(quando os anexos se relacionam entre si ÔÇö tema, estilo,
+                                    unidade ÔÇö n├úo cada um isoladamente)</span
+                                ></label
+                            >
+                            <textarea id="pr-anexos-nota-geral" class="text-sm h-20"></textarea>
+                        </div>
+                    </details>
+
+                    <details class="campo-grupo">
+                        <summary>Oculta├º├úo e conte├║do sens├¡vel</summary>
+                        <div class="campo-grupo-corpo">
+                            <label class="form-label"
+                                >Oculta├º├úo
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(notas sobre oculta├º├úo de dados)</span
+                                ></label
+                            >
+                            <textarea id="pr-ocultacao" class="text-sm h-20"></textarea>
+                            <label class="form-label"
+                                >Conte├║do Sens├¡vel
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(preencher marca o texto para revis├úo em Vers├Áes
+                                    Alternativas)</span
+                                ></label
+                            >
+                            <textarea
+                                id="pr-sensivel"
+                                class="text-sm h-20 border-amber-300 dark:border-amber-700"
+                            ></textarea>
+                            <label class="form-label"
+                                >Vocabul├írio Hiperacionante
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(preencher marca o texto para revis├úo em Vers├Áes
+                                    Alternativas)</span
+                                ></label
+                            >
+                            <textarea
+                                id="pr-hiperacionante"
+                                class="text-sm h-20 border-amber-300 dark:border-amber-700"
+                            ></textarea>
+                        </div>
+                    </details>
+
+                    <details class="campo-grupo">
+                        <summary>Status e Pend├¬ncias</summary>
+                        <div class="campo-grupo-corpo">
+                            <div>
+                                <label class="text-sm font-bold block mb-1">Status</label>
+                                <select id="pr-status">
+                                    <option value="incompleto">­ƒƒí Incompleto</option>
+                                    <option value="completo" selected>ÔÜ¬ Completo</option>
+                                    <option value="publicado">­ƒƒó Publicado</option>
+                                    <option value="migrado">­ƒöÁ Migrado</option>
+                                    <option value="descartado">­ƒö┤ Descartado</option>
+                                </select>
+                            </div>
+
+                            <div
+                                class="p-3 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-lg"
+                            >
+                                <label class="form-label mb-2"
+                                    >Migra├º├úo entre livros
+                                    <span
+                                        class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                        >(preencha para textos ­ƒöÁ Migrados de um livro/se├º├úo para
+                                        outro)</span
+                                    ></label
+                                >
+                                <span class="text-[10px] text-gray-400 dark:text-slate-500"
+                                    >Cortado de</span
+                                >
+                                <div class="grid grid-cols-2 gap-2 mb-2">
+                                    <input
+                                        type="text"
+                                        id="pr-cortado-livro"
+                                        list="sugestoes-livros-migracao-prosa"
+                                        placeholder="Livro de origem"
+                                    />
+                                    <input
+                                        type="text"
+                                        id="pr-cortado-secao"
+                                        list="sugestoes-secoes-migracao-cortado-prosa"
+                                        placeholder="Parte/Se├º├úo de origem"
+                                    />
+                                </div>
+                                <span class="text-[10px] text-gray-400 dark:text-slate-500"
+                                    >Lan├ºado em</span
+                                >
+                                <div class="grid grid-cols-2 gap-2">
+                                    <input
+                                        type="text"
+                                        id="pr-lancado-livro"
+                                        list="sugestoes-livros-migracao-prosa"
+                                        placeholder="Livro de destino"
+                                    />
+                                    <input
+                                        type="text"
+                                        id="pr-lancado-secao"
+                                        list="sugestoes-secoes-migracao-lancado-prosa"
+                                        placeholder="Parte/Se├º├úo de destino"
+                                    />
+                                </div>
+                                <p class="text-[10px] text-gray-400 dark:text-slate-500 mt-1">
+                                    Texto livre ÔÇö aceita sugest├Áes de livros/partes/se├º├Áes j├í
+                                    cadastrados, mas tamb├®m aceita algo que n├úo exista no arquivo
+                                    (ex.: um livro de origem nunca cadastrado aqui). Escolher uma
+                                    Se├º├úo j├í cadastrada preenche o Livro sozinho; escolher o Livro
+                                    filtra as Se├º├Áes sugeridas pra s├│ as daquele livro.
+                                </p>
+                                <datalist id="sugestoes-livros-migracao-prosa"></datalist>
+                                <datalist id="sugestoes-secoes-migracao-cortado-prosa"></datalist>
+                                <datalist id="sugestoes-secoes-migracao-lancado-prosa"></datalist>
+
+                                <label
+                                    class="text-[10px] text-gray-400 dark:text-slate-500 block mt-2"
+                                    >Justificativa
+                                    <span class="normal-case"
+                                        >(raz├úo do arquivamento original, da migra├º├úo/recupera├º├úo,
+                                        ou ambos)</span
+                                    ></label
+                                >
+                                <textarea
+                                    id="pr-migracao-justificativa"
+                                    class="text-sm h-20 border-blue-300 dark:border-blue-700"
+                                ></textarea>
+                            </div>
+
+                            <label class="form-label"
+                                >Pend├¬ncia
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(preencha o que falta resolver ÔÇö o ­ƒƒá aparece sozinho ao lado
+                                    do status enquanto houver texto aqui)</span
+                                ></label
+                            >
+                            <textarea
+                                id="pr-pendencia"
+                                class="text-sm h-20 border-orange-300 dark:border-orange-700"
+                            ></textarea>
+
+                            <label class="form-label"
+                                >Descarte
+                                <span
+                                    class="font-normal normal-case text-gray-400 dark:text-slate-500"
+                                    >(preencha a justificativa para textos ­ƒö┤ Descartados)</span
+                                ></label
+                            >
+                            <textarea
+                                id="pr-descarte"
+                                class="text-sm h-20 border-amber-300 dark:border-amber-700"
+                            ></textarea>
+                        </div>
+                    </details>
+
+
+
                     <div class="flex items-center gap-2">
                         <input type="checkbox" id="pr-pub" />
                         <label class="text-sm font-bold">Publicado</label>
diff --git a/tests/colunas.test.js b/tests/colunas.test.js
index 89f6d97..be9404c 100644
--- a/tests/colunas.test.js
+++ b/tests/colunas.test.js
@@ -23,8 +23,14 @@ globalThis.CustomEvent = class CustomEvent {
     }
 };
 
-const { DEFINICAO_COLUNAS, getColunasAtivas, isColunaAtiva, toggleColuna, moverColuna } =
-    await import('../js/colunas.js');
+const {
+    DEFINICAO_COLUNAS,
+    getColunasAtivas,
+    isColunaAtiva,
+    toggleColuna,
+    moverColuna,
+    resetarColunas,
+} = await import('../js/colunas.js');
 
 // Mesmo prefixo usado internamente em colunas.js (LS_PREFIX, n├úo
 // exportado) ÔÇö duplicado aqui de prop├│sito pra poder inspecionar e
@@ -73,15 +79,16 @@ describe('getColunasAtivas (sem nada salvo ainda ÔÇö estado padr├úo)', () => {
                 'dataEscrita',
                 'dataPublicacao',
                 'estrutura',
-                'status',
                 'pessoas',
+                'status',
             ]);
             assert.deepEqual(getColunasAtivas('prosas'), [
                 'dataEscrita',
                 'dataPublicacao',
                 'vinculo',
-                'pessoas',
                 'genero',
+                'pessoas',
+                'status',
             ]);
         },
     );
@@ -114,8 +121,8 @@ describe('getColunasAtivas (com estado salvo no localStorage)', () => {
             'dataEscrita',
             'dataPublicacao',
             'estrutura',
-            'status',
             'pessoas',
+            'status',
         ]);
     });
 
@@ -125,8 +132,8 @@ describe('getColunasAtivas (com estado salvo no localStorage)', () => {
             'dataEscrita',
             'dataPublicacao',
             'estrutura',
-            'status',
             'pessoas',
+            'status',
         ]);
     });
 
@@ -201,14 +208,14 @@ describe('toggleColuna', () => {
     });
 
     it('ligar uma coluna respeita a ordem de defini├º├úo, n├úo vai pro fim da lista', () => {
-        toggleColuna('poemas', 'elos', true); // "elos" vem depois de "status"/"pessoas" na ordem padr├úo
+        toggleColuna('poemas', 'elos', true); // "elos" vem antes de "pessoas"/"status" na ordem padr├úo
         assert.deepEqual(getColunasAtivas('poemas'), [
             'dataEscrita',
             'dataPublicacao',
             'estrutura',
-            'status',
-            'pessoas',
             'elos',
+            'pessoas',
+            'status',
         ]);
     });
 
@@ -242,26 +249,37 @@ describe('moverColuna', () => {
     beforeEach(resetar);
 
     it('"up" troca de posi├º├úo com o vizinho anterior', () => {
-        // ordem padr├úo: dataEscrita, dataPublicacao, estrutura, status, pessoas, elos, referencias, etiquetas, notas
-        moverColuna('poemas', 'estrutura', 'up');
+        // ordem padr├úo (in├¡cio): idioma, dataEscrita, dataPublicacao, epocaRetratada, contextoHistorico, notas, autoria, envios, reconhecimentos, estrutura, elos...
+        moverColuna('poemas', 'notas', 'up');
         const estado = JSON.parse(localStorage.getItem(LS_PREFIX + 'poemas'));
-        assert.deepEqual(estado.ordem.slice(0, 3), ['dataEscrita', 'estrutura', 'dataPublicacao']);
+        assert.deepEqual(estado.ordem.slice(0, 10), [
+            'idioma',
+            'dataEscrita',
+            'dataPublicacao',
+            'epocaRetratada',
+            'notas',
+            'contextoHistorico',
+            'autoria',
+            'envios',
+            'reconhecimentos',
+            'estrutura',
+        ]);
     });
 
     it('"down" troca de posi├º├úo com o vizinho seguinte', () => {
-        moverColuna('poemas', 'dataEscrita', 'down');
+        moverColuna('poemas', 'idioma', 'down');
         const estado = JSON.parse(localStorage.getItem(LS_PREFIX + 'poemas'));
-        assert.deepEqual(estado.ordem.slice(0, 2), ['dataPublicacao', 'dataEscrita']);
+        assert.deepEqual(estado.ordem.slice(0, 2), ['dataEscrita', 'idioma']);
     });
 
     it('mover a primeira coluna pra "up" n├úo faz nada (j├í est├í no topo)', () => {
-        moverColuna('poemas', 'dataEscrita', 'up');
+        moverColuna('poemas', 'idioma', 'up');
         const estado = JSON.parse(localStorage.getItem(LS_PREFIX + 'poemas'));
         assert.equal(estado, null, 'nem chega a salvar, porque a posi├º├úo-alvo ├® inv├ílida');
     });
 
     it('mover a ├║ltima coluna pra "down" n├úo faz nada (j├í est├í no fim)', () => {
-        moverColuna('poemas', 'lancadoEm', 'down');
+        moverColuna('poemas', 'camposPreenchidos', 'down');
         assert.equal(localStorage.getItem(LS_PREFIX + 'poemas'), null);
     });
 
@@ -288,9 +306,64 @@ describe('moverColuna', () => {
     });
 
     it('N├âO dispara evento quando o movimento ├® inv├ílido (limite, key/tabela inexistente)', () => {
-        moverColuna('poemas', 'dataEscrita', 'up'); // j├í est├í no topo
+        moverColuna('poemas', 'idioma', 'up'); // j├í est├í no topo
         moverColuna('poemas', 'coluna-fantasma', 'up');
         moverColuna('tabela-fantasma', 'x', 'up');
         assert.equal(window._eventos.length, 0);
     });
 });
+
+// ÔöÇÔöÇÔöÇ resetarColunas ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+describe('resetarColunas', () => {
+    beforeEach(resetar);
+
+    it('sem nada personalizado, n├úo quebra e mant├®m o padr├úo', () => {
+        resetarColunas('poemas');
+        assert.deepEqual(getColunasAtivas('poemas'), [
+            'dataEscrita',
+            'dataPublicacao',
+            'estrutura',
+            'pessoas',
+            'status',
+        ]);
+    });
+
+    it('descarta reordena├º├úo e sele├º├úo personalizadas, voltando ao padr├úo de f├íbrica', () => {
+        salvarEstado(
+            'poemas',
+            ['status', 'dataEscrita', 'dataPublicacao', 'estrutura', 'elos', 'referencias'],
+            ['status', 'elos'],
+        );
+        resetarColunas('poemas');
+        assert.deepEqual(getColunasAtivas('poemas'), [
+            'dataEscrita',
+            'dataPublicacao',
+            'estrutura',
+            'pessoas',
+            'status',
+        ]);
+    });
+
+    it('mexe s├│ na tabela indicada, n├úo na outra', () => {
+        salvarEstado('prosas', ['dataEscrita', 'genero', 'vinculo'], ['genero']);
+        resetarColunas('poemas');
+        assert.deepEqual(getColunasAtivas('prosas'), ['genero']);
+    });
+
+    it('tabela desconhecida n├úo quebra e n├úo mexe em nada', () => {
+        assert.doesNotThrow(() => resetarColunas('tabela-fantasma'));
+    });
+
+    it('dispara o evento "colunas:alteradas" com a tabela certa no detail', () => {
+        resetarColunas('prosas');
+        assert.equal(window._eventos.length, 1);
+        assert.equal(window._eventos[0].type, 'colunas:alteradas');
+        assert.equal(window._eventos[0].detail.tabela, 'prosas');
+    });
+
+    it('tabela desconhecida N├âO dispara evento', () => {
+        resetarColunas('tabela-fantasma');
+        assert.equal(window._eventos.length, 0);
+    });
+});
diff --git a/tests/db.test.js b/tests/db.test.js
index 3bb80fd..5e6f805 100644
--- a/tests/db.test.js
+++ b/tests/db.test.js
@@ -10,6 +10,16 @@ import {
     sortPoemas,
     sortElementos,
     calcularCascataColetanea,
+    migrarPapeisPessoa,
+    migrarNomesDePapel,
+    migrarIdioma,
+    migrarReconhecimentos,
+    migrarAutoria,
+    calcularImpactoExclusaoAutor,
+    migrarEpocas,
+    calcularImpactoExclusaoEpoca,
+    mesclarPessoas,
+    mesclarEpocas,
 } from '../js/db.js';
 
 // ÔöÇÔöÇÔöÇ sortSecoes ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
@@ -280,3 +290,550 @@ describe('calcularCascataColetanea (o que seria apagado junto com uma Colet├ónea
         assert.deepEqual(itensIds, [2]);
     });
 });
+
+// ÔöÇÔöÇÔöÇ migrarPapeisPessoa ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+describe('migrarPapeisPessoa (papel string ├║nica ÔåÆ papeis: array, multi-select)', () => {
+    it('converte papel preenchido em array de um item', () => {
+        const itens = [{ pessoas: [{ nome: 'Pedro', papel: 'Retratado(a)' }] }];
+        migrarPapeisPessoa(itens);
+        assert.deepEqual(itens[0].pessoas, [{ nome: 'Pedro', papeis: ['Retratado(a)'] }]);
+    });
+
+    it('converte papel "" (n├úo especificado) em array vazio', () => {
+        const itens = [{ pessoas: [{ nome: 'Pedro', papel: '' }] }];
+        migrarPapeisPessoa(itens);
+        assert.deepEqual(itens[0].pessoas, [{ nome: 'Pedro', papeis: [] }]);
+    });
+
+    it('idempotente: item que j├í tem papeis (array) passa intacto', () => {
+        const itens = [
+            { pessoas: [{ nome: 'Pedro', papeis: ['Retratado(a)', 'Dedicat├írio(a)'] }] },
+        ];
+        migrarPapeisPessoa(itens);
+        assert.deepEqual(itens[0].pessoas, [
+            { nome: 'Pedro', papeis: ['Retratado(a)', 'Dedicat├írio(a)'] },
+        ]);
+    });
+
+    it('ignora item sem pessoas migradas ainda (pessoas n├úo ├® array)', () => {
+        const itens = [{ pessoas: 'Pedro, Dani' }];
+        migrarPapeisPessoa(itens);
+        assert.equal(itens[0].pessoas, 'Pedro, Dani');
+    });
+});
+
+// ÔöÇÔöÇÔöÇ migrarNomesDePapel ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+describe('migrarNomesDePapel (renomea├º├úo de g├¬nero em PAPEIS_PESSOA: Alus├úo/Dedicat├íria/Inspirado por)', () => {
+    it('renomeia os 3 valores antigos pros novos', () => {
+        const itens = [
+            {
+                pessoas: [
+                    { pessoaId: 1, papeis: ['Alus├úo'] },
+                    { pessoaId: 2, papeis: ['Dedicat├íria', 'Inspirado por'] },
+                ],
+            },
+        ];
+        migrarNomesDePapel(itens);
+        assert.deepEqual(itens[0].pessoas, [
+            { pessoaId: 1, papeis: ['Aludido(a)'] },
+            { pessoaId: 2, papeis: ['Dedicat├írio(a)', 'Inspirado(a) por'] },
+        ]);
+    });
+
+    it('dedup: nome antigo + nome novo j├í marcados manualmente no mesmo item viram um s├│', () => {
+        const itens = [{ pessoas: [{ pessoaId: 1, papeis: ['Alus├úo', 'Aludido(a)'] }] }];
+        migrarNomesDePapel(itens);
+        assert.deepEqual(itens[0].pessoas, [{ pessoaId: 1, papeis: ['Aludido(a)'] }]);
+    });
+
+    it('idempotente: rodar de novo n├úo muda nada', () => {
+        const itens = [{ pessoas: [{ pessoaId: 1, papeis: ['Retratado(a)', 'Mencionado(a)'] }] }];
+        migrarNomesDePapel(itens);
+        migrarNomesDePapel(itens);
+        assert.deepEqual(itens[0].pessoas, [
+            { pessoaId: 1, papeis: ['Retratado(a)', 'Mencionado(a)'] },
+        ]);
+    });
+
+    it('ignora item sem pessoas (n├úo ├® array) e pessoa sem papeis (n├úo ├® array)', () => {
+        const itens = [
+            { pessoas: 'Pedro, Dani' },
+            { pessoas: [{ pessoaId: 1, papeis: undefined }] },
+        ];
+        migrarNomesDePapel(itens);
+        assert.equal(itens[0].pessoas, 'Pedro, Dani');
+        assert.equal(itens[1].pessoas[0].papeis, undefined);
+    });
+});
+
+// ÔöÇÔöÇÔöÇ migrarIdioma ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+describe('migrarIdioma (item 9 do plano de schema ÔÇö campo idioma)', () => {
+    it('preenche "pt-BR" em item sem o campo idioma', () => {
+        const itens = [{ titulo: 'A' }];
+        migrarIdioma(itens);
+        assert.equal(itens[0].idioma, 'pt-BR');
+    });
+
+    it('n├úo sobrescreve idioma j├í preenchido, seja o padr├úo ou uma escolha manual', () => {
+        const itens = [{ idioma: 'pt-BR' }, { idioma: 'en' }];
+        migrarIdioma(itens);
+        assert.equal(itens[0].idioma, 'pt-BR');
+        assert.equal(itens[1].idioma, 'en');
+    });
+
+    it('idempotente: rodar de novo n├úo muda nada', () => {
+        const itens = [{ titulo: 'A' }];
+        migrarIdioma(itens);
+        migrarIdioma(itens);
+        assert.equal(itens[0].idioma, 'pt-BR');
+    });
+
+    it('trata string vazia como valor j├í preenchido (n├úo sobrescreve) ÔÇö s├│ undefined dispara o padr├úo', () => {
+        const itens = [{ idioma: '' }];
+        migrarIdioma(itens);
+        assert.equal(itens[0].idioma, '');
+    });
+});
+
+// ÔöÇÔöÇÔöÇ migrarReconhecimentos ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+describe('migrarReconhecimentos (item 8 do plano de schema ÔÇö campo reconhecimentos)', () => {
+    it('preenche reconhecimentos: [] em item sem a tag "Premiados"', () => {
+        const itens = [{ titulo: 'A' }];
+        migrarReconhecimentos(itens);
+        assert.deepEqual(itens[0].reconhecimentos, []);
+    });
+
+    it('item com a tag "Premiados" em sinalizacoesOutros ganha uma entrada em branco e perde a tag', () => {
+        const itens = [{ titulo: 'beija-flor', sinalizacoesOutros: 'Premiados' }];
+        migrarReconhecimentos(itens);
+        assert.deepEqual(itens[0].reconhecimentos, [
+            { premio: '', posicao: '', ano: null, texto: '' },
+        ]);
+        assert.equal(itens[0].sinalizacoesOutros, '');
+    });
+
+    it('remove s├│ a tag "Premiados", preservando as demais tags soltas', () => {
+        const itens = [{ sinalizacoesOutros: 'Premiados, Tradu├º├úo' }];
+        migrarReconhecimentos(itens);
+        assert.equal(itens[0].sinalizacoesOutros, 'Tradu├º├úo');
+    });
+
+    it('n├úo sobrescreve reconhecimentos j├í preenchido (seja vazio ou com dados de uma rodada anterior)', () => {
+        const itens = [
+            { reconhecimentos: [] },
+            {
+                reconhecimentos: [
+                    { premio: 'Concurso X', posicao: '1┬║ lugar', ano: 2020, texto: '' },
+                ],
+            },
+        ];
+        migrarReconhecimentos(itens);
+        assert.deepEqual(itens[0].reconhecimentos, []);
+        assert.equal(itens[1].reconhecimentos[0].premio, 'Concurso X');
+    });
+
+    it('idempotente: rodar de novo n├úo duplica a entrada nem recria a tag', () => {
+        const itens = [{ sinalizacoesOutros: 'Premiados' }];
+        migrarReconhecimentos(itens);
+        migrarReconhecimentos(itens);
+        assert.equal(itens[0].reconhecimentos.length, 1);
+        assert.equal(itens[0].sinalizacoesOutros, '');
+    });
+});
+
+// ÔöÇÔöÇÔöÇ migrarAutoria ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+describe('migrarAutoria (cadastro central de Autores + backfill Victor Leme como Autor)', () => {
+    it('preenche autoria: [{autorId, papel: "Autor"}] em item sem o campo, criando Victor Leme no cadastro', () => {
+        const dbRef = { autores: [], poemas: [{ titulo: 'A' }], prosas: [] };
+        migrarAutoria(dbRef);
+        assert.equal(dbRef.autores.length, 1);
+        assert.equal(dbRef.autores[0].nome, 'Victor Leme');
+        assert.deepEqual(dbRef.poemas[0].autoria, [
+            { autorId: dbRef.autores[0].id, papel: 'Autor' },
+        ]);
+    });
+
+    it('n├úo sobrescreve autoria j├í preenchida (ex.: coautoria marcada manualmente)', () => {
+        const dbRef = {
+            autores: [{ id: 1, nome: 'Victor Leme', sobre: '' }],
+            poemas: [
+                {
+                    autoria: [
+                        { autorId: 1, papel: 'Autor' },
+                        { autorId: 2, papel: 'Coautor' },
+                    ],
+                },
+            ],
+            prosas: [],
+        };
+        migrarAutoria(dbRef);
+        assert.equal(dbRef.poemas[0].autoria.length, 2);
+    });
+
+    it('reaproveita o Victor Leme j├í cadastrado em vez de duplicar', () => {
+        const dbRef = {
+            autores: [{ id: 42, nome: 'Victor Leme', sobre: 'j├í existia' }],
+            poemas: [{ titulo: 'A' }, { titulo: 'B' }],
+            prosas: [{ titulo: 'C' }],
+        };
+        migrarAutoria(dbRef);
+        assert.equal(dbRef.autores.length, 1);
+        assert.equal(dbRef.poemas[0].autoria[0].autorId, 42);
+        assert.equal(dbRef.poemas[1].autoria[0].autorId, 42);
+        assert.equal(dbRef.prosas[0].autoria[0].autorId, 42);
+    });
+
+    it('idempotente: rodar de novo n├úo muda nada nem duplica o cadastro', () => {
+        const dbRef = { autores: [], poemas: [{ titulo: 'A' }], prosas: [] };
+        migrarAutoria(dbRef);
+        const autoriaAntes = dbRef.poemas[0].autoria;
+        migrarAutoria(dbRef);
+        assert.equal(dbRef.autores.length, 1);
+        assert.deepEqual(dbRef.poemas[0].autoria, autoriaAntes);
+    });
+
+    it('n├úo cria Victor Leme no cadastro se n├úo houver item pra migrar', () => {
+        const dbRef = {
+            autores: [],
+            poemas: [{ autoria: [{ autorId: 1, papel: 'Autor' }] }],
+            prosas: [],
+        };
+        migrarAutoria(dbRef);
+        assert.equal(dbRef.autores.length, 0);
+    });
+
+    it('aplica a Poema e Prosa juntos, com o mesmo autorId de Victor Leme', () => {
+        const dbRef = { autores: [], poemas: [{ titulo: 'A' }], prosas: [{ titulo: 'B' }] };
+        migrarAutoria(dbRef);
+        assert.equal(dbRef.poemas[0].autoria[0].autorId, dbRef.prosas[0].autoria[0].autorId);
+    });
+});
+
+// ÔöÇÔöÇÔöÇ calcularImpactoExclusaoAutor ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+describe('calcularImpactoExclusaoAutor (quem referencia o Autor, pra avisar antes de excluir)', () => {
+    it('encontra poemas e prosas onde o autor est├í vinculado, independente do papel', () => {
+        const dbRef = {
+            poemas: [
+                { id: 1, autoria: [{ autorId: 10, papel: 'Autor' }] },
+                { id: 2, autoria: [{ autorId: 20, papel: 'Autor' }] },
+            ],
+            prosas: [{ id: 3, autoria: [{ autorId: 10, papel: 'Coautor' }] }],
+        };
+        const { poemasIds, prosasIds } = calcularImpactoExclusaoAutor(dbRef, 10);
+        assert.deepEqual(poemasIds, [1]);
+        assert.deepEqual(prosasIds, [3]);
+    });
+
+    it('autor sem nenhum v├¡nculo retorna listas vazias', () => {
+        const dbRef = {
+            poemas: [{ id: 1, autoria: [{ autorId: 10, papel: 'Autor' }] }],
+            prosas: [],
+        };
+        const { poemasIds, prosasIds } = calcularImpactoExclusaoAutor(dbRef, 999);
+        assert.deepEqual(poemasIds, []);
+        assert.deepEqual(prosasIds, []);
+    });
+
+    it('funciona mesmo se poemas/prosas estiverem ausentes do db (backup antigo)', () => {
+        const { poemasIds, prosasIds } = calcularImpactoExclusaoAutor({}, 10);
+        assert.deepEqual(poemasIds, []);
+        assert.deepEqual(prosasIds, []);
+    });
+});
+
+// ÔöÇÔöÇÔöÇ migrarEpocas ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+describe('migrarEpocas (item 3 do plano de schema ÔÇö cadastro central de ├ëpocas)', () => {
+    it('cria uma ├ëpoca pra cada nome ├║nico e troca {nome} por {epocaId} no poema', () => {
+        const dbRef = {
+            epocas: [],
+            poemas: [{ id: 1, epocaRetratada: { nome: 'Luto', inicio: null, fim: null } }],
+        };
+        migrarEpocas(dbRef);
+        assert.equal(dbRef.epocas.length, 1);
+        assert.equal(dbRef.epocas[0].nome, 'Luto');
+        assert.equal(dbRef.poemas[0].epocaRetratada.epocaId, dbRef.epocas[0].id);
+        assert.equal(dbRef.poemas[0].epocaRetratada.nome, undefined);
+    });
+
+    it('dedup por nome exato: dois poemas com o mesmo nome apontam pra uma ├║nica ├ëpoca', () => {
+        const dbRef = {
+            epocas: [],
+            poemas: [
+                { id: 1, epocaRetratada: { nome: 'Pandemia' } },
+                { id: 2, epocaRetratada: { nome: 'Pandemia' } },
+            ],
+        };
+        migrarEpocas(dbRef);
+        assert.equal(dbRef.epocas.length, 1);
+        assert.equal(
+            dbRef.poemas[0].epocaRetratada.epocaId,
+            dbRef.poemas[1].epocaRetratada.epocaId,
+        );
+    });
+
+    it('"X" e "X e P├│s" viram duas ├ëpocas separadas (limita├º├úo conhecida, igual Pessoas)', () => {
+        const dbRef = {
+            epocas: [],
+            poemas: [
+                { id: 1, epocaRetratada: { nome: 'Namoro' } },
+                { id: 2, epocaRetratada: { nome: 'Namoro e P├│s' } },
+            ],
+        };
+        migrarEpocas(dbRef);
+        assert.equal(dbRef.epocas.length, 2);
+    });
+
+    it('recorte vem sempre null no dado migrado (n├úo d├í pra adivinhar a partir do nome antigo)', () => {
+        const dbRef = { epocas: [], poemas: [{ id: 1, epocaRetratada: { nome: 'Luto' } }] };
+        migrarEpocas(dbRef);
+        assert.equal(dbRef.poemas[0].epocaRetratada.recorte, null);
+    });
+
+    it('preserva inicio/fim/na do dado antigo', () => {
+        const dbRef = {
+            epocas: [],
+            poemas: [
+                {
+                    id: 1,
+                    epocaRetratada: { nome: 'Luto', inicio: { ano: 2022 }, fim: null, na: false },
+                },
+            ],
+        };
+        migrarEpocas(dbRef);
+        assert.deepEqual(dbRef.poemas[0].epocaRetratada.inicio, { ano: 2022 });
+        assert.equal(dbRef.poemas[0].epocaRetratada.na, false);
+    });
+
+    it('epocaRetratada sem nome (N/A puro ou s├│ datas) migra com epocaId: null', () => {
+        const dbRef = {
+            epocas: [],
+            poemas: [{ id: 1, epocaRetratada: { nome: '', na: true } }],
+        };
+        migrarEpocas(dbRef);
+        assert.equal(dbRef.poemas[0].epocaRetratada.epocaId, null);
+        assert.equal(dbRef.epocas.length, 0);
+    });
+
+    it('poema sem epocaRetratada n├úo ├® afetado', () => {
+        const dbRef = { epocas: [], poemas: [{ id: 1 }] };
+        migrarEpocas(dbRef);
+        assert.equal(dbRef.poemas[0].epocaRetratada, undefined);
+    });
+
+    it('idempotente: item cuja epocaRetratada j├í tem epocaId passa intacto', () => {
+        const dbRef = {
+            epocas: [{ id: 5, nome: 'Luto', contextoRelacao: '', notas: '' }],
+            poemas: [{ id: 1, epocaRetratada: { epocaId: 5, inicio: null, fim: null } }],
+        };
+        migrarEpocas(dbRef);
+        assert.equal(dbRef.epocas.length, 1);
+        assert.equal(dbRef.poemas[0].epocaRetratada.epocaId, 5);
+    });
+
+    it('nome que j├í tem ├ëpoca cadastrada reaproveita o id em vez de duplicar', () => {
+        const dbRef = {
+            epocas: [{ id: 7, nome: 'Luto', contextoRelacao: '', notas: '' }],
+            poemas: [{ id: 1, epocaRetratada: { nome: 'Luto' } }],
+        };
+        migrarEpocas(dbRef);
+        assert.equal(dbRef.epocas.length, 1);
+        assert.equal(dbRef.poemas[0].epocaRetratada.epocaId, 7);
+    });
+});
+
+// ÔöÇÔöÇÔöÇ calcularImpactoExclusaoEpoca ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+describe('calcularImpactoExclusaoEpoca (quem referencia a ├ëpoca, pra avisar antes de excluir)', () => {
+    it('encontra os poemas cuja epocaRetratada aponta pra essa ├®poca', () => {
+        const dbRef = {
+            poemas: [
+                { id: 1, epocaRetratada: { epocaId: 10 } },
+                { id: 2, epocaRetratada: { epocaId: 20 } },
+                { id: 3, epocaRetratada: { epocaId: 10 } },
+            ],
+        };
+        const { poemasIds } = calcularImpactoExclusaoEpoca(dbRef, 10);
+        assert.deepEqual(poemasIds, [1, 3]);
+    });
+
+    it('encontra tamb├®m as prosas cuja epocaRetratada aponta pra essa ├®poca (gap corrigido ÔÇö s├│ checava poemas antes)', () => {
+        const dbRef = {
+            poemas: [{ id: 1, epocaRetratada: { epocaId: 10 } }],
+            prosas: [
+                { id: 100, epocaRetratada: { epocaId: 10 } },
+                { id: 101, epocaRetratada: { epocaId: 20 } },
+            ],
+        };
+        const { poemasIds, prosasIds } = calcularImpactoExclusaoEpoca(dbRef, 10);
+        assert.deepEqual(poemasIds, [1]);
+        assert.deepEqual(prosasIds, [100]);
+    });
+
+    it('├®poca sem nenhum v├¡nculo retorna listas vazias', () => {
+        const dbRef = { poemas: [{ id: 1, epocaRetratada: { epocaId: 10 } }] };
+        const { poemasIds, prosasIds } = calcularImpactoExclusaoEpoca(dbRef, 999);
+        assert.deepEqual(poemasIds, []);
+        assert.deepEqual(prosasIds, []);
+    });
+
+    it('funciona mesmo se poemas/prosas estiverem ausentes do db (backup antigo)', () => {
+        const { poemasIds, prosasIds } = calcularImpactoExclusaoEpoca({}, 10);
+        assert.deepEqual(poemasIds, []);
+        assert.deepEqual(prosasIds, []);
+    });
+});
+
+// ÔöÇÔöÇÔöÇ mesclarPessoas ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+describe('mesclarPessoas (junta duas entradas do cadastro de Pessoas numa s├│)', () => {
+    it('reatribui pessoaId da origem pro destino em poemas e prosas', () => {
+        const dbRef = {
+            pessoas: [
+                { id: 1, nome: 'Pedro', grupoIds: [] },
+                { id: 2, nome: 'Pedro ', grupoIds: [] },
+            ],
+            poemas: [{ id: 10, pessoas: [{ pessoaId: 2, papeis: ['Retratado(a)'] }] }],
+            prosas: [{ id: 20, pessoas: [{ pessoaId: 2, papeis: ['Mencionado(a)'] }] }],
+        };
+        mesclarPessoas(dbRef, 2, 1);
+        assert.equal(dbRef.poemas[0].pessoas[0].pessoaId, 1);
+        assert.equal(dbRef.prosas[0].pessoas[0].pessoaId, 1);
+        assert.equal(dbRef.pessoas.length, 1);
+        assert.equal(dbRef.pessoas[0].id, 1);
+    });
+
+    it('une papeis quando o mesmo item j├í vinculava as duas pessoas, sem deixar entrada duplicada', () => {
+        const dbRef = {
+            pessoas: [
+                { id: 1, nome: 'Pedro', grupoIds: [] },
+                { id: 2, nome: 'Pedro (dup)', grupoIds: [] },
+            ],
+            poemas: [
+                {
+                    id: 10,
+                    pessoas: [
+                        { pessoaId: 1, papeis: ['Retratado(a)'] },
+                        { pessoaId: 2, papeis: ['Dedicat├írio(a)'] },
+                    ],
+                },
+            ],
+            prosas: [],
+        };
+        mesclarPessoas(dbRef, 2, 1);
+        assert.equal(dbRef.poemas[0].pessoas.length, 1);
+        assert.deepEqual(
+            new Set(dbRef.poemas[0].pessoas[0].papeis),
+            new Set(['Retratado(a)', 'Dedicat├írio(a)']),
+        );
+    });
+
+    it('une grupoIds das duas pessoas sem duplicar', () => {
+        const dbRef = {
+            pessoas: [
+                { id: 1, nome: 'Pedro', grupoIds: [100] },
+                { id: 2, nome: 'Pedro (dup)', grupoIds: [100, 200] },
+            ],
+            poemas: [],
+            prosas: [],
+        };
+        mesclarPessoas(dbRef, 2, 1);
+        assert.deepEqual(new Set(dbRef.pessoas[0].grupoIds), new Set([100, 200]));
+    });
+
+    it('n├úo faz nada se origem e destino forem o mesmo id', () => {
+        const dbRef = { pessoas: [{ id: 1, nome: 'Pedro', grupoIds: [] }], poemas: [], prosas: [] };
+        mesclarPessoas(dbRef, 1, 1);
+        assert.equal(dbRef.pessoas.length, 1);
+    });
+
+    it('n├úo faz nada se origem ou destino n├úo existirem no cadastro', () => {
+        const dbRef = { pessoas: [{ id: 1, nome: 'Pedro', grupoIds: [] }], poemas: [], prosas: [] };
+        mesclarPessoas(dbRef, 999, 1);
+        assert.equal(dbRef.pessoas.length, 1);
+    });
+});
+
+// ÔöÇÔöÇÔöÇ mesclarEpocas ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+describe('mesclarEpocas (junta duas entradas do cadastro de ├ëpocas numa s├│)', () => {
+    it('reatribui epocaId da origem pro destino em poemas e prosas', () => {
+        const dbRef = {
+            epocas: [
+                { id: 1, nome: 'Corte de contato', contextoRelacao: '', notas: '' },
+                { id: 2, nome: 'Corte de contato', contextoRelacao: '', notas: '' },
+            ],
+            poemas: [{ id: 10, epocaRetratada: { epocaId: 2 } }],
+            prosas: [{ id: 20, epocaRetratada: { epocaId: 2 } }],
+        };
+        mesclarEpocas(dbRef, 2, 1);
+        assert.equal(dbRef.poemas[0].epocaRetratada.epocaId, 1);
+        assert.equal(dbRef.prosas[0].epocaRetratada.epocaId, 1);
+        assert.equal(dbRef.epocas.length, 1);
+        assert.equal(dbRef.epocas[0].id, 1);
+    });
+
+    it('preenche contextoRelacao/notas do destino a partir da origem, s├│ se o destino estiver vazio', () => {
+        const dbRef = {
+            epocas: [
+                { id: 1, nome: 'Corte de contato', contextoRelacao: '', notas: '' },
+                {
+                    id: 2,
+                    nome: 'Corte de contato',
+                    contextoRelacao: 'Pedro e Victor',
+                    notas: 'per├¡odo dif├¡cil',
+                },
+            ],
+            poemas: [],
+            prosas: [],
+        };
+        mesclarEpocas(dbRef, 2, 1);
+        assert.equal(dbRef.epocas[0].contextoRelacao, 'Pedro e Victor');
+        assert.equal(dbRef.epocas[0].notas, 'per├¡odo dif├¡cil');
+    });
+
+    it('nunca sobrescreve contextoRelacao/notas do destino j├í preenchidos', () => {
+        const dbRef = {
+            epocas: [
+                {
+                    id: 1,
+                    nome: 'Corte de contato',
+                    contextoRelacao: 'Pedro e Victor',
+                    notas: 'nota original',
+                },
+                { id: 2, nome: 'Corte de contato', contextoRelacao: 'Outro contexto', notas: 'outra nota' },
+            ],
+            poemas: [],
+            prosas: [],
+        };
+        mesclarEpocas(dbRef, 2, 1);
+        assert.equal(dbRef.epocas[0].contextoRelacao, 'Pedro e Victor');
+        assert.equal(dbRef.epocas[0].notas, 'nota original');
+    });
+
+    it('n├úo faz nada se origem e destino forem o mesmo id', () => {
+        const dbRef = {
+            epocas: [{ id: 1, nome: 'Luto', contextoRelacao: '', notas: '' }],
+            poemas: [],
+            prosas: [],
+        };
+        mesclarEpocas(dbRef, 1, 1);
+        assert.equal(dbRef.epocas.length, 1);
+    });
+
+    it('n├úo faz nada se origem ou destino n├úo existirem no cadastro', () => {
+        const dbRef = {
+            epocas: [{ id: 1, nome: 'Luto', contextoRelacao: '', notas: '' }],
+            poemas: [],
+            prosas: [],
+        };
+        mesclarEpocas(dbRef, 999, 1);
+        assert.equal(dbRef.epocas.length, 1);
+    });
+});
diff --git a/tests/estatisticas.test.js b/tests/estatisticas.test.js
index 988b6e5..1030d88 100644
--- a/tests/estatisticas.test.js
+++ b/tests/estatisticas.test.js
@@ -24,6 +24,8 @@ function resetarDb() {
     db.elementos = [];
     db.coletaneas = [];
     db.itensColetanea = [];
+    db.pessoas = [];
+    db.grupos = [];
 }
 
 // ÔöÇÔöÇÔöÇ contarPorAno ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
@@ -149,31 +151,45 @@ describe('contarPorTema', () => {
     beforeEach(resetarDb);
 
     it('separa a lista de temas por v├¡rgula, ignorando espa├ºos em volta', () => {
-        db.poemas = [{ sinalizacoes: 'mar, noite' }, { sinalizacoes: 'mar' }];
+        db.poemas = [{ sinalizacoesTema: 'mar, noite' }, { sinalizacoesTema: 'mar' }];
         const { labels, data } = contarPorTema();
         assert.deepEqual(labels, ['mar', 'noite']);
         assert.deepEqual(data, [2, 1]);
     });
 
     it('item sem sinaliza├º├Áes n├úo contribui em nada', () => {
-        db.poemas = [{ sinalizacoes: '' }, { sinalizacoes: null }, { sinalizacoes: undefined }];
+        db.poemas = [
+            { sinalizacoesTema: '' },
+            { sinalizacoesTema: null },
+            { sinalizacoesTema: undefined },
+        ];
         assert.deepEqual(contarPorTema(), { labels: [], data: [] });
     });
 
+    it('combina as 6 categorias de Sinaliza├º├Áes na mesma contagem', () => {
+        db.poemas = [
+            { sinalizacoesEstilo: 'Concretista', sinalizacoesTema: 'mar' },
+            { sinalizacoesRelacao: 'Ôê× Pedrictor', sinalizacoesTom: 'mar' },
+        ];
+        const { labels, data } = contarPorTema();
+        assert.deepEqual(labels.slice().sort(), ['Concretista', 'Ôê× Pedrictor', 'mar'].sort());
+        assert.deepEqual(data[labels.indexOf('mar')], 2);
+    });
+
     it('respeita o par├ómetro `top`, cortando os menos frequentes', () => {
         db.poemas = [
-            { sinalizacoes: 'a' },
-            { sinalizacoes: 'a' },
-            { sinalizacoes: 'a' },
-            { sinalizacoes: 'b' },
-            { sinalizacoes: 'b' },
-            { sinalizacoes: 'c' },
+            { sinalizacoesTema: 'a' },
+            { sinalizacoesTema: 'a' },
+            { sinalizacoesTema: 'a' },
+            { sinalizacoesTema: 'b' },
+            { sinalizacoesTema: 'b' },
+            { sinalizacoesTema: 'c' },
         ];
         assert.deepEqual(contarPorTema(2), { labels: ['a', 'b'], data: [3, 2] });
     });
 
     it('valor padr├úo de `top` ├® 12', () => {
-        db.poemas = Array.from({ length: 15 }, (_, i) => ({ sinalizacoes: `tema${i}` }));
+        db.poemas = Array.from({ length: 15 }, (_, i) => ({ sinalizacoesTema: `tema${i}` }));
         assert.equal(contarPorTema().labels.length, 12);
     });
 });
@@ -181,8 +197,15 @@ describe('contarPorTema', () => {
 describe('contarPorPessoa', () => {
     beforeEach(resetarDb);
 
-    it('mesma l├│gica de contarPorTema, aplicada ao campo pessoas', () => {
-        db.poemas = [{ pessoas: 'Dalton, Dani' }, { pessoas: 'Dalton' }];
+    it('mesma l├│gica de contarPorTema, aplicada ao campo pessoas (resolvido via cadastro central)', () => {
+        db.pessoas = [
+            { id: 1, nome: 'Dalton', grupoIds: [] },
+            { id: 2, nome: 'Dani', grupoIds: [] },
+        ];
+        db.poemas = [
+            { pessoas: [{ pessoaId: 1, papeis: [] }, { pessoaId: 2, papeis: [] }] },
+            { pessoas: [{ pessoaId: 1, papeis: ['Dedicat├írio(a)'] }] },
+        ];
         assert.deepEqual(contarPorPessoa(), { labels: ['Dalton', 'Dani'], data: [2, 1] });
     });
 });
diff --git a/tests/exportar-md.test.js b/tests/exportar-md.test.js
index 8a1b02c..a0c07ec 100644
--- a/tests/exportar-md.test.js
+++ b/tests/exportar-md.test.js
@@ -5,7 +5,7 @@ import { describe, it, beforeEach } from 'node:test';
 import assert from 'node:assert/strict';
 
 import { db } from '../js/db.js';
-import { gerarMarkdownExportacao } from '../js/exportar-md.js';
+import { gerarMarkdownExportacao, contarCamposPreenchidos } from '../js/exportar-md.js';
 
 function resetarDb() {
     db.livros = [];
@@ -16,12 +16,13 @@ function resetarDb() {
     db.elementos = [];
     db.coletaneas = [];
     db.itensColetanea = [];
+    db.autores = [];
 }
 
 describe('gerarMarkdownExportacao ÔÇö Elos e Refer├¬ncias', () => {
     beforeEach(resetarDb);
 
-    it('resolve os ids de conceitos.elos/referencias pros t├¡tulos dos poemas ligados', () => {
+    it('resolve os {id,tipo,texto} de conceitos.elos/referencias pros t├¡tulos dos poemas ligados', () => {
         db.poemas = [
             { id: 1, titulo: 'Poema A', texto: 'linha 1' },
             { id: 2, titulo: 'Poema B', texto: 'linha 2' },
@@ -29,7 +30,13 @@ describe('gerarMarkdownExportacao ÔÇö Elos e Refer├¬ncias', () => {
                 id: 3,
                 titulo: 'Poema C',
                 texto: 'linha 3',
-                conceitos: { elos: [1], referencias: [1, 2] },
+                conceitos: {
+                    elos: [{ id: 1, tipo: '', texto: '' }],
+                    referencias: [
+                        { id: 1, tipo: '', texto: '' },
+                        { id: 2, tipo: '', texto: '' },
+                    ],
+                },
             },
         ];
 
@@ -39,6 +46,32 @@ describe('gerarMarkdownExportacao ÔÇö Elos e Refer├¬ncias', () => {
         assert.match(md, /\*\*Refer├¬ncias:\*\* Poema A, Poema B/);
     });
 
+    it('inclui o r├│tulo (Rela├º├úo+Dire├º├úo) como prefixo e a nota livre entre par├¬nteses quando preenchidos', () => {
+        db.poemas = [
+            { id: 1, titulo: 'Poema A', texto: 'linha 1' },
+            {
+                id: 2,
+                titulo: 'Poema C',
+                texto: 'linha 3',
+                conceitos: {
+                    elos: [
+                        {
+                            id: 1,
+                            relacao: 'Reescrita',
+                            direcao: 'destino',
+                            texto: 'primeira vers├úo, em prosa',
+                        },
+                    ],
+                    referencias: [],
+                },
+            },
+        ];
+
+        const md = gerarMarkdownExportacao([db.poemas[1]]);
+
+        assert.match(md, /\*\*Elos:\*\* Reescrita de: Poema A \(primeira vers├úo, em prosa\)/);
+    });
+
     it('omite as linhas de Elos/Refer├¬ncias quando o item n├úo tem conceitos', () => {
         db.poemas = [{ id: 1, titulo: 'Solo', texto: 'linha ├║nica' }];
 
@@ -48,9 +81,14 @@ describe('gerarMarkdownExportacao ÔÇö Elos e Refer├¬ncias', () => {
         assert.doesNotMatch(md, /\*\*Refer├¬ncias:\*\*/);
     });
 
-    it('ignora ids de elos/refer├¬ncias que n├úo correspondem a nenhum poema existente', () => {
+    it('ignora entradas de elos/refer├¬ncias que n├úo correspondem a nenhum poema existente', () => {
         db.poemas = [
-            { id: 1, titulo: '├Ünico', texto: 'x', conceitos: { elos: [999], referencias: [] } },
+            {
+                id: 1,
+                titulo: '├Ünico',
+                texto: 'x',
+                conceitos: { elos: [{ id: 999, tipo: '', texto: '' }], referencias: [] },
+            },
         ];
 
         const md = gerarMarkdownExportacao(db.poemas);
@@ -58,3 +96,305 @@ describe('gerarMarkdownExportacao ÔÇö Elos e Refer├¬ncias', () => {
         assert.doesNotMatch(md, /\*\*Elos:\*\*/);
     });
 });
+
+describe('gerarMarkdownExportacao ÔÇö Idioma (item 9)', () => {
+    beforeEach(resetarDb);
+
+    it('inclui a linha de Idioma quando o campo est├í preenchido', () => {
+        db.poemas = [{ id: 1, titulo: 'Solo', texto: 'x', idioma: 'en' }];
+        const md = gerarMarkdownExportacao(db.poemas);
+        assert.match(md, /\*\*Idioma:\*\* en/);
+    });
+
+    it('omite a linha de Idioma quando o campo n├úo est├í preenchido (dado ainda n├úo migrado)', () => {
+        db.poemas = [{ id: 1, titulo: 'Solo', texto: 'x' }];
+        const md = gerarMarkdownExportacao(db.poemas);
+        assert.doesNotMatch(md, /\*\*Idioma:\*\*/);
+    });
+});
+
+describe('gerarMarkdownExportacao ÔÇö Autoria (Nome (Papel), via cadastro central)', () => {
+    beforeEach(resetarDb);
+
+    it('inclui a linha de Autoria, no formato "Nome (Papel)", quando o item tem v├¡nculo', () => {
+        db.autores = [{ id: 1, nome: 'Victor Leme', sobre: '' }];
+        db.poemas = [
+            { id: 1, titulo: 'Solo', texto: 'x', autoria: [{ autorId: 1, papel: 'Autor' }] },
+        ];
+        const md = gerarMarkdownExportacao(db.poemas);
+        assert.match(md, /\*\*Autoria:\*\* Victor Leme \(Autor\)/);
+    });
+
+    it('junta mais de um autor com v├¡rgula, cada um com seu pr├│prio papel', () => {
+        db.autores = [
+            { id: 1, nome: 'Victor Leme', sobre: '' },
+            { id: 2, nome: 'Dalton', sobre: '' },
+        ];
+        db.poemas = [
+            {
+                id: 1,
+                titulo: 'Dupla',
+                texto: 'x',
+                autoria: [
+                    { autorId: 1, papel: 'Autor' },
+                    { autorId: 2, papel: 'Coautor' },
+                ],
+            },
+        ];
+        const md = gerarMarkdownExportacao(db.poemas);
+        assert.match(md, /\*\*Autoria:\*\* Victor Leme \(Autor\), Dalton \(Coautor\)/);
+    });
+
+    it('omite a linha de Autoria quando o campo n├úo est├í preenchido (dado ainda n├úo migrado)', () => {
+        db.poemas = [{ id: 1, titulo: 'Solo', texto: 'x' }];
+        const md = gerarMarkdownExportacao(db.poemas);
+        assert.doesNotMatch(md, /\*\*Autoria:\*\*/);
+    });
+
+    it('n├úo conta Autoria em Campos Preenchidos (migra├º├úo sempre preenche ÔÇö mesmo racioc├¡nio do Idioma)', () => {
+        db.autores = [{ id: 1, nome: 'Victor Leme', sobre: '' }];
+        const semAutoria = { id: 1, titulo: 'Solo', texto: 'x' };
+        const comAutoria = {
+            id: 2,
+            titulo: 'Solo',
+            texto: 'x',
+            autoria: [{ autorId: 1, papel: 'Autor' }],
+        };
+        assert.equal(contarCamposPreenchidos(semAutoria), contarCamposPreenchidos(comAutoria));
+    });
+});
+
+describe('gerarMarkdownExportacao ÔÇö Envios e Rea├º├Áes (item 7, lista pessoa+data+meio+rea├º├úo)', () => {
+    beforeEach(resetarDb);
+
+    it('inclui o bloco "### Envios e Rea├º├Áes" com pessoa, meio e data no cabe├ºalho da linha', () => {
+        db.poemas = [
+            {
+                id: 1,
+                titulo: 'Solo',
+                texto: 'x',
+                envios: [
+                    {
+                        pessoa: 'Dani',
+                        data: { dia: 12, mes: 5, ano: 2023 },
+                        meio: 'WhatsApp',
+                        reacao: 'Achou a met├ífora da aragonita bonita.',
+                        notas: '',
+                    },
+                ],
+            },
+        ];
+        const md = gerarMarkdownExportacao(db.poemas);
+        assert.match(md, /### Envios e Rea├º├Áes/);
+        assert.match(
+            md,
+            /- \*\*Dani, via WhatsApp, 12\/05\/2023:\*\* Achou a met├ífora da aragonita bonita\./,
+        );
+    });
+
+    it('lista mais de um envio, um por linha, cada um com seus pr├│prios dados', () => {
+        db.poemas = [
+            {
+                id: 1,
+                titulo: 'Solo',
+                texto: 'x',
+                envios: [
+                    { pessoa: 'Dani', meio: 'WhatsApp', reacao: 'Gostou' },
+                    { pessoa: 'Rafa', meio: 'Instagram', reacao: 'Comentou' },
+                ],
+            },
+        ];
+        const md = gerarMarkdownExportacao(db.poemas);
+        assert.match(md, /- \*\*Dani, via WhatsApp:\*\* Gostou/);
+        assert.match(md, /- \*\*Rafa, via Instagram:\*\* Comentou/);
+    });
+
+    it('mostra a nota entre par├¬nteses e em it├ílico, separada da rea├º├úo', () => {
+        db.poemas = [
+            {
+                id: 1,
+                titulo: 'Solo',
+                texto: 'x',
+                envios: [{ pessoa: 'Dani', reacao: 'Gostou', notas: 'Enviei sem querer 2x' }],
+            },
+        ];
+        const md = gerarMarkdownExportacao(db.poemas);
+        assert.match(md, /Gostou \*\(Enviei sem querer 2x\)\*/);
+    });
+
+    it('omite o bloco quando n├úo h├í envios (campo ausente ou lista vazia)', () => {
+        db.poemas = [
+            { id: 1, titulo: 'Sem campo', texto: 'x' },
+            { id: 2, titulo: 'Lista vazia', texto: 'x', envios: [] },
+        ];
+        const md = gerarMarkdownExportacao(db.poemas);
+        assert.doesNotMatch(md, /### Envios e Rea├º├Áes/);
+    });
+
+    it('conta em Campos Preenchidos (diferente de Idioma/Autoria ÔÇö n├úo ├® preenchido por migra├º├úo)', () => {
+        const semEnvios = { id: 1, titulo: 'Solo', texto: 'x' };
+        const comEnvios = {
+            id: 2,
+            titulo: 'Solo',
+            texto: 'x',
+            envios: [{ pessoa: 'Dani', reacao: 'Gostou' }],
+        };
+        assert.equal(contarCamposPreenchidos(comEnvios), contarCamposPreenchidos(semEnvios) + 1);
+    });
+});
+
+describe('gerarMarkdownExportacao ÔÇö Reconhecimentos (item 8, lista pr├¬mio+posi├º├úo+ano+texto)', () => {
+    beforeEach(resetarDb);
+
+    it('inclui o bloco "### Reconhecimentos" com pr├¬mio, posi├º├úo e ano no cabe├ºalho da linha', () => {
+        db.poemas = [
+            {
+                id: 1,
+                titulo: 'beija-flor',
+                texto: 'x',
+                reconhecimentos: [
+                    {
+                        premio: 'Concurso X',
+                        posicao: '1┬║ lugar',
+                        ano: 2020,
+                        texto: 'Categoria poesia.',
+                    },
+                ],
+            },
+        ];
+        const md = gerarMarkdownExportacao(db.poemas);
+        assert.match(md, /### Reconhecimentos/);
+        assert.match(md, /- \*\*Concurso X, 1┬║ lugar, 2020:\*\* Categoria poesia\./);
+    });
+
+    it('lista mais de um reconhecimento, um por linha, cada um com seus pr├│prios dados', () => {
+        db.poemas = [
+            {
+                id: 1,
+                titulo: 'Solo',
+                texto: 'x',
+                reconhecimentos: [
+                    { premio: 'Concurso X', posicao: '1┬║ lugar', ano: 2020, texto: '' },
+                    { premio: 'Concurso Y', posicao: 'Men├º├úo honrosa', ano: 2021, texto: '' },
+                ],
+            },
+        ];
+        const md = gerarMarkdownExportacao(db.poemas);
+        assert.match(md, /- \*\*Concurso X, 1┬║ lugar, 2020:\*\*/);
+        assert.match(md, /- \*\*Concurso Y, Men├º├úo honrosa, 2021:\*\*/);
+    });
+
+    it('omite campos ausentes do cabe├ºalho da linha (ex.: sem ano)', () => {
+        db.poemas = [
+            {
+                id: 1,
+                titulo: 'Solo',
+                texto: 'x',
+                reconhecimentos: [{ premio: 'Concurso X', posicao: '1┬║ lugar', texto: 'Nota' }],
+            },
+        ];
+        const md = gerarMarkdownExportacao(db.poemas);
+        assert.match(md, /- \*\*Concurso X, 1┬║ lugar:\*\* Nota/);
+    });
+
+    it('omite o bloco quando n├úo h├í reconhecimentos (campo ausente ou lista vazia)', () => {
+        db.poemas = [
+            { id: 1, titulo: 'Sem campo', texto: 'x' },
+            { id: 2, titulo: 'Lista vazia', texto: 'x', reconhecimentos: [] },
+        ];
+        const md = gerarMarkdownExportacao(db.poemas);
+        assert.doesNotMatch(md, /### Reconhecimentos/);
+    });
+
+    it('conta em Campos Preenchidos (diferente de Idioma/Autoria ÔÇö n├úo ├® preenchido por migra├º├úo)', () => {
+        const semReconhecimentos = { id: 1, titulo: 'Solo', texto: 'x' };
+        const comReconhecimentos = {
+            id: 2,
+            titulo: 'Solo',
+            texto: 'x',
+            reconhecimentos: [{ premio: 'Concurso X' }],
+        };
+        assert.equal(
+            contarCamposPreenchidos(comReconhecimentos),
+            contarCamposPreenchidos(semReconhecimentos) + 1,
+        );
+    });
+});
+
+describe('gerarMarkdownExportacao ÔÇö Grupos (Grupo (Pessoa), via cadastro central)', () => {
+    beforeEach(() => {
+        resetarDb();
+        db.pessoas = [];
+        db.grupos = [];
+    });
+
+    it('mostra "Grupo (Pessoa)" pra cada grupo que uma pessoa do texto pertence', () => {
+        db.grupos = [
+            { id: 10, nome: 'Namorado', cor: 'blue' },
+            { id: 11, nome: 'Ex-namorado', cor: 'amber' },
+        ];
+        db.pessoas = [
+            { id: 1, nome: 'Dalton', grupoIds: [10] },
+            { id: 2, nome: 'Pedro', grupoIds: [11] },
+        ];
+        db.poemas = [
+            {
+                id: 1,
+                titulo: 'Poema A',
+                texto: 'x',
+                pessoas: [
+                    { pessoaId: 1, papeis: [] },
+                    { pessoaId: 2, papeis: [] },
+                ],
+            },
+        ];
+
+        const md = gerarMarkdownExportacao(db.poemas);
+
+        assert.match(md, /\*\*Grupos:\*\* Namorado \(Dalton\), Ex-namorado \(Pedro\)/);
+    });
+
+    it('pessoa em mais de um grupo gera um par por grupo, n├úo uma linha combinada', () => {
+        db.grupos = [
+            { id: 10, nome: 'Namorado', cor: 'blue' },
+            { id: 11, nome: 'Ex-namorado', cor: 'amber' },
+        ];
+        db.pessoas = [{ id: 1, nome: 'Pedro', grupoIds: [10, 11] }];
+        db.poemas = [
+            { id: 1, titulo: 'Poema A', texto: 'x', pessoas: [{ pessoaId: 1, papeis: [] }] },
+        ];
+
+        const md = gerarMarkdownExportacao(db.poemas);
+
+        assert.match(md, /\*\*Grupos:\*\* Namorado \(Pedro\), Ex-namorado \(Pedro\)/);
+    });
+
+    it('omite a linha "Grupos" quando nenhuma pessoa do texto pertence a um grupo', () => {
+        db.pessoas = [{ id: 1, nome: 'Sem Grupo', grupoIds: [] }];
+        db.poemas = [
+            { id: 1, titulo: 'Poema A', texto: 'x', pessoas: [{ pessoaId: 1, papeis: [] }] },
+        ];
+
+        const md = gerarMarkdownExportacao(db.poemas);
+
+        assert.doesNotMatch(md, /\*\*Grupos:\*\*/);
+    });
+
+    it('linha "Grupos" ├® independente da linha "Pessoas" (papel do texto Ôëá grupo da pessoa)', () => {
+        db.grupos = [{ id: 10, nome: 'Amigos', cor: 'emerald' }];
+        db.pessoas = [{ id: 1, nome: 'F├íbio', grupoIds: [10] }];
+        db.poemas = [
+            {
+                id: 1,
+                titulo: 'Poema A',
+                texto: 'x',
+                pessoas: [{ pessoaId: 1, papeis: ['Dedicat├írio(a)'] }],
+            },
+        ];
+
+        const md = gerarMarkdownExportacao(db.poemas);
+
+        assert.match(md, /\*\*Pessoas:\*\* F├íbio \(Dedicat├írio\(a\)\)/);
+        assert.match(md, /\*\*Grupos:\*\* Amigos \(F├íbio\)/);
+    });
+});
diff --git a/tests/exportar.test.js b/tests/exportar.test.js
index 8a967d6..ec5cfa0 100644
--- a/tests/exportar.test.js
+++ b/tests/exportar.test.js
@@ -21,6 +21,8 @@ function resetarDb() {
     db.elementos = [];
     db.coletaneas = [];
     db.itensColetanea = [];
+    db.pessoas = [];
+    db.grupos = [];
 }
 
 // ÔöÇÔöÇÔöÇ dataEstaNoIntervalo ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
@@ -127,16 +129,29 @@ describe('correspondeFiltro (filtro combinado: livro, pessoa, tema, data, status
         assert.equal(correspondeFiltro({ paiTipo: 'livro', paiId: 2 }, opcoes), false);
     });
 
-    it('filtro por pessoa: basta UMA das pessoas marcadas bater', () => {
+    it('filtro por pessoa: basta UMA das pessoas marcadas bater (resolvido via cadastro central)', () => {
+        db.pessoas = [
+            { id: 1, nome: 'Dalton', grupoIds: [] },
+            { id: 2, nome: 'Sarinha', grupoIds: [] },
+        ];
         const opcoes = { ...opcoesBase(), pessoasIncluir: ['dalton', 'dani'] };
-        assert.equal(correspondeFiltro({ pessoas: 'Dalton' }, opcoes), true);
-        assert.equal(correspondeFiltro({ pessoas: 'Sarinha' }, opcoes), false);
+        assert.equal(
+            correspondeFiltro({ pessoas: [{ pessoaId: 1, papeis: [] }] }, opcoes),
+            true,
+        );
+        assert.equal(
+            correspondeFiltro({ pessoas: [{ pessoaId: 2, papeis: [] }] }, opcoes),
+            false,
+        );
     });
 
     it('tema a incluir E tema a excluir ao mesmo tempo: exclus├úo vence', () => {
         const opcoes = { ...opcoesBase(), temasIncluir: ['mar'], temasExcluir: ['rascunho'] };
-        assert.equal(correspondeFiltro({ sinalizacoes: 'mar' }, opcoes), true);
-        assert.equal(correspondeFiltro({ sinalizacoes: 'mar, rascunho' }, opcoes), false);
+        assert.equal(correspondeFiltro({ sinalizacoesTema: 'mar' }, opcoes), true);
+        assert.equal(
+            correspondeFiltro({ sinalizacoesTema: 'mar', sinalizacoesOutros: 'rascunho' }, opcoes),
+            false,
+        );
     });
 
     it('status "publicados" exclui rascunhos, e vice-versa', () => {
@@ -206,8 +221,8 @@ describe('gerarExportacaoSeletiva (monta o payload final: itens + colet├óneas)',
         resetarDb();
         db.livros = [{ id: 1, titulo: 'Livro A' }];
         db.poemas = [
-            { id: 10, titulo: 'Poema 1', paiTipo: 'livro', paiId: 1, sinalizacoes: 'mar' },
-            { id: 11, titulo: 'Poema 2', paiTipo: 'livro', paiId: 1, sinalizacoes: 'terra' },
+            { id: 10, titulo: 'Poema 1', paiTipo: 'livro', paiId: 1, sinalizacoesTema: 'mar' },
+            { id: 11, titulo: 'Poema 2', paiTipo: 'livro', paiId: 1, sinalizacoesTema: 'terra' },
         ];
         db.prosas = [{ id: 20, titulo: 'Prosa 1', paiTipo: 'livro', paiId: 1 }];
     });
@@ -292,18 +307,15 @@ describe('gerarTudoFlat (acervo inteiro, sem filtro, no modelo flat)', () => {
         resetarDb();
         db.livros = [{ id: 1, titulo: 'Livro A' }];
         db.poemas = [
-            { id: 10, titulo: 'Poema 1', paiTipo: 'livro', paiId: 1, sinalizacoes: 'mar' },
-            { id: 11, titulo: 'Poema 2', paiTipo: 'livro', paiId: 1, sinalizacoes: 'terra' },
+            { id: 10, titulo: 'Poema 1', paiTipo: 'livro', paiId: 1, sinalizacoesTema: 'mar' },
+            { id: 11, titulo: 'Poema 2', paiTipo: 'livro', paiId: 1, sinalizacoesTema: 'terra' },
         ];
         db.prosas = [{ id: 20, titulo: 'Prosa 1', paiTipo: 'livro', paiId: 1 }];
     });
 
     it('inclui todos os poemas e prosas do acervo, sem filtro nenhum', () => {
         const { itens } = gerarTudoFlat();
-        assert.deepEqual(
-            itens.map((i) => i.id).sort(),
-            [10, 11, 20],
-        );
+        assert.deepEqual(itens.map((i) => i.id).sort(), [10, 11, 20]);
     });
 
     it('cada registro sai com `tipo` e `contexto` resolvidos, igual ├á exporta├º├úo seletiva', () => {
diff --git a/tests/helpers/dom-real.js b/tests/helpers/dom-real.js
index d744ce4..89d793f 100644
--- a/tests/helpers/dom-real.js
+++ b/tests/helpers/dom-real.js
@@ -20,6 +20,13 @@ globalThis.Event = window.Event;
 globalThis.HTMLElement = window.HTMLElement;
 globalThis.MouseEvent = window.MouseEvent;
 globalThis.Node = window.Node;
+// mostrarAviso()/_criarToastEl() (utils.js) chamam requestAnimationFrame pra
+// animar a entrada do toast ÔÇö happy-dom implementa isso no objeto `window`
+// (ver console.log('requestAnimationFrame' in window) => true), mas n├úo o
+// exp├Áe em globalThis sozinho, ent├úo precisa ser copiado aqui como os
+// outros globais acima (mesmo racioc├¡nio do stub em dom-shim.js).
+globalThis.requestAnimationFrame = window.requestAnimationFrame.bind(window);
+globalThis.cancelAnimationFrame = window.cancelAnimationFrame.bind(window);
 
 // Nota: rodar a├º├Áes que chamam save() (ex.: excluir um item) imprime um
 // aviso do autobackup.js ("N├úo foi poss├¡vel gravar o snapshot autom├ítico:
diff --git a/tests/helpers/dom-shim.js b/tests/helpers/dom-shim.js
index 70e32af..9b514e5 100644
--- a/tests/helpers/dom-shim.js
+++ b/tests/helpers/dom-shim.js
@@ -64,3 +64,17 @@ if (typeof globalThis.CustomEvent === 'undefined') {
         }
     };
 }
+
+// mostrarAviso()/_criarToastEl() (utils.js) chamam requestAnimationFrame
+// pra animar a entrada do toast ÔÇö n├úo existe no Node por padr├úo. Dispara
+// s├¡ncrono (n├úo precisa simular o pr├│ximo frame de verdade, s├│ evitar o
+// ReferenceError) ÔÇö suficiente pra testar a l├│gica sem DOM real.
+if (typeof globalThis.requestAnimationFrame === 'undefined') {
+    globalThis.requestAnimationFrame = (cb) => {
+        cb();
+        return 0;
+    };
+}
+if (typeof globalThis.cancelAnimationFrame === 'undefined') {
+    globalThis.cancelAnimationFrame = noop;
+}
diff --git a/tests/render-dom.test.js b/tests/render-dom.test.js
index 37b7b09..b9d71bf 100644
--- a/tests/render-dom.test.js
+++ b/tests/render-dom.test.js
@@ -11,6 +11,7 @@ import assert from 'node:assert/strict';
 document.body.innerHTML = `
     <main>
         <div id="lista-livros"></div>
+        <div id="lista-grupos"></div>
 
         <div id="barra-acoes-poemas" class="hidden">
             <span id="contador-selecao-poemas"></span>
@@ -26,7 +27,9 @@ document.body.innerHTML = `
 `;
 
 const { db } = await import('../js/db.js');
-const { renderLivros, renderPoemas, toggleSelecaoPoema } = await import('../js/render-listas.js');
+const { renderLivros, renderPoemas, renderGrupos, toggleSelecaoPoema } =
+    await import('../js/render-listas.js');
+const { toggleColuna } = await import('../js/colunas.js');
 await import('../js/main.js');
 
 function limparDb() {
@@ -113,3 +116,79 @@ function excluirSelecaoPoemasGlobal() {
     // onclick="excluirSelecaoPoemas()" no index.html.
     window.excluirSelecaoPoemas();
 }
+
+describe('Grupos ÔÇö bolinha de cor no card (DOM real via happy-dom)', () => {
+    beforeEach(() => {
+        db.grupos.length = 0;
+        db.pessoas.length = 0;
+    });
+
+    it('mostra a bolinha com a cor cadastrada do grupo', () => {
+        db.grupos.push({ id: 1, nome: 'Amigos', cor: 'emerald' });
+        renderGrupos();
+
+        const ponto = document.querySelector('[title="Cor do grupo"]');
+        assert.ok(ponto, 'a bolinha de cor deveria estar no card do grupo');
+        assert.match(ponto.className, /bg-emerald-500/);
+    });
+
+    it('grupo sem cor salva (dado anterior ├á feature) cai no padr├úo, sem quebrar', () => {
+        db.grupos.push({ id: 1, nome: 'Legado' });
+        renderGrupos();
+
+        const ponto = document.querySelector('[title="Cor do grupo"]');
+        assert.ok(ponto);
+        assert.match(ponto.className, /bg-blue-500/);
+    });
+});
+
+describe('Poemas ÔÇö coluna "Grupos" (DOM real via happy-dom)', () => {
+    beforeEach(() => {
+        limparDb();
+        db.pessoas.length = 0;
+        db.grupos.length = 0;
+        localStorage.clear(); // estado de colunas salvo (ver colunas.js) come├ºaria "vazado" entre testes
+    });
+
+    it('coluna desligada por padr├úo: cabe├ºalho "Grupos" n├úo aparece', () => {
+        db.poemas.push({ id: 1, titulo: 'Poema A', sequencia: 1 });
+        renderPoemas();
+
+        assert.doesNotMatch(document.getElementById('cabecalho-poemas').textContent, /Grupos/);
+    });
+
+    it('ligar a coluna via toggleColuna mostra o cabe├ºalho e um badge "Grupo (Pessoa)" por v├¡nculo', () => {
+        db.grupos.push({ id: 10, nome: 'Namorado', cor: 'blue' });
+        db.pessoas.push({ id: 1, nome: 'Dalton', grupoIds: [10] });
+        db.poemas.push({
+            id: 1,
+            titulo: 'Poema A',
+            sequencia: 1,
+            pessoas: [{ pessoaId: 1, papeis: [] }],
+        });
+
+        toggleColuna('poemas', 'grupos', true);
+        renderPoemas();
+
+        assert.match(document.getElementById('cabecalho-poemas').textContent, /Grupos/);
+        const celula = document.getElementById('lista-poemas').textContent;
+        assert.match(celula, /Namorado/);
+        assert.match(celula, /Dalton/);
+    });
+
+    it('poema sem ningu├®m em grupo mostra "ÔÇö" na coluna, mesmo com pessoas atribu├¡das', () => {
+        db.pessoas.push({ id: 1, nome: 'Sem Grupo', grupoIds: [] });
+        db.poemas.push({
+            id: 1,
+            titulo: 'Poema A',
+            sequencia: 1,
+            pessoas: [{ pessoaId: 1, papeis: [] }],
+        });
+
+        toggleColuna('poemas', 'grupos', true);
+        renderPoemas();
+
+        const linha = document.querySelector('#lista-poemas tr');
+        assert.match(linha.textContent, /ÔÇö/);
+    });
+});
diff --git a/tests/utils.test.js b/tests/utils.test.js
index d7ee72d..c43a6a9 100644
--- a/tests/utils.test.js
+++ b/tests/utils.test.js
@@ -20,12 +20,34 @@ import {
     itemFaltaDataParaFiltro,
     filtroDataVazio,
     parseFiltroDataRapido,
-    extrairPessoasUnicas,
+    nomesPessoas,
+    nomesGrupos,
+    paresGrupoPessoa,
+    paresAutoria,
+    AUTORIA_PAPEIS,
+    CORES_GRUPO,
+    CORES_GRUPO_PADRAO,
+    classesCorGrupo,
     extrairSinalizacoesUnicas,
     extrairFasesUnicas,
+    IDIOMAS_SUGERIDOS,
+    extrairIdiomasUnicos,
+    extrairMeiosEnviosUnicos,
+    extrairPremiosUnicos,
     filtrarTextos,
     debounce,
     criarRastreadorDeAlteracoes,
+    RELACOES_ELO,
+    TIPOS_REFERENCIA,
+    ROTULOS_RELACAO_ELO,
+    rotuloElo,
+    direcaoInversa,
+    RECORTES_EPOCA,
+    ROTULOS_RECORTE_EPOCA,
+    nomeEpoca,
+    contextoRelacaoEpoca,
+    formatarEpocaRetratada,
+    obterSugestaoEpocaPorId,
 } from '../js/utils.js';
 
 describe('gerarId', () => {
@@ -477,25 +499,14 @@ describe('parseFiltroDataRapido', () => {
     });
 });
 
-describe('extrairPessoasUnicas / extrairSinalizacoesUnicas / extrairFasesUnicas', () => {
-    it('extrai nomes ├║nicos de campos "pessoas" em string separada por v├¡rgula, ordenados', () => {
-        const poemas = [
-            { pessoas: 'Dalton, Dani' },
-            { pessoas: 'Dani, Sarinha' },
-            { pessoas: '' },
-            {},
-        ];
-        assert.deepEqual(extrairPessoasUnicas(poemas), ['Dalton', 'Dani', 'Sarinha']);
-    });
-
-    it('tamb├®m aceita o campo j├í como array', () => {
-        const poemas = [{ pessoas: ['Gaby', 'Karina'] }, { pessoas: ['Karina'] }];
-        assert.deepEqual(extrairPessoasUnicas(poemas), ['Gaby', 'Karina']);
-    });
-
+describe('extrairSinalizacoesUnicas / extrairFasesUnicas', () => {
     it('extrai sinaliza├º├Áes ├║nicas do mesmo jeito', () => {
-        const poemas = [{ sinalizacoes: 'saudade, mar' }, { sinalizacoes: 'mar, chegada' }];
-        assert.deepEqual(extrairSinalizacoesUnicas(poemas), ['chegada', 'mar', 'saudade']);
+        const poemas = [{ sinalizacoesTema: 'saudade, mar' }, { sinalizacoesTema: 'mar, chegada' }];
+        assert.deepEqual(extrairSinalizacoesUnicas(poemas, 'sinalizacoesTema'), [
+            'chegada',
+            'mar',
+            'saudade',
+        ]);
     });
 
     it('extrai fases de vida ├║nicas de livros, ignorando vazias', () => {
@@ -509,14 +520,254 @@ describe('extrairPessoasUnicas / extrairSinalizacoesUnicas / extrairFasesUnicas'
     });
 });
 
+describe('extrairIdiomasUnicos (item 9 ÔÇö campo idioma)', () => {
+    it('sempre inclui a semente IDIOMAS_SUGERIDOS, mesmo com lista vazia', () => {
+        assert.deepEqual(extrairIdiomasUnicos([]), [...IDIOMAS_SUGERIDOS].sort());
+    });
+
+    it('soma idiomas j├í salvos que n├úo est├úo na semente, sem duplicar os que j├í est├úo', () => {
+        const itens = [{ idioma: 'pt-BR' }, { idioma: 'ja' }, { idioma: 'en' }];
+        const resultado = extrairIdiomasUnicos(itens);
+        assert.ok(resultado.includes('ja'));
+        assert.equal(resultado.filter((i) => i === 'en').length, 1);
+        assert.deepEqual(resultado, [...resultado].sort());
+    });
+
+    it('ignora itens sem idioma (ex.: dado ainda n├úo migrado)', () => {
+        const itens = [{ idioma: 'ja' }, {}, { titulo: 'sem idioma' }];
+        assert.deepEqual(extrairIdiomasUnicos(itens), ['ja', ...IDIOMAS_SUGERIDOS].sort());
+    });
+});
+
+describe('extrairMeiosEnviosUnicos (item 7 ÔÇö campo envios)', () => {
+    it('lista vazia sem envios retorna array vazio (sem semente fixa, ao contr├írio de idioma)', () => {
+        assert.deepEqual(extrairMeiosEnviosUnicos([]), []);
+    });
+
+    it('soma meios de todos os itens, sem duplicar, em ordem alfab├®tica', () => {
+        const itens = [
+            { envios: [{ meio: 'WhatsApp' }, { meio: 'Instagram' }] },
+            { envios: [{ meio: 'WhatsApp' }] },
+        ];
+        assert.deepEqual(extrairMeiosEnviosUnicos(itens), ['Instagram', 'WhatsApp']);
+    });
+
+    it('ignora itens sem envios ou com envios sem meio', () => {
+        const itens = [
+            {},
+            { envios: [] },
+            { envios: [{ pessoa: 'Dani' }] }, // sem meio
+            { envios: [{ meio: 'presencial' }] },
+        ];
+        assert.deepEqual(extrairMeiosEnviosUnicos(itens), ['presencial']);
+    });
+});
+
+describe('extrairPremiosUnicos (item 8 ÔÇö campo reconhecimentos)', () => {
+    it('lista vazia sem reconhecimentos retorna array vazio (sem semente fixa, ao contr├írio de idioma)', () => {
+        assert.deepEqual(extrairPremiosUnicos([]), []);
+    });
+
+    it('soma pr├¬mios de todos os itens, sem duplicar, em ordem alfab├®tica', () => {
+        const itens = [
+            { reconhecimentos: [{ premio: 'Concurso Y' }, { premio: 'Concurso X' }] },
+            { reconhecimentos: [{ premio: 'Concurso X' }] },
+        ];
+        assert.deepEqual(extrairPremiosUnicos(itens), ['Concurso X', 'Concurso Y']);
+    });
+
+    it('ignora itens sem reconhecimentos ou com reconhecimentos sem premio', () => {
+        const itens = [
+            {},
+            { reconhecimentos: [] },
+            { reconhecimentos: [{ posicao: '1┬║ lugar' }] }, // sem premio
+            { reconhecimentos: [{ premio: 'Concurso Z' }] },
+        ];
+        assert.deepEqual(extrairPremiosUnicos(itens), ['Concurso Z']);
+    });
+});
+
+describe('nomesPessoas / nomesGrupos (resolu├º├úo via cadastro central)', () => {
+    const pessoasCadastro = [
+        { id: 1, nome: 'Dani', grupoIds: [10, 11] },
+        { id: 2, nome: 'Dalton', grupoIds: [10] },
+    ];
+    const gruposCadastro = [
+        { id: 10, nome: 'Amigos' },
+        { id: 11, nome: 'Melhores Amigos' },
+    ];
+
+    it('resolve pessoaId ÔåÆ nome via o cadastro passado', () => {
+        const item = {
+            pessoas: [
+                { pessoaId: 2, papeis: ['Mencionado(a)'] },
+                { pessoaId: 1, papeis: [] },
+            ],
+        };
+        assert.deepEqual(nomesPessoas(item, pessoasCadastro), ['Dalton', 'Dani']);
+    });
+
+    it('ignora pessoaId sem correspond├¬ncia no cadastro, sem quebrar os demais', () => {
+        const item = {
+            pessoas: [
+                { pessoaId: 999, papeis: [] },
+                { pessoaId: 1, papeis: [] },
+            ],
+        };
+        assert.deepEqual(nomesPessoas(item, pessoasCadastro), ['Dani']);
+    });
+
+    it('devolve array vazio quando item.pessoas n├úo ├® array (dado ainda n├úo migrado)', () => {
+        assert.deepEqual(nomesPessoas({}), []);
+        assert.deepEqual(nomesPessoas({ pessoas: 'Dani' }), []);
+    });
+
+    it('resolve grupoIds de uma Pessoa ÔåÆ nomes via o cadastro de grupos', () => {
+        const dani = pessoasCadastro[0];
+        assert.deepEqual(nomesGrupos(dani, gruposCadastro), ['Amigos', 'Melhores Amigos']);
+    });
+
+    it('devolve array vazio pra pessoa sem grupo, ou pessoa ausente', () => {
+        assert.deepEqual(nomesGrupos({ grupoIds: [] }, gruposCadastro), []);
+        assert.deepEqual(nomesGrupos(null, gruposCadastro), []);
+    });
+});
+
+describe('paresGrupoPessoa (pares Grupo+Pessoa de um item, achatados)', () => {
+    const pessoasCadastro = [
+        { id: 1, nome: 'Dalton', grupoIds: [10] },
+        { id: 2, nome: 'Pedro', grupoIds: [10, 11] },
+        { id: 3, nome: 'Sem Grupo', grupoIds: [] },
+    ];
+    const gruposCadastro = [
+        { id: 10, nome: 'Namorado', cor: 'blue' },
+        { id: 11, nome: 'Ex-namorado', cor: 'amber' },
+    ];
+
+    it('gera um par {grupo, pessoa} por grupo de cada pessoa do item', () => {
+        const item = {
+            pessoas: [
+                { pessoaId: 1, papeis: [] },
+                { pessoaId: 2, papeis: [] },
+            ],
+        };
+        const pares = paresGrupoPessoa(item, pessoasCadastro, gruposCadastro);
+        assert.deepEqual(
+            pares.map((p) => [p.grupo.nome, p.pessoa.nome]),
+            [
+                ['Namorado', 'Dalton'],
+                ['Namorado', 'Pedro'],
+                ['Ex-namorado', 'Pedro'],
+            ],
+        );
+    });
+
+    it('pessoa em mais de um grupo gera um par por grupo, n├úo uma linha combinada', () => {
+        const item = { pessoas: [{ pessoaId: 2, papeis: [] }] };
+        const pares = paresGrupoPessoa(item, pessoasCadastro, gruposCadastro);
+        assert.equal(pares.length, 2);
+    });
+
+    it('pessoa sem grupo n├úo gera par nenhum', () => {
+        const item = { pessoas: [{ pessoaId: 3, papeis: [] }] };
+        assert.deepEqual(paresGrupoPessoa(item, pessoasCadastro, gruposCadastro), []);
+    });
+
+    it('ignora pessoaId ou grupoId sem correspond├¬ncia no cadastro', () => {
+        const item = {
+            pessoas: [
+                { pessoaId: 999, papeis: [] },
+                { pessoaId: 1, papeis: [] },
+            ],
+        };
+        const pares = paresGrupoPessoa(item, pessoasCadastro, [
+            { id: 999, nome: 'Grupo Fantasma' },
+        ]);
+        assert.deepEqual(pares, []);
+    });
+
+    it('devolve array vazio quando item.pessoas n├úo ├® array', () => {
+        assert.deepEqual(paresGrupoPessoa({}, pessoasCadastro, gruposCadastro), []);
+    });
+});
+
+describe('paresAutoria (Autor/Coautor, resolu├º├úo via cadastro central)', () => {
+    const autoresCadastro = [
+        { id: 1, nome: 'Victor Leme', sobre: '' },
+        { id: 2, nome: 'Dalton', sobre: '' },
+    ];
+
+    it('AUTORIA_PAPEIS tem exatamente Autor e Coautor', () => {
+        assert.deepEqual(AUTORIA_PAPEIS, ['Autor', 'Coautor']);
+    });
+
+    it('resolve autorId ÔåÆ autor (objeto) + papel, na ordem do item', () => {
+        const item = {
+            autoria: [
+                { autorId: 1, papel: 'Autor' },
+                { autorId: 2, papel: 'Coautor' },
+            ],
+        };
+        const pares = paresAutoria(item, autoresCadastro);
+        assert.deepEqual(
+            pares.map((p) => [p.autor.nome, p.papel]),
+            [
+                ['Victor Leme', 'Autor'],
+                ['Dalton', 'Coautor'],
+            ],
+        );
+    });
+
+    it('ignora autorId sem correspond├¬ncia no cadastro (autor exclu├¡do), sem quebrar os demais', () => {
+        const item = {
+            autoria: [
+                { autorId: 999, papel: 'Autor' },
+                { autorId: 1, papel: 'Autor' },
+            ],
+        };
+        const pares = paresAutoria(item, autoresCadastro);
+        assert.deepEqual(
+            pares.map((p) => p.autor.nome),
+            ['Victor Leme'],
+        );
+    });
+
+    it('devolve array vazio quando item.autoria n├úo ├® array (dado ainda n├úo migrado)', () => {
+        assert.deepEqual(paresAutoria({}), []);
+        assert.deepEqual(paresAutoria({ autoria: 'Victor Leme' }), []);
+    });
+});
+
+describe('CORES_GRUPO / classesCorGrupo (paleta curada de cor por Grupo)', () => {
+    it('paleta n├úo inclui rose ÔÇö cor j├í reservada pra badge de Pessoa', () => {
+        assert.ok(!CORES_GRUPO.some((c) => c.chave === 'rose'));
+    });
+
+    it('cada cor da paleta resolve pra uma classe pr├│pria (claro + escuro)', () => {
+        const classes = new Set(CORES_GRUPO.map((c) => classesCorGrupo(c.chave)));
+        assert.equal(classes.size, CORES_GRUPO.length, 'nenhuma cor deveria colidir com outra');
+        CORES_GRUPO.forEach(({ chave }) => {
+            const cls = classesCorGrupo(chave);
+            assert.match(cls, new RegExp(`bg-${chave}-100`));
+            assert.match(cls, /dark:bg-.*-900/);
+        });
+    });
+
+    it('cor desconhecida ou ausente cai no padr├úo, sem quebrar', () => {
+        assert.equal(classesCorGrupo('cor-que-nao-existe'), classesCorGrupo(CORES_GRUPO_PADRAO));
+        assert.equal(classesCorGrupo(undefined), classesCorGrupo(CORES_GRUPO_PADRAO));
+        assert.equal(classesCorGrupo(null), classesCorGrupo(CORES_GRUPO_PADRAO));
+    });
+});
+
 describe('filtrarTextos (busca com sintaxe estilo Google)', () => {
     const itens = [
         {
             id: 1,
             titulo: 'Beira do Mar',
             ano: 2023,
-            sinalizacoes: 'saudade',
-            pessoas: 'Dalton',
+            _buscaSinalizacoes: 'saudade',
+            _buscaPessoas: 'dalton',
             notas: '',
             _livros: 'Fragmentos',
         },
@@ -524,8 +775,8 @@ describe('filtrarTextos (busca com sintaxe estilo Google)', () => {
             id: 2,
             titulo: 'Rascunho Solto',
             ano: 2021,
-            sinalizacoes: 'rascunho',
-            pessoas: 'Dani',
+            _buscaSinalizacoes: 'rascunho',
+            _buscaPessoas: 'dani',
             notas: '',
             _livros: '',
         },
@@ -533,16 +784,26 @@ describe('filtrarTextos (busca com sintaxe estilo Google)', () => {
             id: 3,
             titulo: 'Chegada ao Mar',
             ano: 2023,
-            sinalizacoes: '',
-            pessoas: '',
+            _buscaSinalizacoes: '',
+            _buscaPessoas: '',
             notas: 'nota sobre Dalton',
             _livros: '',
+            conteudoSensivel: 'men├º├úo a suic├¡dio',
+        },
+        {
+            id: 4,
+            titulo: '├ürvore S├│',
+            ano: 2022,
+            _buscaSinalizacoes: '',
+            _buscaPessoas: '',
+            notas: '',
+            _livros: '',
         },
     ];
 
     it('sem query, retorna a lista intacta', () => {
-        assert.equal(filtrarTextos(itens, '').length, 3);
-        assert.equal(filtrarTextos(itens, '   ').length, 3);
+        assert.equal(filtrarTextos(itens, '').length, 4);
+        assert.equal(filtrarTextos(itens, '   ').length, 4);
     });
 
     it('termo solto busca em todos os campos gerais (t├¡tulo, ano, etiquetas, pessoas, notas, livros)', () => {
@@ -565,7 +826,7 @@ describe('filtrarTextos (busca com sintaxe estilo Google)', () => {
         const r = filtrarTextos(itens, '-rascunho');
         assert.deepEqual(
             r.map((i) => i.id),
-            [1, 3],
+            [1, 3, 4],
         );
     });
 
@@ -588,9 +849,43 @@ describe('filtrarTextos (busca com sintaxe estilo Google)', () => {
         );
     });
 
+    it('prefixo papel: busca s├│ nos pap├®is marcados, sem olhar nome de pessoa', () => {
+        const comPapeis = [
+            { id: 20, titulo: 'A', _buscaPapeis: 'melhor amiga confidente' },
+            { id: 21, titulo: 'B', _buscaPapeis: 'dedicataria' },
+            { id: 22, titulo: 'C', _buscaPapeis: '' },
+        ];
+        assert.deepEqual(
+            filtrarTextos(comPapeis, 'papel:confidente').map((i) => i.id),
+            [20],
+        );
+        // "amiga" s├│ aparece como papel, n├úo como nome de pessoa ÔÇö n├úo teria
+        // achado nada via pessoa: se n├úo houvesse algu├®m chamado assim
+        assert.deepEqual(
+            filtrarTextos(comPapeis, 'papel:amiga').map((i) => i.id),
+            [20],
+        );
+    });
+
+    it('prefixo grupo: acha quem menciona algu├®m que pertence ├áquele Grupo', () => {
+        const comGrupos = [
+            { id: 30, titulo: 'A', _buscaGrupos: 'Fam├¡lia Melhores Amigos' },
+            { id: 31, titulo: 'B', _buscaGrupos: 'Fam├¡lia' },
+            { id: 32, titulo: 'C', _buscaGrupos: '' },
+        ];
+        assert.deepEqual(
+            filtrarTextos(comGrupos, 'grupo:familia').map((i) => i.id),
+            [30, 31],
+        );
+        assert.deepEqual(
+            filtrarTextos(comGrupos, 'grupo:"melhores amigos"').map((i) => i.id),
+            [30],
+        );
+    });
+
     it('combina prefixo de campo com exclus├úo: -etiqueta:rascunho', () => {
         const r = filtrarTextos(itens, '-etiqueta:rascunho');
-        assert.deepEqual(r.map((i) => i.id).sort(), [1, 3]);
+        assert.deepEqual(r.map((i) => i.id).sort(), [1, 3, 4]);
     });
 
     it('busca ├® case-insensitive', () => {
@@ -605,6 +900,170 @@ describe('filtrarTextos (busca com sintaxe estilo Google)', () => {
         const r = filtrarTextos(itens, 'xyz:mar');
         assert.equal(r.length, 0); // nenhum item tem literalmente "xyz:mar" em algum campo
     });
+
+    it('busca ignora acento tanto no termo quanto no valor comparado', () => {
+        assert.deepEqual(
+            filtrarTextos(itens, 'arvore').map((i) => i.id),
+            [4],
+        ); // termo sem acento acha t├¡tulo "├ürvore S├│"
+        assert.deepEqual(
+            filtrarTextos(itens, '├írvore').map((i) => i.id),
+            [4],
+        ); // termo com acento tamb├®m acha, do mesmo jeito
+    });
+
+    it('prefixo de campo aceita acento ou n├úo, indiferentemente (sensivel:/sens├¡vel:)', () => {
+        assert.deepEqual(
+            filtrarTextos(itens, 'sensivel:suicidio').map((i) => i.id),
+            [3],
+        );
+        assert.deepEqual(
+            filtrarTextos(itens, 'sens├¡vel:suic├¡dio').map((i) => i.id),
+            [3],
+        );
+    });
+
+    it('campo:* acha quem tem aquele campo preenchido, n├úo importa com o qu├¬', () => {
+        assert.deepEqual(
+            filtrarTextos(itens, 'sensivel:*').map((i) => i.id),
+            [3],
+        );
+    });
+
+    it('-campo:* inverte: acha quem tem aquele campo vazio', () => {
+        assert.deepEqual(
+            filtrarTextos(itens, '-sensivel:*')
+                .map((i) => i.id)
+                .sort(),
+            [1, 2, 4],
+        );
+    });
+
+    it('prefixo genero: restringe a busca ao campo G├¬nero (s├│ existe em Prosa)', () => {
+        const prosas = [
+            { id: 10, titulo: 'Carta ao Mar', genero: 'Cartas' },
+            { id: 11, titulo: 'Di├ílogo Curto', genero: 'Di├ílogos' },
+        ];
+        assert.deepEqual(
+            filtrarTextos(prosas, 'genero:cartas').map((i) => i.id),
+            [10],
+        );
+    });
+
+    it('prefixo idioma: restringe a busca ao campo Idioma (item 9)', () => {
+        const comIdioma = [
+            { id: 40, titulo: 'A', idioma: 'pt-BR' },
+            { id: 41, titulo: 'B', idioma: 'en' },
+            { id: 42, titulo: 'C', idioma: 'ja' },
+        ];
+        assert.deepEqual(
+            filtrarTextos(comIdioma, 'idioma:en').map((i) => i.id),
+            [41],
+        );
+    });
+
+    it('prefixo autor: restringe a busca ao campo de Autoria (nome + papel)', () => {
+        const comAutoria = [
+            { id: 50, titulo: 'A', _buscaAutoria: 'Victor Leme Autor' },
+            { id: 51, titulo: 'B', _buscaAutoria: 'Dalton Coautor' },
+            { id: 52, titulo: 'C', _buscaAutoria: 'Victor Leme Autor' },
+        ];
+        assert.deepEqual(
+            filtrarTextos(comAutoria, 'autor:dalton').map((i) => i.id),
+            [51],
+        );
+        assert.deepEqual(
+            filtrarTextos(comAutoria, 'autor:coautor').map((i) => i.id),
+            [51],
+        );
+    });
+
+    it('prefixo epoca: restringe a busca ao campo de ├ëpoca Retratada (nome + recorte)', () => {
+        const comEpoca = [
+            { id: 60, titulo: 'A', _buscaEpoca: 'Corte de contato Momento' },
+            { id: 61, titulo: 'B', _buscaEpoca: 'Luto Repercuss├úo (e P├│s)' },
+            { id: 62, titulo: 'C', _buscaEpoca: 'Corte de contato Momento' },
+        ];
+        assert.deepEqual(
+            filtrarTextos(comEpoca, 'epoca:luto').map((i) => i.id),
+            [61],
+        );
+        assert.deepEqual(
+            filtrarTextos(comEpoca, 'epoca:contato').map((i) => i.id),
+            [60, 62],
+        );
+    });
+
+    it('busca geral (sem prefixo) tamb├®m acha por autoria, grupo, papel e ├®poca', () => {
+        const itensRelacionais = [
+            { id: 70, titulo: 'A', _buscaAutoria: 'Victor Leme Autor' },
+            { id: 71, titulo: 'B', _buscaGrupos: 'Fam├¡lia Melhores Amigos' },
+            { id: 72, titulo: 'C', _buscaPapeis: 'dedicataria' },
+            { id: 73, titulo: 'D', _buscaEpoca: 'Corte de contato Momento' },
+            { id: 74, titulo: 'E' },
+        ];
+        assert.deepEqual(
+            filtrarTextos(itensRelacionais, 'leme').map((i) => i.id),
+            [70],
+        );
+        assert.deepEqual(
+            filtrarTextos(itensRelacionais, 'familia').map((i) => i.id),
+            [71],
+        );
+        assert.deepEqual(
+            filtrarTextos(itensRelacionais, 'dedicataria').map((i) => i.id),
+            [72],
+        );
+        assert.deepEqual(
+            filtrarTextos(itensRelacionais, 'contato').map((i) => i.id),
+            [73],
+        );
+    });
+
+    it('prefixo envio: restringe a busca ao campo de Envios (pessoa + meio + rea├º├úo + notas)', () => {
+        const comEnvios = [
+            { id: 60, titulo: 'A', _buscaEnvios: 'Dani WhatsApp Achou linda ' },
+            { id: 61, titulo: 'B', _buscaEnvios: 'Rafa Instagram Curtiu muito ' },
+            { id: 62, titulo: 'C', _buscaEnvios: '' },
+        ];
+        assert.deepEqual(
+            filtrarTextos(comEnvios, 'envio:dani').map((i) => i.id),
+            [60],
+        );
+        assert.deepEqual(
+            filtrarTextos(comEnvios, 'envio:instagram').map((i) => i.id),
+            [61],
+        );
+        assert.deepEqual(
+            filtrarTextos(comEnvios, 'envio:curtiu').map((i) => i.id),
+            [61],
+        );
+    });
+
+    it('prefixo reconhecimento: restringe a busca ao campo de Reconhecimentos (premio + posi├º├úo + ano + texto)', () => {
+        const comReconhecimentos = [
+            { id: 70, titulo: 'A', _buscaReconhecimentos: 'Concurso X 1┬║ lugar 2020 ' },
+            { id: 71, titulo: 'B', _buscaReconhecimentos: 'Concurso Y Men├º├úo honrosa 2021 ' },
+            { id: 72, titulo: 'C', _buscaReconhecimentos: '' },
+        ];
+        assert.deepEqual(
+            filtrarTextos(comReconhecimentos, 'reconhecimento:concurso').map((i) => i.id),
+            [70, 71],
+        );
+        assert.deepEqual(
+            filtrarTextos(comReconhecimentos, 'reconhecimentos:men├º├úo').map((i) => i.id),
+            [71],
+        );
+        assert.deepEqual(
+            filtrarTextos(comReconhecimentos, 'reconhecimento:2020').map((i) => i.id),
+            [70],
+        );
+    });
+
+    it('"*" sem prefixo de campo n├úo vira presen├ºa ÔÇö ├® termo literal (n├úo bate em nada aqui)', () => {
+        const r = filtrarTextos(itens, '*');
+        assert.equal(r.length, 0);
+    });
 });
 
 // ÔöÇÔöÇÔöÇ debounce ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
@@ -773,3 +1232,294 @@ describe('criarRastreadorDeAlteracoes', () => {
         },
     );
 });
+
+describe('RELACOES_ELO / TIPOS_REFERENCIA / rotuloElo (redesenho Rela├º├úo+Dire├º├úo)', () => {
+    it('RELACOES_ELO cobre as 8 rela├º├Áes bilaterais, incluindo D├¡ptico e Outro', () => {
+        for (const relacao of [
+            'Reescrita',
+            'Continuidade',
+            'Tradu├º├úo',
+            'Varia├º├úo',
+            'Vers├úo',
+            'Resposta',
+            'D├¡ptico',
+            'Outro',
+        ]) {
+            assert.ok(RELACOES_ELO.includes(relacao), `RELACOES_ELO deveria incluir "${relacao}"`);
+        }
+        assert.equal(RELACOES_ELO.length, 8);
+    });
+
+    it('TIPOS_REFERENCIA cobre as rela├º├Áes unidirecionais, incluindo o novo tipo Aceno a', () => {
+        for (const tipo of [
+            'Personagem em comum',
+            'Imagem central compartilhada',
+            'Aceno a',
+            'Outro',
+        ]) {
+            assert.ok(
+                TIPOS_REFERENCIA.includes(tipo),
+                `TIPOS_REFERENCIA deveria incluir "${tipo}"`,
+            );
+        }
+    });
+
+    it('rotuloElo devolve o r├│tulo certo pros dois lados de cada rela├º├úo assim├®trica', () => {
+        assert.equal(rotuloElo('Reescrita', 'origem'), 'Reescrito em');
+        assert.equal(rotuloElo('Reescrita', 'destino'), 'Reescrita de');
+        assert.equal(rotuloElo('Continuidade', 'origem'), 'Continuado em');
+        assert.equal(rotuloElo('Continuidade', 'destino'), 'Continua├º├úo de');
+        assert.equal(rotuloElo('Tradu├º├úo', 'origem'), 'Traduzido para');
+        assert.equal(rotuloElo('Tradu├º├úo', 'destino'), 'Tradu├º├úo de');
+        assert.equal(rotuloElo('Varia├º├úo', 'origem'), 'Variado em');
+        assert.equal(rotuloElo('Varia├º├úo', 'destino'), 'Varia├º├úo de');
+        assert.equal(rotuloElo('Vers├úo', 'origem'), 'Vers├úo anterior de');
+        assert.equal(rotuloElo('Vers├úo', 'destino'), 'Vers├úo oficial de');
+        assert.equal(rotuloElo('Resposta', 'origem'), 'Respondido em');
+        assert.equal(rotuloElo('Resposta', 'destino'), 'Resposta a');
+    });
+
+    it('rotuloElo devolve o mesmo r├│tulo nos dois lados pra D├¡ptico e Outro', () => {
+        assert.equal(rotuloElo('D├¡ptico', 'origem'), 'D├¡ptico com');
+        assert.equal(rotuloElo('D├¡ptico', 'destino'), 'D├¡ptico com');
+        assert.equal(rotuloElo('Outro', 'origem'), 'Outro');
+        assert.equal(rotuloElo('Outro', 'destino'), 'Outro');
+    });
+
+    it('rotuloElo devolve string vazia pra rela├º├úo n├úo reconhecida (elo legado sem rela├º├úo definida)', () => {
+        assert.equal(rotuloElo('', 'origem'), '');
+        assert.equal(rotuloElo(undefined, 'destino'), '');
+    });
+
+    it('direcaoInversa troca origem por destino e vice-versa', () => {
+        assert.equal(direcaoInversa('origem'), 'destino');
+        assert.equal(direcaoInversa('destino'), 'origem');
+    });
+
+    it('direcaoInversa preserva valores vazios/desconhecidos sem inventar dire├º├úo', () => {
+        assert.equal(direcaoInversa(''), '');
+        assert.equal(direcaoInversa(undefined), undefined);
+    });
+
+    it('ROTULOS_RELACAO_ELO tem um par origem/destino pra cada uma das 8 rela├º├Áes', () => {
+        for (const relacao of RELACOES_ELO) {
+            assert.ok(
+                ROTULOS_RELACAO_ELO[relacao],
+                `ROTULOS_RELACAO_ELO deveria ter entrada pra "${relacao}"`,
+            );
+            assert.ok(ROTULOS_RELACAO_ELO[relacao].origem);
+            assert.ok(ROTULOS_RELACAO_ELO[relacao].destino);
+        }
+    });
+});
+
+// ÔöÇÔöÇÔöÇ ├ëpoca (item 3 do plano de schema) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+describe('RECORTES_EPOCA / ROTULOS_RECORTE_EPOCA', () => {
+    it('RECORTES_EPOCA tem exatamente momento e repercuss├úo', () => {
+        assert.deepEqual(RECORTES_EPOCA, ['momento', 'repercuss├úo']);
+    });
+
+    it('ROTULOS_RECORTE_EPOCA tem r├│tulo pros dois valores', () => {
+        assert.equal(ROTULOS_RECORTE_EPOCA.momento, 'Momento');
+        assert.equal(ROTULOS_RECORTE_EPOCA.repercuss├úo, 'Momento e Repercuss├úo');
+    });
+});
+
+describe('nomeEpoca (resolu├º├úo epocaId ÔåÆ nome via cadastro central)', () => {
+    const epocasCadastro = [
+        { id: 1, nome: 'Corte de contato', contextoRelacao: 'Pedro e Victor' },
+        { id: 2, nome: 'Luto' },
+    ];
+
+    it('resolve epocaId pro nome cadastrado', () => {
+        assert.equal(nomeEpoca({ epocaId: 1 }, epocasCadastro), 'Corte de contato');
+        assert.equal(nomeEpoca({ epocaId: 2 }, epocasCadastro), 'Luto');
+    });
+
+    it('devolve string vazia quando epocaId n├úo existe mais no cadastro (├®poca exclu├¡da)', () => {
+        assert.equal(nomeEpoca({ epocaId: 999 }, epocasCadastro), '');
+    });
+
+    it('devolve string vazia pra epoca ausente/null', () => {
+        assert.equal(nomeEpoca(null, epocasCadastro), '');
+        assert.equal(nomeEpoca(undefined, epocasCadastro), '');
+    });
+
+    it('cai no fallback direto (.nome cru) pra dado ainda n├úo migrado, mesmo sem epocaId', () => {
+        assert.equal(nomeEpoca({ nome: 'Pandemia' }, epocasCadastro), 'Pandemia');
+    });
+
+    it('funciona mesmo sem cadastro passado (default [])', () => {
+        assert.equal(nomeEpoca({ epocaId: 1 }), '');
+    });
+});
+
+describe('contextoRelacaoEpoca (resolu├º├úo epocaId ÔåÆ contexto do relacionamento)', () => {
+    const epocasCadastro = [
+        { id: 1, nome: 'Corte de contato', contextoRelacao: 'Pedro e Victor' },
+        { id: 2, nome: 'Luto' },
+    ];
+
+    it('resolve epocaId pro contexto cadastrado', () => {
+        assert.equal(contextoRelacaoEpoca({ epocaId: 1 }, epocasCadastro), 'Pedro e Victor');
+    });
+
+    it('devolve string vazia quando a ├®poca n├úo tem contexto preenchido', () => {
+        assert.equal(contextoRelacaoEpoca({ epocaId: 2 }, epocasCadastro), '');
+    });
+
+    it('devolve string vazia sem epocaId (dado legado n├úo migrado)', () => {
+        assert.equal(contextoRelacaoEpoca({ nome: 'Pandemia' }, epocasCadastro), '');
+    });
+
+    it('devolve string vazia pra epoca ausente/null', () => {
+        assert.equal(contextoRelacaoEpoca(null, epocasCadastro), '');
+        assert.equal(contextoRelacaoEpoca(undefined, epocasCadastro), '');
+    });
+
+    it('devolve string vazia quando epocaId n├úo existe mais no cadastro (├®poca exclu├¡da)', () => {
+        assert.equal(contextoRelacaoEpoca({ epocaId: 999 }, epocasCadastro), '');
+    });
+});
+
+describe('formatarEpocaRetratada (item 3 ÔÇö epocaId + recorte)', () => {
+    const epocasCadastro = [{ id: 1, nome: 'Corte de contato' }];
+
+    it('N/A com nome resolvido mostra "Nome (N/A)"', () => {
+        assert.equal(
+            formatarEpocaRetratada({ epocaId: 1, na: true }, epocasCadastro),
+            'Corte de contato (N/A)',
+        );
+    });
+
+    it('N/A sem nome mostra s├│ "N/A"', () => {
+        assert.equal(formatarEpocaRetratada({ na: true }, epocasCadastro), 'N/A');
+    });
+
+    it('nome + intervalo + recorte, todos presentes', () => {
+        const epoca = {
+            epocaId: 1,
+            inicio: { ano: 2023 },
+            fim: { ano: 2024 },
+            recorte: 'repercuss├úo',
+        };
+        assert.equal(
+            formatarEpocaRetratada(epoca, epocasCadastro),
+            'Corte de contato e p├│s (2023 ÔÇô 2024)',
+        );
+    });
+
+    it('recorte "momento" continua marcado em colchete no fim (sem ambiguidade a resolver)', () => {
+        const epoca = {
+            epocaId: 1,
+            inicio: { ano: 2023 },
+            fim: { ano: 2024 },
+            recorte: 'momento',
+        };
+        assert.equal(
+            formatarEpocaRetratada(epoca, epocasCadastro),
+            'Corte de contato (2023 ÔÇô 2024) [Momento]',
+        );
+    });
+
+    it('sem recorte n├úo mostra o colchete', () => {
+        const epoca = { epocaId: 1, inicio: { ano: 2023 }, fim: null };
+        assert.equal(
+            formatarEpocaRetratada(epoca, epocasCadastro),
+            'Corte de contato (A partir de 2023)',
+        );
+    });
+
+    it('s├│ nome, sem datas nem recorte', () => {
+        assert.equal(formatarEpocaRetratada({ epocaId: 1 }, epocasCadastro), 'Corte de contato');
+    });
+
+    it('s├│ intervalo, sem nome (├®poca ainda n├úo cadastrada/resolvida)', () => {
+        const epoca = { inicio: { ano: 2020 }, fim: { ano: 2021 } };
+        assert.equal(formatarEpocaRetratada(epoca, epocasCadastro), '2020 ÔÇô 2021');
+    });
+
+    it('campo totalmente vazio devolve travess├úo', () => {
+        assert.equal(formatarEpocaRetratada({}, epocasCadastro), 'ÔÇö');
+    });
+
+    it('epoca null/ausente devolve travess├úo, igual ao campo vazio', () => {
+        assert.equal(formatarEpocaRetratada(null, epocasCadastro), 'ÔÇö');
+        assert.equal(formatarEpocaRetratada(undefined, epocasCadastro), 'ÔÇö');
+    });
+
+    describe('com Contexto do relacionamento cadastrado', () => {
+        const epocasComContexto = [
+            { id: 1, nome: 'Corte de contato', contextoRelacao: 'Pedro e Victor' },
+        ];
+
+        it('contexto entra na frente, separado por "ÔÇó"', () => {
+            const epoca = { epocaId: 1, inicio: { ano: 2023 }, fim: { ano: 2024 } };
+            assert.equal(
+                formatarEpocaRetratada(epoca, epocasComContexto),
+                'Pedro e Victor ÔÇó Corte de contato (2023 ÔÇô 2024)',
+            );
+        });
+
+        it('contexto + N/A', () => {
+            assert.equal(
+                formatarEpocaRetratada({ epocaId: 1, na: true }, epocasComContexto),
+                'Pedro e Victor ÔÇó Corte de contato (N/A)',
+            );
+        });
+
+        it('contexto + recorte "repercuss├úo"', () => {
+            const epoca = { epocaId: 1, inicio: { ano: 2023 }, recorte: 'repercuss├úo' };
+            assert.equal(
+                formatarEpocaRetratada(epoca, epocasComContexto),
+                'Pedro e Victor ÔÇó Corte de contato e p├│s (A partir de 2023)',
+            );
+        });
+
+        it('sem contexto cadastrado (├®poca sem o campo preenchido) n├úo mostra o "ÔÇó"', () => {
+            const epocasSemContexto = [{ id: 1, nome: 'Luto' }];
+            assert.equal(
+                formatarEpocaRetratada({ epocaId: 1 }, epocasSemContexto),
+                'Luto',
+            );
+        });
+    });
+});
+
+describe('obterSugestaoEpocaPorId (sugest├úo de datas/contexto pro poema mais recente com a mesma ├®poca)', () => {
+    it('devolve datas e contexto do poema de maior id (mais recente) com o mesmo epocaId', () => {
+        const poemas = [
+            {
+                id: 100,
+                epocaRetratada: { epocaId: 1, inicio: { ano: 2020 }, fim: null },
+                contextoHistorico: 'primeiro',
+            },
+            {
+                id: 200,
+                epocaRetratada: { epocaId: 1, inicio: { ano: 2021 }, fim: { ano: 2022 } },
+                contextoHistorico: 'mais recente',
+            },
+        ];
+        const sugestao = obterSugestaoEpocaPorId(poemas, 1);
+        assert.deepEqual(sugestao.inicio, { ano: 2021 });
+        assert.deepEqual(sugestao.fim, { ano: 2022 });
+        assert.equal(sugestao.contextoHistorico, 'mais recente');
+    });
+
+    it('ignora poemas com epocaId diferente', () => {
+        const poemas = [
+            { id: 1, epocaRetratada: { epocaId: 2 }, contextoHistorico: 'outra ├®poca' },
+        ];
+        assert.equal(obterSugestaoEpocaPorId(poemas, 1), null);
+    });
+
+    it('devolve null quando epocaId n├úo ├® informado', () => {
+        assert.equal(obterSugestaoEpocaPorId([{ id: 1 }], null), null);
+        assert.equal(obterSugestaoEpocaPorId([{ id: 1 }], undefined), null);
+    });
+
+    it('devolve null quando n├úo h├í nenhum poema com essa ├®poca ainda', () => {
+        assert.equal(obterSugestaoEpocaPorId([{ id: 1, epocaRetratada: null }], 5), null);
+    });
+});
